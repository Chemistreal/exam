# 깃허브 자동 저장 켜기 (10분, 한 번만)

응시 기록은 지금 **구글 시트에만** 있습니다. 잘못 지우거나 덮어써도 되돌릴
방법이 없습니다. 아래를 한 번 해 두면 매일 새벽 깃허브에 한 벌씩 쌓입니다.

> **이 두 가지는 대신 해 드릴 수 없습니다.** 토큰 발급은 선생님 깃허브 계정
> 로그인이, 붙여넣기는 구글 계정 로그인이 필요합니다. 대신 **맞게 됐는지
> 확인해 주는 버튼**(`checkBackupSetup`)을 만들어 뒀습니다.

---

## 1. 깃허브 토큰 만들기 (한 번, 두 저장소에 같이 씁니다)

github.com 에 로그인한 상태에서:

1. 오른쪽 위 프로필 › **Settings**
2. 왼쪽 맨 아래 **Developer settings**
3. **Personal access tokens › Fine-grained tokens** › `Generate new token`
4. 이렇게 채웁니다:

   | 칸 | 넣을 것 |
   |---|---|
   | Token name | `chemistreal-backup` (아무 이름이나) |
   | Expiration | `No expiration` 또는 1년 |
   | Repository access | **Only select repositories** → `Chemistreal/exam` 과 `Chemistreal/DT` 둘 다 |
   | Permissions › Repository › **Contents** | **Read and write** ← 이것 하나가 핵심 |

5. `Generate token` → 나오는 `github_pat_...` 문자열을 복사합니다.

> ⚠ 이 화면을 벗어나면 **다시 볼 수 없습니다.** 복사해서 잠깐 메모장에 두세요.

---

## 2. 파이널 시트에 넣기

1. 성적기록 스프레드시트 › **확장 프로그램 › Apps Script**
2. 왼쪽 톱니바퀴 **프로젝트 설정** › 아래쪽 **스크립트 속성** › `속성 추가`
   - 속성: `GITHUB_TOKEN`
   - 값: 1번에서 복사한 `github_pat_...`
   - **저장**
3. 왼쪽 `< >` 편집기로 돌아와, 위쪽 함수 선택 상자에서
   **`setupBackupTriggers`** 를 고르고 **실행**
   - 처음이면 권한 승인 창이 뜹니다 → 허용
4. 같은 상자에서 **`checkBackupSetup`** 을 고르고 **실행**
   - 아래 실행 로그에 이렇게 나오면 끝입니다:

   ```
   ✓ GITHUB_TOKEN 있음
   ✓ 저장소 확인 · Chemistreal/exam (공개)
   ✓ 쓰기 권한 있음
   ✓ 기록 129줄 읽힘
   ✓ 이름 → 코드 됨 (예: s7k3m2n8p1q)
   ✓ 자동 실행 걸림 · dailyBackup
   ● 모두 정상입니다. 오늘 밤부터 저절로 올라갑니다.
   ```

   `✗` 가 있으면 그 줄에 무엇을 고쳐야 하는지 적혀 있습니다.

5. (원하시면) **`dailyBackup`** 을 실행해 지금 바로 한 벌 올려 봅니다.
   깃허브 저장소의 `backup/` 폴더에 오늘 날짜 파일이 생깁니다.

---

## 3. DT 시트에도 같이 (2번과 똑같이)

DT 스프레드시트에서 2번을 그대로 반복합니다. **토큰은 1번에서 만든 것을
그대로 씁니다**(두 저장소를 다 넣어 뒀으므로).

---

## 무엇이 올라가나

| 언제 | 무엇 |
|---|---|
| 매일 03시 | `backup/YYYY-MM-DD.json` — 그날의 전체 기록 |
| 매일 04시 | `cohort/baseline.json` — 석차·또래 정답률 모집단 |
| 일 20시 | `report/YYYY-Www.md` — 주간 요약 |
| 일 21시 (DT) | `backup/roster-YYYY-MM-DD.json` — 반 명단 |

## 이름은 안 올라갑니다

저장소가 공개이고, 한 번 올라간 것은 **커밋 이력에 영영 남습니다.**
그래서 이름 대신 코드(`s7k3m2n8p1q`)를 싣습니다.

- 같은 학생은 늘 같은 코드 → 날짜별 백업을 견줄 수 있습니다
- 이름만 아는 사람은 코드를 만들어 볼 수 없습니다(이 프로젝트에만 있는 소금을 섞습니다)
- 공유 링크도 안 싣습니다 — 거기에 이름이 들어 있습니다

**이름↔코드 표는 시트의 `_이름코드` 탭**에만 쌓입니다.
잘못 지운 행은 깃허브에서 되살려 이 표로 이름을 붙이면 됩니다.

> 시트를 **통째로** 잃으면 기록은 다 남지만 누구 것인지는 모릅니다.
> 구글 시트에는 버전 기록과 휴지통이 있어 그런 일은 드물고, 이 백업이 막으려는
> 것은 *잘못된 수정·삭제* 쪽입니다.

---

## 되돌리는 법

1. 깃허브 저장소 › `backup/` › 되돌리고 싶은 날짜 파일을 엽니다
2. 그날의 기록이 코드와 함께 들어 있습니다
3. 시트의 `_이름코드` 탭에서 코드로 이름을 찾습니다
4. 필요한 줄만 시트에 다시 넣습니다

## 껐다 켜기

- **끄기**: 스크립트 속성에서 `GITHUB_TOKEN` 을 지우면 아무것도 안 올라갑니다
  (채점은 그대로 돌아갑니다)
- **다시 켜기**: 토큰을 다시 넣으면 그날 밤부터 재개됩니다

> `CODE_SALT` 속성은 **지우지 마세요.** 지우면 코드가 새로 만들어져
> 예전 백업과 이어지지 않습니다.
