# CHEMISTREAL · 화학 성적표와 오답 카드

대치동 다원교육 영재관 조준모 화학. 시험을 채점해 성적표를 내주고, 틀린
문항마다 문제 그림 · 해설 · 오개념 한 줄 · 같은 개념의 새 문제를 붙여 주는
정적 웹앱이다. 서버가 없고 브라우저에서 바로 돈다.

> 이 파일은 **저장소의 지금 모습**을 적는다. 2026-07-07 판은 그때 한 번
> 묶어 보낸 꾸러미(`deploy/`)를 설명하고 있었는데, 그 폴더는 이 저장소에
> 없고 적혀 있던 크기(index.html 876KB)도 오래전 값이었다.

## 지금 들어 있는 것

| | |
|---|---|
| 시험 | **39회차 · 2310문항** |
| 기출동형(새로 집필한 독자 문항) | **2490문항** |
| 문항 그림(크롭) | **2310장** |
| 개념 강의 | **125편** |
| 화면 | **258장** |
| CI 검사 | **102개** |

회차 묶음 — JMChC 모의고사 15 · 화올 기출 14 · 기출동형 4 · 2024~2026 심화 5 ·
산과염기 60제 1.

화올 기출은 **2009~2024** 가 들어 있다(2020 은 안 넣기로 했다 — 아래 참조).
그 가운데 10회차는 대회가 낸 **공식 정답률**을, 7회차는 **폐기·채점제외
문항**을 데이터에 담고 있다.

> 정답률은 2026-08-09 에 **여덟 회차가 한 칸씩 밀려 있던 것을 바로잡았다.**
> 1번 자리에 2번 값이 앉아 있었다. 크롭에 인쇄된 값으로 맞췄고, 폐기 문항에
> 정답률이 인쇄되지 않는다는 점을 `tools/rate_check.py` 가 지킨다.

## 돌리는 법

화면을 열려면 그냥 파일을 열면 된다. 검사를 돌리려면 저장소 뿌리에서 정적
서버를 띄운 뒤 한 줄이다.

```bash
python3 -m http.server 8931 &
node tests/run.js                       # 브라우저 검사는 playwright 없으면 건너뛴다
```

`tests/run.js` 는 `tests/*.js` 를 스스로 찾아 돌고, **CI 목록에 없는 검사가
있으면 짚어 준다.** 만들어 놓고 CI 에 안 걸면 아무도 안 돌리기 때문이다.
건너뛴 것은 초록으로 세지 않는다.

## 자물쇠

한 번 걸린 결함은 규칙으로 바꿔 둔다. 지키는 자들이 무엇을 막는지만 적는다.

| 자 | 막는 것 |
|---|---|
| `tools/crop_align.py` | 문제지에 섞인 딱지 때문에 크롭이 한 칸씩 밀리는 것 |
| `tools/ci_deps.py` | **자가 CI 에서 못 도는 것** (걸어 뒀는데 안 돌아가는 것) |
| `tests/parent-walk.js` | **학부모 성적표 링크가 선생님 콘솔로 새는 것** · 목차·해설에 들어가면 못 돌아 나오는 것 |
| `tests/report-voice.js` | **성적표가 사람에게 말하는 방식이 되돌아가는 것** (B10~B15 · 숫자가 그대로인지도 같이 본다) |
| `tools/sw_freshness.py` | **낡으면 안 되는 자료가 캐시에서 먼저 나오는 것** (석차의 분모가 조용히 옛 값이 된다) |
| `tools/cohort_cover.py` | **연도누적 인원이 빠지거나 조용히 줄어드는 것** (2009~2016 여덟은 그대로 두기로 정함 · 심화 셋은 ⚖ A8) |
| `tests/weak-first.js` | **학생이 풀어 나가는 자리**(오답 클리닉·오답 정밀 분석)가 취약한 단원부터가 아니게 되는 것 |
| `tests/hub-a11y.js` | 통합 셸을 키보드·낭독기로 못 쓰게 되는 것 |
| `tests/hub-calls.js` | 줄여 놓은 창구 호출이 말없이 도로 늘어나는 것 |
| `tools/hub_audit.py` | 통합 셸이 잰 값에서 되돌아가는 것 |
| `tools/lie_check.py` | **자가 거짓말하는 것** (참·거짓 예시를 맞히는지) |
| `tools/page_doors.py` | 아무 데서도 안 걸리는 화면이 조용히 남는 것 |
| `tools/pages_budget.py` | 배포 한도(1GB)에 벽으로 닿는 것 |
| `tools/name_key.py` | 같은 학생이 앱마다 다른 이름으로 갈리는 것 (정한 뒤부터) |
| `tests/narrow.js` | 휴대폰 폭(360px)에서 화면이 옆으로 밀리는 것 |
| `tools/noindex.py` | 성적·명단이 뜨는 화면이 검색에 잡히는 것 |
| `tools/store_ledger.py` | 브라우저에 무엇을 남기는지 적어 두지 않는 것 |
| `tools/msg_ledger.py` | 사람에게 하던 말이 조용히 사라지는 것 |
| `tools/term_drift.py` | 정한 용어 표기를 어기는 것 |
| `tools/lec_selfcheck.py` | 숙제에 스스로 확인할 기준이 빠지는 것 |
| `tools/mis_sync.py` | 카탈로그·강의·오답 카드의 오개념 목록이 갈라지는 것 |
| `tools/const_sync.py` | 한 회차 안에서 문제지와 해설이 다른 상수를 쓰는 것 |
| `tools/page_honesty.py` | 시제품이 완성품처럼 보이는 것 |
| `tools/lec_readtime.py` | 강의에 적힌 읽는 시간이 글과 어긋나는 것 |
| `tools/om_cover.py` | 오개념 한 줄이 없는 유형 |
| `tools/gen_omlib.py` | 오개념표가 두 화면에서 갈라지는 것 |
| `tools/gen_expl_html.py` | 해설이 비거나, 글과 해설지 꼴이 다른 말을 하는 것 |
| `tools/dh_validate.py` · `dh_lint.py` · `dh_number.py` | 동형문항의 구조·정답 쏠림·셈이 답에 안 닿는 것 |
| `tools/void_check.py` · `rate_check.py` | 폐기 문항·공식 정답률이 조용히 사라지는 것 |
| `tools/pdf_answer_leak.py` | **문제지 PDF 에 답이 실려 나가는 것** |
| `tools/page_exams.py` · `concept_table.py` | 화면마다 품은 회차 목록·개념표가 갈라지는 것 |
| `tools/audit_pages.py` · `theme.py` | 글자 대비 4.5:1 미달, 옷이 갈라지는 것 |
| `tests/page-health.js` | 화면이 열리다가 터지는 것 (258장 전수) |
| `tools/font_block.py` | 바깥 글꼴이 첫 그림을 인질로 잡는 것 |

**바깥 CSS·JS 파일로 빼지 않는다.** 바깥 stylesheet 를 만나면 브라우저는
그리기를 멈추고 기다린다 — 글꼴 한 줄 때문에 첫 화면이 13초였던 일이 있다.
같은 조각을 화면마다 안에 박아 넣고, 갈라지지 않게 자로 잰다.

## 안 넣기로 한 것

**화올 2003~2008 · 2020 — 넣지 않는다** (2026-08-09, 선생님 결정).

한동안 "자료가 오면 넣을 자리" 로 적어 두었던 회차들이다. 넣지 않기로
정했으니 할 일 목록에서 뺀다. 사정은 남겨 둔다 — 안 그러면 자료를 다시
찾아낸 사람이 같은 자리를 또 판다.

    2003~2008   문제지 PDF 가 없다. HWP 본문만 있어 크롭을 뜰 수 없다
    2020        온라인 시험(60문항)이라 문제지 PDF 는 있는데 정답표가 없다

2020 과 얽힌 자국이 하나 더 있었다. `sol-final-hwol-2020.html` 이 제목만
2020 이고 담긴 것은 **2019 회차의 정답**이었다. 2026-08-08 에 지웠고, 없는
회차의 해설지가 남지 못하도록 `tools/gen_sol_page.py --check` 가 지킨다.

## 아직 못 채운 자리

**응시 기록이 없는 23회차.** 석차·수상 확률은 응시 기록 위에 선다. 기록이
없는 회차는 "비슷한 난이도의 실전이라면" 이라는 가정 위에 서 있다
(`tools/cut_link.py` 가 회차마다 그 사정을 적어 준다). 시험지 난이도만은
공식 정답률로 메워 두었다. 기록은 앞으로 쌓인다.

기록이 적을 때 학생이 보는 숫자에는 **표본 크기가 반드시 같이 적힌다**
(`응시 1명 기준의 잠정치 …`). 그 약속은 `tests/rank-baseline.js` 가 지킨다.

## 같이 보면 좋은 것

- `START_HERE_index.html` · `system-guide.html` — 파일 269개의 차례와,
  진단 R&D 를 어떤 일 순서로 쓰는지
- `프로젝트_가이드.md` — ⚠ **이 저장소가 아니라 먼저 만든 앱**(`study64-report`)의
  구조를 적은 글이다(2026-06-12). 여기 적힌 `관리자.html`·`해설-{id}.html` 은 이
  저장소에 없다. 설계가 어디서 왔는지를 보려면 읽되, 설명서로 쓰지 않는다
- `DEPLOY_CHECKLIST.md` · `AUTODEPLOY.md` — 반영 절차
- `tools/INDEX.md` — 자 예순둘의 차례. 어느 자가 무엇을 재는지 (자동 생성)
- `tools/README.md` — 기출동형을 **어떻게 집필하는가**. 열 가지 원칙과
  그것이 생긴 사연(틀린 문헌값·뒤집힌 서열·숨은 전제…)
- [개선 200턴](docs/%EA%B0%9C%EC%84%A0-200%ED%84%B4.md) — 네 저장소를 가로지르는 개선 체크리스트와 **선생님이 정한 것**
- [허브 200턴](docs/%ED%97%88%EB%B8%8C-200%ED%84%B4.md) — 통합 셸(`hub.html`) 하나만 보는 200턴 계획. **다 돌았다** —
  고친 것 열둘 · 재기만 하고 고칠 것이 없던 것 스물셋 · 선생님이 정할 칸 스물하나.
  그 스물하나 가운데 **열셋을 2026-08-09 에 받았고 다섯이 코드를 바꿨다**
  (표 맨 아래에 답과 바뀐 것이 나란히 있다). 남은 여덟은 낭독기·한 손·3초처럼
  사람이 해 봐야 아는 것들이다
- [내용 400턴](docs/%EB%82%B4%EC%9A%A9-400%ED%84%B4.md) — **내용**을 보는 400턴 계획. **다 돌았고 ⚖ 도 다 닫혔다** —
  고친 것 스물셋 · 재기만 하고 고칠 것이 없던 것 스물다섯 · **자가 열두 번 거짓말했다**.
  선생님이 정한 것: 용어는 지금대로 · 엔트로피는 **경우의 수**로 ·
  예제는 **모두 제작**(37편 → 125/125). 나머지 열셋은 근거를 적고 닫았다.
  이어서 **검수 큐 295개를 사람이 다 열어 봤고**(해설 열둘 고침), 그러다
  **공식 정답률이 여덟 회차에서 한 칸 밀려 있는 것**을 찾아 바로잡았다
- [성적표를 읽는 사람](docs/%EC%84%B1%EC%A0%81%ED%91%9C%EB%A5%BC-%EC%9D%BD%EB%8A%94-%EC%82%AC%EB%9E%8C.md) — 26/60 을 맞은 학생의 성적표를 **화면으로 그려 문장만 읽은** 것.
  자로 못 재는 쪽이다 — 한 문서가 같은 아이를 세 번 '밖' 이라 부르고, 한 장에
  `0%` 가 세 번 나오고, 우연을 '정복' 이라 부르고 있었다. 선생님이 여섯을 다
  정하셨고(B10~B15) 고쳤다. **숫자는 하나도 안 바꿨다** — 무엇을 먼저 말하고
  몇 번 말하는가만 바꿨다
- [받은 말](docs/%EB%B0%9B%EC%9D%80-%EB%A7%90.md) — **사람이 우리에게 한 말**과 그 뒤에 무엇을 했는지.
  `msg_ledger` 가 우리가 하는 말을 지키는 자라면, 이건 그 반대쪽이다.
  ⚠ 여섯 건 가운데 **셋은 재어 보니 말과 달랐다** — 말을 자기 편한 대로
  해석하지 않는 것이 이 문서의 규칙이다
- [문장대장](docs/%EB%AC%B8%EC%9E%A5%EB%8C%80%EC%9E%A5.md) — 화면이 사람에게 하는 말 50,607문장 (자동 생성)
- [검수대장](docs/%EA%B2%80%EC%88%98%EB%8C%80%EC%9E%A5.md) — 사람이 먼저 볼 문항 (자동 생성). **큐는 비어 있다** —
  295개를 다 봤다. 새 회차가 들어오면 다시 찬다
- `docs/` — 그 밖의 감사 리포트와 결정 기록
