# 앱별 전수 조사 — 네 앱에서 지금 무엇을 고쳐야 하나 (2026-08-10)

> 대상: **exam** · **DT** · **study64-report** · **KMChC**
> 이 문서는 **잰 것만** 적는다. "좋아 보인다/나빠 보인다" 는 안 적는다.
> 고치는 문서가 아니라 **어디를 고쳐야 하는지 세어 둔 문서**다.

> ✅ **①~⑦ 을 같은 날 고쳤다.** 무엇을 했는지는 맨 아래 「6. 고친 것」에 있다.
> 재면서 **두 군데를 내가 잘못 쟀다.** 그 자리도 거기 적어 두었다.

## 0. 네 앱을 한 장에

| | exam | DT | study64-report | KMChC |
|---|---|---|---|---|
| 화면 | **258장** | 148장(루트) | 25장(루트 6 + 조각 19) | 7장 |
| 자(tools/*.py) | **70개** | 11개 | 2개 | 6개 |
| CI 걸음 | **107** | 18 | 3 | 9 |
| 지금 자를 다 돌리면 | 전부 초록 | 전부 초록 | 전부 초록 | 전부 초록 |
| 작업본 | 148M | **283M** | 172M | 2.5M |
| 가장 큰 화면 | index 949KB | diagnosis_app **1,098KB** | index **1,046KB** | index 238KB |

**초록이라는 말의 뜻이 앱마다 다르다.** exam 은 107걸음이 초록이고 KMChC 는
9걸음이 초록이다. study64-report 는 3걸음이다. 아래는 그 차이를 센 것이다.

---

## 1. 지금 어긋나 있는 것 — 일곱

고치면 바로 값이 되는 것부터. 순서는 급한 순이다.

### ① KMChC 의 화면 검수 자가 **아무것도 못 막는다**

```
$ python3 tools/audit_pages.py --check ; echo $?
0                      ← 결함이 있어도 0 이다. 종료 코드가 아예 없다.
```

세 가지가 한꺼번에 어긋나 있다.

- `tools/audit_pages.py` 가 **200줄짜리 옛 판**이다(exam 259 · DT/study64 270).
  `--check` 를 읽는 자리도, `sys.exit` 도 없다
- 그래서 `--check` 를 줘도 0 을 돌려준다
- CI(`engine-sync.yml`)도 **`--check` 없이** 부른다

> DT 에서 여섯 달 동안 152장이 걸린 채 초록불이었던 것과 **똑같은 모양**이다
> (`docs/내용-400턴.md`). 그때 남긴 문장이 그대로 들어맞는다 —
> **재는 것과 막는 것은 다르다.**

지금은 7장이 다 깨끗해서 숨기고 있는 것이 없다. **다음 한 장이 문제다.**

### ② exam 의 화면 검수 자가 **열한 번째 거짓말 고침을 못 받았다**

같은 자를 네 앱이 나눠 쓰는데 판이 셋으로 갈라져 있다.

| | 줄 수 | 열한 번째 고침(같은 이름의 색을 다 모으기) |
|---|---|---|
| study64-report | 270 | ✔ 들어 있다 (**커밋된 곳은 여기 하나뿐이었다**) |
| DT | 270 | ⚠ 고쳐는 놓고 **커밋을 안 했다** — 작업본에만 있었다 |
| **exam** | 259 | ✗ 아직 `vs = dict(VAR.findall(s))` 다 |
| KMChC | 200 | ✗ (①번과 같은 옛 판) |

`dict()` 로 모으면 **같은 이름의 값이 하나만 남아** 밝은 판과 어두운 판을 함께
가진 화면에서 한쪽 팔레트가 통째로 사라진다. study64-report `english.html` 이
그래서 1.00:1 로 잡혔던 자리다.

exam 의 258장을 새 판으로 돌려 봤다 — **지금은 0장이 걸린다.** 즉 숨기고 있는
결함은 없고, **exam 이 두 벌 옷을 입은 화면을 하나라도 만들면 그때부터 못 본다.**

> `README.md` 는 "자는 exam·DT 저장소와 **같은 판**을 쓴다 — 갈라지면 한쪽만
> 고쳐진다" 고 적어 두었다. 갈라졌다.

### ③ study64-report 의 하나뿐인 행동 검사가 **못 돌면서 초록이다**

> ⚠ **이 칸의 절반은 내가 잘못 쟀다.** 아래 취소선 부분은 사실이 아니다 —
> 자는 제대로 `exit 2` 를 낸다. 내가 `node … | tail -6` 뒤에서 종료 코드를
> 읽어 **파이프 끝의 `tail`** 을 본 것이다. 6절에 적어 두었다.

- README 는 이 파일이 **검사 75개**를 돈다고 적어 두었다
- ~~`jsdom` 이 없으면 안 돌고, **안 돌아도 0** 을 돌려준다~~ → **틀렸다. 2 로 끝난다**
- `tests.yml` 은 이 파일을 **아예 안 부른다** ✔ 맞다
- 기본 경로가 `/mnt/user-data/outputs/index.html` — zip 시절의 자리라 저장소에서
  인자 없이 돌리면 '파일 없음' 이다 ✔ 맞다
- `tests/` 디렉터리가 없다 ✔ 맞다

exam 의 규칙은 이렇다 — *"건너뛴 것은 초록으로 세지 않는다"*(`README.md`).
여기서는 **아무도 안 부르니 세어지지도 않았다.**

### ④ DT 에 **문이 없는 화면이 여섯 장** 있다

저장소 안 어디에서도(html·gs·json·md) 이름이 불리지 않는 화면이다.

| 화면 | 크기 |
|---|---|
| `diagnosis_app.html` | 1,098KB |
| `parent_report.html` | 1,090KB |
| `admin_console.html` | 1,082KB |
| `OX_grader.html` · `OX_grader_prescription.html` · `dualcoding_8types.html` | — |

앞의 셋만 **3.2MB** 다. 학생·학부모가 받는 링크는 앱스크립트가 보내는
`exam/hub.html` 하나뿐이고, 자료 화면 132장은 `materials.json` 을 통해 거기서
열린다(`tools/gen_materials.py` 가 지킨다). 나머지 16장 가운데 `index.html` 이
거는 것은 7장, 다른 데서 불리는 것이 3장, **나머지 여섯이 이것들이다.**

살아 있는 화면인지 버린 화면인지는 **사람만 안다.** exam 에는 이 자리를 보는
자가 있다(`tools/orphan_scan.py`, 지금 2장 — 둘 다 일부러 둔 것). DT 에는 없다.

### ⑤ study64-report 의 README 가 **없는 파일 아홉**을 설명한다

README 가 "업로드 파일 목록 (총 15개)" 이라 적어 놓은 것 가운데

    없다:  prism_apps_script.gs · PRISM_설문지_OMR.pdf · 기준선_검토용.md
           12극해설_01~06.md (여섯 장)

거꾸로 **저장소에 있는데 README 에 한 줄도 없는 것이 열하나**다.

    report.html · answers.html · english.html · v2.html · 관리자.html
    eng2p/ (544파일) · media/ (276파일 158MB) · tools/ · scripts/ · og-study64.png

`index.html` 크기도 "1040KB" 로 적혀 있는데 지금 1.1MB 다.

> exam 의 README 는 이 병을 한 번 앓고 머리에 이렇게 적어 두었다 —
> *"이 파일은 **저장소의 지금 모습**을 적는다."* 여기에도 필요하다.

### ⑥ DT 의 README 가 **한 줄**이다

```
# DT
```

화면 148장 · 자 11개 · CI 18걸음짜리 저장소의 설명서가 이것뿐이다. 무엇을
하는 앱인지, 어떻게 돌리는지, 어느 자가 무엇을 막는지 적힌 데가 없다.

### ⑦ KMChC 의 문서가 **한 번 쓰고 끝난 절차서**다

- `README.md` 는 "1. Apps Script 기존 코드 전체 삭제 → 붙여넣기…" 로 시작하는
  **일회성 이관 순서**다. 그 절차는 이미 끝났고, 거기 적힌 파일 이름
  (`report_fast.html` · `answers_fast.html` · `관리자_shortid_v2.html` ·
  `index_shortid.html` · `AppsScript_fastgithub.gs`) 은 **다섯 다 없다**
- `HANDOFF.md` 는 "현행 최종본은 `live_system/` 의 8개 HTML" 이라 적는데
  `live_system/` · `apps_script/` · `offline_pdf/` **세 폴더 다 없고**, 화면은
  8장이 아니라 7장이다

---

## 2. 자물쇠가 갈라진 자리

같은 종류의 결함을 어느 앱은 막고 어느 앱은 안 막는다.

| 자물쇠 | exam | DT | study64 | KMChC |
|---|---|---|---|---|
| 대비 · 글씨 · 뼈대 | ✔ | ✔ | ✔ | **못 막음**(①) |
| 옷이 갈라지는 것 | ✔ | ✔ | — | ✔ |
| 그림 위 글씨(실제 브라우저) | ✔ | ✔ | — | ✔ |
| 화면 안 JS 가 깨짐 | ✔ | ✔ | ✔(CI 안에 직접) | ✔ |
| 바깥 글꼴이 첫 그림을 막음 | ✔ | ✔ | — | ✔ |
| 입력칸에 이름 | ✔ | ✔ | — | ✔ |
| 검색 노출 | ✔ | ✔ | ✔ | ✔ |
| **자가 거짓말** | ✔ | ✔ | — | — |
| 종이에서 사라지는 자리 | ✔ | ✔ | — | — |
| 사람에게 하는 말이 사라짐 | ✔ | — | — | — |
| 용어 표기가 갈라짐 | ✔ | — | — | — |
| 시제품이 완성품처럼 보임 | ✔ | — | — | — |
| **아무도 안 거는 화면** | ✔ | — (④) | — | — |
| 똑같은 사본이 갈라짐 | ✔ | — | — | — |
| 화면이 열리다 터짐(전수) | ✔ | — | — | — |
| 브라우저에 남기는 칸 | — | ✔ | — | — |

가장 눈에 띄는 두 칸.

- **study64-report 는 다섯 칸이 비어 있다.** 학부모가 휴대폰으로 여는 성적
  리포트인데 `theme.py`·`font_block.py`·`input_labels.py`·실제 브라우저 검사가
  없다. 이 저장소는 2026-08-09 에 **처음** 자로 재어졌고 여섯 장 가운데
  다섯 장이 걸렸다(`.github/workflows/tests.yml` 머리말)
- **`store_ledger.py` 는 DT 에만 있다.** 브라우저에 무엇을 남기는지 적어 두는
  자인데, exam 도 KMChC 도 남기는 것이 있고 적어 둔 데는 없다

---

## 3. 앱별로

### exam — 코드가 아니라 **자료**가 남았다

107걸음이 초록이고, 검수 큐도 비었다(295/295). 남은 것은 두 가지다.

| 무엇 | 지금 | 사람이 해야 하나 |
|---|---|---|
| 응시 기록이 아예 없는 회차 | **23회차** | 기록이 쌓여야 한다 — 코드로 못 만든다 |
| 화면 검수 자의 판 | 259줄(옛 판) | ②번 — 옮겨 오면 된다 |
| 허브에서 사람이 해 봐야 아는 칸 | **여덟** | 낭독기·한 손·3초 |

23회차는 `tools/cut_link.py` 가 회차마다 "비슷한 난이도의 실전이라면" 이라는
가정 위에 서 있다고 적어 준다. 시험지 난이도만은 공식 정답률로 메워 두었고,
그 정답률은 2026-08-09 에 자리를 바로잡았다.

### DT — 화면은 많고 **지도가 없다**

| 무엇 | 잰 값 |
|---|---|
| 화면 | 148장 (문제지 46 · OMR 46 · 해설 35 · 앱 21) |
| `materials.json` 이 거는 것 | 132장 ✔ |
| 문이 없는 것 | **6장**(④) |
| README | **한 줄**(⑥) |
| 작업본 | **283M** — `volumes/` 56M · `truthbooks/` 54M · `appdata/` 46M |
| 1MB 넘는 화면 | **3장** (diagnosis_app · parent_report · admin_console) |

`volumes/` 안의 PDF 다섯 개만 73MB 다. Pages 로 나가는 저장소라 이 무게는
그대로 배포 용량이다.

### study64-report — **처음 재어 본 지 하루** 된 앱

| 무엇 | 잰 값 |
|---|---|
| CI 걸음 | **3** (exam 107) |
| 자 | **2** (exam 70) |
| `tests/` | **없다** |
| 하나뿐인 행동 검사 | 못 돌면서 초록(③) |
| README 와 실물 | 없는 것 9 · 안 적힌 것 11(⑤) |
| `media/` | 276파일 **158MB** (Pages 한도 1GB 의 16%) |
| `eng2p/` | 544파일 11MB — **조립하는 코드가 저장소에 없다** |

`eng2p/` 는 md 408 · js 60 · py 37 · html 19 로 된 **딴 살림**인데, 그 조각을
붙이는 코드도, 그 결과물도 저장소에 없다(`english.html` 안에 조각 글이 없다).
이름을 아는 파일은 `tools/audit_pages.py` 하나뿐 — 그것도 "조각은 건너뛴다"
고 배운 자리다(여덟 번째 거짓말).

### KMChC — 작고 깨끗한데 **자물쇠가 헐겁다**

| 무엇 | 잰 값 |
|---|---|
| 화면 | 7장 · 2.5M — 넷 중 가장 가볍다 |
| 검사 | 3개 다 초록 (엔진 동기화 33쌍 · 읽기 창구 · 실제 브라우저 대비) |
| 화면 검수 자 | **못 막는다**(①) |
| 문서 | 절차서 하나 · 인수인계 하나, **둘 다 실물과 어긋난다**(⑦) |
| 문이 없는 화면 | 4장 (`report.html` 은 짧은 링크로 열리니 정상) |

`tests/engine-sync.js` 는 좋은 자다 — 채점 엔진이 화면과 앱스크립트에서
갈라지지 않는지 33쌍을 맞춰 본다. 네 앱 가운데 이런 자를 가진 것은 여기뿐이다.

---

## 4. 네 앱이 **아무도 안 재는 것**

자가 하나도 없는 자리다. 있으면 좋겠다는 뜻이 아니라, **없다는 것을 적어 두는
것**이 이 표의 목적이다.

| 무엇 | 왜 아무도 안 재나 |
|---|---|
| 화면이 실제로 **몇 초에** 뜨는가 | exam 만 첫 그림을 막는 글꼴을 본다. 나머지 셋은 1MB 짜리 화면을 그냥 둔다 |
| **휴대폰 폭**에서 무너지는가 | viewport 태그가 있는지만 본다. 360px 에서 표가 넘치는지는 안 본다 |
| 앱스크립트 창구가 **죽었을 때** | exam·DT 는 검사가 있다. study64·KMChC 는 없다 |
| 같은 학생이 **두 앱에서 다른 이름** | 네 앱이 같은 학생을 다루는데 이름·반 표기를 맞춰 보는 자가 없다 |
| 저장소가 **배포 한도**에 얼마나 가까운가 | study64 158MB · DT 283MB. Pages 한도는 1GB 다 |

---

## 5. 손대는 순서 (제안)

값이 큰 것부터. 앞의 넷은 반나절 안쪽이다.

1. **KMChC `audit_pages.py` 를 DT 판으로 갈고 CI 에 `--check` 를 붙인다** — ①
2. **exam `audit_pages.py` 를 DT 판으로 갈아 넷을 한 판으로 모은다** — ②
3. **study64 `prism_regression.js` 가 못 돌면 빨간불이 되게 하고 CI 에 건다** — ③
4. **DT 의 문 없는 여섯 장을 선생님이 살릴지 버릴지 정한다** — ④ ⚖
5. README 셋을 지금 모습으로 다시 적는다 — ⑤⑥⑦
6. study64 에 `theme.py`·`font_block.py`·`input_labels.py` 를 옮긴다
7. DT 에 `orphan_scan.py` 를 옮긴다

**4번은 사람이 정할 칸이다.** 나머지는 옮기고 붙이는 일이다.

---

## 6. 고친 것 (2026-08-10, 같은 날)

### 내가 잘못 잰 것 둘 — 먼저 적는다

**③ 을 잘못 쟀다.** "안 돌아도 0 을 돌려준다" 고 적었는데 **틀렸다.**
`prism_regression.js` 는 jsdom 이 없으면 제대로 `exit 2` 를 낸다. 내가 그 값을
`node … | tail -6` 뒤에서 읽는 바람에 파이프 끝의 `tail` 종료 코드를 본 것이다.
**자가 아니라 내가 거짓말했다.**

③ 에서 실제로 맞았던 것은 둘이다 — CI 가 이 파일을 **한 번도 안 불렀고**,
기본 경로가 `/mnt/user-data/outputs/index.html`(zip 시절의 자리)이라 저장소에서
인자 없이 돌리면 '파일 없음' 이었다. 둘 다 고쳤고, 걸어 놓고 보니 **75개 전부
통과**한다.

**② 도 반만 맞았다.** "DT·study64 는 고침이 있다" 고 적었는데, DT 는 고쳐 놓고
**커밋을 안 하고 있었다**(작업본에만 있었다). 커밋된 판은 study64-report 하나뿐
이었다. 이번에 네 곳을 한 판으로 모으면서 DT 것도 같이 커밋했다.

### 일곱 자리

| | 무엇을 했나 |
|---|---|
| ① KMChC 가 못 막던 것 | 200줄 옛 판 → **271줄 한 판**. CI 에 `--check` 를 붙였다. 일부러 깨뜨린 장을 넣어 **1 을 돌려주는지 확인**했다(넣으면 1 · 치우면 0) |
| ② 갈라진 판 | exam 259 · DT 270(미커밋) · study64 270 · KMChC 200 → **네 곳 다 271줄 한 판**. 파일 머리에 `판 2026-08-10` 을 적었다. CI 는 남의 저장소를 못 보므로 **이 줄은 자가 아니라 약속**이다 |
| ③ 안 돌던 검사 75개 | 기본 경로를 이 저장소 `index.html` 로 고치고 CI 에 걸었다. 75/75 통과 |
| ④ 문 없는 화면 | **선생님이 셋을 지우기로 정했다** — `diagnosis_app` · `parent_report` · `admin_console`(3.2MB). 지웠다. 나머지 셋(`OX_grader` · `OX_grader_prescription` · `dualcoding_8types`)은 살리고 `index.html` 에 문을 냈다. 자리에 **`tools/page_doors.py`** 를 세웠다 — 화면 145장 · 문 없는 장 0장 |
| ⑤ study64 README | 없는 파일 아홉을 지우고, 안 적혀 있던 열하나를 적었다 |
| ⑥ DT README | `# DT` 한 줄 → 화면·자물쇠·지운 것·채점이 어디 사는지 |
| ⑦ KMChC 문서 | README 를 지금 모습으로 다시 적고, `HANDOFF.md`·`INDEX.md` 머리에 **"이 글은 zip 시절의 구조다"** 를 적었다 |

### 곁들여 나온 것

- **study64-report 에 자 셋을 들여왔다.** `font_block` 이 처음 재어 **세 장**을
  잡았고(학부모가 휴대폰으로 여는 `answers`·`report`·`관리자` 가 바깥 글꼴을
  그리기 앞에 물고 있었다) 풀었다. `input_labels` 는 106칸 가운데 **73칸**을
  이어 주었다
- **`tools/theme.py` 는 study64 에 안 들여왔다.** PRISM 은 자기 옷(크림 바탕·
  세리프)을 입고 있어 기계가 남의 팔레트를 덮어씌우면 학부모 화면이 통째로
  달라진다 — **⚖ 선생님이 정할 칸**
- **`input_labels` 의 남은 33칸도 CI 에 안 걸었다.** 무엇을 넣는 칸인지 화면을
  봐야 알고, placeholder 를 베끼면 낭독기가 "예 2 편집" 이라고 읽는다. 33칸에
  이름이 붙으면 그때 건다 — **매번 빨간불인 CI 는 아무도 안 본다**

### ⚠ 새로 나온 것 — KMChC `report-data/` 의 학생 리포트 58개

고치다가 나왔다. `report.html?id=…` 이 읽는 JSON 에 **학생 실명 · 학년 · 전체
응답**이 들어 있고, `Chemistreal/KMChC` 는 **공개 저장소**다(GitHub Pages 를
쓰기 때문이다).

    { "id":"r05540…", "name":"홍길동", "grade":"중2", "answers":"…", "savedAt":… }

`tools/noindex.py` 는 **화면(HTML)에만** `noindex` 를 붙일 수 있다. JSON 에는
못 붙인다. 그러니 남는 보호막이 "주소를 남이 모른다" 하나인데, **공개
저장소에서는 그 주소가 목록으로 통째로 보인다.**

지우면 **이미 학부모에게 나간 링크가 깨진다.** 기계가 정할 일이 아니라
KMChC `README.md` 에 선택지 넷을 적어 두었다 — 그대로 · 저장소를 비공개로 ·
이름을 코드로 · `report-data/` 를 걷어내기.

> 다른 세 앱에는 같은 것이 없다. DT 의 반 명단 백업은 이름이 아니라 **가려진
> 학생 코드**(`s64211e174h5`)를 담고, exam·study64 는 학생별 파일을 아예 저장소에
> 두지 않는다.

### 손대는 순서에서 남은 것

| 남은 것 | 왜 안 했나 |
|---|---|
| study64 입력칸 33개에 이름 | 화면을 봐야 안다 |
| study64 에 `theme.py` | ⚖ 학부모가 보는 화면이 달라진다 |
| KMChC `report-data/` | ⚖ 이미 나간 링크가 깨진다 |
| DT 283MB · study64 158MB | Pages 한도 1GB. 아직 여유가 있다 |
| 네 앱이 아무도 안 재는 다섯 가지 | 4절 그대로 남아 있다 |
