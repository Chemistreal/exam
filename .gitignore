__pycache__/
*.pyc

node_modules/

# 구글 계정 열쇠. 실수로 커밋되면 저장소 이력에 영원히 남는다.
.clasprc.json
.clasp.json
creds.json
client_secret*.json

# 검사마다 몇 초 걸렸는지 잰 값(tests/run.js --fast 가 쓴다). 기계마다 다르고
# 돌릴 때마다 바뀐다 — 담아 두면 커밋마다 흔들린다. 없으면 처음 한 번은
# 전부 돌리고 시간을 적어 둔다.
tests/durations.json
