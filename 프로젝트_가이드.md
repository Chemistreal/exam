> ## ⚠ 이 문서가 설명하는 것은 **이 저장소가 아니다** (2026-08-09 확인)
>
> 여기 적힌 `관리자.html` · `해설-{id}.html` · `extract_answers.py` ·
> `gen_haeseol.py` 는 이 저장소에 **없다.** 이 글은 먼저 만든 다른 앱
> (`study64-report`)의 구조를 적은 것이고, 그 뒤로 이 저장소가 갈라져 나왔다.
> 마지막으로 손댄 것이 2026-06-12 다.
>
> 처음 오신 분이 이 문서를 따라가면 없는 파일을 찾게 된다. 지금 이 저장소의
> 모습은 아래 셋을 보시면 된다.
>
> | 어디서 시작하나 | 무엇이 있나 |
> |---|---|
> | `README.md` | 저장소의 지금 모습 · 자물쇠 예순여섯 · 안 넣기로 한 것 |
> | `START_HERE_index.html` | 파일 269개를 갈래로 묶은 차례 |
> | `system-guide.html` | 진단 R&D 를 **어떤 일 순서로** 쓰는가 |
>
> 아래 내용은 지우지 않고 둔다. 저 앱은 아직 돌아가고 있고, 이 저장소의
> 설계가 어디서 왔는지가 여기 적혀 있다. **다만 이 저장소의 설명서는 아니다.**

# 조준모 화학 · 성적 분석 시스템 — 프로젝트 가이드

> 새 대화를 시작할 때 이 문서와 함께 아래 "필수 업로드 파일"을 올리면, 작업을 이어서 진행할 수 있습니다.

---

## 0. 한 줄 요약

교사가 학생 OMR 답안(1~4)을 입력하면 **프리미엄 총괄평가 리포트**가 렌더되는 단일 파일 웹앱(`index.html`)과, 교사 분석 콘솔(`관리자.html`), 시험별 해설 페이지(`해설-{id}.html`)로 구성된 시스템. GitHub Pages에 정적 호스팅. **엔진 하나에 시험을 데이터로 끼워 넣는 구조**라 시험 추가가 쉽다.

---

## 1. 파일 구성

### 배포 파일 (GitHub Pages의 같은 폴더에 업로드)
| 파일 | 역할 | 비고 |
|---|---|---|
| `index.html` | 메인 앱 (리포트 엔진 + 모든 시험 데이터 내장) | ~184KB |
| `관리자.html` | 교사 콘솔 (전체 명단·문항분석·취약개념·CSV·링크 일괄) | 독립 실행, 검색엔진 비공개 |
| `해설-{id}.html` | 시험별 해설 페이지 (정답표 + 문항 네비 + 원본 해설지 이미지) | 시험당 1개, 각 3~5MB |
| `apps-script.gs` | Google Apps Script (성적을 구글 시트에 저장/조회) | 시트에 붙여넣고 웹앱 배포 |
| `sw.js`, `manifest.json`, `icon-192.png`, `icon-512.png`, `og.png` | PWA(오프라인·설치·미리보기) | |
| `exams.json` | (선택) 코드 수정 없이 시험을 추가하는 외부 데이터 | 없으면 무시됨 |

해설 파일 명명 규칙: `해설-{시험id}.html`. 단, **화학1 1-3단원만 예외로 `해설.html`** (먼저 만들어 올린 파일이라 유지). index.html의 시험 정의에 `haeseol:"파일명"`으로 지정 가능.

### 작업 도구 (저장소에 올릴 필요 없음 · 시험 추가 시 사용)
| 파일 | 역할 |
|---|---|
| `extract_answers.py` | 해설/답 PDF → 정답·정답률·문항페이지·풀이텍스트 자동 추출 (JSON) |
| `gen_haeseol.py` | 설정(JSON) + 해설 PDF → `해설-{id}.html` 생성 |
| `_haeseol_template.html` | 해설 페이지 템플릿 (gen_haeseol.py가 사용. `{{TITLE}}{{SUB}}{{NAV}}{{ROWS}}{{PAGES}}` 치환) |
| `exams.example.json` | exams.json 스키마 예시 |

---

## 2. 아키텍처 (핵심)

- **엔진은 영역에 무관(exam-agnostic).** 시험마다 자기 영역 체계를 가진다.
  - `exam.areaOrder`: 영역 표시 순서 배열
  - `exam.areaGroup`: 영역→단원 매핑 객체
  - 헬퍼 `areaOrderFor / areaGroupFor / orderedAreasOf / groupOrderFor`가 시험별 값을 쓰고, 순서에 없는 영역도 자동 포함한다. (전역 `AREA_ORDER/AREA_GROUP`는 화학1 기본 폴백)
- **시험 데이터 한 문항** = `[번호, 정답(1~4), 정답률, 영역, 유형]` (+선택적으로 page, misc).
- **정상 열화:** 시드(누적 점수)가 없으면 백분위는 "추정", OM(오개념)이 없으면 오개념 카드만 생략된다. 시험이 불완전해도 리포트는 뜬다.
- **해설 링크:** 리포트 정오표의 "해설 →"는 `exam.haeseol || 해설-{id}.html` 의 `#q{문항}` 으로 연결.
- **정답률 추정 표시:** `exam.estP:true`면 정오표에 별표(*)와 "추정값" 주석이 붙는다.
- **OM 주의:** `mkExam()`은 전역 `OM`(화학1 1-3단원 오개념)을 문항번호로 자동 첨부한다. **다른 시험에 mkExam을 쓰면 오개념이 잘못 붙으므로**, 신규 시험은 `questions:Q.map(r=>({n,ans,p,area,type,page:0,misc:null}))` 형태로 직접 정의할 것.

---

## 3. 새 시험 추가하는 법

### 방법 A — exams.json (코드 수정 없음, 권장)
`index.html`과 같은 폴더에 `exams.json` 배열을 두면, 앱이 로드 시 자동으로 시험을 추가한다. (http(s)에서만 동작 — file://은 건너뜀)

스키마 (`exams.example.json` 참고):
```json
[
  {
    "id": "kch2-u1",                         // 영문/숫자 (해설 파일명·링크에 쓰임)
    "title": "화학2 1단원 모의고사",
    "range": "물질의 상태와 용액",
    "source": "조준모의고사",
    "N": 0,                                   // 응시 인원(시드 있으면 생략 가능)
    "estP": true,                             // 정답률이 추정값이면 true
    "haeseol": "해설-kch2-u1.html",            // (선택) 해설 파일명
    "areaOrder": ["기체","액체와 고체","용액"],
    "areaGroup": {"기체":"I. 물질의 상태와 용액","액체와 고체":"I. 물질의 상태와 용액","용액":"I. 물질의 상태와 용액"},
    "questions": [
      [1,2,60,"기체","보일법칙"],
      [2,3,55,"용액","총괄성"]
      // [번호, 정답, 정답률, 영역, 유형]  (정답률 모르면 추정값)
    ],
    "seed": [[6,4,3], [4,2,2]]                // (선택) 학생별 [영역1점수,영역2점수,...] (areaOrder 순서). 있으면 백분위·석차 실측
  }
]
```
추가로 할 일: `해설-{id}.html`을 만들어(아래 4번) 같은 폴더에 업로드. 끝.

### 방법 B — index.html 내장
대량/영구 시험은 index.html에 직접 넣어도 된다.
1. `const Q_XXX=[[n,ans,p,area,type],...];`를 `function mkExam` 앞에 삽입.
2. `EXAMS` 배열에 항목 추가:
```js
{id:"xxx",title:"...",range:"...",source:"...",N:0,estP:true,haeseol:"해설-xxx.html",
 areaOrder:[...], areaGroup:{...},
 questions:Q_XXX.map(r=>({n:r[0],ans:r[1],p:r[2],area:r[3],type:r[4],page:0,misc:null}))},
```

---

## 4. 시험 데이터 만드는 파이프라인 (도구 사용)

1. **정답·정답률 자동 추출**
   ```bash
   python3 extract_answers.py "해설.pdf" out.json
   ```
   - 1페이지 정답표에서 정답을 직접 파싱(가장 신뢰도 높음). 복수정답(②,③)도 감지.
   - 본문은 `문제 N` 라벨이 있으면 라벨 기준, 없으면 단독 동그라미 기준으로 정답률·문항페이지·풀이텍스트 추출(자동 판별).
   - 출력: `{ans, rate, qpage, blocks, multi, method}`

2. **영역·유형 분류 (전문 판단 — 자동 불가)**
   - `out.json`의 `blocks`(문항별 풀이/문제 텍스트)를 읽고 각 문항의 `area`·`type`을 정한다.
   - 화학1: 화학의 기초/화학식량/몰/기체/양적관계(=I.화학의 첫걸음), 원소의 기원/원자모형/전자배치/주기율(=II.원자의 세계), 분자의 구조/모양/극성(=III.화학 결합과 분자)
   - 화학2: 기체/액체와 고체/용액(=I.물질의 상태와 용액), 반응엔탈피/화학평형(=II.반응엔탈피와 평형), 반응속도(=III.반응속도), 산과 염기/산화환원과 전기화학(=IV.산·염기와 산화환원)
   - 정답률이 없으면 영역별 난이도로 추정하고 `estP:true`로 표시.

3. **해설 페이지 생성**
   ```bash
   python3 gen_haeseol.py config.json    # → 해설-{id}.html
   ```
   `config.json` 예:
   ```json
   {"id":"kch2-u1","title":"화학2 1단원 모의고사 · 해설","sub":"물질의 상태와 용액 · 조준모 화학",
    "sol_pdf":"해설.pdf","dpi":120,
    "questions":[[1,2,60,"기체","보일법칙"], ...],
    "qpage":{"1":2,"2":2, ...}}   // extract_answers.py의 qpage를 그대로 넣으면 라벨/동그라미 매핑 사용
   ```
   - `qpage`를 주면 그 매핑을 쓰고, 없으면 단독 동그라미로 매핑한다.
   - 원본 해설지 페이지를 이미지로 담는다(수식·도표 보존). 정답·정답률·개념은 상단 검색표에 텍스트로.

4. **앱에 등록** — 방법 A(exams.json) 또는 B(index.html).

---

## 5. 스타일·작업 규칙 (반드시 준수)

- **em대시(—) 절대 금지.** 가운뎃점(·)·쉼표·괄호로 대체.
- 한글은 **literal UTF-8**로 (절대 \uXXXX 이스케이프 금지).
- **간결·효율.** 아부 대신 정직한 비판. 근거 없는 의사과학 만들지 않기(예: 점수만으로 "두뇌 유형" 만들지 않음 → 응답 패턴 기반 "학습 성향"만 정직하게).
- **이미 업로드한 파일은 다시 만들지 않기.** 변경된 파일만 출력.
- 검증 우선: 수정 후 `node _final.js`(33종 회귀) + 문법 체크 + Playwright E2E.
- 코드/리포트에 불릿·과한 볼드 남발 금지.

### 문법·회귀 체크 명령
```bash
# 메인 스크립트 추출 후 문법 검사
node -e "const fs=require('fs');const h=fs.readFileSync('index.html','utf8');const b=[...h.matchAll(/<script>([\\s\\S]*?)<\\/script>/g)].map(m=>m[1]).sort((a,b)=>b.length-a.length)[0];fs.writeFileSync('_check.js',b);" && node --check _check.js
node _final.js   # 33종 검증 (kch1to3 기준)
```

---

## 6. 현재 시험 현황

| id | 시험 | 상태 | 정답률 | 시드 | 해설 |
|---|---|---|---|---|---|
| `kch1to3` | 화학1 1-3단원 모의고사 | 활성 | 실측(57/60) | 422명 내장 | `해설.html` |
| `kch1to2` | 화학1 1-2단원 모의고사 | 활성 | 실측(56/60) + 추정4 | 없음(응시 0) | `해설-kch1to2.html` |
| `kch1u1` | 화학1 1단원 모의고사 | 활성 | **추정**(영역별) | 없음 | `해설-kch1u1.html` |
| `kch2final` | 화학2 총괄평가 | 활성 | **추정**(영역별) | 없음 | `해설-kch2final.html` |
| `chem2-1` | 화학2 1단원 모의고사 | 준비 중(disabled) | — | — | — |

### 다음 할 일 / 알려진 이슈
- **화학2 1단원 보류:** 답 PDF가 문제 라벨·정렬된 정답표식이 없고 풀이 내 동그라미가 잘못 잡혀 문항별 영역 자동 분류 불가. 정답표 60개는 정확. **32번은 복수정답(②,③)** → OMR 단일 입력과 충돌하므로 처리 방침 필요. 문제 원본은 HWP. → 문제 PDF를 받으면 분류해 완성.
- **추정 정답률 교체:** `kch1u1`, `kch1to2`(4문항), `kch2final`은 정답률이 추정값. 기채점 성적 엑셀이 오면 실측으로 교체 + 각 시험 `seed` 채우기(백분위·석차 실측화).
- **시드 채우기:** 기채점 엑셀의 학생별 영역 점수를 `areaOrder` 순서 배열로 변환해 `exam.seed`(exams.json) 또는 `SEEDS`(index.html)에 넣으면 됨.

---

## 7. 배포 (GitHub Pages)

- 저장소: `github.com/Chemistreal/...`의 `exam/` 폴더(예시). 같은 폴더에 위 배포 파일들을 올린다.
- 변경된 파일만 같은 경로로 덮어쓰면 반영.
- 해설 파일은 시험당 1개. 화학1 1-3단원은 `해설.html` 유지(재업로드 불필요).

---

## 8. Google Sheet 저장 (apps-script.gs)

- 앱이 성적을 구글 시트에 저장/조회. 엔드포인트: index.html의 `SHEET_ENDPOINT`(Apps Script 웹앱 /exec URL).
- **시트 열 순서:** A열=학생이름, B열=공유링크, 이후 저장시각·수험번호·응시일·학교·학년·시험·원점수·만점·백점환산·백분위·석차·전체누적인원·맞은개수·영역별 득점·답안(60). (총 17열)
- apps-script.gs 수정 시: 코드 전체 교체 → 배포 관리 → 기존 배포 편집 → **새 버전**(URL 유지). 열 순서를 바꿨으면 기존 '성적기록' 시트를 한 번 비우거나 탭 삭제 후 재저장.
- 보안(선택): 스크립트의 `SECRET`과 앱의 '동기화 키'를 같은 값으로 맞추면 키 없는 접근 차단. 비워두면 키 없이 동작.

---

## 9. 공유 링크 형식 (참고)

`index.html#r=<시험id>.<base36답안>.<수험번호>.<이름>`
- base36답안 = 답안을 5진수 리틀엔디언으로 묶어 36진수로 인코딩(문항순).
- 이 링크로 열면 리포트만(학부모 모드)으로 표시되고 입력/관리 UI는 숨겨짐.
- `관리자.html`의 학생 행 "링크" 버튼이 이 링크를 생성한다.

---

## 필수 업로드 파일 (새 대화 시작 시)

새 대화에서 작업을 이어가려면 다음을 올리세요:
1. 이 문서 (`프로젝트_가이드.md`)
2. `index.html` (현재 엔진 + 시험 데이터)
3. 작업 도구: `extract_answers.py`, `gen_haeseol.py`, `_haeseol_template.html`
4. `exams.example.json` (스키마 참고)
5. (시험 추가 시) 해당 시험의 **문제 PDF + 해설/답 PDF**
6. (정답률·시드 실측화 시) **기채점 성적 엑셀(.xlsm/.xlsx)**

`관리자.html`, `apps-script.gs`, `sw.js`, `manifest.json`, 아이콘, 기존 `해설-*.html`은 이미 배포돼 있으므로, 그 파일들을 직접 수정할 때만 올리면 됩니다.
