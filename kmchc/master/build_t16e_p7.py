# -*- coding: utf-8 -*-
"""T16 P7 (M02851~M02860) — 두 값을 한자리에 놓을 때 이름과 단위를 먼저 갈라 둔다

  각도: C16-001[6] · 003[3] · 005[6] · 006[8] · 008[6] · 010[7] · 011[1] · 014[3] ·
        017[1] · 020[7]

  ★정답 자리 4·2·1·2·4·1·3·4·2·3★ (앞 꼬리 1-3-1-4-2-1-3-4 를 이어 G3f 를 간격 5 까지 셈했다)
  병합 뒤 ①715②715③715④715 — ★편차 0★

  ★길이 규칙 — P6 에서 배운 것★
  한 선지만 고치면 그 길이가 다른 선지와 새로 동률이 된다(P6 은 손질 뒤 동률이 5→8 로
  늘었다). ★네 선지의 길이를 처음부터 함께 정하고 층지게 짓는다.★
  3위: 851 · 853 · 855 · 857 · 859 / 2위: 852 · 854 · 856 · 858 · 860
"""
import re

import batch_template as T

T.START_ID = 'M02851'
T.COUNT = 10
T.EXPECT_LEN = 2850
T.THEME = '전자친화도·전기음성도'
T.TT = 16
T.UNIT = 'I'
T.BATCH_NOTE = 'T16 P7 — 두 값을 한자리에 놓을 때 이름과 단위를 먼저 갈라 둔다.'


def q(i, sk, tr, dok, df, esr, stem, cs, ans, proof, wr, dev, calc, scen, obj,
      lead, prose, wmap, diag):
    x = T.mk(i, sk, tr, dok, df, esr, stem, cs, ans, proof, wr, dev, calc, scen, obj=obj)
    T.sol(x, lead, prose, wmap, diag)
    return x


def build():
    it = []

    # ── M02851 ── C16-001[6] 값이 크다는 말 (일반·쉬움) 정답 ④ [3위] ──
    it.append(q('M02851', '전자 친화도가 크다는 말을 방출량으로 풀어 적기',
        '일반', 2, '쉬움', 0.58,
        '어떤 원소의 전자 친화도가 크다고 한다. 이 말을 풀어 적으려 한다. 옳은 것은?',
        ['중성 원자에서 전자를 떼기 어렵다는 뜻이다',
         '전자를 받는 데 힘이 많이 든다는 뜻이다',
         '전자를 받아들이는 개수가 많다는 뜻',
         '전자를 받을 때 많은 열을 내놓게 된다'], 3,
        '전자 친화도는 기체 상태의 원자가 전자를 받아들일 때 내놓는 에너지의 양이므로, 값이 크다는 '
        '것은 그만큼 많이 내놓는다는 뜻이다',
        [('중성 원자에서 전자를 떼기 어렵다는 뜻이다', '이온화 에너지로 바꿔 읽음', 'conserv'),
         ('전자를 받는 데 힘이 많이 든다는 뜻이다', '내놓는 쪽을 드는 쪽으로 뒤집음', 'sign'),
         ('전자를 받아들이는 개수가 많다는 뜻', '내놓는 양을 받는 개수로 바꿈', 'scale')],
        '값이 무엇의 양인지 적고 크다를 그 양에 붙이기',
        '전자 친화도 = 받아들일 때 내놓는 에너지의 양 / 크다 → 많이 내놓음 / ① 떼는 쪽 / '
        '② 드는 쪽 / ③ 개수가 아님',
        '값이 크다고 적힌 어떤 원소', '값이 무엇의 양인지 아는지 확인',
        '값이 무엇의 양인지 적고 크다를 그 양에 붙여 봐.',
        '전자 친화도는 기체 원자가 전자를 받을 때 내놓는 에너지의 양이야. 크다면 많이 내놓지 '
        '— ④가 옳아. '
        '①은 중성 원자에서 전자를 떼기 어렵다는 뜻이다 라고 했어. 떼는 쪽은 다른 값이야. '
        '②는 전자를 받는 데 힘이 많이 든다는 뜻이다 라고 했어. 내놓는 쪽이지. '
        '③은 전자를 받아들이는 개수가 많다는 뜻 이라고 했어. 개수가 아니라 내놓는 양이야. '
        '크다·작다를 풀 때는 그 값이 무엇의 양인지부터 적는다.',
        {'중성 원자에서 전자를 떼기 어렵다는 뜻이다': '떼는 쪽은 달라.',
         '전자를 받는 데 힘이 많이 든다는 뜻이다': '내놓는 쪽이야.',
         '전자를 받아들이는 개수가 많다는 뜻': '개수가 아냐.'},
        '무엇의 양인지 적었는가?'))

    # ── M02852 ── C16-003[3] 두 요인이 맞설 때 (심화·어려움) 정답 ② [2위] ──
    it.append(q('M02852', '두 요인이 맞설 때 어느 쪽이 이기는지 따져 보기',
        '심화', 4, '어려움', 0.26,
        '같은 족에서 셋째 주기부터 아래로 내려가면 핵전하는 커지지만 껍질도 늘어난다. 전자 친화도를 두고 이 두 가지가 '
        '맞설 때 옳은 것은?',
        ['핵전하가 이겨 값이 커지는 쪽으로 간다',
         '껍질이 늘어난 몫이 더 크게 들어간다',
         '두 가지가 꼭 같아 값이 그대로다',
         '맞서는 자리에서는 값을 못 정한다'], 1,
        '아래로 갈수록 핵전하도 커지지만 껍질이 늘어 전자가 앉을 자리가 훨씬 멀어지므로 껍질 쪽의 '
        '몫이 대체로 더 크게 들어 값이 작아진다(둘째 주기는 자리가 좁아 벗어난다)',
        [('핵전하가 이겨 값이 커지는 쪽으로 간다', '이기는 쪽을 맞바꿈', 'sign'),
         ('두 가지가 꼭 같아 값이 그대로다', '맞섬의 결과를 0 으로 봄', 'scale'),
         ('맞서는 자리에서는 값을 못 정한다', '정해지는 것을 지움', 'conserv')],
        '두 요인이 값을 어느 쪽으로 미는지 따로 적고 크기를 견주기',
        '핵전하 ↑ → 값을 키우는 쪽 / 껍질 ↑ → 멀어짐 → 값을 줄이는 쪽 / 셋째 주기 아래로는 껍질 쪽이 '
        '더 큼 → 값 작아짐 / ① 거꾸로 / ③ 같지 않음 / ④ 정해짐',
        '아래로 내려간 같은 족', '맞서는 두 요인의 크기를 견주는지 확인',
        '두 요인이 값을 어느 쪽으로 미는지 따로 적고 크기를 견주어 봐.',
        '셋째 주기 아래로는 껍질이 늘어난 몫이 더 커. 그래서 값이 작아지지 '
        '— ②가 옳아. '
        '①은 핵전하가 이겨 값이 커지는 쪽으로 간다 라고 했어. 거꾸로야. '
        '③은 두 가지가 꼭 같아 값이 그대로다 라고 했어. 같지 않지. '
        '④는 맞서는 자리에서는 값을 못 정한다 라고 했어. 어느 쪽이 큰지 보면 정해져. '
        '요인이 맞서면 어느 쪽 몫이 더 큰지 견준다.',
        {'핵전하가 이겨 값이 커지는 쪽으로 간다': '거꾸로야.',
         '두 가지가 꼭 같아 값이 그대로다': '같지 않아.',
         '맞서는 자리에서는 값을 못 정한다': '견주면 정해져.'},
        '어느 쪽 몫이 큰지 견주었는가?'))

    # ── M02853 ── C16-005[6] 경향과 예외를 한 문장에 (일반·중간) 정답 ① [3위] ──
    it.append(q('M02853', '경향과 예외를 한 문장에 함께 담아 적기',
        '일반', 3, '중간', 0.40,
        '전자 친화도의 족 경향과 그 예외를 한 문장으로 적으려 한다. 문장을 짜는 방법으로 옳은 것은?',
        ['대체로 작아지되 첫 자리는 예외로 둔다',
         '경향을 적을 때는 예외를 함께 적지 않는다',
         '예외만 적고 경향은 적지 않아도 된다',
         '두 가지를 적으면 문장이 틀리게 된다'], 0,
        '경향과 예외를 한 문장에 담으면 어디까지 서는 말인지가 그 자리에서 드러나므로, 대체로라는 '
        '단서와 예외를 함께 적는다',
        [('경향을 적을 때는 예외를 함께 적지 않는다', '함께 적어야 할 것을 막아 세움', 'conserv'),
         ('예외만 적고 경향은 적지 않아도 된다', '적어야 할 절반을 버림', 'scale'),
         ('두 가지를 적으면 문장이 틀리게 된다', '함께 적으면 틀린다고 뒤집음', 'sign')],
        '한 문장에 담았을 때 무엇이 드러나는지 적기',
        '경향 + 단서 + 예외를 한 문장에 → 어디까지 서는 말인지 그 자리에서 드러남 / ② 예외를 버리면 '
        '서는 자리가 사라짐 / ③ 절반을 버림 / ④ 오히려 옳아짐',
        '족의 경향과 그 예외', '한 문장에 담는 방법을 아는지 확인',
        '한 문장에 담았을 때 무엇이 드러나는지 적어 봐.',
        '실제로 17족은 불소가 염소보다 작아 첫 칸이 벗어나. 경향과 단서와 예외를 한 문장에 담으면 어디까지 서는 말인지 드러나 — ①이 옳아. '
        '②는 경향을 적을 때는 예외를 함께 적지 않는다 라고 했어. 예외를 함께 적어야 어디까지 서는 '
        '말인지 드러나지. '
        '③은 예외만 적고 경향은 적지 않아도 된다 라고 했어. 절반을 버리는 거야. '
        '④는 두 가지를 적으면 문장이 틀리게 된다 라고 했어. 오히려 옳아지잖아. '
        '경향을 적을 때는 서는 자리와 벗어나는 자리를 함께 담는다.',
        {'경향을 적을 때는 예외를 함께 적지 않는다': '함께 적어야지.',
         '예외만 적고 경향은 적지 않아도 된다': '절반을 버려.',
         '두 가지를 적으면 문장이 틀리게 된다': '오히려 옳아져.'},
        '무엇이 드러나는지 적었는가?'))

    # ── M02854 ── C16-006[8] 두 값의 단위가 같음 (일반·쉬움) 정답 ② [2위] ──
    it.append(q('M02854', '이온화 에너지와 전자 친화도의 단위가 같음을 적기',
        '일반', 2, '쉬움', 0.58,
        '이온화 에너지와 전자 친화도를 한 표에 실으려 한다. 두 값의 단위에 대해 옳은 것은?',
        ['이온화 에너지에만 단위를 붙여 적는다',
         '둘 다 같은 에너지 단위를 쓴다',
         '하나는 단위가 없는 값이 된다',
         '단위가 달라 나란히 못 싣는다'], 1,
        '두 값 모두 드나드는 에너지의 양이므로 kJ/mol 처럼 같은 에너지 단위를 쓰고, 그래서 한 표에 '
        '나란히 실을 수 있다',
        [('이온화 에너지에만 단위를 붙여 적는다', '한쪽에만 단위가 있다고 봄', 'conserv'),
         ('하나는 단위가 없는 값이 된다', '전기 음성도의 규약을 가져옴', 'unit'),
         ('단위가 달라 나란히 못 싣는다', '같은 것을 다르다고 함', 'sign')],
        '두 값이 저마다 무엇의 양인지 적기',
        '이온화 에너지: 드는 에너지의 양 / 전자 친화도: 나오는 에너지의 양 → 둘 다 에너지 '
        '→ 같은 단위 / ① 둘 다 단위가 있음 / ③ 단위가 없는 것은 전기 음성도 / ④ 실을 수 있음',
        '한 표에 실으려는 두 값', '단위가 같은 값을 아는지 확인',
        '두 값이 저마다 무엇의 양인지 적어 봐.',
        '둘 다 드나드는 에너지의 양이야. 그래서 같은 에너지 단위를 쓰지 — ②가 옳아. '
        '①은 이온화 에너지에만 단위를 붙여 적는다 라고 했어. 둘 다 kJ/mol 을 쓰지. '
        '③은 하나는 단위가 없는 값이 된다 라고 했어. 그건 전기 음성도지. '
        '④는 단위가 달라 나란히 못 싣는다 라고 했어. 실을 수 있어. '
        '단위를 물으면 두 값이 저마다 무엇의 양인지 적는다.',
        {'이온화 에너지에만 단위를 붙여 적는다': '둘 다 붙여.',
         '하나는 단위가 없는 값이 된다': '전기 음성도야.',
         '단위가 달라 나란히 못 싣는다': '실을 수 있어.'},
        '무엇의 양인지 적었는가?'))

    # ── M02855 ── C16-008[6] 음이온이 될 때 반지름이 커짐 (일반·중간) 정답 ④ [3위] ──
    it.append(q('M02855', '음이온이 될 때 반지름이 커지는 것을 잇기',
        '일반', 3, '중간', 0.42,
        '염소 원자가 전자를 하나 받아 음이온이 되었다. 크기에 대해 옳은 것은?',
        ['전자가 늘었으니 크기는 그대로 있다',
         '전자가 늘어 핵이 당겨 작아진다',
         '전자가 늘면 크기를 잴 수 없어진다',
         '전자끼리 밀쳐 내 반지름이 커진다'], 3,
        '핵의 양성자 수는 그대로인데 전자만 하나 늘어 서로 밀치는 힘이 커지므로 전자 구름이 퍼져 '
        '반지름이 커진다',
        [('전자가 늘었으니 크기는 그대로 있다', '달라지는 것을 지움', 'conserv'),
         ('전자가 늘어 핵이 당겨 작아진다', '커지는 쪽을 뒤집음', 'sign'),
         ('전자가 늘면 크기를 잴 수 없어진다', '잴 수 있는 것을 지움', 'proc')],
        '양성자 수와 전자 수가 각각 어떻게 되었는지 적기',
        '양성자: 그대로 / 전자: 하나 늚 → 전자끼리 밀침이 커짐 → 구름이 퍼짐 → 반지름 커짐 / '
        '① 달라짐 / ② 커지는 쪽 / ③ 잴 수 있음',
        '전자를 하나 받은 염소', '이온이 될 때의 크기 변화를 아는지 확인',
        '양성자 수와 전자 수가 각각 어떻게 되었는지 적어 봐.',
        '양성자는 그대로인데 전자만 늘어 서로 밀쳐. 그래서 반지름이 커지지 — ④가 옳아. '
        '①은 전자가 늘었으니 크기는 그대로 있다 라고 했어. 달라지잖아. '
        '②는 전자가 늘어 핵이 당겨 작아진다 라고 했어. 커지는 쪽이야. '
        '③은 전자가 늘면 크기를 잴 수 없어진다 라고 했어. 잴 수 있지. '
        '크기가 바뀌는지 물으면 양성자와 전자를 따로 세어 적는다.',
        {'전자가 늘었으니 크기는 그대로 있다': '달라져.',
         '전자가 늘어 핵이 당겨 작아진다': '커지는 쪽이야.',
         '전자가 늘면 크기를 잴 수 없어진다': '잴 수 있어.'},
        '양성자와 전자를 따로 적었는가?'))

    # ── M02856 ── C16-010[7] 둘을 한 낱말로 뭉뚱그리지 않기 (심화·어려움) 정답 ① [2위] ──
    it.append(q('M02856', '전자 친화도와 전기 음성도를 한 낱말로 부르지 않기',
        '심화', 4, '어려움', 0.26,
        '어떤 학생이 전자 친화도와 전기 음성도를 아울러 "전자를 좋아하는 정도"라고 불렀다. 이렇게 '
        '부른 것에 대해 옳은 것은?',
        ['값을 세우는 자리가 달라 못 묶는다',
         '두 값은 값을 세우는 자리가 같다',
         '두 값은 이름을 가릴 것 없는 값이다',
         '한 낱말로 부르면 더 쉬워진다'], 0,
        '전자 친화도는 홀로 있는 기체 원자에서 정하고 전기 음성도는 결합한 자리에서 따지는 값이라 값을 세우는 자리부터 '
        '다르므로 한 낱말로 묶으면 어느 쪽인지 흐려진다',
        [('두 값은 값을 세우는 자리가 같다', '재는 자리가 같다고 봄', 'surface'),
         ('두 값은 이름을 가릴 것 없는 값이다', '이름이 하는 일을 지움', 'conserv'),
         ('한 낱말로 부르면 더 쉬워진다', '흐려지는 것을 쉬워진다고 봄', 'scale')],
        '두 값이 각각 어디에서 세우는 값인지 적기',
        '전자 친화도: 홀로 있는 기체 원자 / 전기 음성도: 결합한 두 원자 사이 → 값을 세우는 자리가 다름 '
        '→ 한 낱말로 묶으면 어느 쪽인지 흐려짐 / ② 재는 자리가 다름 / ③ 이름이 갈라 줌 / ④ 어려워짐',
        '두 값을 한 낱말로 부른 자리', '이름이 갈라 주는 일을 아는지 확인',
        '두 값이 각각 어디에서 세우는 값인지 적어 봐.',
        '하나는 홀로 있는 원자에서 정하고 하나는 결합한 자리에서 따져. 한 낱말로 못 묶지 — ①이 옳아. '
        '②는 두 값은 값을 세우는 자리가 같다 라고 했어. 하나는 홀로, 하나는 결합한 자리야. '
        '③은 두 값은 이름을 가릴 것 없는 값이다 라고 했어. 이름이 둘을 갈라 주잖아. '
        '④는 한 낱말로 부르면 더 쉬워진다 라고 했어. 오히려 어려워지지. '
        '닮은 두 값은 이름을 갈라 두어야 헷갈리지 않는다.',
        {'두 값은 값을 세우는 자리가 같다': '자리가 달라.',
         '두 값은 이름을 가릴 것 없는 값이다': '이름이 갈라 줘.',
         '한 낱말로 부르면 더 쉬워진다': '어려워져.'},
        '값을 세우는 자리를 적었는가?'))

    # ── M02857 ── C16-011[1] 까닭을 반지름으로 (일반·중간) 정답 ③ [3위] ──
    it.append(q('M02857', '전기 음성도가 커지는 까닭을 반지름으로 적기',
        '일반', 3, '중간', 0.42,
        '같은 족에서 위로 갈수록 전기 음성도가 커진다. 그 까닭을 반지름으로 적으려 한다. '
        '옳은 것은?',
        ['반지름이 커서 전자쌍을 잘 당겨 온다',
         '반지름은 당기는 힘과 상관이 없다',
         '반지름이 작아 전자쌍이 핵에 가깝다',
         '반지름이 작으면 전자쌍을 되레 밀어낸다'], 2,
        '위로 갈수록 껍질이 적어 반지름이 작고, 그만큼 공유 전자쌍이 핵에 가까이 놓여 더 세게 '
        '끌리므로 전기 음성도가 커진다',
        [('반지름이 커서 전자쌍을 잘 당겨 온다', '크고 작음을 맞바꿈', 'sign'),
         ('반지름은 당기는 힘과 상관이 없다', '거리가 드는 것을 지움', 'conserv'),
         ('반지름이 작으면 전자쌍을 되레 밀어낸다', '당김을 밀어냄으로 뒤집음', 'scale')],
        '전자쌍이 핵에서 얼마나 떨어져 있는지 적기',
        '위로 갈수록 껍질 적음 → 반지름 작음 → 전자쌍이 핵에 가까움 → 세게 끌림 → 전기 음성도 큼 / '
        '① 거꾸로 / ② 거리가 듦 / ④ 당기는 쪽',
        '위로 올라간 같은 족', '까닭을 거리로 대는지 확인',
        '전자쌍이 핵에서 얼마나 떨어져 있는지 적어 봐.',
        '위로 갈수록 반지름이 작아 전자쌍이 핵에 가까워. 그만큼 세게 끌리지 — ③이 옳아. '
        '①은 반지름이 커서 전자쌍을 잘 당겨 온다 라고 했어. 크고 작음이 뒤바뀌었어. '
        '②는 반지름은 당기는 힘과 상관이 없다 라고 했어. 거리가 힘에 들잖아. '
        '④는 반지름이 작으면 전자쌍을 되레 밀어낸다 라고 했어. 당기는 쪽이지. '
        '까닭을 거리로 댈 때는 무엇이 얼마나 떨어졌는지 적는다.',
        {'반지름이 커서 전자쌍을 잘 당겨 온다': '뒤바뀌었어.',
         '반지름은 당기는 힘과 상관이 없다': '거리가 들어.',
         '반지름이 작으면 전자쌍을 되레 밀어낸다': '당기는 쪽이야.'},
        '얼마나 떨어졌는지 적었는가?'))

    # ── M02858 ── C16-014[3] 두 자리를 짚어 표시하기 (일반·중간) 정답 ④ [2위] ──
    it.append(q('M02858', '주기율표에서 두 원소의 자리를 짚어 표시해 두기',
        '일반', 2, '중간', 0.48,
        '두 원소의 전기 음성도를 견주기로 하고 주기율표를 펼쳤다. 표에서 먼저 할 일로 옳은 것은?',
        ['두 원소의 이름만으로 값이 정해져 있다',
         '표를 훑어도 자리는 알 수 없다',
         '무게가 큰 쪽이 값도 크다고 본다',
         '두 자리를 각각 짚어서 표시해 둔다'], 3,
        '자리를 짚어 두어야 같은 주기인지 같은 족인지가 눈에 들어오고 그다음에 어느 축의 경향을 '
        '댈지 정할 수 있다',
        [('두 원소의 이름만으로 값이 정해져 있다', '이름이 값을 정해 준다고 봄', 'surface'),
         ('표를 훑어도 자리는 알 수 없다', '표에서 자리를 찾을 수 있음을 지움', 'conserv'),
         ('무게가 큰 쪽이 값도 크다고 본다', '무게로 값을 못 박음', 'causal')],
        '자리를 짚어 두면 무엇이 눈에 들어오는지 적기',
        '두 자리를 짚음 → 같은 주기인지 족인지 보임 → 어느 축의 경향을 댈지 정해짐 / ① 이름으로는 '
        '값이 정해지지 않음 / ② 표에서 찾을 수 있음 / ③ 무게로 정해지지 않음',
        '두 원소를 견주려 편 주기율표', '자리부터 짚는지 확인',
        '자리를 짚어 두면 무엇이 눈에 들어오는지 적어 봐.',
        '자리를 짚어 두면 같은 주기인지 족인지가 보여. 그래야 축을 정하지 — ④가 옳아. '
        '①은 두 원소의 이름만으로 값이 정해져 있다 라고 했어. 이름만으로는 값이 정해지지 않아. '
        '②는 표를 훑어도 자리는 알 수 없다 라고 했어. 표에서 자리를 찾을 수 있잖아. '
        '③은 무게가 큰 쪽이 값도 크다고 본다 라고 했어. 무게로 정해지는 값이 아니야. '
        '표를 펼치면 견줄 자리부터 짚어 표시한다.',
        {'두 원소의 이름만으로 값이 정해져 있다': '이름으로는 안 정해져.',
         '표를 훑어도 자리는 알 수 없다': '찾을 수 있어.',
         '무게가 큰 쪽이 값도 크다고 본다': '무게로 안 정해져.'},
        '자리를 짚어 표시했는가?'))

    # ── M02859 ── C16-017[1] 전기 음성도에 단위가 없음 (심화·중간) 정답 ② [3위] ──
    it.append(q('M02859', '전기 음성도에 단위가 붙지 않는 까닭을 적기',
        '심화', 3, '중간', 0.36,
        '전기 음성도 표에는 값만 있고 단위가 적혀 있지 않았다. 그 까닭으로 옳은 것은?',
        ['표를 만들 때 단위를 빠뜨린 것이다',
         '서로 견주어 매긴 값이기 때문이다',
         '단위가 너무 길어 줄여 적은 것이다',
         '값이 작아 단위를 붙이지 않는다'], 1,
        '전기 음성도는 어느 원소를 얼마로 둘지 기준을 정한 뒤 나머지를 그에 견주어 매긴 값이라 '
        '직접 재어 얻은 양이 아니고, 그래서 붙일 단위가 없다',
        [('표를 만들 때 단위를 빠뜨린 것이다', '까닭이 있는 자리를 실수로 봄', 'proc'),
         ('단위가 너무 길어 줄여 적은 것이다', '없는 까닭을 만들어 붙임', 'causal'),
         ('값이 작아 단위를 붙이지 않는다', '크기와 단위를 이어 버림', 'scale')],
        '이 값이 재어 얻은 양인지 견주어 매긴 값인지 적기',
        '전기 음성도: 기준을 정하고 견주어 매김 → 직접 재어 얻은 양이 아님 → 붙일 단위가 없음 / '
        '전자 친화도는 재는 양 → kJ/mol / ① 실수가 아님 / ③ 그런 단위가 없음 / ④ 크기와 무관',
        '단위가 비어 있는 값 칸', '단위가 없는 까닭을 아는지 확인',
        '이 값이 재어 얻은 양인지 견주어 매긴 값인지 적어 봐.',
        '전기 음성도는 견주어 매긴 값이라 직접 재어 얻은 양이 아니야. 붙일 단위가 없지 — ②가 옳아. '
        '①은 표를 만들 때 단위를 빠뜨린 것이다 라고 했어. 실수가 아니야. '
        '③은 단위가 너무 길어 줄여 적은 것이다 라고 했어. 그런 단위가 아예 없지. '
        '④는 값이 작아 단위를 붙이지 않는다 라고 했어. 크기와는 무관해. '
        '단위가 비어 있으면 그 값이 재는 양인지부터 적는다.',
        {'표를 만들 때 단위를 빠뜨린 것이다': '실수가 아냐.',
         '단위가 너무 길어 줄여 적은 것이다': '그런 단위 없어.',
         '값이 작아 단위를 붙이지 않는다': '크기와 무관해.'},
        '재는 양인지 적었는가?'))

    # ── M02860 ── C16-020[7] 경향과 규칙을 다른 말로 (일반·중간) 정답 ③ [2위] ──
    it.append(q('M02860', '경향과 규칙을 다른 말로 갈라 부르기',
        '일반', 3, '중간', 0.38,
        '노트에 "주기율표의 규칙"이라고 적어 두고 전자 친화도의 경향을 그 아래 적었다. 이 '
        '적음에 대해 옳은 것은?',
        ['벗어난 자리가 없으니 규칙이라 적는다',
         '규칙과 경향은 아예 같은 말이 된다',
         '벗어난 자리가 있어 경향이라 한다',
         '둘 다 적지 않는 편이 더 낫다'], 2,
        '이 노트에서는 벗어남이 없으면 규칙, 있으면 경향으로 갈라 적기로 했고 같은 주기에서 오른쪽으로 갈 때 2족·18족 같은 벗어난 '
        '자리가 있으므로, 경향이라고 적어야 문장이 맞는다',
        [('벗어난 자리가 없으니 규칙이라 적는다', '벗어난 자리가 있는데 없다고 함', 'sign'),
         ('규칙과 경향은 아예 같은 말이 된다', '갈리는 두 말을 하나로 봄', 'conserv'),
         ('둘 다 적지 않는 편이 더 낫다', '적어 둘 값이 있음을 지움', 'scale')],
        '벗어나는 자리가 있는지 없는지로 두 말을 갈라 적기',
        '규칙: 벗어남이 없음 / 경향: 벗어남이 있을 수 있음 / 같은 주기에서 오른쪽으로 갈 때 2족·18족·15족이 벗어남 '
        '→ 경향 / ① 벗어난 자리가 있음 / ② 갈리는 말 / ④ 적어 둘 값이 있음',
        '규칙이라 적어 둔 노트 한 줄', '두 말을 갈라 쓰는지 확인',
        '벗어나는 자리가 있는지 없는지로 두 말을 갈라 적어 봐.',
        '이 노트는 벗어남이 없을 때만 규칙이라 적기로 했어. 벗어난 자리가 있으니 경향이라 적어야지 '
        '— ③이 옳아. '
        '①은 벗어난 자리가 없으니 규칙이라 적는다 라고 했어. 2족·18족에서 벗어나잖아. '
        '②는 규칙과 경향은 아예 같은 말이 된다 라고 했어. 갈리는 두 말이지. '
        '④는 둘 다 적지 않는 편이 더 낫다 라고 했어. 적어 둘 값이 있잖아. '
        '말을 고를 때는 벗어나는 자리가 있는지로 가른다.',
        {'벗어난 자리가 없으니 규칙이라 적는다': '벗어난 자리 있어.',
         '규칙과 경향은 아예 같은 말이 된다': '갈리는 말이야.',
         '둘 다 적지 않는 편이 더 낫다': '적어 둘 값 있어.'},
        '벗어나는 자리로 갈랐는가?'))

    return it


_ANON = re.compile(r'원소 (?:가|나|다|A|B|C)(?=의|와|과|는|은|를|이|가|,|·|\s|$)')


def local_checks(items):
    """T15 지역 저작 점검 — P3 의 것을 그대로 잇는다(⑧ 겹침 · ⑨ 길이 포함)."""
    bad = []
    ALLOWED = {'proc', 'sign', 'surface', 'conserv', 'unit', 'scale', 'overgen', 'causal'}
    for x in items:
        ts = [w['type'] for w in x['distractors']]
        if len(set(ts)) < 3:
            bad.append(f"{x['id']} 오답 유형이 겹친다 — {ts}")
        for t in ts:
            if t not in ALLOWED:
                bad.append(f"{x['id']} 허용되지 않은 유형 {t}")
        # ★★이 검사는 P3 3차까지 한 번도 울 수 없었다★★ — sol() 이 해설 끝에 붙이는
        #   단평 블록이 오답 문면을 ★글자 그대로★ 되풀이하므로 `in x['solution']` 은 언제나
        #   참이었다. 해설 산문에서 인용이 흐트러져도 검사는 통과한다(defender 와
        #   factchecker 가 각각 두 자리를 손으로 잡아 냈다). ▸ ★산문 문단만 본다.★
        prose = x['solution'].split('\n\n')[1] if '\n\n' in x['solution'] else x['solution']
        #   ▸ 산문은 간접 인용이라 종결어미가 바뀐다('…때문이다' → '…때문이라고 했어').
        #     ★어미를 뗀 몸통★ 이 그대로 있는지만 본다 — 몸통이 어긋나면 인용이 흐트러진 것이다.
        for w in x['distractors']:
            core = re.sub(r'(이)?다$', '', x['choices'][w['opt']]).strip()
            if core not in prose:
                bad.append(f"{x['id']} {w['opt'] + 1}번 오답 인용이 해설 산문과 어긋난다")
        if not re.search(r'[①②③④](가|이)? 옳아', x['solution']):
            bad.append(f"{x['id']} 해설에 정답 확정 어구가 없다")
        J = ('를|을|은|의|으로|로|에서|에|과|보다|처럼|까지|부터|이고|이다|이야|이라|이지')
        for f in ('stem', 'solution', 'answer_proof'):
            for m in re.finditer(rf'(?<=[가-힣]) ({J})(?=[\s,.·)?!]|$)', x[f]):
                bad.append(f"{x['id']} {f} 조사 앞 빈칸 — {x[f][max(0, m.start() - 14):m.end()]!r}")
        tails = [c.strip()[-3:] for c in x['choices']]
        for t in set(tails):
            if tails.count(t) == 3 and tails[x['answer']] != t:
                bad.append(f"{x['id']} 어미 「{t}」 셋 대 정답 하나 — 꼴로 갈린다")

        def tk(s):
            return {w for w in re.findall(r'[가-힣]{2,}', s)}
        for i in range(4):
            for j in range(i + 1, 4):
                if x['answer'] in (i, j):
                    continue
                a, b = tk(x['choices'][i]), tk(x['choices'][j])
                if a and b and len(a & b) / len(a | b) >= 0.62:
                    bad.append(f"{x['id']} 오답 {i + 1}·{j + 1} 자카드 "
                               f"{len(a & b) / len(a | b):.2f}")
    for x in items:
        s = x['stem']
        if '톱니' in s and '세로축' not in s:
            bad.append(f"{x['id']} 톱니를 쓰면서 세로축을 적지 않았다")
        anon = _ANON.search(s)
        named = re.search(r'(리튬|베릴륨|붕소|탄소|질소|산소|플루오린|네온|나트륨|마그네슘|염소'
                          r'|칼륨|알루미늄|헬륨|수소|아르곤|브로민|Li|Be|B\b|C\b|N\b|O\b|F\b|Ne|Na)', s)
        if anon and named:
            bad.append(f"{x['id']} 익명 원소와 원소 이름을 함께 인쇄했다 — "
                       f"{anon.group(0)!r} · {named.group(0)!r}")
        if '크게 뛰' in s or '급증' in s:
            if not re.search(r'비\(뒤 ÷ 앞\)|비로 재|앞까지의 개수', s):
                bad.append(f"{x['id']} 급증의 셈법을 발문이 정하지 않았다")
    g = sum(1 for x in items if x['track'] == '일반')
    lo = 7 if len(items) >= 10 else max(2, len(items) // 2)
    if g < lo:
        bad.append(f'일반 {g}제 — {len(items)}문항 배치의 하한은 {lo}')
    e = sum(1 for x in items if x['difficulty'] == '쉬움')
    if e > 3:
        bad.append(f'쉬움 {e}제 — 상한 3')

    # ⑧ ★정답이 발문과의 어휘 겹침 유일 최다이면 실패★ (P1 이 값을 치른 자리)
    for x in items:
        st = set(re.findall(r'[0-9A-Za-z가-힣]{2,}', x['stem']))
        ov = [len(set(re.findall(r'[0-9A-Za-z가-힣]{2,}', c)) & st) for c in x['choices']]
        if ov[x['answer']] == max(ov) and ov.count(max(ov)) == 1:
            bad.append(f"{x['id']} 정답이 발문과의 어휘 겹침 최다(유일) — {ov}")

    # ⑨ ★선지 길이 — 정답이 유일 최장/최단이거나 산포가 0.25 를 넘으면 실패★ (P3 이 값을 치렀다)
    for x in items:
        L = [len(c) for c in x['choices']]
        a = L[x['answer']]
        if a == max(L) and L.count(a) == 1:
            bad.append(f"{x['id']} 정답이 유일 최장 — {L}")
        if a == min(L) and L.count(a) == 1:
            bad.append(f"{x['id']} 정답이 유일 최단 — {L}")
        sp = (max(L) - min(L)) / (sum(L) / len(L))
        if sp > 0.25:
            bad.append(f"{x['id']} 선지 길이 산포 {sp:.2f} > 0.25 — {L}")

    print(f"  ▸ 배치 요약 — 유형 {sorted({w['type'] for x in items for w in x['distractors']})}")
    if bad:
        print('\n  ❌ 저작 점검 실패')
        for b in bad:
            print('     ·', b)
        raise SystemExit(1)
    print('  ✅ 저작 점검 통과')



if __name__ == '__main__':
    items = build()
    local_checks(items)
    T.verify(items)
    T.merge_and_house(items)
