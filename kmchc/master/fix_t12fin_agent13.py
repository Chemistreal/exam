# -*- coding: utf-8 -*-
"""T12 마감 14제(M01957~M01970) 층5 순회 — 13차 조치 · 마감.

12차 순회 결과
  defender    **0 건** — 정답 붕괴 0 · 오답 승격 0. 앞선 회차에 지적된 다섯
              문항의 탈출구가 모두 막혔음을 확인.
  solver      문항 **내부** 결함 0 건. 남은 둘은 문항 **사이** 누출.
  factchecker 사실 오류 0 · 계산 오류 0 · 번호 모순 0. 남은 일곱은 표현·조건 층위.

13차는 그 아홉을 닫는다. 새 설계는 없다.

────────────────────────────────────────────────────────────────
㉠ M01961 ④ — 밖에서 무효화되고, ③ 과 포함 관계다 (solver · 중대)
────────────────────────────────────────────────────────────────
두 가지가 겹쳤다.
  (가) M01963 발문의 "발머 계열의 **첫 네 선**" 과 M01970 발문의 발머 선
       **다섯 개 나열(397 포함)** 이 "눈에 보이는 네 선이 전부" 를 밖에서
       거짓으로 확정한다. 문항을 읽지 않고도 ④ 가 지워진다.
  (나) ④ ⇒ ③ 이다(네 선이 전부라면 개수는 유한하고 가장 짧은 선도 있다).
       단일정답 규칙만으로 둘 다 탈락하고, 남은 ①·② 는 정반대 서술이라
       사실상 2지선다가 된다.

→ ④ 를 개수와 무관한 명제로 바꾼다. '선 사이의 간격이 어디서나 거의 같아
눈금자처럼 고르다' — 준위 간격이 균일하다는 고전적 오개념이고, ①(짧은 쪽으로
촘촘) · ②(짧은 쪽으로 넓어짐) 과 함께 간격 문제의 세 갈래를 이룬다. ③ 은
개수와 극한이라는 다른 축이라 포함 관계가 없다.

★교훈★ 오답 넷을 짤 때 **서로 포함 관계인 쌍이 없는지** 보아야 한다. 포함
관계가 있으면 내용을 몰라도 둘이 함께 지워진다. (ㄱ)의 형제 조항으로 올린다.
그리고 11차에서 ④ 를 B 덫으로 넣을 때 **같은 배치의 다른 두 발문이 이미 그
사실을 말하고 있다**는 것을 보지 못했다 — 새 선지를 넣을 때도 누출 검사를
다시 돌려야 한다.

────────────────────────────────────────────────────────────────
㉡ M01967 → M01963 — 상수 다리로 계산이 우회된다 (solver · 중대)
────────────────────────────────────────────────────────────────
9차에서 M01967 을 계산형으로 바꾸며 발문에 "이온화 에너지 k … **k 에 해당하는
빛의 파장은 91.1 nm**" 를 넣었다. M01963 은 "이온화 에너지를 1312 kJ/mol 이라
할 때" 로 시작한다. 두 발문을 이으면 1312 kJ/mol ↔ 91.1 nm 가 서고,

    1312 × 91.1 / 410 = 291.5

한 번의 나눗셈으로 M01963 이 끝난다. 그 문항이 재려던 '410 nm 를 6 → 2 로
동정하는' 단계가 통째로 우회된다.

→ M01967 의 발문을 **에너지를 거치지 않는 파장 관계식**으로 바꾼다.

    1/λ = (1/91.1 nm) × (1/m² − 1/n²)

계열 한계는 n → ∞ 이므로 λ = 91.1 × m², 파셴은 91.1 × 9 = 819.9 nm 로 답도
오답 셋도 그대로다. 발문에서 에너지와 k 가 사라지므로 1312 kJ/mol 과 이어지는
다리가 끊긴다.

★교훈★ 문항을 계산형으로 바꾸면 **발문에 상수가 하나 더 들어간다.** 그
상수가 다른 문항의 상수와 같은 물리량을 가리키면 그 순간 두 문항이 이어진다.
계산형 전환은 누출 검사를 반드시 다시 통과해야 한다.

받아들이지 않은 것 — solver 는 M01964 도 "발문이 에너지가 커진다고 알려 주니
c = λν 하나로 답이 확정된다" 고 했으나, ④(λ↑ ν↓)는 c = λν 를 **지킨다**.
④ 를 지우려면 E = hν 가 따로 필요하다. 두 관계가 다 있어야 하므로 유지한다.

────────────────────────────────────────────────────────────────
㉢ factchecker 일곱 — 표현·조건 층위
────────────────────────────────────────────────────────────────
1. M01959 자가진단 "보존은 아직 유효하다" — 본문과 ③ 반박은 "화학 반응에서"
   라는 조건을 달았는데 요약 줄만 무조건형이다. 핵반응에서는 원자가 생기고
   없어진다. → 조건 복원.
2. M01970 자가진단 "파장이 짧을수록 출발 준위가 높다" — 도착이 고정된 **한
   계열 안에서만** 성립한다. 파셴 한계 820 nm 와 발머 6→2 의 410 nm 가 곧바로
   반례다. → "한 계열 안에서는" 을 붙인다.
3. M01963 "발머 계열 한계(∞ → 2)" — M01961 이 "한계는 극한이지 선이 아니다"
   라고 못 박은 바로 그 자리를 화살표 전이처럼 적었다. 세 해설의 어법을
   맞춘다.
4. M01962 "나머지 셋을 왜 할 수 있는지" 라고 예고하고 넷을 들었다. 넷째('선의
   무늬')는 선지에 없어 판정에 기여하지 않는 헛도는 근거다. → 뺀다.
   (11차에서 '교재에 실린 까닭' 단정을 걷어내며 남긴 꼬리다.)
5. M01961 "1/n² 은 9분의 1 · 16분의 1 · 25분의 1" — n 을 넷 들고 분수는 셋만
   적었다. → 1/36 을 채운다.
6. M01968 "몰당 250 kJ 보다 작지 않아야 하는데" — 이 문항은 '못 끊는 쪽/끊는
   쪽' 역방향 함정을 이미 안고 있어 이중 부정이 겹친다. → 직서로.
7. M01960 "여기서 흔히 걸리는 자리가 둘 있어" 뒤의 ∞ 행 설명이 같은 문단
   앞부분의 비교를 수치까지 되풀이한다. → 앞을 남기고 뒤를 축약.
   (11차에서 지시어를 값 비교로 바꾸며 만든 중복이다.)

★이 배치가 남긴 것★ 8차부터 13차까지, 매 회차의 조치가 다음 회차의 지적을
하나씩 만들었다. 8차 → M01964 누출 · 9b → M01966 번호 충돌 · 9차 → M01963
우회 다리 · 11차 → M01962 꼬리와 M01960 중복. **수정은 국소적이지만 문항은
집합으로 읽힌다.** T13 에서는 조치 뒤 누출 검사를 별도 단계로 세운다.
"""
import json

BANK = 'master/master_bank.json'
MK = ['①', '②', '③', '④']

bank = json.load(open(BANK, encoding='utf-8'))
by = {x['id']: x for x in bank}


def parts(it):
    lines = it['solution'].split('\n')
    lead = lines[0]
    diag = [L for L in lines if L.startswith('자가진단:')][0]
    a = it['answer']
    head = f'[정답] {MK[a]} {it["choices"][a]} — '
    hit = [L for L in lines if L.startswith('[정답] ')]
    assert len(hit) == 1 and hit[0].startswith(head), it['id']
    cor = hit[0][len(head):]
    reb = {}
    for k in range(4):
        if k == a:
            continue
        pfx = f'{MK[k]} {it["choices"][k]}: '
        h = [L for L in lines if L.startswith(pfx)]
        assert len(h) == 1, (it['id'], k)
        reb[k] = h[0][len(pfx):]
    return lead, cor, reb, diag


def emit(it, lead, cor, reb, diag):
    a = it['answer']
    body = [lead, '', f'[정답] {MK[a]} {it["choices"][a]} — {cor}', '']
    body += [f'{MK[k]} {it["choices"][k]}: {reb[k]}' for k in range(4) if k != a]
    body += ['', diag]
    it['solution'] = '\n'.join(body)


def sub(it, old, new, n=1):
    assert it['solution'].count(old) == n, f"{it['id']}: '{old[:40]}' {it['solution'].count(old)}회"
    it['solution'] = it['solution'].replace(old, new)


def field(it, key, old, new):
    assert it[key].count(old) == 1, (it['id'], key)
    it[key] = it[key].replace(old, new)


def dmeta(it, idx, error=None, typ=None):
    for dd in it['distractors']:
        if dd['opt'] == idx:
            if error:
                dd['error'] = error
            if typ:
                dd['type'] = typ
            return
    raise AssertionError(f"{it['id']}: distractor {idx} 없음")


def retext(it, idx, new_txt, new_reb=None):
    assert idx != it['answer'], it['id']
    lead, cor, reb, diag = parts(it)
    it['choices'][idx] = new_txt
    if new_reb is not None:
        reb[idx] = new_reb
    emit(it, lead, cor, reb, diag)


def audit(it):
    lead, cor, reb, diag = parts(it)
    a = it['answer']
    for k in range(4):
        if k == a:
            continue
        for tail in ('가 옳아', '이 옳아', '가 맞아', '이 맞아'):
            assert MK[k] + tail not in cor, f"{it['id']}: 정답 본문에 {MK[k]}{tail}"
    assert (MK[a] + '가 옳아' in cor) or (MK[a] + '이 옳아' in cor), f"{it['id']}: 정답 번호 미표시"


def g3(it):
    L, a = [len(c) for c in it['choices']], it['answer']
    assert not (L[a] == max(L) and L.count(max(L)) == 1), f"{it['id']}: G3 유일최장"
    assert not (L[a] == min(L) and L.count(min(L)) == 1), f"{it['id']}: G3 유일최단"
    s = sorted(L)
    if (s[1] + s[2]) / 2 >= 8:
        sp = (s[3] - s[0]) / ((s[1] + s[2]) / 2)
        assert sp <= 0.25, f"{it['id']}: G3b 산포 {sp:.2f}"
    return L, sorted(range(4), key=lambda i: -L[i]).index(a) + 1


# ── ㉠ M01961 : ④ 를 개수와 무관한 명제로 · 분수 짝 맞춤 ─────────────
it = by['M01961']
retext(it, 3, '선 사이의 간격이 어디서나 거의 같아 눈금자처럼 고르다',
       '준위가 눈금자처럼 고르게 놓여 있다고 본 거야. 준위 에너지는 −k/n² 이라 '
       'n 이 커질수록 간격이 급히 좁아지지. 그래서 선도 짧은 파장 쪽으로 갈수록 '
       '빽빽해져 — 고를 수가 없어.')
dmeta(it, 3, error='준위가 균일한 간격으로 놓여 있다고 보아 선도 고르게 늘어선다고 함', typ='scale')
sub(it, 'n 을 3 · 4 · 5 · 6 … 으로 키우면 1/n² 은 9분의 1 · 16분의 1 · 25분의 1 로 빠르게 작아지지.',
    'n 을 3 · 4 · 5 · 6 … 으로 키우면 1/n² 은 9분의 1 · 16분의 1 · 25분의 1 · 36분의 1 로 '
    '빠르게 작아지고, 줄어드는 폭 자체도 함께 작아지지.')
sub(it, '눈에 보이는 네 선은 그 무한한 줄 가운데 파장이 긴 쪽 넷일 뿐이고, 그보다 짧은 쪽으로도 '
        '선은 계속 이어져. 준위가 위로 갈수록 촘촘해진다는 사실 하나가 이 모습을 다 만들어 내는 거야.',
    '눈에 보이는 넷은 그 무한한 줄 가운데 파장이 긴 쪽일 뿐이고, 그보다 짧은 쪽으로도 선은 '
    '계속 이어져. 준위가 위로 갈수록 촘촘해진다는 사실 하나가 이 모습을 다 만들어 내는 거야.')
field(it, 'answer_proof', '눈에 보이는 네 선은 그 가운데 긴 쪽 넷일 뿐이다',
      '선 사이의 간격은 고르지 않고 짧은 파장 쪽으로 갈수록 좁아진다')

# ── ㉡ M01967 : 발문을 파장 관계식으로 — 1312 과의 다리를 끊는다 ─────
it = by['M01967']
it['stem'] = ('수소의 선스펙트럼에서 두 준위 사이의 파장은 1/λ = (1/91.1 nm) × (1/m² − 1/n²) '
              '로 주어진다. 파셴 계열(도착 준위 3)의 선들이 모여드는 끝자리 파장은 얼마인가?')
sub(it, '이제 에너지와 파장을 잇자. 괄호가 1 일 때 에너지가 k 이고 그 빛의 파장이 91.1 nm '
        '라고 했지. 파장은 에너지에 반비례하니까 에너지가 괄호 값만큼 줄면 파장은 그만큼 '
        '늘어나. 곧 파장은 91.1 을 괄호 값으로 나눈 것이고, 나누는 것이 1/9 이니 91.1 에 '
        '9 를 곱하는 셈이지.',
    '이제 식에 넣자. 1/λ 이 1/91.1 곱하기 괄호 값이니, λ 는 91.1 을 괄호 값으로 나눈 것이야. '
    '나누는 것이 1/9 이니 91.1 에 9 를 곱하는 셈이지.')
sub(it, '② 약 273.3 nm: 91.1 에 3 을 곱한 값이야. 도착 준위가 3 인 것은 맞게 봤는데 괄호가 '
        '1/3 이 아니라 1/3² 곧 1/9 이라는 것을 놓쳤어. 에너지는 n 의 제곱에 걸려 있거든.',
    '② 약 273.3 nm: 91.1 에 3 을 곱한 값이야. 도착 준위가 3 인 것은 맞게 봤는데 괄호가 '
    '1/3 이 아니라 1/3² 곧 1/9 이라는 것을 놓쳤어. 식의 괄호 안은 준위 번호의 제곱이거든.')
field(it, 'answer_proof', '괄호가 1 일 때의 파장이 91.1 nm 이고 파장은 에너지에 반비례하므로 '
                          '계열 한계 파장은 91.1 ÷ (1/m²) = 91.1 × m² 이다',
      '1/λ 이 1/91.1 에 괄호 값을 곱한 것이므로 계열 한계 파장은 91.1 ÷ (1/m²) = 91.1 × m² 이다')
assert 'k' not in it['stem'] and '에너지' not in it['stem'], 'M01967: 발문에 에너지 다리가 남음'

# ── ㉢ factchecker 일곱 ──────────────────────────────────────────────
sub(by['M01959'], '자가진단: 질량이 같다 = 동위원소가 뒤집음. 보존은 아직 유효하다.',
    '자가진단: 질량이 같다 = 동위원소가 뒤집음. 화학 반응에서는 보존이 아직 유효하다.')

sub(by['M01970'], '자가진단: 왼쪽이 짧은 쪽. 파장이 짧을수록 출발 준위가 높다.',
    '자가진단: 왼쪽이 짧은 쪽. 한 계열 안에서는 파장이 짧을수록 출발 준위가 높다.')

sub(by['M01963'], '이것은 발머 계열 한계(∞ → 2)의 에너지라 이 네 선보다 크지.',
    '이것은 출발 준위를 한없이 올린 극한값, 곧 발머 계열 한계라 이 네 선보다 크지.')
sub(by['M01963'], '그 값은 출발을 ∞ 로 보낸 발머 계열 한계라 이 네 선 어느 것보다도 크지.',
    '그 값은 출발 준위를 한없이 올렸을 때 다가가는 극한, 곧 발머 계열 한계라 이 네 선 '
    '어느 것보다도 크지.')

sub(by['M01962'], '어느 원소가 더 짧은 파장 쪽에 선을 내는지도 왼쪽인지 오른쪽인지만 보면 '
                  '알 수 있고. 원소마다 선의 무늬가 다르다는 것도 눈금 없이 읽히는 쪽이야.',
    '어느 원소가 더 짧은 파장 쪽에 선을 내는지도 왼쪽인지 오른쪽인지만 보면 알 수 있고.')

sub(by['M01968'], '결합을 끊으려면 몰당 250 kJ 보다 작지 않아야 하는데',
    '결합을 끊으려면 몰당 250 kJ 이상이어야 하는데')

sub(by['M01960'], '여기서 흔히 걸리는 자리가 둘 있어. 하나는 ∞ 행이야. ∞ 와 6 사이는 '
                  'k/36 이라 작아 보이지만 0.0278 k 이고, 5 와 6 사이는 0.0122 k 로 그보다 '
                  '더 작지. 표에서 두 끝을 찾을 때는 식이 커지고 작아지는 방향을 먼저 정하면 '
                  '칸을 다 세지 않아도 돼. 다른 하나는',
    '여기서 흔히 걸리는 자리가 둘 있어. 하나는 앞에서 본 ∞ 행이고, 표에서 두 끝을 찾을 '
    '때는 식이 커지고 작아지는 방향을 먼저 정하면 칸을 다 세지 않아도 돼. 다른 하나는')

# ── 검사 ─────────────────────────────────────────────────────────────
for n in range(1957, 1971):
    it = by[f'M0{n}']
    audit(it)
    L, r = g3(it)
    assert len(it['solution']) >= 300, it['id']
    if n in (1960, 1961, 1967):
        print(f'  {it["id"]} 정답 {MK[it["answer"]]} · 길이 {L} · 순위 {r} · 해설 {len(it["solution"])}자')

json.dump(bank, open(BANK, 'w', encoding='utf-8'), ensure_ascii=False, indent=1)
print('13차 조치 완료 — 마감 14제 닫음')
