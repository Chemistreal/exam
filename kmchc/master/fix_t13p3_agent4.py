# -*- coding: utf-8 -*-
"""T13 P3 — 4차 조치 (3차 순회 · 정밀도만. 선지 셋은 뜻을 바꾸지 않고 못 박는 손질).

    solver      ★답이 바뀌거나 복수정답이 되는 결함 열 문항 어디에도 없음★
                3차에 손댄 네 자리를 모두 '적절·부작용 없음' 으로 판정.
                M01998 ① 주어 삽입은 "F1 유일성이 확보되었다" 고 실효를 확인.
                남은 넷은 요령성(모순쌍·절대어법·표기 이탈) 경미.
    defender    ★진짜 복수정답 후보 0건★ 3차 수정 두 건 모두 '해결'.
                남은 것은 하 3 · 그중 둘만 값싸게 닫힌다.
    factchecker ★사실 오류 1건★ (M01992 ② 반박 — 내 2차 수정이 만든 것) + △ 7.
    sim         A 0 / B 0 / ★C 8★ / D 10 · D 오작동 0.

★세 회차 연속 같은 무늬 — 조치가 다음 회차의 지적을 만든다★
  1차 조치 → 2차에 중 2 / 2차 조치 → 3차에 사실 오류 1 + 매달린 절 1.
  다만 등급은 계속 내려간다(F2 1 → 0 → 0, 사실 오류 2 → 0 → 1[문면]).
  이번 회차는 **선지의 뜻을 바꾸지 않고 못 박기만** 한다. 새 오답을 짓지 않는다.

────────────────────────────────────────────────────────────────
㉠ 사실 오류 — M01992 ② 반박 (factchecker · 필수)
────────────────────────────────────────────────────────────────
"l 로 비교하는 것은 껍질이 같을 때만이야. 껍질이 다르면 먼저 n 을 봐야 해." 에서
뒤 문장에 한정어가 없어 **일반 규칙처럼 읽힌다.** 다전자 원자에서는 껍질이 달라도
n 만으로 정해지지 않는다(4s < 3d). ★게다가 M01991 이 "n+l 이 작은 것부터" 를
가르치므로 두 해설이 같은 사실을 서로 다르게 적고 있었다.★ 계를 못 박는다.

────────────────────────────────────────────────────────────────
㉡ 선지 셋 — 뜻은 그대로, 빠져나갈 구멍만 막는다
────────────────────────────────────────────────────────────────
· M01998 ④ (defender 하) — ① 에는 '구리' 를 박고 ④ 에는 안 박은 비대칭이 남아,
  주어를 **크로뮴**으로 놓으면(3d⁴4s² → 3d⁵4s¹, 홀전자 4 → 6) 참이 된다.
  같은 처방을 대칭으로 적용한다. ③ 은 길이를 맞추려 어미만 고쳤다(뜻 불변).
· M01993 ④ (defender 하) — 'l 이 방향의 **가짓수**(2l+1)를 정한다' 는 참이라
  "l 이 있어야 방향 구별이 가능하다" 는 느슨한 독법이 아주 조금 남았다.
  '방향을 구별해 주는 것' → '서로 구별해 주는 양자수' 로 주체를 mₗ 에 못 박는다.
· M01996 ③ (solver 경미) — 넷 중 ② 만 유보를 단 중도 진술이라 'hedged option =
  정답' 요령에 걸렸다. 2차에 ④ 를 유보형으로 바꿨는데도 ④ 는 단정으로 끝나
  어조가 고르지 않았다. ③ 도 유보형으로 맞춰 2:2 로 만든다.
  ▸ 오답 경로는 그대로다 — 여전히 l ≤ n 오적용(n = 4 에 g 가 있다)이 핵심이다.

────────────────────────────────────────────────────────────────
㉢ factchecker △ 일곱 (전부 해설 문면)
────────────────────────────────────────────────────────────────
· M01991 '부껍질 번호' → '부양자수 l' 로 용어 통일(M01993·M01996 과 어긋났다)
  + 구리가 **예외 배치**임을 한 마디 — 이 문항이 가르치는 차례대로면 Cu 는
  4s²3d⁹ 로 예측되므로, 그 한 마디가 없으면 문항 안에서 모순으로 읽힌다.
· M01992 4f 문장을 참고로 낮춘다(고교생이 확인할 수 없는 근거).
· M01994 '고립된 원자 안에서는' 한정 두 곳 + '넷째 전자' → '넷째 2p 전자'
  (전체 넷째 전자는 2s 전자다).
· M01995 's 오비탈은 하나뿐' → '2s 는 오비탈이 하나뿐'.
· M01997 "[Ar] 만 적는 것은 옳은 표기" 한 줄 — 앞 문장이 표기를 금지하는 것으로
  오독될 여지를 마저 없앤다.
· M01999 ★매달린 이유절★ — "…곧 들뜬 상태라서 답이 아니지" 가 **② 가 들뜬
  상태여서 답이 아니다** 로 읽힌다(사실과 반대). 두 문장으로 끊는다.
  + '흡수해서 나타나는 배치가 아니야' 는 발문에 없는 낱말이라 기준어로 바꾸고,
  + 도입부가 둘만 들고 "셋을 갈라야" 라 했으므로 셋을 명시한다.

────────────────────────────────────────────────────────────────
★받아들이지 않은 것 — 셋 다 sim 의 죽은 선지 수리안★
────────────────────────────────────────────────────────────────
㉮ **M01998 ② (sim 3차)** — ★sim 이 이 문항에 세 회차 연속 제안을 냈다★
   (1차 ④ · 2차 ② · 3차 ②). 멈춤 규칙이 말하는 되풀이 신호다. 게다가 sim 스스로
   "세 개 중 회수 기대가 가장 낮다" 고 적었고, defender 는 세 회차 내내
   "'언제나' 를 빼면 즉시 F2" 라 했다. **F2 우선으로 현행 유지.**
㉯ **M01994 ② (sim 3차)** — 진단은 날카롭다. 내 3차 문안이 "왜 홀전자 배치가
   낮은가" 라는 물음의 **답이 되는 꼴이 아니라서**(논리적 유형 불일치) 오개념
   보유자도 튕겨낸다는 것이다. 그러나 sim 안 '같은 부껍질 안에서도 s, p, d 순으로'
   는 ★'부껍질' 을 '껍질' 로 한 글자 바꿔 읽으면 참★ 이 되어(3s < 3p < 3d)
   defender 가 세 회차 내리 잡아 온 바로 그 무늬다. 그리고 이 ② 는 defender 가
   3차에 "해결" 로 못 박은 자리다 — **F2 를 풀어 매력을 사지 않는다.**
   ▸ ★구조적으로 좁은 자리로 기록한다★ — 이 물음의 까닭 자리는 파울리(①)·
     반발(정답 ③)·스핀(④)이 이미 가져갔고, 남은 넷째 인과로 F2 없이 설 수 있는
     것이 마땅치 않다. 개념 대장 C13-006 주로 남긴다.
㉰ **M01997 ② (sim 3차)** — 제안 '[Ne] 3s² 3p⁶ 4s²' 는 16 자라 네 선지가 12 자로
   나란한 이 문항의 **G3b 산포를 0.33 으로 깨뜨린다**(한도 0.25). 길이를 맞추면서
   같은 착지점을 주는 대안을 찾지 못했다.
▸ 공통 근거 — sim 스스로 2차에 "4 인 시뮬에서 문항당 미배정 1 개는 구조적" 이라
  적었다. 죽은 선지 하나는 실측 0.422 구간이고 0 개(0.446)와 거의 같다.

★기록만 하는 것★ M01997 ③(defender 하→중) — defender 스스로 "복수정답은
성립하지 않는다 · 명백히 거짓" 이라 적었으므로 F2 가 아니라 이의제기 소지다.
solver 는 지적 없음, sim 은 "가장 깨끗한 C-트랩" 으로 꼽았고 2차에 실제로 C 를
잡았다. 대체안은 착지점을 잃거나 G3b 를 깨뜨린다. 셋 중 둘이 유지 쪽이다.
"""
import json
import os
import sys
from collections import Counter

BASE = os.path.dirname(os.path.abspath(__file__))
sys.path.insert(0, BASE)
import batch_template as T                                    # noqa: E402

BANK = 'master/master_bank.json'
MK = ['①', '②', '③', '④']

bank = json.load(open(BANK, encoding='utf-8'))
by = {x['id']: x for x in bank}
before_ans = {i: by[f'M0{i}']['answer'] for i in range(1991, 2001)}
untouched = {i: json.dumps(by[f'M0{i}']['choices'], ensure_ascii=False)
             for i in (1991, 1992, 1994, 1995, 1997, 1999, 2000)}


def sub(it, old, new, n=1):
    assert it['solution'].count(old) == n, f"{it['id']}: '{old[:40]}' {it['solution'].count(old)}회"
    it['solution'] = it['solution'].replace(old, new)


def retext(it, idx, new, error=None):
    """선지 하나의 문면만 갈고 해설 안의 같은 문자열도 함께 바꾼다."""
    old = it['choices'][idx]
    assert new not in it['choices'], (it['id'], new)
    it['choices'][idx] = new
    assert len(set(it['choices'])) == 4, it['id']
    n = it['solution'].count(old)
    assert n >= 1, f"{it['id']}: 해설에 옛 선지 문면이 없다"
    it['solution'] = it['solution'].replace(old, new)
    if error is not None:
        for d in it['distractors']:
            if d['opt'] == idx:
                d['error'] = error


# ══ M01991 ── 용어 통일 + 구리가 예외임을 한 마디 (factchecker △) ═══════
sub(by['M01991'],
    '원소로 보면 3d 가 처음 열 개로 꽉 차는 것은 구리이지만 아연까지는 3d 블록이 이어지고, '
    '그 아연 다음 원소인 갈륨에서 비로소 4p 에 전자가 들어가기 시작해. 참고로 이 차례는 '
    '껍질 번호와 부껍질 번호를 더한 값이 작은 것부터, 값이 같으면 껍질 번호가 작은 것부터라고 '
    '외워도 같은 결과가 나와.',
    '원소로 보면 3d 가 처음 열 개로 꽉 차는 것은 구리인데 구리는 예외 배치라 한 칸 먼저 찬 '
    '것이고, 3d 블록은 아연까지 이어져. 그 아연 다음 원소인 갈륨에서 비로소 4p 에 전자가 '
    '들어가기 시작하지. 참고로 이 차례는 주양자수 n 과 부양자수 l 을 더한 값이 작은 것부터, '
    '값이 같으면 n 이 작은 것부터라고 외워도 같은 결과가 나와.')
sub(by['M01991'], '3d 는 3+2 로 5, 4p 는 4+1 로 5 인데 껍질 번호가 3 인 3d 가 먼저 오지.',
    '3d 는 3+2 로 5, 4p 는 4+1 로 5 인데 n 이 3 인 3d 가 먼저 오지.')

# ══ M01992 ── ★사실 오류★ ② 반박에 계를 못 박는다 + △ ═══════════════
sub(by['M01992'],
    'l 로 비교하는 것은 껍질이 같을 때만이야. 껍질이 다르면 먼저 n 을 봐야 해.',
    'l 로 비교하는 것은 껍질이 같을 때만이야. 게다가 전자가 하나인 수소에서는 l 이 에너지에 '
    '아예 관여하지 않으니, 껍질이 다른 둘은 n 만 보면 돼. 다전자 원자라면 껍질이 달라도 n 만으로 '
    '정해지지 않아 — 4s 가 3d 보다 낮은 것이 그 예야.')
sub(by['M01992'],
    '실제로 4f 를 채우기 시작하는 원소들에서는 5s 가 4f 보다 낮지만, 수소에서는 그 순서가 '
    '뒤집힌다.',
    '다전자 원자에서 잰 차례가 5s 를 먼저 부르는 것도 그 때문이지만, 수소에서는 그 순서가 '
    '뒤집힌다.')

# ══ M01993 ── ④ 구별 주체를 mₗ 에 못 박는다 (defender 하) ═══════════════
retext(by['M01993'], 3, '같은 부껍질에 든 오비탈들을 서로 구별해 주는 양자수가 l 이다',
       error='l 과 mₗ 을 뒤바꿔 외움 — 부껍질 안에서 오비탈을 가르는 것은 자기양자수다')

# ══ M01994 ── 고립 원자 한정 + '넷째 2p 전자' (factchecker △) ═══════════
sub(by['M01994'], '같은 부껍질의 오비탈들은 에너지가 서로 같아.',
    '고립된 원자 안에서는 같은 부껍질의 오비탈들이 에너지가 서로 같아.')
sub(by['M01994'], '산소의 넷째 전자에서 처음 짝이 생기는 것도 이 때문이지.',
    '산소의 넷째 2p 전자에서 처음 짝이 생기는 것도 이 때문이지.')
sub(by['M01994'], '같은 부껍질의 세 p 오비탈은 방향만 다르고 에너지가 똑같아.',
    '고립된 원자에서 같은 부껍질의 세 p 오비탈은 방향만 다르고 에너지가 똑같아.')

# ══ M01995 ── 어느 s 인지 밝힌다 (factchecker △) ════════════════════════
sub(by['M01995'], 's 오비탈은 하나뿐이고 두 자리가 다 찼으니 홀전자가 없어',
    '2s 는 오비탈이 하나뿐이고 그 두 자리가 다 찼으니 홀전자가 없어')

# ══ M01996 ── ③ 도 유보형으로 맞춰 어조를 고른다 (solver 경미) ══════════
retext(by['M01996'], 2, '주껍질 번호가 4 이면 g 오비탈이 있으나 채워지지는 않는다',
       error='l ≤ n 으로 잘못 적용 — l < n 이므로 l = 4 에는 n ≥ 5 가 필요하다')

# ══ M01997 ── [Ar] 만 적는 것은 옳다는 한 줄 (factchecker △) ════════════
sub(by['M01997'], '[Ar] 안에 이미 그것이 들어 있으니까.',
    '[Ar] 안에 이미 그것이 들어 있으니까. 심지를 바꾸려면 뒤를 비우고 [Ar] 이라고만 '
    '적어야 하고, 그렇게 적은 것은 옳은 표기야.')

# ══ M01998 ── ④ 에도 주어를 박는다 (defender 하) + ③ 길이 맞춤 ══════════
retext(by['M01998'], 2, '3d 가 하나도 남김없이 채워진 배치가 특히 안정하기 때문에')
retext(by['M01998'], 3, '구리에서 전자가 옮겨 가면 홀전자가 늘어 훈트에 맞기 때문에',
       error='구리에서는 4s²3d⁹ 도 4s¹3d¹⁰ 도 홀전자가 똑같이 1 개라 늘지 않는다')
sub(by['M01998'], '이때 3d 는 정원 열을 남김없이 채운 상태가 되지.',
    '이때 3d 는 정원 열을 하나도 남김없이 채운 상태가 되지.')

# ══ M01999 ── 매달린 이유절·기준어·'셋' 명시 (factchecker △) ════════════
x = by['M01999']
sub(x, '"바닥상태가 아니다" 와 "있을 수 없다" 는 아주 다른 말이야. 셋을 갈라야 해.',
    '"있을 수 없다" 와 "바닥상태다" 와 "들뜬 상태다" 는 서로 다른 말이야. 셋을 갈라야 해.')
sub(x, '문제가 찾는 것은 규칙을 다 지키면서 바닥상태만 아닌 것, 곧 들뜬 상태라서 답이 아니지.',
    '문제가 찾는 것은 규칙을 다 지키면서 바닥상태만 아닌 것, 곧 들뜬 상태야. ②는 바닥상태 '
    '그 자체라서 답이 아니지.')
sub(x, '규칙은 다 지켰어. 다만 이것이 질소의 바닥상태라서, 흡수해서 나타나는 배치가 아니야.',
    '규칙은 다 지켰어. 다만 이것이 질소의 바닥상태 그 자체라서 들뜬 상태가 아니야.')

# ══ 검사 ═══════════════════════════════════════════════════════════════
seq = [by[f'M0{i}']['answer'] for i in range(1991, 2001)]
assert Counter(seq) == Counter({0: 3, 1: 3, 2: 2, 3: 2}), dict(Counter(seq))
for i in range(1991, 2001):
    z = by[f'M0{i}']
    assert z['answer'] == before_ans[i], f'M0{i}: 정답 자리가 바뀌었다'
    if i in untouched:
        assert json.dumps(z['choices'], ensure_ascii=False) == untouched[i], \
            f'M0{i}: 손대지 않기로 한 문항인데 선지가 바뀌었다'
    assert len(z['choices']) == 4 and len(set(z['choices'])) == 4, z['id']
    assert {d['opt'] for d in z['distractors']} == {0, 1, 2, 3} - {z['answer']}, z['id']
    assert len(z['solution']) >= 300, (i, len(z['solution']))
    assert '★' not in z['solution'] and '★' not in z['stem'], f'M0{i}: G10 강조 표기'
    L = [len(c) for c in z['choices']]
    a = z['answer']
    assert not (L[a] == max(L) and L.count(max(L)) == 1), (i, 'G3 유일최장', L)
    assert not (L[a] == min(L) and L.count(min(L)) == 1), (i, 'G3 유일최단', L)
    s = sorted(L)
    if (s[1] + s[2]) / 2 >= 8:
        sp = (s[3] - s[0]) / ((s[1] + s[2]) / 2)
        assert sp <= 0.25, (i, f'G3b {sp:.2f}', L)
    head = z['solution'].split('\n\n')[1]
    assert (MK[a] + '가 옳아' in head) or (MK[a] + '이 옳아' in head), z['id']
    for k in range(4):
        assert (MK[k] + ' ' + z['choices'][k]) in z['solution'], (z['id'], k)
    assert z.get('objective'), z['id']

# 이번에 못 박은 것들을 문면으로 검산
assert '4s 가 3d 보다 낮은 것이 그 예야' in by['M01992']['solution']
assert '구리에서' in by['M01998']['choices'][3], 'M01998 ④ 에 주어가 없다'
assert '언제나' in by['M01998']['choices'][1], "M01998 ② 의 '언제나' 가 사라졌다"
assert '양자수가 l 이다' in by['M01993']['choices'][3]
assert '있으나 채워지지는 않는다' in by['M01996']['choices'][2]
assert '②는 바닥상태 그 자체라서 답이 아니지' in by['M01999']['solution']
assert '고립된 원자' in by['M01994']['solution']
assert '주양자수 n 과 부양자수 l 을 더한 값' in by['M01991']['solution']

json.dump(bank, open(BANK, 'w', encoding='utf-8'), ensure_ascii=False, indent=1)
print('4차 조치 완료 — 사실 오류 1 · 선지 못 박기 3(뜻 불변) · 해설 △ 7')
print('  받지 않은 것 3(sim 죽은 선지 수리안 · 근거는 머리말) · 정답 자리 전부 불변')
