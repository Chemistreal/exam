# -*- coding: utf-8 -*-
"""T16 마감 4제 (M02951~M02954) — 단위와 뿌리와 말버릇을 한자리에서 묶는다

  ★에이전트 초안 → render_batch.py 로 옮겨 적음★ — 문면은 초안 그대로다.
"""
import re

import batch_template as T

T.START_ID = 'M02951'
T.COUNT = 4
T.EXPECT_LEN = 2950
T.THEME = '전자친화도·전기음성도'
T.TT = 16
T.UNIT = 'I'
T.BATCH_NOTE = "T16 마감 4제 — 단위와 뿌리와 말버릇을 한자리에서 묶는다."


def q(i, sk, tr, dok, df, esr, stem, cs, ans, proof, wr, dev, calc, scen, obj,
      lead, prose, wmap, diag):
    x = T.mk(i, sk, tr, dok, df, esr, stem, cs, ans, proof, wr, dev, calc, scen, obj=obj)
    T.sol(x, lead, prose, wmap, diag)
    return x


def build():
    it = []

    it.append(q("M02951", "값에 붙은 단위를 보고 어느 성질의 값인지 되짚기",
        "일반", 2, "쉬움", 0.58,
        "염소의 두 성질을 각각 349 kJ/mol과 3.0으로 적어 두었다. 이 두 수에 대한 설명으로 옳은 것은?",
        ["숫자가 큰 쪽이 전기 음성도를 나타낸다",
         "전기 음성도도 에너지 단위를 붙여 적는다",
         "단위가 붙은 쪽이 전자 친화도 값이다",
         "표기가 달라 두 값은 뿌리도 다르다"], 2,
        "349 kJ/mol은 에너지 단위를 달고 있으므로 기체 원자가 전자 하나를 받을 때 내놓는 에너지인 전자 친화도이다.",
        [("숫자가 큰 쪽이 전기 음성도를 나타낸다", "수의 크기만 보고 값의 이름을 바꿔 붙였다", "surface"),
         ("전기 음성도도 에너지 단위를 붙여 적는다", "견주어 매긴 값에 에너지 단위를 달았다", "unit"),
         ("표기가 달라 두 값은 뿌리도 다르다", "적는 꼴이 다르니 까닭도 다르다고 이었다", "causal")],
        "두 수 옆에 단위가 붙었는지부터 갈라 적기",
        "349 kJ/mol → 에너지 단위 있음 → 전자 친화도 / 3.0 → 단위 없음 → 전기 음성도 / ① 큰 수 349에 kJ/mol이 붙었으니 그쪽이 전자 친화도 / ② 전기 음성도는 견주어 매긴 값이라 단위가 없음 / ③ 옳음 / ④ 두 값 모두 유효 핵전하와 반지름에서 나와 뿌리는 같음",
        "염소 자료에 나란히 적힌 349 kJ/mol과 3.0", "단위의 있고 없음만으로 전자 친화도와 전기 음성도를 갈라내는지 확인한다.",
        "두 수 옆에 단위가 있는지부터 표시해 적어 봐.",
        "단위부터 갈라 보면 길이 열려. 349 kJ/mol은 에너지 단위를 달고 있으니 기체 원자가 전자 하나를 받을 때 내놓는 에너지, 곧 전자 친화도야. 3.0은 단위 없이 견주어 매긴 수이니 전기 음성도지 — ③이 옳아. ①은 숫자가 큰 쪽이 전기 음성도를 나타낸다라고 했는데, 큰 수 349에는 kJ/mol이 붙어 있으니 그쪽이 전자 친화도야. ②는 전기 음성도도 에너지 단위를 붙여 적는다라고 했지만, 전기 음성도는 당기는 힘을 서로 견주어 매긴 값이라 단위가 없어. ④는 표기가 달라 두 값은 뿌리도 다르다라고 했는데, 두 값 모두 유효 핵전하가 크고 반지름이 작을수록 대체로 커지니 뿌리는 오히려 같아. 단위가 붙었으면 에너지, 단위가 없으면 견줌이야.",
        {"숫자가 큰 쪽이 전기 음성도를 나타낸다": "큰 수에 단위가 붙었어",
         "전기 음성도도 에너지 단위를 붙여 적는다": "견준 값이라 단위 없어",
         "표기가 달라 두 값은 뿌리도 다르다": "뿌리는 둘 다 같아"},
        "단위가 붙었는지부터 살펴 값의 이름을 되짚었는가?"))

    it.append(q("M02952", "발문이 떼는 값을 묻는지 얻는 값을 묻는지 가려내기",
        "일반", 3, "중간", 0.44,
        "기체 상태에서 원자 하나가 전자를 받아들이는 과정을 다룬 자료가 있다. 이 자료가 다루는 값에 대한 설명으로 옳은 것은?",
        ["공유 전자쌍을 당기는 힘을 견준 값이다",
         "이웃과 떨어진 채로 재는 전자 친화도이다",
         "전자를 떼어 낼 때 드는 에너지를 가리킨다",
         "에너지를 흡수해야 전자가 달라붙는다"], 1,
        "자료가 다루는 것은 기체 원자가 전자를 얻는 과정이므로 이 값은 이웃과 떨어진 채로 재는 전자 친화도이다.",
        [("공유 전자쌍을 당기는 힘을 견준 값이다", "결합한 자리에서 매기는 값을 끌어다 놓았다", "surface"),
         ("전자를 떼어 낼 때 드는 에너지를 가리킨다", "얻는 과정을 떼는 과정으로 뒤집어 읽었다", "proc"),
         ("에너지를 흡수해야 전자가 달라붙는다", "내놓는 에너지를 들어오는 쪽으로 돌렸다", "sign")],
        "자료가 받는 이야기인지 떼는 이야기인지부터 갈라 적기",
        "받는 과정 → 전자 친화도 / 떼는 과정 → 이온화 에너지 / 결합한 자리 → 전기 음성도 / ① 결합한 두 원자 사이의 값이라 재는 자리가 다름 / ② 옳음 / ③ 떼는 데 드는 값은 이온화 에너지 / ④ 전자 친화도는 얻을 때 내놓는 에너지로 매긴 값",
        "기체 원자 하나가 전자를 받아들이는 자료의 값 고르기", "얻는 과정을 다룬 자료에서 전자 친화도를 골라내고 이웃한 두 값과 갈라 보는지 확인한다.",
        "자료가 전자를 떼는지 얻는지 한 낱말로 적어 봐.",
        "먼저 자료가 어느 쪽 이야기인지 갈라야 해. 전자를 받아들이는 과정이고 원자는 기체 상태로 홀로 있으니, 여기서 재는 값은 전자 친화도야 — ②가 옳아. ①은 공유 전자쌍을 당기는 힘을 견준 값이다라고 했는데, 그것은 결합한 두 원자 사이에서 매기는 전기 음성도라 재는 자리가 아예 달라. ③은 전자를 떼어 낼 때 드는 에너지를 가리킨다라고 했지만, 떼는 쪽은 이온화 에너지야. ④는 에너지를 흡수해야 전자가 달라붙는다라고 했는데, 전자 친화도는 전자를 얻을 때 내놓는 에너지로 매긴 값이라 방향이 반대야. 떼는지 얻는지, 홀로인지 결합했는지를 먼저 갈라라.",
        {"공유 전자쌍을 당기는 힘을 견준 값이다": "그건 결합한 자리 값",
         "전자를 떼어 낼 때 드는 에너지를 가리킨다": "떼는 값은 이온화야",
         "에너지를 흡수해야 전자가 달라붙는다": "얻을 때 내놓는 값"},
        "떼는 값과 얻는 값을 발문에서 먼저 갈라냈는가?"))

    it.append(q("M02953", "뿌리가 같은 값들의 순위가 어긋나는 자리 짚기",
        "일반", 3, "중간", 0.41,
        "전기 음성도는 불소가 4.0으로 가장 큰데, 전자 친화도는 염소가 349 kJ/mol로 불소의 328 kJ/mol보다 크다. 이에 대한 설명으로 옳은 것은?",
        ["뿌리가 같아도 첫째 원소는 서로 다르다",
         "반지름이 작아질수록 두 값이 모두 작아진다",
         "뿌리가 같으므로 두 값의 순위도 나란하다",
         "불소가 좁아서 전자 친화도가 더 크다"], 0,
        "두 값은 유효 핵전하와 껍질 수라는 같은 뿌리를 쓰지만 전기 음성도의 첫째는 불소이고 전자 친화도의 첫째는 염소이므로 첫째 원소는 서로 다르다.",
        [("반지름이 작아질수록 두 값이 모두 작아진다", "커지는 쪽과 작아지는 쪽을 거꾸로 적었다", "sign"),
         ("뿌리가 같으므로 두 값의 순위도 나란하다", "뿌리가 같다는 말을 순위까지 늘려 잡았다", "overgen"),
         ("불소가 좁아서 전자 친화도가 더 크다", "좁으면 무조건 크다고 까닭을 잘못 이었다", "causal")],
        "두 값에서 첫째를 차지한 원소를 각각 적기",
        "전기 음성도 첫째 → 불소 4.0 / 전자 친화도 첫째 → 염소 349 kJ/mol / 두 값의 뿌리 → 유효 핵전하와 껍질 수로 같음 / ① 옳음 / ② 반지름이 작을수록 전기 음성도는 커져 모두 작아진다가 깨짐 / ③ 불소와 염소에서 이미 순위가 뒤집힘 / ④ 불소는 좁아 들어온 전자가 밀쳐 328로 349보다 작음",
        "불소 4.0과 염소 349 kJ/mol이 서로 다른 첫째를 차지한 자리", "같은 뿌리를 쓰는 값이라도 순위까지 같지는 않음을 짚는지 확인한다.",
        "두 값에서 첫째를 차지한 원소를 각각 적어 봐.",
        "두 값은 유효 핵전하와 껍질 수라는 같은 뿌리를 쓰지만, 자료가 보여 주듯 전기 음성도의 첫째는 불소이고 전자 친화도의 첫째는 염소야 — ①이 옳아. ②는 반지름이 작아질수록 두 값이 모두 작아진다라고 했는데, 반지름이 작을수록 핵이 전자를 더 세게 당겨 전기 음성도는 오히려 커져. ③은 뿌리가 같으므로 두 값의 순위도 나란하다라고 했지만, 불소와 염소에서 이미 순위가 뒤집히고 18족에서는 전기 음성도를 매기지도 않아. ④는 불소가 좁아서 전자 친화도가 더 크다라고 했는데, 불소는 좁은 만큼 들어온 전자가 다른 전자와 밀쳐 349보다 작은 328이 나와. 뿌리가 같다는 말이 값까지 같다는 말은 아니야.",
        {"반지름이 작아질수록 두 값이 모두 작아진다": "전기 음성도는 커져",
         "뿌리가 같으므로 두 값의 순위도 나란하다": "첫째부터 이미 뒤집혀",
         "불소가 좁아서 전자 친화도가 더 크다": "좁아서 밀치니 작아져"},
        "뿌리가 같다는 말과 순위가 같다는 말을 갈라 두었는가?"))

    it.append(q("M02954", "경향 문장에 단서를 달아 단정형을 피하기",
        "일반", 3, "중간", 0.47,
        "같은 주기에서 원자 번호가 커질 때 전자 친화도가 어떻게 변하는지 한 문장으로 적으려 한다. 이 문장을 적는 방법으로 옳은 것은?",
        ["경향만 알면 원소마다 견줄 까닭이 없다",
         "예외가 없으니 그냥 커진다고 단정해도 된다",
         "맨 오른쪽 원소가 가장 크다고 적는다",
         "대체로 커진다고 적고 예외 자리를 밝힌다"], 3,
        "같은 주기에서 전자 친화도는 2족과 18족에서 뚝 떨어지므로 대체로 커진다고 적고 예외 자리를 밝히는 문장이 옳다.",
        [("경향만 알면 원소마다 견줄 까닭이 없다", "경향이 있으면 확인이 필요 없다고 이었다", "causal"),
         ("예외가 없으니 그냥 커진다고 단정해도 된다", "예외를 지운 채 경향을 통째로 늘려 잡았다", "overgen"),
         ("맨 오른쪽 원소가 가장 크다고 적는다", "주기 표의 자리만 보고 끝을 첫째로 삼았다", "surface")],
        "경향에서 벗어나는 족이 있는지부터 적기",
        "같은 주기 오른쪽 → 전자 친화도 대체로 증가 / 2족과 18족 → 새 전자가 한 칸 높은 자리로 가 뚝 떨어짐 / 17족 → 가장 큼 / ① 예외가 있어 원소별 확인이 필요 / ② 2족과 18족이라는 예외가 있음 / ③ 맨 오른쪽 18족은 값을 사실상 매기지 않음 / ④ 옳음",
        "같은 주기 전자 친화도 경향을 한 문장으로 옮겨 적는 자리", "경향을 적을 때 대체로와 예외 자리를 함께 남기는지 확인한다.",
        "경향에서 벗어나는 족을 먼저 적어 봐.",
        "같은 주기에서 오른쪽으로 갈수록 전자 친화도는 대체로 커지지만, 2족과 18족에서는 새로 들어올 전자가 한 칸 높은 자리로 가야 해서 값이 뚝 떨어져. 그러니 경향을 옮겨 적을 때는 대체로라는 말과 예외 자리를 함께 남겨야 해 — ④가 옳아. ①은 경향만 알면 원소마다 견줄 까닭이 없다라고 했는데, 예외가 있으니 원소별로 값을 확인해야 해. ②는 예외가 없으니 그냥 커진다고 단정해도 된다라고 했지만, 2족과 18족이 바로 그 예외야. ③은 맨 오른쪽 원소가 가장 크다고 적는다라고 했는데, 맨 오른쪽 18족은 값을 사실상 매기지 않고 17족이 가장 커. 경향은 대체로 서는 말이니 단서 없이 단정하지 마.",
        {"경향만 알면 원소마다 견줄 까닭이 없다": "예외 있어 확인 필요",
         "예외가 없으니 그냥 커진다고 단정해도 된다": "2족과 18족이 예외",
         "맨 오른쪽 원소가 가장 크다고 적는다": "18족 아닌 17족이야"},
        "경향 문장에 대체로와 예외 자리를 함께 적었는가?"))

    return it


_ANON = re.compile(r'원소 (?:가|나|다|A|B|C)(?=의|와|과|는|은|를|이|가|,|·|\s|$)')


def local_checks(items):
    """T15 지역 저작 점검 — P3 의 것을 그대로 잇는다(⑧ 겹침 · ⑨ 길이 포함)."""
    bad = []
    ALLOWED = {'proc', 'sign', 'surface', 'conserv', 'unit', 'scale', 'overgen', 'causal'}
    for x in items:
        ts = [w['type'] for w in x['distractors']]
        if len(set(ts)) < 3:
            bad.append(f"{x['id']} 오답 유형이 겹친다 — {ts}")
        for t in ts:
            if t not in ALLOWED:
                bad.append(f"{x['id']} 허용되지 않은 유형 {t}")
        # ★★이 검사는 P3 3차까지 한 번도 울 수 없었다★★ — sol() 이 해설 끝에 붙이는
        #   단평 블록이 오답 문면을 ★글자 그대로★ 되풀이하므로 `in x['solution']` 은 언제나
        #   참이었다. 해설 산문에서 인용이 흐트러져도 검사는 통과한다(defender 와
        #   factchecker 가 각각 두 자리를 손으로 잡아 냈다). ▸ ★산문 문단만 본다.★
        prose = x['solution'].split('\n\n')[1] if '\n\n' in x['solution'] else x['solution']
        #   ▸ 산문은 간접 인용이라 종결어미가 바뀐다('…때문이다' → '…때문이라고 했어').
        #     ★어미를 뗀 몸통★ 이 그대로 있는지만 본다 — 몸통이 어긋나면 인용이 흐트러진 것이다.
        for w in x['distractors']:
            core = re.sub(r'(이)?다$', '', x['choices'][w['opt']]).strip()
            if core not in prose:
                bad.append(f"{x['id']} {w['opt'] + 1}번 오답 인용이 해설 산문과 어긋난다")
        if not re.search(r'[①②③④](가|이)? 옳아', x['solution']):
            bad.append(f"{x['id']} 해설에 정답 확정 어구가 없다")
        J = ('를|을|은|의|으로|로|에서|에|과|보다|처럼|까지|부터|이고|이다|이야|이라|이지')
        for f in ('stem', 'solution', 'answer_proof'):
            for m in re.finditer(rf'(?<=[가-힣]) ({J})(?=[\s,.·)?!]|$)', x[f]):
                bad.append(f"{x['id']} {f} 조사 앞 빈칸 — {x[f][max(0, m.start() - 14):m.end()]!r}")
        tails = [c.strip()[-3:] for c in x['choices']]
        for t in set(tails):
            if tails.count(t) == 3 and tails[x['answer']] != t:
                bad.append(f"{x['id']} 어미 「{t}」 셋 대 정답 하나 — 꼴로 갈린다")

        def tk(s):
            return {w for w in re.findall(r'[가-힣]{2,}', s)}
        for i in range(4):
            for j in range(i + 1, 4):
                if x['answer'] in (i, j):
                    continue
                a, b = tk(x['choices'][i]), tk(x['choices'][j])
                if a and b and len(a & b) / len(a | b) >= 0.62:
                    bad.append(f"{x['id']} 오답 {i + 1}·{j + 1} 자카드 "
                               f"{len(a & b) / len(a | b):.2f}")
    for x in items:
        s = x['stem']
        if '톱니' in s and '세로축' not in s:
            bad.append(f"{x['id']} 톱니를 쓰면서 세로축을 적지 않았다")
        anon = _ANON.search(s)
        named = re.search(r'(리튬|베릴륨|붕소|탄소|질소|산소|플루오린|네온|나트륨|마그네슘|염소'
                          r'|칼륨|알루미늄|헬륨|수소|아르곤|브로민|Li|Be|B\b|C\b|N\b|O\b|F\b|Ne|Na)', s)
        if anon and named:
            bad.append(f"{x['id']} 익명 원소와 원소 이름을 함께 인쇄했다 — "
                       f"{anon.group(0)!r} · {named.group(0)!r}")
        if '크게 뛰' in s or '급증' in s:
            if not re.search(r'비\(뒤 ÷ 앞\)|비로 재|앞까지의 개수', s):
                bad.append(f"{x['id']} 급증의 셈법을 발문이 정하지 않았다")
    g = sum(1 for x in items if x['track'] == '일반')
    lo = 7 if len(items) >= 10 else max(2, len(items) // 2)
    if g < lo:
        bad.append(f'일반 {g}제 — {len(items)}문항 배치의 하한은 {lo}')
    e = sum(1 for x in items if x['difficulty'] == '쉬움')
    if e > 3:
        bad.append(f'쉬움 {e}제 — 상한 3')

    # ⑧ ★정답이 발문과의 어휘 겹침 유일 최다이면 실패★ (P1 이 값을 치른 자리)
    for x in items:
        st = set(re.findall(r'[0-9A-Za-z가-힣]{2,}', x['stem']))
        ov = [len(set(re.findall(r'[0-9A-Za-z가-힣]{2,}', c)) & st) for c in x['choices']]
        if ov[x['answer']] == max(ov) and ov.count(max(ov)) == 1:
            bad.append(f"{x['id']} 정답이 발문과의 어휘 겹침 최다(유일) — {ov}")

    # ⑨ ★선지 길이 — 정답이 유일 최장/최단이거나 산포가 0.25 를 넘으면 실패★ (P3 이 값을 치렀다)
    for x in items:
        L = [len(c) for c in x['choices']]
        a = L[x['answer']]
        if a == max(L) and L.count(a) == 1:
            bad.append(f"{x['id']} 정답이 유일 최장 — {L}")
        if a == min(L) and L.count(a) == 1:
            bad.append(f"{x['id']} 정답이 유일 최단 — {L}")
        sp = (max(L) - min(L)) / (sum(L) / len(L))
        if sp > 0.25:
            bad.append(f"{x['id']} 선지 길이 산포 {sp:.2f} > 0.25 — {L}")

    print(f"  ▸ 배치 요약 — 유형 {sorted({w['type'] for x in items for w in x['distractors']})}")
    if bad:
        print('\n  ❌ 저작 점검 실패')
        for b in bad:
            print('     ·', b)
        raise SystemExit(1)
    print('  ✅ 저작 점검 통과')



if __name__ == '__main__':
    items = build()
    local_checks(items)
    T.verify(items)
    T.merge_and_house(items)
