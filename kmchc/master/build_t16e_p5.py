# -*- coding: utf-8 -*-
"""T16 P5 (M02831~M02840) — 값을 견주기 전에 무엇을 재고 무엇을 뺐는지 적는다

  각도: C16-001[8] · 002[6] · 003[4] · 004[8] · 005[5] · 009[1] · 010[2] · 013[8] ·
        017[2] · 019[8]

  ★정답 자리 3·4·2·1·3·1·4·2·4·2★ (앞 꼬리 4-1-3-4-3-2-1-3 을 이어 G3f 를 간격 5 까지 셈했다)
  병합 뒤 ①710②710③710④710 — ★편차 0★

  ★길이 규칙★ 3위: 831 · 833 · 835 · 837 · 839 / 2위: 832 · 834 · 836 · 838 · 840
  ▸ 다 지은 뒤 `python3 lenplan.py build_t16e_p5` 로 확인한다(동률은 통계에서 빠진다).
"""
import re

import batch_template as T

T.START_ID = 'M02831'
T.COUNT = 10
T.EXPECT_LEN = 2830
T.THEME = '전자친화도·전기음성도'
T.TT = 16
T.UNIT = 'I'
T.BATCH_NOTE = 'T16 P5 — 값을 견주기 전에 무엇을 재고 무엇을 뺐는지 적는다.'


def q(i, sk, tr, dok, df, esr, stem, cs, ans, proof, wr, dev, calc, scen, obj,
      lead, prose, wmap, diag):
    x = T.mk(i, sk, tr, dok, df, esr, stem, cs, ans, proof, wr, dev, calc, scen, obj=obj)
    T.sol(x, lead, prose, wmap, diag)
    return x


def build():
    it = []

    # ── M02831 ── C16-001[8] 정의에서 빠뜨리기 쉬운 낱말 (일반·쉬움) 정답 ③ [3위] ──
    it.append(q('M02831', '전자 친화도의 정의에서 빠뜨리기 쉬운 낱말을 짚기',
        '일반', 2, '쉬움', 0.56,
        '어떤 학생이 전자 친화도를 "원자가 전자를 얻을 때의 에너지"라고만 적었다. 이 정의에서 '
        '빠진 것으로 옳은 것은?',
        ['전자를 몇 개 얻는지가 빠져 있다',
         '어떤 원소인지가 빠져 있는 것이다',
         '기체 상태라는 말이 빠져 있다',
         '에너지라는 낱말이 빠져 있다'], 2,
        '전자 친화도는 따로 떨어진 기체 상태의 원자를 두고 재는 값이므로 상태를 적지 않으면 어디서 '
        '잰 값인지가 흐려진다',
        [('전자를 몇 개 얻는지가 빠져 있다', '적힌 것을 빠졌다고 봄', 'conserv'),
         ('어떤 원소인지가 빠져 있는 것이다', '정의에 들지 않는 것을 찾음', 'proc'),
         ('에너지라는 낱말이 빠져 있다', '이미 적힌 낱말을 다시 찾음', 'surface')],
        '정의에 꼭 들어가야 할 조건을 하나씩 세어 보기',
        '들어가야 할 것: 기체 상태 · 원자 하나 · 전자 하나 · 방출하는 에너지 / 적힌 것: 원자 · '
        '전자 · 에너지 → 빠진 것은 상태 / ① 하나임은 짐작됨 / ② 정의는 원소를 가리지 않음 / '
        '④ 적혀 있음',
        '상태를 적지 않은 정의 한 줄', '정의의 조건을 세어 보는지 확인',
        '정의에 꼭 들어가야 할 조건을 하나씩 세어 봐.',
        '전자 친화도는 기체 상태의 원자에서 재는 값이야. 그 말이 빠졌지 — ③이 옳아. '
        '①은 전자를 몇 개 얻는지가 빠져 있다 라고 했어. 하나임은 짐작이 되잖아. '
        '②는 어떤 원소인지가 빠져 있는 것이다 라고 했어. 정의는 원소를 가리지 않아. '
        '④는 에너지라는 낱말이 빠져 있다 라고 했어. 이미 적혀 있지. '
        '정의를 볼 때는 들어가야 할 조건을 하나씩 센다.',
        {'전자를 몇 개 얻는지가 빠져 있다': '짐작이 돼.',
         '어떤 원소인지가 빠져 있는 것이다': '가리지 않아.',
         '에너지라는 낱말이 빠져 있다': '적혀 있어.'},
        '조건을 하나씩 세었는가?'))

    # ── M02832 ── C16-002[6] 부호 붙은 값끼리 견주기 (일반·중간) 정답 ④ [2위] ──
    it.append(q('M02832', '부호가 붙은 값끼리 견줄 때 절댓값을 함께 보기',
        '일반', 3, '중간', 0.38,
        '한 표에 −328 과 +73 이 나란히 적혀 있었다. 어느 쪽이 전자를 더 잘 받아들이는지 '
        '가리려 한다. 옳은 것은?',
        ['숫자가 큰 쪽인 328 쪽이 잘 받는다',
         '부호를 떼고 큰 쪽만 고르면 된다',
         '두 값은 견줄 수가 없는 짝이다',
         '부호를 먼저 보고 방향부터 가른다'], 3,
        '부호가 다르면 방출과 흡수라는 다른 방향을 가리키므로 크기를 견주기 전에 어느 쪽이 방출인지 '
        '방향부터 갈라야 한다',
        [('숫자가 큰 쪽인 328 쪽이 잘 받는다', '방향을 보지 않고 크기만 봄', 'scale'),
         ('부호를 떼고 큰 쪽만 고르면 된다', '방향을 지우고 크기만 남김', 'unit'),
         ('두 값은 견줄 수가 없는 짝이다', '견줄 수 있는 길을 지움', 'conserv')],
        '두 값의 부호가 각각 무엇을 가리키는지 먼저 적기',
        '부호가 다름 → 한쪽은 방출, 한쪽은 흡수 → 방향이 다름 / 방향을 가른 뒤에야 크기를 견줌 / '
        '① 방향을 안 봄 / ② 방향이 사라짐 / ③ 방향을 가르면 견줄 수 있음',
        '부호가 엇갈린 두 값', '크기보다 방향을 먼저 보는지 확인',
        '두 값의 부호가 각각 무엇을 가리키는지 먼저 적어 봐.',
        '부호가 다르면 방향이 다른 거야. 방향부터 갈라야지 — ④가 옳아. '
        '①은 숫자가 큰 쪽인 328 쪽이 잘 받는다 라고 했어. 방향을 보지 않았어. '
        '②는 부호를 떼고 큰 쪽만 고르면 된다 라고 했어. 방향이 사라지잖아. '
        '③은 두 값은 견줄 수가 없는 짝이다 라고 했어. 방향을 가르면 견줄 수 있어. '
        '부호가 엇갈리면 크기보다 방향을 먼저 본다.',
        {'숫자가 큰 쪽인 328 쪽이 잘 받는다': '방향을 안 봤어.',
         '부호를 떼고 큰 쪽만 고르면 된다': '방향이 사라져.',
         '두 값은 견줄 수가 없는 짝이다': '가르면 견줘.'},
        '부호가 가리키는 것을 적었는가?'))

    # ── M02833 ── C16-003[4] 가려막기 (일반·중간) 정답 ② [3위] ──
    it.append(q('M02833', '가려막기가 커지면 붙드는 힘이 약해짐을 잇기',
        '일반', 3, '중간', 0.40,
        '안쪽 껍질의 전자가 늘어나면 바깥으로 들어오는 전자가 느끼는 힘이 달라진다. 그 '
        '까닭으로 옳은 것은?',
        ['안쪽 전자가 핵을 더욱 세게 만든다',
         '안쪽 전자가 핵의 힘을 가려 준다',
         '안쪽 전자는 바깥과 상관이 없다',
         '안쪽 전자가 무게만 늘려 준다'], 1,
        '안쪽 껍질의 전자들이 핵과 바깥 전자 사이에 끼어 핵의 끌어당김을 가리므로, 안쪽 전자가 '
        '많을수록 바깥에서 느끼는 힘이 줄어든다',
        [('안쪽 전자가 핵을 더욱 세게 만든다', '가리는 쪽을 세게 하는 쪽으로 뒤집음', 'sign'),
         ('안쪽 전자는 바깥과 상관이 없다', '가려막기가 있음을 지움', 'conserv'),
         ('안쪽 전자가 무게만 늘려 준다', '힘 이야기를 무게로 바꿈', 'surface')],
        '핵과 바깥 전자 사이에 무엇이 끼어 있는지 적기',
        '안쪽 껍질의 전자: 핵과 바깥 전자 사이에 낌 → 핵의 끌어당김을 가림 → 바깥에서 느끼는 힘 '
        '줄어듦 / ① 거꾸로 / ③ 가려막기가 있음 / ④ 무게가 아님',
        '안쪽 전자가 늘어난 어떤 원자', '가려막기의 구실을 아는지 확인',
        '핵과 바깥 전자 사이에 무엇이 끼어 있는지 적어 봐.',
        '안쪽 전자가 핵과 바깥 전자 사이에 끼어 끌어당김을 가려 — ②가 옳아. '
        '①은 안쪽 전자가 핵을 더욱 세게 만든다 라고 했어. 거꾸로야. '
        '③은 안쪽 전자는 바깥과 상관이 없다 라고 했어. 가려막기가 있잖아. '
        '④는 안쪽 전자가 무게만 늘려 준다 라고 했어. 무게 이야기가 아니지. '
        '힘이 달라지면 그 사이에 무엇이 끼었는지 적는다.',
        {'안쪽 전자가 핵을 더욱 세게 만든다': '거꾸로야.',
         '안쪽 전자는 바깥과 상관이 없다': '가려막기가 있어.',
         '안쪽 전자가 무게만 늘려 준다': '무게가 아냐.'},
        '사이에 낀 것을 적었는가?'))

    # ── M02834 ── C16-004[8] 두 경향을 겹쳐 읽지 않기 (심화·어려움) 정답 ① [2위] ──
    it.append(q('M02834', '전자 친화도의 경향을 이온화 에너지의 경향과 겹쳐 읽지 않기',
        '심화', 4, '어려움', 0.26,
        '어떤 학생이 "이온화 에너지가 오른쪽으로 커지니 전자 친화도도 똑같이 커진다" 라고 '
        '적었다. 이 말에 대해 옳은 것은?',
        ['방향은 비슷해도 벗어난 자리가 다르다',
         '두 경향은 언제나 똑같이 함께 움직인다',
         '두 경향은 서로 반대로 움직인다',
         '전자 친화도에는 경향이 아예 없다'], 0,
        '두 값 모두 오른쪽으로 대체로 커지지만 벗어나는 자리가 서로 달라서, 한쪽의 경향을 그대로 '
        '옮겨 적으면 그 자리에서 틀린다',
        [('두 경향은 언제나 똑같이 함께 움직인다', '닮은 자리를 모든 자리로 넓힘', 'overgen'),
         ('두 경향은 서로 반대로 움직인다', '방향까지 뒤집어 버림', 'sign'),
         ('전자 친화도에는 경향이 아예 없다', '있는 경향을 지움', 'conserv')],
        '두 값이 각각 어디에서 경향을 벗어나는지 적기',
        '이온화 에너지·전자 친화도: 오른쪽으로 대체로 커짐(방향은 닮음) / 벗어나는 자리는 서로 '
        '다름 → 그대로 옮기면 틀림 / ② 언제나는 아님 / ③ 반대는 아님 / ④ 경향은 있음',
        '두 경향을 겹쳐 읽은 미룸', '닮음과 같음을 가르는지 확인',
        '두 값이 각각 어디에서 경향을 벗어나는지 적어 봐.',
        '방향은 닮았지만 벗어나는 자리가 서로 달라. 그대로 옮기면 그 자리에서 틀리지 '
        '— ①이 옳아. '
        '②는 두 경향은 언제나 똑같이 함께 움직인다 라고 했어. 언제나는 아니야. '
        '③은 두 경향은 서로 반대로 움직인다 라고 했어. 반대도 아니지. '
        '④는 전자 친화도에는 경향이 아예 없다 라고 했어. 경향은 있어. '
        '두 경향을 겹쳐 읽기 전에 벗어나는 자리를 견준다.',
        {'두 경향은 언제나 똑같이 함께 움직인다': '언제나는 아냐.',
         '두 경향은 서로 반대로 움직인다': '반대도 아냐.',
         '전자 친화도에는 경향이 아예 없다': '경향은 있어.'},
        '벗어나는 자리를 견주었는가?'))

    # ── M02835 ── C16-005[5] 예외를 까닭으로 적기 (일반·중간) 정답 ③ [3위] ──
    it.append(q('M02835', '예외를 외우는 대신 까닭으로 적어 두기',
        '일반', 3, '중간', 0.40,
        '경향에서 벗어난 자리를 노트에 적어 두려 한다. 적는 방법으로 옳은 것은?',
        ['벗어난 원소의 이름만 죽 적어 둔다',
         '벗어난 값을 소수점 자리까지 적어 둔다',
         '벗어난 까닭을 한 줄로 적어 둔다',
         '벗어난 자리는 그냥 지나쳐 둔다'], 2,
        '까닭을 적어 두면 다른 자리에서 비슷한 일이 생겨도 같은 잣대로 풀 수 있지만 이름이나 값만 '
        '적어 두면 그 자리에서만 쓸 수 있다',
        [('벗어난 원소의 이름만 죽 적어 둔다', '외울 목록으로 바꿔 놓음', 'proc'),
         ('벗어난 값을 소수점 자리까지 적어 둔다', '쓸모없는 자릿수를 옮겨 적음', 'scale'),
         ('벗어난 자리는 그냥 지나쳐 둔다', '적어 둘 값이 있음을 지움', 'conserv')],
        '적어 둔 것이 다른 자리에서도 쓰이는지 따져 보기',
        '까닭을 적음 → 다른 자리에서도 같은 잣대로 풀림 / 이름·값만 적음 → 그 자리에서만 쓰임 / '
        '① 외울 목록 / ② 자릿수는 쓸모없음 / ④ 적어 둘 값이 있음',
        '경향에서 벗어난 자리 한둘', '적을 것을 까닭에 두는지 확인',
        '적어 둔 것이 다른 자리에서도 쓰이는지 따져 봐.',
        '까닭을 적어 두면 다른 자리에서도 같은 잣대로 풀려 — ③이 옳아. '
        '①은 벗어난 원소의 이름만 죽 적어 둔다 라고 했어. 외울 목록이 될 뿐이야. '
        '②는 벗어난 값을 소수점 자리까지 적어 둔다 라고 했어. 자릿수는 쓸모가 없지. '
        '④는 벗어난 자리는 그냥 지나쳐 둔다 라고 했어. 적어 둘 값이 있잖아. '
        '적어 둘 것을 고를 때는 다른 자리에서도 쓰이는지 따진다.',
        {'벗어난 원소의 이름만 죽 적어 둔다': '외울 목록이야.',
         '벗어난 값을 소수점 자리까지 적어 둔다': '쓸모가 없어.',
         '벗어난 자리는 그냥 지나쳐 둔다': '적어 둘 값 있어.'},
        '다른 자리에서 쓰이는지 따졌는가?'))

    # ── M02836 ── C16-009[1] 첫째와 둘째 값을 갈라 적기 (일반·중간) 정답 ① [2위] ──
    it.append(q('M02836', '첫 번째 전자 친화도와 두 번째 값을 갈라 적기',
        '일반', 2, '중간', 0.46,
        '어떤 표에 산소의 전자 친화도가 하나만 적혀 있었다. 이 값이 무엇을 가리키는지 정하려 '
        '한다. 옳은 것은?',
        ['첫 번째 전자를 받을 때의 값이다',
         '두 전자를 받을 때 값을 더해 둔다',
         '두 번째 전자를 받은 값이다',
         '두 값 사이의 한가운데를 잡는다'], 0,
        '따로 밝히지 않고 전자 친화도라고 적으면 첫 번째 전자를 받아들일 때의 값을 가리키며, 두 '
        '번째 값은 따로 이름을 붙여 적는다',
        [('두 전자를 받을 때 값을 더해 둔다', '두 값을 하나로 합쳐 봄', 'scale'),
         ('두 번째 전자를 받은 값이다', '첫째와 둘째를 맞바꿈', 'sign'),
         ('두 값 사이의 한가운데를 잡는다', '없는 셈을 만들어 붙임', 'proc')],
        '표에 따로 이름이 붙어 있는지부터 살피기',
        '따로 밝히지 않음 → 첫 번째 값 / 두 번째 값은 이름을 따로 붙임 / ② 더한 값이 아님 / '
        '③ 첫째가 먼저 / ④ 가운데를 잡지 않음',
        '값이 하나만 적힌 산소 칸', '첫째와 둘째를 가르는지 확인',
        '표에 따로 이름이 붙어 있는지부터 살펴 봐.',
        '따로 밝히지 않았으면 첫 번째 값이야. 둘째는 이름을 따로 붙이지 — ①이 옳아. '
        '②는 두 전자를 받을 때 값을 더해 둔다 라고 했어. 더한 값이 아니야. '
        '③은 두 번째 전자를 받은 값이다 라고 했어. 첫째가 먼저지. '
        '④는 두 값 사이의 한가운데를 잡는다 라고 했어. 그런 셈은 하지 않아. '
        '값이 하나만 적혀 있으면 무엇이 생략되었는지 살핀다.',
        {'두 전자를 받을 때 값을 더해 둔다': '더한 값이 아냐.',
         '두 번째 전자를 받은 값이다': '첫째가 먼저야.',
         '두 값 사이의 한가운데를 잡는다': '그런 셈 없어.'},
        '생략된 것을 살폈는가?'))

    # ── M02837 ── C16-010[2] 무엇을 기준으로 매겼는지 (심화·중간) 정답 ④ [3위] ──
    it.append(q('M02837', '전기 음성도의 값이 어떤 기준으로 매겨졌는지 적기',
        '심화', 3, '중간', 0.34,
        '전기 음성도 표에서 가장 큰 값이 4.0 으로 적혀 있었다. 이 숫자가 어떻게 정해졌는지 '
        '물었다. 옳은 것은?',
        ['재어 보니 정확히 그 값이 나왔다',
         '에너지를 그대로 옮겨 적은 값이다',
         '원자 하나에서 직접 잰 값이다',
         '기준을 정해 견주어 매긴 값이다'], 3,
        '전기 음성도는 어느 원소를 얼마로 둘지 기준을 먼저 정한 뒤 나머지를 그에 견주어 매긴 값이라, '
        '재어서 그대로 나온 숫자가 아니다',
        [('재어 보니 정확히 그 값이 나왔다', '매긴 값을 잰 값으로 봄', 'surface'),
         ('에너지를 그대로 옮겨 적은 값이다', '다른 값의 갈래를 가져옴', 'unit'),
         ('원자 하나에서 직접 잰 값이다', '결합한 자리를 홀로로 봄', 'conserv')],
        '이 값이 재어 나온 값인지 매긴 값인지 적기',
        '전기 음성도: 기준을 정하고 견주어 매김 → 매긴 값 / 재어 그대로 나온 숫자가 아님 / '
        '① 잰 값이 아님 / ② 에너지가 아님 / ③ 결합한 자리에서 견줌',
        '가장 큰 값이 4.0 인 표', '매긴 값과 잰 값을 가르는지 확인',
        '이 값이 재어 나온 값인지 매긴 값인지 적어 봐.',
        '전기 음성도는 기준을 정해 놓고 견주어 매긴 값이야 — ④가 옳아. '
        '①은 재어 보니 정확히 그 값이 나왔다 라고 했어. 재어 나온 숫자가 아니야. '
        '②는 에너지를 그대로 옮겨 적은 값이다 라고 했어. 에너지가 아니지. '
        '③은 원자 하나에서 직접 잰 값이다 라고 했어. 결합한 자리에서 견주잖아. '
        '숫자를 만나면 잰 값인지 매긴 값인지 먼저 적는다.',
        {'재어 보니 정확히 그 값이 나왔다': '잰 값이 아냐.',
         '에너지를 그대로 옮겨 적은 값이다': '에너지가 아냐.',
         '원자 하나에서 직접 잰 값이다': '결합한 자리야.'},
        '잰 값인지 매긴 값인지 적었는가?'))

    # ── M02838 ── C16-013[8] 준금속 자리 (일반·쉬움) 정답 ② [2위] ──
    it.append(q('M02838', '금속과 비금속 사이의 자리를 어떻게 다룰지 적기',
        '일반', 2, '쉬움', 0.56,
        '주기율표에서 금속과 비금속의 경계에 놓인 원소를 만났다. 전자 친화도를 어림하려 할 때 '
        '옳은 것은?',
        ['금속이라고 여기고 아주 작게 본다',
         '두 쪽 사이의 값으로 보아 둔다',
         '비금속이라 여기고 크게 잡는다',
         '경계에서는 값이 아예 없어진다'], 1,
        '경계에 놓인 원소는 금속의 성질과 비금속의 성질을 함께 보이므로 전자 친화도도 양쪽의 사이에 '
        '놓인 값으로 어림하는 편이 맞다',
        [('금속이라고 여기고 아주 작게 본다', '한쪽으로 몰아 못 박음', 'overgen'),
         ('비금속이라 여기고 크게 잡는다', '다른 한쪽으로 몰아 못 박음', 'sign'),
         ('경계에서는 값이 아예 없어진다', '있는 값을 없다고 함', 'conserv')],
        '이 원소가 어느 쪽 성질을 보이는지 둘 다 적기',
        '경계의 원소: 금속의 성질과 비금속의 성질을 함께 보임 → 값도 사이에 놓임 / ① 한쪽으로 '
        '몰음 / ③ 다른 쪽으로 몰음 / ④ 값은 있음',
        '경계에 놓인 어떤 원소', '경계를 사이 값으로 다루는지 확인',
        '이 원소가 어느 쪽 성질을 보이는지 둘 다 적어 봐.',
        '경계의 원소는 두 쪽 성질을 함께 보여. 값도 사이에 놓이지 — ②가 옳아. '
        '①은 금속이라고 여기고 아주 작게 본다 라고 했어. 한쪽으로 몰았어. '
        '③은 비금속이라 여기고 크게 잡는다 라고 했어. 다른 쪽으로 몰았지. '
        '④는 경계에서는 값이 아예 없어진다 라고 했어. 값은 있어. '
        '경계에 놓인 것은 한쪽으로 몰지 말고 사이에 둔다.',
        {'금속이라고 여기고 아주 작게 본다': '한쪽으로 몰았어.',
         '비금속이라 여기고 크게 잡는다': '다른 쪽으로 몰았어.',
         '경계에서는 값이 아예 없어진다': '값은 있어.'},
        '두 쪽 성질을 다 적었는가?'))

    # ── M02839 ── C16-017[2] 두 값을 한 표에 적는 규약 (일반·중간) 정답 ④ [3위] ──
    it.append(q('M02839', '두 값을 한 표에 나란히 적을 때의 규약 지키기',
        '일반', 3, '중간', 0.40,
        '전자 친화도와 전기 음성도를 한 표에 나란히 싣기로 했다. 표를 만드는 방법으로 '
        '옳은 것은?',
        ['두 값을 한 칸에 함께 적어 둔다',
         '단위가 없는 쪽만 골라서 싣는다',
         '두 값을 더해서 한 줄로 줄여 적는다',
         '칸을 나누고 머리말에 단위를 적는다'], 3,
        '두 값은 갈래도 단위도 다르므로 칸을 갈라 싣고 머리말에 각각 무엇의 값이며 단위가 무엇인지 '
        '적어 두어야 읽는 사람이 헷갈리지 않는다',
        [('두 값을 한 칸에 함께 적어 둔다', '갈라야 할 것을 한데 둠', 'conserv'),
         ('단위가 없는 쪽만 골라서 싣는다', '실어야 할 것을 버림', 'scale'),
         ('두 값을 더해서 한 줄로 줄여 적는다', '갈래가 다른 값을 셈으로 섞음', 'proc')],
        '두 값의 갈래와 단위가 같은지 먼저 적기',
        '전자 친화도: 에너지 · kJ/mol / 전기 음성도: 상대값 · 단위 없음 → 갈래도 단위도 다름 '
        '→ 칸을 갈라 싣고 머리말에 적음 / ① 헷갈림 / ② 버릴 수 없음 / ③ 더할 수 없음',
        '두 값을 함께 실으려는 표', '갈래가 다른 값을 갈라 싣는지 확인',
        '두 값의 갈래와 단위가 같은지 먼저 적어 봐.',
        '갈래도 단위도 다르니 칸을 갈라 싣고 머리말에 적어야 해 — ④가 옳아. '
        '①은 두 값을 한 칸에 함께 적어 둔다 라고 했어. 읽는 사람이 헷갈리지. '
        '②는 단위가 없는 쪽만 골라서 싣는다 라고 했어. 버릴 수 없는 값이야. '
        '③은 두 값을 더해서 한 줄로 줄여 적는다 라고 했어. 더할 수 있는 값이 아니야. '
        '표를 만들 때는 갈래가 다른 값을 갈라 싣는다.',
        {'두 값을 한 칸에 함께 적어 둔다': '헷갈려.',
         '단위가 없는 쪽만 골라서 싣는다': '버릴 수 없어.',
         '두 값을 더해서 한 줄로 줄여 적는다': '더할 수 없어.'},
        '갈래와 단위를 적었는가?'))

    # ── M02840 ── C16-019[8] 이온의 값과 원자의 값 (심화·어려움) 정답 ② [2위] ──
    it.append(q('M02840', '이온에서 잰 값과 원자에서 잰 값을 섞지 않기',
        '심화', 4, '어려움', 0.26,
        '어떤 학생이 원자 반지름 표와 이온 반지름 표를 한데 놓고 크기를 죽 늘어놓았다. 이 '
        '늘어놓음에 대해 옳은 것은?',
        ['같은 원소끼리라면 섞어도 괜찮은 일이다',
         '두 표는 잰 자리가 서로 다른 값이다',
         '이온 쪽이 언제나 더 작게 나온다',
         '두 표는 사실 같은 값을 적은 것이다'], 1,
        '원자에서 잰 값과 이온에서 잰 값은 전자의 수부터 다른 서로 다른 알갱이의 값이므로, 한 줄에 '
        '섞어 늘어놓으면 견줌이 어그러진다',
        [('같은 원소끼리라면 섞어도 괜찮은 일이다', '원소가 같으면 같다고 봄', 'surface'),
         ('이온 쪽이 언제나 더 작게 나온다', '음이온이 커지는 자리를 지움', 'sign'),
         ('두 표는 사실 같은 값을 적은 것이다', '다른 두 값을 하나로 뭉갬', 'conserv')],
        '두 표가 각각 무엇에서 잰 값인지 적기',
        '원자 반지름: 중성 원자에서 잼 / 이온 반지름: 전자를 주고받은 뒤에 잼 → 알갱이가 다름 '
        '→ 섞어 늘어놓지 못함 / ① 원소가 같아도 알갱이가 다름 / ③ 음이온은 커짐 / ④ 다른 값',
        '두 표를 한데 놓고 늘어놓은 자리', '잰 자리를 갈라 보는지 확인',
        '두 표가 각각 무엇에서 잰 값인지 적어 봐.',
        '하나는 중성 원자에서, 하나는 이온에서 잰 값이야. 알갱이가 다르지 — ②가 옳아. '
        '①은 같은 원소끼리라면 섞어도 괜찮은 일이다 라고 했어. 원소가 같아도 알갱이가 달라. '
        '③은 이온 쪽이 언제나 더 작게 나온다 라고 했어. 음이온은 오히려 커지지. '
        '④는 두 표는 사실 같은 값을 적은 것이다 라고 했어. 서로 다른 값이야. '
        '표를 겹쳐 놓기 전에 무엇에서 잰 값인지 적는다.',
        {'같은 원소끼리라면 섞어도 괜찮은 일이다': '알갱이가 달라.',
         '이온 쪽이 언제나 더 작게 나온다': '음이온은 커져.',
         '두 표는 사실 같은 값을 적은 것이다': '다른 값이야.'},
        '잰 자리를 적었는가?'))

    return it


_ANON = re.compile(r'원소 (?:가|나|다|A|B|C)(?=의|와|과|는|은|를|이|가|,|·|\s|$)')


def local_checks(items):
    """T15 지역 저작 점검 — P3 의 것을 그대로 잇는다(⑧ 겹침 · ⑨ 길이 포함)."""
    bad = []
    ALLOWED = {'proc', 'sign', 'surface', 'conserv', 'unit', 'scale', 'overgen', 'causal'}
    for x in items:
        ts = [w['type'] for w in x['distractors']]
        if len(set(ts)) < 3:
            bad.append(f"{x['id']} 오답 유형이 겹친다 — {ts}")
        for t in ts:
            if t not in ALLOWED:
                bad.append(f"{x['id']} 허용되지 않은 유형 {t}")
        # ★★이 검사는 P3 3차까지 한 번도 울 수 없었다★★ — sol() 이 해설 끝에 붙이는
        #   단평 블록이 오답 문면을 ★글자 그대로★ 되풀이하므로 `in x['solution']` 은 언제나
        #   참이었다. 해설 산문에서 인용이 흐트러져도 검사는 통과한다(defender 와
        #   factchecker 가 각각 두 자리를 손으로 잡아 냈다). ▸ ★산문 문단만 본다.★
        prose = x['solution'].split('\n\n')[1] if '\n\n' in x['solution'] else x['solution']
        #   ▸ 산문은 간접 인용이라 종결어미가 바뀐다('…때문이다' → '…때문이라고 했어').
        #     ★어미를 뗀 몸통★ 이 그대로 있는지만 본다 — 몸통이 어긋나면 인용이 흐트러진 것이다.
        for w in x['distractors']:
            core = re.sub(r'(이)?다$', '', x['choices'][w['opt']]).strip()
            if core not in prose:
                bad.append(f"{x['id']} {w['opt'] + 1}번 오답 인용이 해설 산문과 어긋난다")
        if not re.search(r'[①②③④](가|이)? 옳아', x['solution']):
            bad.append(f"{x['id']} 해설에 정답 확정 어구가 없다")
        J = ('를|을|은|의|으로|로|에서|에|과|보다|처럼|까지|부터|이고|이다|이야|이라|이지')
        for f in ('stem', 'solution', 'answer_proof'):
            for m in re.finditer(rf'(?<=[가-힣]) ({J})(?=[\s,.·)?!]|$)', x[f]):
                bad.append(f"{x['id']} {f} 조사 앞 빈칸 — {x[f][max(0, m.start() - 14):m.end()]!r}")
        tails = [c.strip()[-3:] for c in x['choices']]
        for t in set(tails):
            if tails.count(t) == 3 and tails[x['answer']] != t:
                bad.append(f"{x['id']} 어미 「{t}」 셋 대 정답 하나 — 꼴로 갈린다")

        def tk(s):
            return {w for w in re.findall(r'[가-힣]{2,}', s)}
        for i in range(4):
            for j in range(i + 1, 4):
                if x['answer'] in (i, j):
                    continue
                a, b = tk(x['choices'][i]), tk(x['choices'][j])
                if a and b and len(a & b) / len(a | b) >= 0.62:
                    bad.append(f"{x['id']} 오답 {i + 1}·{j + 1} 자카드 "
                               f"{len(a & b) / len(a | b):.2f}")
    for x in items:
        s = x['stem']
        if '톱니' in s and '세로축' not in s:
            bad.append(f"{x['id']} 톱니를 쓰면서 세로축을 적지 않았다")
        anon = _ANON.search(s)
        named = re.search(r'(리튬|베릴륨|붕소|탄소|질소|산소|플루오린|네온|나트륨|마그네슘|염소'
                          r'|칼륨|알루미늄|헬륨|수소|아르곤|브로민|Li|Be|B\b|C\b|N\b|O\b|F\b|Ne|Na)', s)
        if anon and named:
            bad.append(f"{x['id']} 익명 원소와 원소 이름을 함께 인쇄했다 — "
                       f"{anon.group(0)!r} · {named.group(0)!r}")
        if '크게 뛰' in s or '급증' in s:
            if not re.search(r'비\(뒤 ÷ 앞\)|비로 재|앞까지의 개수', s):
                bad.append(f"{x['id']} 급증의 셈법을 발문이 정하지 않았다")
    g = sum(1 for x in items if x['track'] == '일반')
    lo = 7 if len(items) >= 10 else max(2, len(items) // 2)
    if g < lo:
        bad.append(f'일반 {g}제 — {len(items)}문항 배치의 하한은 {lo}')
    e = sum(1 for x in items if x['difficulty'] == '쉬움')
    if e > 3:
        bad.append(f'쉬움 {e}제 — 상한 3')

    # ⑧ ★정답이 발문과의 어휘 겹침 유일 최다이면 실패★ (P1 이 값을 치른 자리)
    for x in items:
        st = set(re.findall(r'[0-9A-Za-z가-힣]{2,}', x['stem']))
        ov = [len(set(re.findall(r'[0-9A-Za-z가-힣]{2,}', c)) & st) for c in x['choices']]
        if ov[x['answer']] == max(ov) and ov.count(max(ov)) == 1:
            bad.append(f"{x['id']} 정답이 발문과의 어휘 겹침 최다(유일) — {ov}")

    # ⑨ ★선지 길이 — 정답이 유일 최장/최단이거나 산포가 0.25 를 넘으면 실패★ (P3 이 값을 치렀다)
    for x in items:
        L = [len(c) for c in x['choices']]
        a = L[x['answer']]
        if a == max(L) and L.count(a) == 1:
            bad.append(f"{x['id']} 정답이 유일 최장 — {L}")
        if a == min(L) and L.count(a) == 1:
            bad.append(f"{x['id']} 정답이 유일 최단 — {L}")
        sp = (max(L) - min(L)) / (sum(L) / len(L))
        if sp > 0.25:
            bad.append(f"{x['id']} 선지 길이 산포 {sp:.2f} > 0.25 — {L}")

    print(f"  ▸ 배치 요약 — 유형 {sorted({w['type'] for x in items for w in x['distractors']})}")
    if bad:
        print('\n  ❌ 저작 점검 실패')
        for b in bad:
            print('     ·', b)
        raise SystemExit(1)
    print('  ✅ 저작 점검 통과')



if __name__ == '__main__':
    items = build()
    local_checks(items)
    T.verify(items)
    T.merge_and_house(items)
