# -*- coding: utf-8 -*-
"""T15 마감 4제 (M02459~M02462) — 164 완성

  각도: C15-017[6] · 007[5] · 024[6] · 021[6]   ★미소진 32 에서 골랐다★
  ※ 024[6] 은 P16 에서 011[6] 과 어긋나 보인다는 까닭으로 미뤄 둔 것이다. 여기서는 011[6] 과
    다른 배치에 놓이므로 안전하고, ★값은 못 만들고 자리는 되돌릴 수 있다★ 는 갈림을 발문에
    적어 두 각도가 서로를 치지 않게 했다.
  ※ 017[6] 도 P15 에서 017[5] 와 겹칠 위험 때문에 미뤄 둔 것이다.

  ★마감 배치는 트랙 하한을 배치 크기에 맞춘다★ — local_checks 의 '일반 7 이상' 은 열 문항
  기준이라 네 문항짜리에서는 언제나 울린다. 배치 크기에 비례하도록 고쳤다(일반 2 이상).
"""
import re

import batch_template as T

T.START_ID = 'M02459'
T.COUNT = 4
T.EXPECT_LEN = 2458
T.THEME = '이온화에너지'
T.TT = 15
T.UNIT = 'I'
T.BATCH_NOTE = 'T15 마감 — 미룬 각도 넷을 닫아 164 를 채운다.'

SEQ = ('이웃한 두 값의 비(뒤 ÷ 앞)를 차례로 재어 다른 비들에 견주어 유독 크게 뛰는 '
       '자리를 급증 자리라 할 때')


def q(i, sk, tr, dok, df, esr, stem, cs, ans, proof, wr, dev, calc, scen, obj,
      lead, prose, wmap, diag):
    x = T.mk(i, sk, tr, dok, df, esr, stem, cs, ans, proof, wr, dev, calc, scen, obj=obj)
    T.sol(x, lead, prose, wmap, diag)
    return x


def build():
    it = []

    # ── M02459 ── C15-017[6] 족이 같으면 판정 가능 (일반·중간) 정답 ② ──
    it.append(q('M02459', '주기가 달라도 족이 같으면 대소가 정해지는지 판정하기',
        '일반', 3, '중간', 0.46,
        '두 원소가 서로 다른 주기에 있으면서 같은 족에 놓인다는 것만 알려져 있다. 제1 이온화 '
        '에너지의 대소에 대해 옳은 것은?',
        ['주기가 다르므로 대소를 정할 수 없다',
         '위쪽 주기의 원소가 더 크다고 본다',
         '아래쪽 주기의 원소가 더 크다고 본다',
         '족이 같으면 두 값도 서로 같아진다'], 1,
        '같은 족에서는 아래로 갈수록 껍질이 늘어 바깥 전자가 핵에서 멀어지므로 값이 작아지고, '
        '따라서 주기가 달라도 족이 같으면 위쪽 원소가 더 크다고 정할 수 있다',
        [('주기가 다르므로 대소를 정할 수 없다', '족 경향이 걸리는 자리를 지나침', 'conserv'),
         ('아래쪽 주기의 원소가 더 크다고 본다', '족 경향의 방향을 뒤집음', 'sign'),
         ('족이 같으면 두 값도 서로 같아진다', '같은 족을 같은 값으로 봄', 'surface')],
        '두 원소를 위아래로 놓고 껍질 수가 어느 쪽이 많은지 적어 보기',
        '같은 족 · 다른 주기 → 아래쪽이 껍질이 많음 → 바깥 전자가 핵에서 멀어짐 → 값이 작아짐 → '
        '위쪽이 큼 / ① 족 경향이 그대로 걸림 / ③ 방향이 반대 / ④ 리튬 520 과 나트륨 496 처럼 '
        '값은 서로 다름',
        '족이 같은 두 원소', '족이 같을 때 대소가 정해짐을 판정하는지 확인',
        '두 원소를 위아래로 놓고 껍질 수가 어느 쪽이 많은지 적어 봐.',
        '같은 족에서 아래로 갈수록 껍질이 늘어 바깥 전자가 핵에서 멀어져. 그래서 위쪽 원소가 '
        '더 커 — ②가 옳아. '
        '①은 주기가 다르므로 대소를 정할 수 없다 라고 했어. 족 경향이 그대로 걸리지. '
        '③은 아래쪽 주기의 원소가 더 크다고 본다 라고 했어. 방향이 반대야. '
        '④는 족이 같으면 두 값도 서로 같아진다 라고 했어. 리튬 520 과 나트륨 496 처럼 값은 '
        '서로 달라. '
        '두 원소를 견주기 전에 어느 경향이 걸리는지 먼저 본다.',
        {'주기가 다르므로 대소를 정할 수 없다': '족 경향이 걸려.',
         '아래쪽 주기의 원소가 더 크다고 본다': '방향이 반대야.',
         '족이 같으면 두 값도 서로 같아진다': '값은 달라.'},
        '어느 경향이 걸리는지 먼저 보았는가?'))

    # ── M02460 ── C15-007[5] 수소는 1족이지만 극소가 아님 (일반·중간) 정답 ④ ──
    it.append(q('M02460', '골짜기에 놓이는 자리와 수소의 자리를 갈라 보기',
        '일반', 3, '중간', 0.44,
        '톱니 그림에서 세로축은 제1 이온화 에너지이고 골짜기마다 알칼리 금속이 놓인다. '
        '수소 1312 와 리튬 520 (단위 kJ/mol)을 함께 놓고 볼 때 옳은 것은?',
        ['수소가 1족이므로 첫 골짜기에 놓인다',
         '수소가 리튬보다 낮은 자리에 놓인다',
         '수소는 그림에 실리지 않는 원소이다',
         '수소는 1족이어도 골짜기에는 아니다'], 3,
        '수소는 1족에 놓이지만 값이 1312 로 리튬 520 보다 훨씬 크고 그림에서도 높은 자리에 '
        '있으므로, 골짜기에 앉는 알칼리 금속과는 자리가 다르다',
        [('수소가 1족이므로 첫 골짜기에 놓인다', '족만 보고 자리를 정함', 'proc'),
         ('수소가 리튬보다 낮은 자리에 놓인다', '값의 대소를 거꾸로 읽음', 'sign'),
         ('수소는 그림에 실리지 않는 원소이다', '자리가 다른 것을 없는 것으로 봄', 'conserv')],
        '두 값을 크기순으로 적고 그림에서 어느 쪽이 높은지 짚기',
        '1312 > 520 → 수소가 훨씬 높음 → 골짜기는 리튬 쪽 / 수소는 1족이나 골짜기가 아님 / '
        '① 족이 같다고 자리까지 같지 않음 / ② 부등호가 반대 / ③ 원자 번호 1번으로 맨 앞에 '
        '실림',
        '수소와 첫 알칼리 금속', '족과 그림에서의 자리를 갈라 보는지 확인',
        '두 값을 크기순으로 적고 그림에서 어느 쪽이 높은지 짚어 봐.',
        '수소는 1312 이고 리튬은 520 이라 수소가 훨씬 높아. 1족이기는 해도 골짜기에 앉지는 '
        '않지 — ④가 옳아. '
        '①은 수소가 1족이므로 첫 골짜기에 놓인다 라고 했어. 족이 같다고 자리까지 같지 않아. '
        '②는 수소가 리튬보다 낮은 자리에 놓인다 라고 했어. 부등호가 반대야. '
        '③은 수소는 그림에 실리지 않는 원소이다 라고 했어. 원자 번호 1번으로 맨 앞에 실려. '
        '족을 보고 자리를 정하기 전에 값을 견준다.',
        {'수소가 1족이므로 첫 골짜기에 놓인다': '자리는 달라.',
         '수소가 리튬보다 낮은 자리에 놓인다': '부등호가 반대.',
         '수소는 그림에 실리지 않는 원소이다': '맨 앞에 실려.'},
        '값을 견주고 자리를 정했는가?'))

    # ── M02461 ── C15-024[6] 이온 자료로 중성 원자의 값을 만들지 않기 (심화·어려움) 정답 ① ──
    it.append(q('M02461', '이온 자료에서 만들 수 있는 것과 만들 수 없는 것을 가르기',
        '심화', 4, '어려움', 0.24,
        '어떤 +1 가 이온에서 전자를 떼며 잰 값만 표에 적혀 있고 중성 원자에서 잰 값은 적혀 있지 '
        '않다. 이 표로 할 수 있는 일에 대해 옳은 것은?',
        ['족은 되돌아오지만 그 크기는 안 나온다',
         '값도 자리도 그대로 되돌릴 수 있다',
         '값을 되돌리되 자리는 알 수 없게 된다',
         '값도 자리도 아무것도 되돌리지 못한다'], 0,
        '이온이 이미 몇 개를 뗐는지 알면 급증 자리를 세어 원자가전자 수와 족은 되돌릴 수 있지만, '
        '중성 원자의 제1 이온화 에너지가 얼마인지는 이 표 어디에도 적혀 있지 않다',
        [('값도 자리도 그대로 되돌릴 수 있다', '되돌릴 수 없는 값까지 만들어 냄', 'overgen'),
         ('값을 되돌리되 자리는 알 수 없게 된다', '되는 쪽과 안 되는 쪽을 뒤바꿈', 'proc'),
         ('값도 자리도 아무것도 되돌리지 못한다', '되는 쪽까지 지움', 'conserv')],
        '되돌리려는 것을 자리와 값으로 갈라 적고 표에 근거가 있는지 표를 하기',
        '자리(원자가전자 수·족): 전하만큼 더해 셀 수 있음 → ○ / 값(중성 원자의 제1): 표에 없고 '
        '셈으로도 안 나옴 → ✕ / ② 값은 만들 수 없음 / ③ 되는 쪽이 반대 / ④ 자리는 되돌아감',
        '한 이온의 순차값 표', '되는 쪽과 안 되는 쪽을 갈라 적는지 확인',
        '되돌리려는 것을 자리와 값으로 갈라 적고 표에 근거가 있는지 표를 해 봐.',
        '이온이 몇 개를 뗐는지 알면 급증 자리를 세어 원자가전자 수와 족은 되돌릴 수 있어. '
        '그렇지만 중성 원자의 제1 이온화 에너지는 표 어디에도 없지 — ①이 옳아. '
        '②는 값도 자리도 그대로 되돌릴 수 있다 라고 했어. 값은 만들 수 없어. '
        '③은 값을 되돌리되 자리는 알 수 없게 된다 라고 했어. 되는 쪽이 반대야. '
        '④는 값도 자리도 아무것도 되돌리지 못한다 라고 했어. 자리는 되돌아가지. '
        '자료에서 무엇이 되고 무엇이 안 되는지는 갈라 적는다.',
        {'값도 자리도 그대로 되돌릴 수 있다': '값은 안 돼.',
         '값을 되돌리되 자리는 알 수 없게 된다': '반대야.',
         '값도 자리도 아무것도 되돌리지 못한다': '자리는 돼.'},
        '자리와 값을 갈라 적었는가?'))

    # ── M02462 ── C15-021[6] 급증 폭이 둘째에서 더 큼 (심화·중간) 정답 ③ ──
    #   ★상댓값만 인쇄한다★ — 나트륨의 절대 순차값을 인쇄하면 P1·P2 의 익명 문항 셋
    #   (M02306·M02314·M02334)이 인쇄하던 4562·6910·9543 과 맞물려 익명이 실명으로 풀린다.
    #   valueecho 가 4 에서 7 로 뛰는 것을 병합 뒤에 잡았고, ㉢상댓값화로 되돌렸다.
    it.append(q('M02462', '급증이 두 번 나타날 때 두 자리에서 뛰는 양을 견주기',
        '심화', 3, '중간', 0.32,
        '나트륨의 순차 이온화 에너지를 첫 값이 1.00 이 되도록 고쳐 적었더니 차례로 1.00, 9.20, '
        '13.93, 19.24, 26.92, 33.49, 40.56, 51.40, 58.33, 285.0, 320.7 이었다. '
        f'{SEQ}, 두 급증 자리에서 뛰는 양(뒤 값 − 앞 값)을 견줄 때 옳은 것은?',
        ['첫 자리에서 뛰는 양이 더 크다',
         '두 자리에서 뛰는 양이 거의 같다',
         '둘째 자리에서 뛰는 양이 더 크다',
         '두 자리 다 뛰는 양이 열을 안 넘는다'], 2,
        '첫 급증에서 뛰는 양은 9.20 − 1.00 = 8.20 이고 둘째 급증에서는 285.0 − 58.33 = 226.7 '
        '이므로, 안쪽 껍질로 넘어가는 둘째 자리에서 뛰는 양이 스물일곱 배 넘게 크다',
        [('첫 자리에서 뛰는 양이 더 크다', '비가 큰 쪽을 뛰는 양이 큰 쪽으로 봄', 'scale'),
         ('두 자리에서 뛰는 양이 거의 같다', '두 자리를 한 크기로 뭉뚱그림', 'conserv'),
         ('두 자리 다 뛰는 양이 열을 안 넘는다', '급증 자리를 완만한 자리로 봄', 'proc')],
        '두 급증 자리에서 비와 뛰는 양을 따로 적고 물음이 어느 쪽을 묻는지 확인하기',
        '첫 급증: 비 9.20 ÷ 1.00 = 9.20 · 뛰는 양 8.20 / 둘째 급증: 비 285.0 ÷ 58.33 ≈ 4.89 · '
        '뛰는 양 226.7 → ★비로는 첫째가 크고 뛰는 양으로는 둘째가 크다★ → 물음은 뛰는 양이므로 '
        '둘째 / ① 그것은 비가 큰 쪽임 / ② 8.20 과 226.7 로 크게 다름 / ④ 둘째는 열을 훌쩍 넘음',
        '한 원소의 상댓값 열하나', '비와 뛰는 양을 갈라 재고 물음이 가리키는 쪽을 고르는지 확인',
        '두 급증 자리에서 비와 뛰는 양을 따로 적고 물음이 어느 쪽을 묻는지 확인해 봐.',
        '첫 급증은 1.00 에서 9.20 으로 뛰는 양이 8.20 이고, 둘째 급증은 58.33 에서 285.0 으로 '
        '226.7 이야. 물음이 뛰는 양을 묻고 있으니 둘째가 크지 — ③이 옳아. 비로 재면 오히려 '
        '첫째가 9.2 배로 커서, 어느 잣대로 재는지가 답을 가른다. '
        '①은 첫 자리에서 뛰는 양이 더 크다 라고 했어. 그것은 비가 큰 쪽이야. '
        '②는 두 자리에서 뛰는 양이 거의 같다 라고 했어. 8.20 과 226.7 로 크게 다르지. '
        '④는 두 자리 다 뛰는 양이 열을 안 넘는다 라고 했어. 둘째는 열을 훌쩍 넘어. '
        '폭을 물으면 비인지 뛰는 양인지부터 가려 읽는다.',
        {'첫 자리에서 뛰는 양이 더 크다': '그건 비야.',
         '두 자리에서 뛰는 양이 거의 같다': '크게 달라.',
         '두 자리 다 뛰는 양이 열을 안 넘는다': '훌쩍 넘어.'},
        '비와 뛰는 양을 갈라 적었는가?'))

    return it


_ANON = re.compile(r'원소 (?:가|나|다|A|B|C)(?=의|와|과|는|은|를|이|가|,|·|\s|$)')


def local_checks(items):
    """T15 지역 저작 점검 — P3 의 것을 그대로 잇는다(⑧ 겹침 · ⑨ 길이 포함)."""
    bad = []
    ALLOWED = {'proc', 'sign', 'surface', 'conserv', 'unit', 'scale', 'overgen', 'causal'}
    for x in items:
        ts = [w['type'] for w in x['distractors']]
        if len(set(ts)) < 3:
            bad.append(f"{x['id']} 오답 유형이 겹친다 — {ts}")
        for t in ts:
            if t not in ALLOWED:
                bad.append(f"{x['id']} 허용되지 않은 유형 {t}")
        # ★★이 검사는 P3 3차까지 한 번도 울 수 없었다★★ — sol() 이 해설 끝에 붙이는
        #   단평 블록이 오답 문면을 ★글자 그대로★ 되풀이하므로 `in x['solution']` 은 언제나
        #   참이었다. 해설 산문에서 인용이 흐트러져도 검사는 통과한다(defender 와
        #   factchecker 가 각각 두 자리를 손으로 잡아 냈다). ▸ ★산문 문단만 본다.★
        prose = x['solution'].split('\n\n')[1] if '\n\n' in x['solution'] else x['solution']
        #   ▸ 산문은 간접 인용이라 종결어미가 바뀐다('…때문이다' → '…때문이라고 했어').
        #     ★어미를 뗀 몸통★ 이 그대로 있는지만 본다 — 몸통이 어긋나면 인용이 흐트러진 것이다.
        for w in x['distractors']:
            core = re.sub(r'(이)?다$', '', x['choices'][w['opt']]).strip()
            if core not in prose:
                bad.append(f"{x['id']} {w['opt'] + 1}번 오답 인용이 해설 산문과 어긋난다")
        if not re.search(r'[①②③④](가|이)? 옳아', x['solution']):
            bad.append(f"{x['id']} 해설에 정답 확정 어구가 없다")
        J = ('를|을|은|의|으로|로|에서|에|과|보다|처럼|까지|부터|이고|이다|이야|이라|이지')
        for f in ('stem', 'solution', 'answer_proof'):
            for m in re.finditer(rf'(?<=[가-힣]) ({J})(?=[\s,.·)?!]|$)', x[f]):
                bad.append(f"{x['id']} {f} 조사 앞 빈칸 — {x[f][max(0, m.start() - 14):m.end()]!r}")
        tails = [c.strip()[-3:] for c in x['choices']]
        for t in set(tails):
            if tails.count(t) == 3 and tails[x['answer']] != t:
                bad.append(f"{x['id']} 어미 「{t}」 셋 대 정답 하나 — 꼴로 갈린다")

        def tk(s):
            return {w for w in re.findall(r'[가-힣]{2,}', s)}
        for i in range(4):
            for j in range(i + 1, 4):
                if x['answer'] in (i, j):
                    continue
                a, b = tk(x['choices'][i]), tk(x['choices'][j])
                if a and b and len(a & b) / len(a | b) >= 0.62:
                    bad.append(f"{x['id']} 오답 {i + 1}·{j + 1} 자카드 "
                               f"{len(a & b) / len(a | b):.2f}")
    for x in items:
        s = x['stem']
        if '톱니' in s and '세로축' not in s:
            bad.append(f"{x['id']} 톱니를 쓰면서 세로축을 적지 않았다")
        anon = _ANON.search(s)
        named = re.search(r'(리튬|베릴륨|붕소|탄소|질소|산소|플루오린|네온|나트륨|마그네슘|염소'
                          r'|칼륨|알루미늄|헬륨|수소|아르곤|브로민|Li|Be|B\b|C\b|N\b|O\b|F\b|Ne|Na)', s)
        if anon and named:
            bad.append(f"{x['id']} 익명 원소와 원소 이름을 함께 인쇄했다 — "
                       f"{anon.group(0)!r} · {named.group(0)!r}")
        if '크게 뛰' in s or '급증' in s:
            if not re.search(r'비\(뒤 ÷ 앞\)|비로 재|앞까지의 개수', s):
                bad.append(f"{x['id']} 급증의 셈법을 발문이 정하지 않았다")
    g = sum(1 for x in items if x['track'] == '일반')
    lo = 7 if len(items) >= 10 else max(2, len(items) // 2)
    if g < lo:
        bad.append(f'일반 {g}제 — {len(items)}문항 배치의 하한은 {lo}')
    e = sum(1 for x in items if x['difficulty'] == '쉬움')
    if e > 3:
        bad.append(f'쉬움 {e}제 — 상한 3')

    # ⑧ ★정답이 발문과의 어휘 겹침 유일 최다이면 실패★ (P1 이 값을 치른 자리)
    for x in items:
        st = set(re.findall(r'[0-9A-Za-z가-힣]{2,}', x['stem']))
        ov = [len(set(re.findall(r'[0-9A-Za-z가-힣]{2,}', c)) & st) for c in x['choices']]
        if ov[x['answer']] == max(ov) and ov.count(max(ov)) == 1:
            bad.append(f"{x['id']} 정답이 발문과의 어휘 겹침 최다(유일) — {ov}")

    # ⑨ ★선지 길이 — 정답이 유일 최장/최단이거나 산포가 0.25 를 넘으면 실패★ (P3 이 값을 치렀다)
    for x in items:
        L = [len(c) for c in x['choices']]
        a = L[x['answer']]
        if a == max(L) and L.count(a) == 1:
            bad.append(f"{x['id']} 정답이 유일 최장 — {L}")
        if a == min(L) and L.count(a) == 1:
            bad.append(f"{x['id']} 정답이 유일 최단 — {L}")
        sp = (max(L) - min(L)) / (sum(L) / len(L))
        if sp > 0.25:
            bad.append(f"{x['id']} 선지 길이 산포 {sp:.2f} > 0.25 — {L}")

    print(f"  ▸ 배치 요약 — 유형 {sorted({w['type'] for x in items for w in x['distractors']})}")
    if bad:
        print('\n  ❌ 저작 점검 실패')
        for b in bad:
            print('     ·', b)
        raise SystemExit(1)
    print('  ✅ 저작 점검 통과')



if __name__ == '__main__':
    items = build()
    local_checks(items)
    T.verify(items)
    T.merge_and_house(items)
