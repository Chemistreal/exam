# -*- coding: utf-8 -*-
"""T12 P4 · 에이전트 8차 순회 조치.

  · factchecker ✗ 2건 — ★둘 다 M01837, 둘 다 7차 수정이 만든 것★ · △ 3건.
                7차의 초점 확인 사항(M01839 ④ 반박)은 '문제없음'으로 닫혔다.
  · solver / defender (별도 절)

★아홉 번째다★ 5·6·7·8차 모두 직전 회차의 수정이 다음 회차의 지적을 만들었다.
  이번에는 한 회차 안에서 ★한 문항의 두 자리★가 동시에 어긋났다는 점이 새롭다.
  7차에 M01837 해설 범위를 발문에 맞춰 넓히면서 두 문장을 새로 썼는데, 둘 다 틀렸다.

  ✗1 자가진단 "돌턴에서 톰슨을 거쳐 러더퍼드까지 살아남은 것은 원자의 중성"
     같은 해설 첫 문단이 "돌턴은 전하 이야기를 아예 하지 않았어"라고 못 박는다.
     ★전하를 다루지 않은 모형에서 '중성'이 살아남을 수는 없다.★
     7차에 factchecker 가 '자가진단이 발문보다 범위가 좁다'고 했고 나는 범위를 넓혔다.
     그러나 넓혀야 했던 것은 ★어느 명제가 어느 단계에서 왔는지를 적는 일★이지
     계승 구간을 통째로 늘리는 일이 아니었다. 지금은 둘을 나누어 적는다.
  ✗2 도입 "전기가 원자 이야기에 들어온 것은 톰슨이 전자를 찾아낸 다음부터야"
     과학사로 거짓이다 — 패러데이의 전기분해 법칙(1833)이 물질의 양과 전하량의 비례를
     이미 보였고, 스토니가 톰슨 이전(1874/1891)에 최소 전하 단위에 electron 이라는 이름을 붙였다.
     음극선 연구 자체도 톰슨보다 앞선다. ★'원자 모형 안에서'로 한정하면 참이 된다.★
     ◆교훈◆ 범위를 메우려고 급히 쓴 곁가지 한 줄이 단원 밖 사실을 단정한다.
             ★'아예·부터·처음'이 들어간 문장은 곁가지라도 반드시 연대를 확인할 것.★

★M01841 곁가지는 세 번째 지적이라 이번에 조치한다★
  5차 factchecker 가 스스로 제안한 문면인데 6차·8차가 잇달아 '지금 선지에 대응 없음'이라 했다.
  6차에는 회차 간 반전으로 보고 기록만 했으나, ★같은 지적이 두 회차에 걸쳐 반복되면 반전이 아니다.★
  반전은 두 상태를 오갈 때이고, 이것은 한 방향으로 두 번 눌린 것이다.
  5차의 요구(② 반박과 모순되지 않을 것)는 그 절을 아예 빼도 충족된다 — 뺀다.
"""
import json, sys, os

BANK = os.path.join(os.path.dirname(__file__), 'master_bank.json')

STEM_SET = {
    # ★solver 8차 — 확신도 '보통' 하나의 원인★
    #   '0 을 적지 않고' 가 '값은 0 인데 굳이 안 적는다'로 읽혀 ③('전이는 있지만 빛이 없다')에
    #   발판을 준다. 값을 전제하지 않는 문면으로 바꾸면 갈림이 사라진다.
    #   ①('0 은 표에 적을 수 없는 값')은 학생이 스스로 0 을 가정하는 오답이라 그대로 살아 있다.
    'M01842': ("주양자수 n 으로 정해지는 두 준위 사이의 전이 에너지의 크기를 표로 만들면, "
               "출발과 도착이 같은 대각선 칸에는 값을 적지 않고 '−' 로 비워 둔다. 그 까닭으로 옳은 것은?"),
    # solver 8차 편집상 흠 — 조사가 어색하다.
    'M01838': ("같은 전기장에 대전 입자를 통과시키면 진로가 휘어진다. "
               "휘는 정도는 전하량에 비례하고 질량에 반비례한다. "
               "갑은 전하량 1·질량 1, 을은 전하량 3·질량 2, 병은 전하량 2·질량 4 라 하자. "
               "휘는 정도가 큰 것부터 늘어놓은 것은?"),
}

CH_SET = {
    # solver 8차 편집상 흠 — 7차에 비문을 고치며 넣은 '죽'이 다른 선지의 문어체와 결이 다르다.
    #   ★뜻과 길이를 그대로 두고 어투만 맞춘다★ — 이 자리는 반전 자리라 의미를 건드리면 안 된다.
    'M01841': ["수소가 내놓는 빛의 에너지가 몇 가지로 정해져 있다",
               "수소 원자마다 내놓는 빛의 색이 조금씩 달라 이어져 있다",
               "검은 바탕은 그 빛을 수소가 흡수했다는 뜻이다",
               "빛을 내놓는 것은 전자가 아니라 원자핵 쪽이다"],
}

SOL_REPL = [
    # ✗2 — '원자 모형 안에서'로 한정해야 참이 된다.
    ('M01837',
     "먼저 짚어 둘 것 하나 — 돌턴은 전하 이야기를 아예 하지 않았어. "
     "전기가 원자 이야기에 들어온 것은 톰슨이 전자를 찾아낸 다음부터야.",
     "먼저 짚어 둘 것 하나 — 돌턴의 원자설에는 전하 개념이 없었어. "
     "원자 모형 안에 (+)와 (−)가 자리를 갖게 된 것은 톰슨이 전자를 찾아낸 뒤부터야."),
    # ✗1 — 계승 구간을 통째로 늘리지 말고, 어느 명제가 어느 단계에서 왔는지를 나누어 적는다.
    ('M01837',
     "자가진단: 돌턴에서 톰슨을 거쳐 러더퍼드까지 살아남은 것은 원자의 중성, "
     "톰슨에서 러더퍼드로 넘어가며 바뀐 것은 (+)전하의 자리.",
     "자가진단: 넷 가운데 살아남은 것은 원자의 중성 — 톰슨이 세운 그 대목을 러더퍼드가 그대로 두었다. "
     "돌턴에게서 온 불가분성은 톰슨 단계에서 이미 무너졌다."),
    # △ — 지금 선지 어디에도 대응하지 않는 절. 6차·8차 연속 지적이라 뺀다.
    ('M01841',
     "다른 원소의 원자도 같은 자리에 선을 내는지, 빛을 내놓는 것이 전자인지 핵인지는 이 사진만으로 가릴 수 없어.",
     "빛을 내놓는 것이 전자인지 핵인지 같은 것은 이 사진만으로 가릴 수 없어."),
    # △ — 바뀌지 않는 것은 축 위의 좌표가 아니라 순서다.
    ('M01845',
     "가운데 있는 발머는 뒤집어도 가운데라 자리가 그대로야.",
     "가운데 있는 발머는 뒤집어도 여전히 가운데 순서야."),
    ('M01845',
     "④ 발머 계열과 자리를 맞바꾼다: 발머는 가운데라 뒤집어도 그대로야.",
     "④ 발머 계열과 자리를 맞바꾼다: 발머는 가운데라 뒤집어도 순서가 그대로야."),
    # △ — K 를 n=0 으로 세면 출발·도착이 함께 밀려 선지에 없는 조합이 된다.
    #      출발만 밀리는 경우로 한정해야 실제 오답 선지와 대응한다.
    ('M01844',
     "셋째 걸음에서 K 를 n=0 으로 세어 한 칸씩 밀려.",
     "셋째 걸음에서 K 를 n=0 으로 세어 출발 껍질이 한 칸 밀려(그러면 ③ 쪽으로 가지)."),
    ('M01841',
     "② 수소 원자마다 내놓는 빛의 색이 조금씩 달라 죽 이어진다:",
     "② 수소 원자마다 내놓는 빛의 색이 조금씩 달라 이어져 있다:"),
]

WATCH_ADD = {
    'M01837': ("★사실 오류 주의(8차·이 해설은 두 자리가 동시에 뚫렸다)★ "
               "(1) 자가진단을 '돌턴에서 … 러더퍼드까지 살아남은 것은 원자의 중성'으로 되돌리지 말 것 — "
               "돌턴 원자설에는 전하 개념 자체가 없어 '중성'이 거기서 살아남을 수 없다. "
               "발문 범위와 해설을 맞추려면 계승 구간을 늘릴 것이 아니라 어느 명제가 어느 단계에서 왔는지를 나누어 적어야 한다. "
               "(2) '전기가 원자 이야기에 들어온 것은 톰슨 다음부터'로 되돌리지 말 것 — "
               "패러데이의 전기분해 법칙(1833)과 스토니의 electron 명명(1891)이 톰슨보다 앞선다. "
               "★'원자 모형 안에서'로 한정해야 참이 된다.★"),
    'M01841': ("◇곁가지 정리(8차)◇ '다른 원소의 원자도 같은 자리에 선을 내는지'를 되돌리지 말 것 — "
               "지금 선지 어디에도 대응하지 않아 6차·8차에 연속으로 지적됐다. "
               "5차의 요구(② 반박과 모순되지 않을 것)는 그 절을 빼는 것으로도 충족된다."),
    'M01842': ("◇발문 중립화(8차)◇ 발문을 '0 을 적지 않고'로 되돌리지 말 것 — "
               "'값은 0 인데 굳이 안 적는다'로 읽혀 ③('전이는 있지만 빛이 없다')에 발판을 준다. "
               "①('0 은 표에 적을 수 없는 값')은 학생이 스스로 0 을 가정하는 오답이라 발문이 0 을 말하지 않아도 산다."),
    'M01845': ("◇표현 경계(8차)◇ '발머는 뒤집어도 자리가 그대로'로 되돌리지 말 것 — "
               "바뀌지 않는 것은 세 계열의 순서상 위치이지 축 위의 실제 좌표가 아니다."),
}

STALE = [
    ('M01837', '돌턴에서 톰슨을 거쳐 러더퍼드까지 살아남은'),
    ('M01837', '전기가 원자 이야기에 들어온 것은'),
    ('M01841', '다른 원소의 원자도 같은 자리에'),
    ('M01845', '자리가 그대로야'),
    ('M01844', 'n=0 으로 세어 한 칸씩 밀려'),
    ('M01841', '죽 이어진다'),
]


def spread(ch):
    L = sorted(len(str(x)) for x in ch)
    return (L[3] - L[0]) / ((L[1] + L[2]) / 2) if L[1] + L[2] else 0.0


def len_rank(it):
    L = [len(str(x)) for x in it['choices']]
    order = sorted(range(4), key=lambda i: (-L[i], i))
    return order.index(it['answer']) + 1


def main():
    bank = json.load(open(BANK, encoding='utf-8'))
    items = bank if isinstance(bank, list) else bank['items']
    idx = {x['id']: x for x in items}
    n = 0

    for fid, st in STEM_SET.items():
        assert idx[fid]['stem'] != st
        idx[fid]['stem'] = st; n += 1
    for fid, ch in CH_SET.items():
        assert len(ch) == 4
        idx[fid]['choices'] = ch; n += 1
    for fid, old, new in SOL_REPL:
        s = idx[fid]['solution']
        assert old in s, f"{fid} 원문 없음: {old[:44]}"
        idx[fid]['solution'] = s.replace(old, new); n += 1
    for fid, add in WATCH_ADD.items():
        v = idx[fid].setdefault('verified', {})
        v['watch'] = (v.get('watch', '') + ' / ' + add).strip(' /'); n += 1

    print(f"조치 {n}곳\n")

    bad = []
    for fid, phrase in STALE:
        if phrase in idx[fid]['solution']:
            print(f"⛔ STALE {fid} 해설에 옛 문구가 남았다: '{phrase}'"); bad.append('STALE')

    ranks = {}
    for i in range(1837, 1847):
        fid = f'M0{i}'
        it = idx[fid]
        L = [len(str(x)) for x in it['choices']]
        r = len_rank(it); sp = spread(it['choices']); sol = it['solution']
        ranks[r] = ranks.get(r, 0) + 1
        print(f"{fid} {it['track']} 답{'①②③④'[it['answer']]} 길이{L} 순위{r} 산포{sp:.2f} 해설{len(sol)}자")
        if sp > 0.25 and sorted(L)[1] >= 8:
            print(f"  ⛔ G3b 산포 {sp:.2f}"); bad.append('G3b')
        if L[it['answer']] == max(L) and L.count(max(L)) == 1:
            print("  ⛔ G3 정답=유일 최장"); bad.append('G3')
        if L[it['answer']] == min(L) and L.count(min(L)) == 1:
            print("  ⛔ G3 정답=유일 최단"); bad.append('G3')
        if len(sol) < 300:
            print("  ⛔ G9 해설 300자 미만"); bad.append('G9')
        if '**' in sol or '**' in it['stem']:
            print("  ⛔ G10 마크다운 강조"); bad.append('G10')
        for d in it['distractors']:
            mark = '①②③④'[d['opt']]
            if mark not in sol:
                print(f"  ⛔ G9 오답 {mark} 미언급"); bad.append('G9')

    pos = {}
    for i in range(1837, 1847):
        a = idx[f'M0{i}']['answer']; pos[a] = pos.get(a, 0) + 1
    print("\n위치 " + ' '.join(f"{'①②③④'[k]}{pos.get(k,0)}" for k in range(4)) +
          f" · 길이순위 {dict(sorted(ranks.items()))}")
    for r, c in ranks.items():
        if c > 4:
            print(f"  ⛔ G3c 순위 {r} 가 {c}/10"); bad.append('G3c')

    if bad:
        print("\n⛔ 위반 " + ', '.join(sorted(set(bad)))); sys.exit(1)
    print("✅ 규칙 위반 없음")

    if '--apply' in sys.argv:
        json.dump(bank, open(BANK, 'w', encoding='utf-8'), ensure_ascii=False, indent=1)
        print("✅ 반영 완료")
    else:
        print("※ 검증만. 반영하려면 --apply")


if __name__ == '__main__':
    main()
