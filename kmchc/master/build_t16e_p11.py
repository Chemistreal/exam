# -*- coding: utf-8 -*-
"""T16 P11 (M02891~M02900) — 값을 적기 전에 어디서 어떻게 잰 값인지 밝힌다

  각도: C16-001[5] · 002[1] · 003[7] · 006[3] · 007[8] · 011[4] · 009[5] · 013[5] ·
        016[8] · 014[7]

  정답 자리 2-3-4-3-1-2-3-1-4-1 — anspick.py 로 ①3②2③3④2 를 집어 은행을 725 넷으로
  고르게 맞춘다.
  3위: 891 · 893 · 895 · 897 · 899 / 2위: 892 · 894 · 896 · 898 · 900 (동률 0)

  ★은행 2,900제 고비★
"""
import re

import batch_template as T

T.START_ID = 'M02891'
T.COUNT = 10
T.EXPECT_LEN = 2890
T.THEME = '전자친화도·전기음성도'
T.TT = 16
T.UNIT = 'I'
T.BATCH_NOTE = 'T16 P11 — 값을 적기 전에 어디서 어떻게 잰 값인지 밝힌다.'


def q(i, sk, tr, dok, df, esr, stem, cs, ans, proof, wr, dev, calc, scen, obj,
      lead, prose, wmap, diag):
    x = T.mk(i, sk, tr, dok, df, esr, stem, cs, ans, proof, wr, dev, calc, scen, obj=obj)
    T.sol(x, lead, prose, wmap, diag)
    return x


def build():
    it = []

    # ── M02891 ── C16-001[5] 고체나 용액에서 재는 값이 아님 (일반·중간) 정답 ② [3위] ──
    it.append(q('M02891', '고체나 용액에서 재는 값이 아님을 짚기',
        '일반', 3, '중간', 0.44,
        '어떤 학생이 소금물에 담근 염소로 전자 친화도를 재어 보겠다고 했다. 이 말에 대해 '
        '옳은 것은?',
        ['소금물에 담근 채로 재면 더 정확해진다',
         '기체로 떨어져 있는 원자에서 정한다',
         '고체 조각에서 재도 같은 값이 나온다',
         '어디서 재든 값은 달라지지 않는다'], 1,
        '전자 친화도는 기체 상태로 서로 떨어져 있는 원자 하나가 전자를 받을 때의 값으로 정해 두었고, '
        '용액이나 고체에서는 이웃한 입자가 함께 끌어당겨 그 값이 나오지 않는다',
        [('소금물에 담근 채로 재면 더 정확해진다', '정한 자리를 다른 자리로 옮김', 'proc'),
         ('고체 조각에서 재도 같은 값이 나온다', '이웃 입자가 드는 것을 지움', 'conserv'),
         ('어디서 재든 값은 달라지지 않는다', '재는 자리를 지움', 'surface')],
        '이 값이 어떤 상태의 원자에 대해 정해졌는지 적기',
        '전자 친화도: 기체 상태의 떨어진 원자 하나 / 용액·고체: 이웃 입자가 함께 당김 → 다른 값 / '
        '① 자리를 옮김 / ③ 같지 않음 / ④ 자리에 따라 달라짐',
        '소금물에 담근 염소를 두고 한 말', '재는 상태를 아는지 확인',
        '이 값이 어떤 상태의 원자에 대해 정해졌는지 적어 봐.',
        '전자 친화도는 기체로 떨어져 있는 원자 하나에 대해 정한 값이야 — ②가 옳아. '
        '①은 소금물에 담근 채로 재면 더 정확해진다 라고 했어. 정한 자리를 옮긴 셈이지. '
        '③은 고체 조각에서 재도 같은 값이 나온다 라고 했어. 이웃 입자가 함께 당기잖아. '
        '④는 어디서 재든 값은 달라지지 않는다 라고 했어. 자리에 따라 달라져. '
        '값을 재겠다는 말을 만나면 어떤 상태에서 정한 값인지 적는다.',
        {'소금물에 담근 채로 재면 더 정확해진다': '자리를 옮겼어.',
         '고체 조각에서 재도 같은 값이 나온다': '이웃이 당겨.',
         '어디서 재든 값은 달라지지 않는다': '자리에 따라 달라.'},
        '어떤 상태에서 정한 값인지 적었는가?'))

    # ── M02892 ── C16-002[1] 책마다 부호가 다를 수 있음 (일반·중간) 정답 ③ [2위] ──
    it.append(q('M02892', '책마다 부호 규약이 다를 수 있음을 짚기',
        '일반', 3, '중간', 0.46,
        '한 책은 염소의 값을 349 로 적고 다른 책은 -349 로 적었다. 두 책을 함께 볼 때 '
        '옳은 것은?',
        ['부호는 어느 책에서나 똑같이 적혀 있다',
         '부호가 다르면 잰 대상도 다르다',
         '표마다 어떤 규약으로 적었는지 본다',
         '부호가 헷갈리면 그냥 떼고 읽는다'], 2,
        '내놓는 에너지를 양수로 적는 책도 있고 드나든 열을 음수로 적는 책도 있으므로, 표마다 어떤 '
        '규약으로 적었는지 먼저 보아야 값을 잘못 읽지 않는다',
        [('부호는 어느 책에서나 똑같이 적혀 있다', '갈리는 규약을 하나로 봄', 'conserv'),
         ('부호가 다르면 잰 대상도 다르다', '적는 방법을 잰 대상의 차이로 봄', 'surface'),
         ('부호가 헷갈리면 그냥 떼고 읽는다', '규약을 확인하지 않고 건너뜀', 'proc')],
        '두 책이 저마다 무엇을 양수로 적었는지 적기',
        '349: 내놓는 에너지를 양수로 적음 / -349: 드나든 열을 음수로 적음 → 같은 사실을 다른 '
        '규약으로 적음 / ① 갈림 / ② 잰 대상은 같음 / ④ 확인해야 함',
        '같은 값을 다르게 적은 두 책', '규약을 먼저 보는지 확인',
        '두 책이 저마다 무엇을 양수로 적었는지 적어 봐.',
        '한 책은 내놓는 에너지를 양수로, 다른 책은 드나든 열을 음수로 적은 거야 '
        '— ③이 옳아. '
        '①은 부호는 어느 책에서나 똑같이 적혀 있다 라고 했어. 책마다 갈리잖아. '
        '②는 부호가 다르면 잰 대상도 다르다 라고 했어. 잰 것은 같은 염소의 값이야. '
        '④는 부호가 헷갈리면 그냥 떼고 읽는다 라고 했어. 규약부터 확인해야지. '
        '부호가 어긋나 보이면 표마다의 규약을 먼저 본다.',
        {'부호는 어느 책에서나 똑같이 적혀 있다': '책마다 갈려.',
         '부호가 다르면 잰 대상도 다르다': '대상은 같아.',
         '부호가 헷갈리면 그냥 떼고 읽는다': '규약부터 봐야지.'},
        '규약을 먼저 보았는가?'))

    # ── M02893 ── C16-003[7] 세 요인을 한 줄로 (일반·중간) 정답 ④ [3위] ──
    it.append(q('M02893', '전자를 받아들이는 힘의 세 요인을 한 줄로 늘어놓기',
        '일반', 3, '중간', 0.42,
        '전자를 받아들이는 힘이 무엇에 달렸는지 한 줄로 적으려 한다. 적는 방법으로 '
        '옳은 것은?',
        ['반지름 하나만 적어 두면 넉넉하다고 본다',
         '세 가지 가운데 둘만 골라서 적는다',
         '핵전하만 적고 나머지는 접어 둔다',
         '핵전하와 반지름과 들어갈 자리를 잇는다'], 3,
        '유효 핵전하가 클수록, 반지름이 작을수록, 들어갈 자리가 낮을수록 전자를 세게 받아들이므로 '
        '세 가지를 한 줄에 이어 적어야 무엇이 값을 움직이는지 한눈에 보인다',
        [('반지름 하나만 적어 두면 넉넉하다고 본다', '세 가지 가운데 하나만 남김', 'scale'),
         ('세 가지 가운데 둘만 골라서 적는다', '적어야 할 요인을 하나 버림', 'proc'),
         ('핵전하만 적고 나머지는 접어 둔다', '나머지 요인을 지움', 'conserv')],
        '값을 움직이는 것이 몇 가지인지 세어 적기',
        '유효 핵전하 큼 → 세게 받음 / 반지름 작음 → 세게 받음 / 들어갈 자리 낮음 → 세게 받음 / '
        '세 가지를 한 줄로 이음 / ① 하나로는 모자람 / ② 하나를 버림 / ③ 나머지가 듦',
        '한 줄로 적으려는 세 요인', '요인을 빠뜨리지 않는지 확인',
        '값을 움직이는 것이 몇 가지인지 세어 적어 봐.',
        '유효 핵전하와 반지름과 들어갈 자리, 이렇게 셋이야. 한 줄로 이어 적지 '
        '— ④가 옳아. '
        '①은 반지름 하나만 적어 두면 넉넉하다고 본다 라고 했어. 하나로는 모자라. '
        '②는 세 가지 가운데 둘만 골라서 적는다 라고 했어. 하나를 버리면 모자라. '
        '③은 핵전하만 적고 나머지는 접어 둔다 라고 했어. 나머지도 값에 들어. '
        '무엇에 달렸는지 물으면 요인을 빠짐없이 이어 적는다.',
        {'반지름 하나만 적어 두면 넉넉하다고 본다': '하나론 모자라.',
         '세 가지 가운데 둘만 골라서 적는다': '하나를 버려.',
         '핵전하만 적고 나머지는 접어 둔다': '나머지도 들어.'},
        '세 요인을 이어 적었는가?'))

    # ── M02894 ── C16-006[3] 한쪽이 크면 다른 쪽도 큰지 (일반·중간) 정답 ③ [2위] ──
    it.append(q('M02894', '이온화 에너지가 크면 전자 친화도도 큰지 따져 보기',
        '일반', 3, '중간', 0.40,
        '이온화 에너지가 큰 원소는 전자 친화도도 큰지 물었다. 두 값의 사이에 대해 '
        '옳은 것은?',
        ['한쪽이 크면 다른 쪽도 반드시 크게 나온다',
         '두 값은 서로 아무 상관이 없는 값이다',
         '대체로 같은 쪽이나 어긋나는 자리가 있다',
         '한쪽이 크면 다른 쪽은 작아지게 된다'], 2,
        '두 값 모두 유효 핵전하가 클수록 커지므로 대체로 같은 쪽으로 가지만, 18족은 이온화 '
        '에너지가 가장 큰데도 전자 친화도는 사실상 없어 어긋난다',
        [('한쪽이 크면 다른 쪽도 반드시 크게 나온다', '대체로 서는 말을 못 박음', 'overgen'),
         ('두 값은 서로 아무 상관이 없는 값이다', '같은 뿌리를 지움', 'conserv'),
         ('한쪽이 크면 다른 쪽은 작아지게 된다', '같은 쪽을 맞선 쪽으로 뒤집음', 'sign')],
        '두 값이 같은 뿌리를 쓰는지, 어긋나는 자리가 있는지 적기',
        '유효 핵전하 큼 → 두 값 모두 커짐 → 대체로 같은 쪽 / 18족: 이온화 에너지 가장 큼 · 전자 '
        '친화도 거의 없음 → 어긋남 / ① 못 박을 수 없음 / ② 뿌리가 같음 / ④ 맞서지 않음',
        '두 값을 나란히 놓고 던진 물음', '대체로와 예외를 함께 다루는지 확인',
        '두 값이 같은 뿌리를 쓰는지, 어긋나는 자리가 있는지 적어 봐.',
        '뿌리가 같아 대체로 같은 쪽으로 가. 그런데 18족에서 어긋나지 — ③이 옳아. '
        '①은 한쪽이 크면 다른 쪽도 반드시 크게 나온다 라고 했어. 못 박을 수는 없어. '
        '②는 두 값은 서로 아무 상관이 없는 값이다 라고 했어. 뿌리가 같잖아. '
        '④는 한쪽이 크면 다른 쪽은 작아지게 된다 라고 했어. 맞서는 사이가 아니야. '
        '두 값의 사이를 물으면 대체로와 어긋난 자리를 함께 적는다.',
        {'한쪽이 크면 다른 쪽도 반드시 크게 나온다': '못 박을 순 없어.',
         '두 값은 서로 아무 상관이 없는 값이다': '뿌리가 같아.',
         '한쪽이 크면 다른 쪽은 작아지게 된다': '맞서지 않아.'},
        '대체로와 예외를 함께 적었는가?'))

    # ── M02895 ── C16-007[8] 자릿수를 그대로 옮기기 (일반·쉬움) 정답 ① [3위] ──
    it.append(q('M02895', '표에 적힌 자릿수를 그대로 옮겨 적기',
        '일반', 2, '쉬움', 0.62,
        '328.2 라고 적힌 칸을 노트에 베껴 두려 한다. 베껴 적는 방법으로 옳은 것은?',
        ['표에 적힌 자릿수를 그대로 옮겨 적는다',
         '자릿수를 줄여서 어림한 값으로 적어 둔다',
         '소수점 아래는 모두 지우고 적는 쪽이 낫다',
         '자릿수는 스스로 늘려서 적어도 된다'], 0,
        '표의 자릿수는 그 값을 얼마나 촘촘히 재었는지를 담고 있으므로, 줄이거나 늘리지 말고 적힌 '
        '그대로 옮겨야 잰 만큼의 촘촘함이 지켜진다',
        [('자릿수를 줄여서 어림한 값으로 적어 둔다', '잰 만큼의 촘촘함을 버림', 'scale'),
         ('소수점 아래는 모두 지우고 적는 쪽이 낫다', '적힌 자리를 지움', 'conserv'),
         ('자릿수는 스스로 늘려서 적어도 된다', '재지 않은 자리를 만들어 붙임', 'unit')],
        '표의 자릿수가 무엇을 담고 있는지 적기',
        '328.2: 소수 첫째 자리까지 잼 → 그만큼 촘촘함 / 줄이면 잃고 늘리면 없는 것을 만듦 → '
        '그대로 옮김 / ② 잃음 / ③ 지움 / ④ 만들어 붙임',
        '소수점이 붙은 표의 한 칸', '자릿수를 지키는지 확인',
        '표의 자릿수가 무엇을 담고 있는지 적어 봐.',
        '자릿수는 얼마나 촘촘히 잰 값인지를 담고 있어. 그대로 옮겨야지 — ①이 옳아. '
        '②는 자릿수를 줄여서 어림한 값으로 적어 둔다 라고 했어. 잰 만큼을 버리는 거야. '
        '③은 소수점 아래는 모두 지우고 적는 쪽이 낫다 라고 했어. 적힌 자리를 지웠지. '
        '④는 자릿수는 스스로 늘려서 적어도 된다 라고 했어. 재지 않은 자리를 만든 셈이야. '
        '값을 옮길 때는 적힌 자릿수를 그대로 지킨다.',
        {'자릿수를 줄여서 어림한 값으로 적어 둔다': '잰 만큼을 버려.',
         '소수점 아래는 모두 지우고 적는 쪽이 낫다': '적힌 자리를 지웠어.',
         '자릿수는 스스로 늘려서 적어도 된다': '없는 자리를 만들어.'},
        '자릿수를 그대로 옮겼는가?'))

    # ── M02896 ── C16-011[4] 어느 축이 더 가파른지 (심화·어려움) 정답 ② [2위] ──
    it.append(q('M02896', '전기 음성도가 어느 축에서 더 가파르게 바뀌는지 적기',
        '심화', 4, '어려움', 0.28,
        '전기 음성도가 리튬 1.0 에서 플루오린 4.0 으로, 플루오린 4.0 에서 아이오딘 2.7 로 '
        '바뀐다. 두 축의 가파름에 대해 옳은 것은?',
        ['두 축의 변화가 서로 똑같이 가파르게 간다',
         '가로 쪽 변화가 세로 쪽보다 가파르다',
         '세로 쪽 변화가 더 가파르게 나온다',
         '가파름은 원소 이름 차례로 정해진다'], 1,
        '한 주기를 가로지르면 1.0 에서 4.0 으로 3.0 이 바뀌고 한 족을 내려가면 4.0 에서 2.7 로 '
        '1.3 이 바뀌므로, 가로 쪽 변화가 더 가파르다',
        [('두 축의 변화가 서로 똑같이 가파르게 간다', '3.0 과 1.3 을 같다고 봄', 'scale'),
         ('세로 쪽 변화가 더 가파르게 나온다', '가파른 축을 맞바꿈', 'sign'),
         ('가파름은 원소 이름 차례로 정해진다', '이름을 잣대로 삼음', 'conserv')],
        '두 축에서 값이 얼마나 바뀌는지 각각 적기',
        '가로: 1.0 → 4.0, 바뀜 3.0 / 세로: 4.0 → 2.7, 바뀜 1.3 → 3.0 이 더 큼 → 가로가 가파름 / '
        '① 같지 않음 / ③ 맞바꿈 / ④ 이름은 잣대가 아님',
        '두 축의 값이 적힌 세 원소', '두 축의 변화 폭을 견주는지 확인',
        '두 축에서 값이 얼마나 바뀌는지 각각 적어 봐.',
        '가로로는 3.0 이 바뀌고 세로로는 1.3 이 바뀌어. 가로 쪽이 가파르지 — ②가 옳아. '
        '①은 두 축의 변화가 서로 똑같이 가파르게 간다 라고 했어. 3.0 과 1.3 은 다르잖아. '
        '③은 세로 쪽 변화가 더 가파르게 나온다 라고 했어. 가파른 축이 맞바뀌었어. '
        '④는 가파름은 원소 이름 차례로 정해진다 라고 했어. 이름은 잣대가 아니지. '
        '가파름을 물으면 두 축에서 바뀐 폭을 각각 적는다.',
        {'두 축의 변화가 서로 똑같이 가파르게 간다': '3.0 과 1.3 은 달라.',
         '세로 쪽 변화가 더 가파르게 나온다': '맞바뀌었어.',
         '가파름은 원소 이름 차례로 정해진다': '이름은 잣대가 아냐.'},
        '두 축의 폭을 각각 적었는가?'))

    # ── M02897 ── C16-009[5] 한 값을 두 번 쓰지 않기 (일반·중간) 정답 ③ [3위] ──
    it.append(q('M02897', '첫 번째 값과 두 번째 값을 갈라서 표를 읽기',
        '일반', 3, '중간', 0.44,
        '한 표에 산소의 첫 번째 전자 친화도와 두 번째 전자 친화도가 나란히 적혀 있다. '
        '이 표를 읽는 방법으로 옳은 것은?',
        ['두 값의 차를 내어 한 칸에 적는다',
         '첫 번째 값을 두 번째 자리에도 적는다',
         '첫 번째와 두 번째를 갈라서 읽는다',
         '두 번째 값은 없는 셈 치고 본다'], 2,
        '첫 번째는 중성 원자가 전자를 받을 때의 값이고 두 번째는 음이온이 전자를 더 받을 때의 '
        '값이라 서로 다른 자리의 값이므로, 두 칸을 갈라서 읽어야 한다',
        [('두 값의 차를 내어 한 칸에 적는다', '갈라 읽어야 할 자리를 빼기로 뭉갬', 'proc'),
         ('첫 번째 값을 두 번째 자리에도 적는다', '한 값을 두 자리에 겹쳐 씀', 'conserv'),
         ('두 번째 값은 없는 셈 치고 본다', '적혀 있는 값을 지움', 'scale')],
        '두 칸이 저마다 어떤 자리의 값인지 적기',
        '첫 번째: 중성 원자가 받음 / 두 번째: 음이온이 더 받음 → 서로 다른 자리 → 갈라 읽음 / '
        '① 뺄 자리가 아님 / ② 겹쳐 씀 / ④ 적힌 값을 지움',
        '두 칸이 나란한 산소의 표', '두 값을 갈라 읽는지 확인',
        '두 칸이 저마다 어떤 자리의 값인지 적어 봐.',
        '첫 번째는 중성 원자가 받을 때, 두 번째는 음이온이 더 받을 때야 — ③이 옳아. '
        '①은 두 값의 차를 내어 한 칸에 적는다 라고 했어. 두 자리는 빼는 사이가 아니야. '
        '②는 첫 번째 값을 두 번째 자리에도 적는다 라고 했어. 한 값을 겹쳐 쓴 거야. '
        '④는 두 번째 값은 없는 셈 치고 본다 라고 했어. 적힌 값을 지웠잖아. '
        '두 칸이 나란하면 저마다 어떤 자리의 값인지부터 적는다.',
        {'두 값의 차를 내어 한 칸에 적는다': '뺄 자리가 아냐.',
         '첫 번째 값을 두 번째 자리에도 적는다': '겹쳐 썼어.',
         '두 번째 값은 없는 셈 치고 본다': '적힌 값을 지웠어.'},
        '두 자리를 갈라 읽었는가?'))

    # ── M02898 ── C16-013[5] 금속과 비금속으로 갈라 적기 (일반·쉬움) 정답 ① [2위] ──
    it.append(q('M02898', '금속과 비금속으로 갈라 값의 크기를 적기',
        '일반', 2, '쉬움', 0.60,
        '전자 친화도를 금속과 비금속으로 갈라 적으려 한다. 크기에 대해 옳은 것은?',
        ['비금속이 크고 금속이 작은 쪽이다',
         '금속이 크고 비금속이 작은 쪽이 된다',
         '금속과 비금속의 값이 서로 같다',
         '금속과 비금속의 값이 뒤섞인다'], 0,
        '비금속은 전자를 받아 안정한 배치에 가까워지므로 값이 크고, 금속은 전자를 내주는 쪽이라 '
        '받아도 얻는 것이 적어 값이 작다',
        [('금속이 크고 비금속이 작은 쪽이 된다', '두 무리를 맞바꿈', 'sign'),
         ('금속과 비금속의 값이 서로 같다', '갈리는 것을 지움', 'conserv'),
         ('금속과 비금속의 값이 뒤섞인다', '갈리는 두 무리를 뒤섞음', 'scale')],
        '두 무리가 전자를 받는 쪽인지 내주는 쪽인지 적기',
        '비금속: 받아서 안정한 배치에 가까워짐 → 값 큼 / 금속: 내주는 쪽 → 값 작음 / ② 맞바꿈 / '
        '③ 갈림 / ④ 뒤섞이지 않음',
        '금속과 비금속으로 갈라 적으려는 표', '두 무리의 크기를 아는지 확인',
        '두 무리가 전자를 받는 쪽인지 내주는 쪽인지 적어 봐.',
        '비금속은 받는 쪽이라 값이 크고, 금속은 내주는 쪽이라 작아 — ①이 옳아. '
        '②는 금속이 크고 비금속이 작은 쪽이 된다 라고 했어. 두 무리가 맞바뀌었지. '
        '③은 금속과 비금속의 값이 서로 같다 라고 했어. 뚜렷이 갈리잖아. '
        '④는 금속과 비금속의 값이 뒤섞인다 라고 했어. 두 무리는 뚜렷이 갈리지. '
        '무리로 가를 때는 받는 쪽인지 내주는 쪽인지 적는다.',
        {'금속이 크고 비금속이 작은 쪽이 된다': '맞바뀌었어.',
         '금속과 비금속의 값이 서로 같다': '갈려.',
         '금속과 비금속의 값이 뒤섞인다': '뒤섞이지 않아.'},
        '받는 쪽인지 내주는 쪽인지 적었는가?'))

    # ── M02899 ── C16-016[8] 값이 없음과 0 이 다름 (심화·어려움) 정답 ④ [3위] ──
    it.append(q('M02899', '값이 없다는 것과 값이 0 이라는 것을 가르기',
        '심화', 4, '어려움', 0.30,
        '전기 음성도 표에서 네온 칸이 빈칸으로 남아 있었다. 이 빈칸을 읽는 방법으로 '
        '옳은 것은?',
        ['값이 없다는 것은 곧 0 이라는 뜻이 된다',
         '값이 없으면 가장 작은 값으로 적어 둔다',
         '두 말은 표에서 같은 뜻으로 쓰인다',
         '값을 정하지 않은 자리와 0 은 다르다'], 3,
        '전기 음성도는 결합한 자리에서 매기는 값인데 네온은 웬만해서 결합하지 않아 매길 자리가 '
        '없는 것이므로, 값이 0 인 것과는 다르다',
        [('값이 없다는 것은 곧 0 이라는 뜻이 된다', '자리가 없음을 크기 0 으로 봄', 'scale'),
         ('값이 없으면 가장 작은 값으로 적어 둔다', '없는 자리에 값을 만들어 붙임', 'unit'),
         ('두 말은 표에서 같은 뜻으로 쓰인다', '갈리는 두 말을 하나로 봄', 'surface')],
        '이 칸이 왜 비었는지, 매길 자리가 있는지 적기',
        '전기 음성도: 결합한 자리에서 매김 / 네온: 결합하지 않음 → 매길 자리가 없음 → 빈칸 / '
        '0 은 매겼는데 크기가 0 이라는 뜻 → 다름 / ① 다름 / ② 만들어 붙임 / ③ 갈림',
        '빈칸으로 남은 네온 자리', '없음과 0 을 가르는지 확인',
        '이 칸이 왜 비었는지, 매길 자리가 있는지 적어 봐.',
        '네온은 결합하지 않으니 매길 자리 자체가 없어. 0 이라고 매긴 것과는 달라 '
        '— ④가 옳아. '
        '①은 값이 없다는 것은 곧 0 이라는 뜻이 된다 라고 했어. 두 가지는 다르지. '
        '②는 값이 없으면 가장 작은 값으로 적어 둔다 라고 했어. 없는 자리에 값을 만든 거야. '
        '③은 두 말은 표에서 같은 뜻으로 쓰인다 라고 했어. 갈리는 두 말이잖아. '
        '빈칸을 만나면 매길 자리가 있는지부터 적는다.',
        {'값이 없다는 것은 곧 0 이라는 뜻이 된다': '둘은 달라.',
         '값이 없으면 가장 작은 값으로 적어 둔다': '값을 만들었어.',
         '두 말은 표에서 같은 뜻으로 쓰인다': '갈리는 말이야.'},
        '매길 자리가 있는지 적었는가?'))

    # ── M02900 ── C16-014[7] 견줄 수 없는 짝 (일반·중간) 정답 ① [2위] ──
    it.append(q('M02900', '견줄 수 없는 짝을 만나면 그렇게 적기',
        '일반', 3, '중간', 0.42,
        '표를 보지 않고 경향만으로 두 원소를 견주려는데, 주기의 경향과 족의 경향이 서로 맞서는 짝이었다. '
        '이때 적을 말로 옳은 것은?',
        ['경향으로는 어렵다고 적고 표를 찾는다',
         '축이 달라도 경향만으로 정해 둔다',
         '두 원소 가운데 아무 쪽이나 적는다',
         '주기의 경향과 족의 경향을 겹쳐서 댄다'], 0,
        '주기의 경향과 족의 경향은 서로 다른 축의 이야기라 축이 어긋난 두 원소는 경향만으로 '
        '앞뒤를 정할 수 없으므로, 그렇게 적고 표의 값을 보아야 한다',
        [('축이 달라도 경향만으로 정해 둔다', '정할 수 없는 것을 정함', 'overgen'),
         ('두 원소 가운데 아무 쪽이나 적는다', '고를 수 없는 자리에서 고름', 'proc'),
         ('주기의 경향과 족의 경향을 겹쳐서 댄다', '다른 축의 이야기를 겹침', 'conserv')],
        '두 원소가 같은 축 위에 있는지 적기',
        '주기도 다르고 족도 다름 → 같은 축 위가 아님 → 경향만으로는 앞뒤를 못 정함 → 그렇게 '
        '적고 표를 봄 / ② 정할 수 없음 / ③ 고를 수 없음 / ④ 겹치면 어긋남',
        '축이 어긋난 두 원소', '정할 수 없음을 적는지 확인',
        '두 원소가 같은 축 위에 있는지 적어 봐.',
        '주기도 족도 다르니 같은 축 위가 아니야. 경향만으로는 못 정하지 — ①이 옳아. '
        '②는 축이 달라도 경향만으로 정해 둔다 라고 했어. 정할 수 없는 자리야. '
        '③은 두 원소 가운데 아무 쪽이나 적는다 라고 했어. 고를 수 있는 자리가 아니지. '
        '④는 주기의 경향과 족의 경향을 겹쳐서 댄다 라고 했어. 겹치면 어긋나. '
        '축이 어긋나면 못 정한다고 적고 표를 본다.',
        {'축이 달라도 경향만으로 정해 둔다': '정할 수 없어.',
         '두 원소 가운데 아무 쪽이나 적는다': '고를 자리 아냐.',
         '주기의 경향과 족의 경향을 겹쳐서 댄다': '겹치면 어긋나.'},
        '같은 축 위인지 적었는가?'))

    return it


_ANON = re.compile(r'원소 (?:가|나|다|A|B|C)(?=의|와|과|는|은|를|이|가|,|·|\s|$)')


def local_checks(items):
    """T15 지역 저작 점검 — P3 의 것을 그대로 잇는다(⑧ 겹침 · ⑨ 길이 포함)."""
    bad = []
    ALLOWED = {'proc', 'sign', 'surface', 'conserv', 'unit', 'scale', 'overgen', 'causal'}
    for x in items:
        ts = [w['type'] for w in x['distractors']]
        if len(set(ts)) < 3:
            bad.append(f"{x['id']} 오답 유형이 겹친다 — {ts}")
        for t in ts:
            if t not in ALLOWED:
                bad.append(f"{x['id']} 허용되지 않은 유형 {t}")
        # ★★이 검사는 P3 3차까지 한 번도 울 수 없었다★★ — sol() 이 해설 끝에 붙이는
        #   단평 블록이 오답 문면을 ★글자 그대로★ 되풀이하므로 `in x['solution']` 은 언제나
        #   참이었다. 해설 산문에서 인용이 흐트러져도 검사는 통과한다(defender 와
        #   factchecker 가 각각 두 자리를 손으로 잡아 냈다). ▸ ★산문 문단만 본다.★
        prose = x['solution'].split('\n\n')[1] if '\n\n' in x['solution'] else x['solution']
        #   ▸ 산문은 간접 인용이라 종결어미가 바뀐다('…때문이다' → '…때문이라고 했어').
        #     ★어미를 뗀 몸통★ 이 그대로 있는지만 본다 — 몸통이 어긋나면 인용이 흐트러진 것이다.
        for w in x['distractors']:
            core = re.sub(r'(이)?다$', '', x['choices'][w['opt']]).strip()
            if core not in prose:
                bad.append(f"{x['id']} {w['opt'] + 1}번 오답 인용이 해설 산문과 어긋난다")
        if not re.search(r'[①②③④](가|이)? 옳아', x['solution']):
            bad.append(f"{x['id']} 해설에 정답 확정 어구가 없다")
        J = ('를|을|은|의|으로|로|에서|에|과|보다|처럼|까지|부터|이고|이다|이야|이라|이지')
        for f in ('stem', 'solution', 'answer_proof'):
            for m in re.finditer(rf'(?<=[가-힣]) ({J})(?=[\s,.·)?!]|$)', x[f]):
                bad.append(f"{x['id']} {f} 조사 앞 빈칸 — {x[f][max(0, m.start() - 14):m.end()]!r}")
        tails = [c.strip()[-3:] for c in x['choices']]
        for t in set(tails):
            if tails.count(t) == 3 and tails[x['answer']] != t:
                bad.append(f"{x['id']} 어미 「{t}」 셋 대 정답 하나 — 꼴로 갈린다")

        def tk(s):
            return {w for w in re.findall(r'[가-힣]{2,}', s)}
        for i in range(4):
            for j in range(i + 1, 4):
                if x['answer'] in (i, j):
                    continue
                a, b = tk(x['choices'][i]), tk(x['choices'][j])
                if a and b and len(a & b) / len(a | b) >= 0.62:
                    bad.append(f"{x['id']} 오답 {i + 1}·{j + 1} 자카드 "
                               f"{len(a & b) / len(a | b):.2f}")
    for x in items:
        s = x['stem']
        if '톱니' in s and '세로축' not in s:
            bad.append(f"{x['id']} 톱니를 쓰면서 세로축을 적지 않았다")
        anon = _ANON.search(s)
        named = re.search(r'(리튬|베릴륨|붕소|탄소|질소|산소|플루오린|네온|나트륨|마그네슘|염소'
                          r'|칼륨|알루미늄|헬륨|수소|아르곤|브로민|Li|Be|B\b|C\b|N\b|O\b|F\b|Ne|Na)', s)
        if anon and named:
            bad.append(f"{x['id']} 익명 원소와 원소 이름을 함께 인쇄했다 — "
                       f"{anon.group(0)!r} · {named.group(0)!r}")
        if '크게 뛰' in s or '급증' in s:
            if not re.search(r'비\(뒤 ÷ 앞\)|비로 재|앞까지의 개수', s):
                bad.append(f"{x['id']} 급증의 셈법을 발문이 정하지 않았다")
    g = sum(1 for x in items if x['track'] == '일반')
    lo = 7 if len(items) >= 10 else max(2, len(items) // 2)
    if g < lo:
        bad.append(f'일반 {g}제 — {len(items)}문항 배치의 하한은 {lo}')
    e = sum(1 for x in items if x['difficulty'] == '쉬움')
    if e > 3:
        bad.append(f'쉬움 {e}제 — 상한 3')

    # ⑧ ★정답이 발문과의 어휘 겹침 유일 최다이면 실패★ (P1 이 값을 치른 자리)
    for x in items:
        st = set(re.findall(r'[0-9A-Za-z가-힣]{2,}', x['stem']))
        ov = [len(set(re.findall(r'[0-9A-Za-z가-힣]{2,}', c)) & st) for c in x['choices']]
        if ov[x['answer']] == max(ov) and ov.count(max(ov)) == 1:
            bad.append(f"{x['id']} 정답이 발문과의 어휘 겹침 최다(유일) — {ov}")

    # ⑨ ★선지 길이 — 정답이 유일 최장/최단이거나 산포가 0.25 를 넘으면 실패★ (P3 이 값을 치렀다)
    for x in items:
        L = [len(c) for c in x['choices']]
        a = L[x['answer']]
        if a == max(L) and L.count(a) == 1:
            bad.append(f"{x['id']} 정답이 유일 최장 — {L}")
        if a == min(L) and L.count(a) == 1:
            bad.append(f"{x['id']} 정답이 유일 최단 — {L}")
        sp = (max(L) - min(L)) / (sum(L) / len(L))
        if sp > 0.25:
            bad.append(f"{x['id']} 선지 길이 산포 {sp:.2f} > 0.25 — {L}")

    print(f"  ▸ 배치 요약 — 유형 {sorted({w['type'] for x in items for w in x['distractors']})}")
    if bad:
        print('\n  ❌ 저작 점검 실패')
        for b in bad:
            print('     ·', b)
        raise SystemExit(1)
    print('  ✅ 저작 점검 통과')



if __name__ == '__main__':
    items = build()
    local_checks(items)
    T.verify(items)
    T.merge_and_house(items)
