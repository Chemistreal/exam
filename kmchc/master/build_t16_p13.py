# -*- coding: utf-8 -*-
"""T16 P13 (M02583~M02592) — 표를 읽고 값을 다루는 손버릇

  각도: C16-011[7] · 011[8] · 012[7] · 012[8] · 014[7] · 014[8] · 014[9] · 018[7] ·
        019[7] · 019[8]   ★2차 확장으로 새로 넣은 44 개에서 골랐다★

  ★이 배치는 '무엇을 아는가' 가 아니라 '표 앞에서 무엇을 하는가' 를 묻는다★ — T16 은 앞서
  열두 배치가 거의 다 판정·가르기였다. 절차 갈래를 한 배치로 모아 결이 겹치는 것을 막는다.

  ★정답 자리 2·4·3·1·4·2·1·3·2·4 — 은행에서 셈하고 build() 로 맞댄다★
  (병합 뒤 목표 ①648②648③648④648 — ★편차 0★. 처음 셈은 4·2·3·1·4·2·1·3·2·4 였는데
   ④ 이 앞 배치의 -7·-3 에 이어 1·5 로 놓여 ★간격 4 로 넷★ 이 되기에 첫 자리를 ② 로 옮겼다)

  ★T15·T16 P1~P12 의 규약을 그대로 이어 건다★
   ㉠ 오답 셋이 같은 오류 유형을 나눠 갖지 않게 ㉡ 오답의 근거절을 참으로 두지 않기
   ㉢ 정답이 발문 어구를 되받지 않게 · 단정과 유보를 섞기 ㉣ 뜻·길이·어미·극성을 한 묶음으로
   ㉥ 교과가 참으로 가르치는 명제를 오답에 두지 않기 ㉧ 길이는 오답을 움직여 맞춘다
   ㉩ 한 마디가 정답에만 모이지 않게(G3g) ㉪ 발문이 반드시 써야 하는 낱말은 정답이 피한다
   ㉫ 정답 자리를 옮기면 계산 줄의 번호를 다시 센다
   ㉬ 한 배치에서 같은 개념의 두 각도가 서로의 해설이 되지 않게
   ㉭ 자리 이름(scenario)에 그 문항만의 낱말을 넣는다
   ㉮ ★정답의 꼬리말이 발문에만 있는 낱말이 되지 않게★(P10 M02560 '것은' · P12 M02582
     '있다' — 겹침 검사는 어휘의 뜻을 보지 않으므로 군더더기 낱말에서도 운다)
"""
import re

import batch_template as T

T.START_ID = 'M02583'
T.COUNT = 10
T.EXPECT_LEN = 2582
T.THEME = '화학결합'
T.TT = 17
T.UNIT = 'I'
T.BATCH_NOTE = 'T16 P13 — 표를 읽고 값을 다루는 손버릇.'


def q(i, sk, tr, dok, df, esr, stem, cs, ans, proof, wr, dev, calc, scen, obj,
      lead, prose, wmap, diag):
    x = T.mk(i, sk, tr, dok, df, esr, stem, cs, ans, proof, wr, dev, calc, scen, obj=obj)
    T.sol(x, lead, prose, wmap, diag)
    return x


def build():
    it = []

    # ── M02583 ── C16-011[7] 표에서 두 값을 골라 견주기 (일반·중간) 정답 ② ──
    it.append(q('M02583', '전기음성도 표에서 견줄 두 값을 골라 오기',
        '일반', 3, '중간', 0.46,
        '전기음성도 표에는 여러 원소의 값이 적혀 있다. 한 결합의 치우침을 보려 할 때 표에서 '
        '값을 고르는 방법으로 옳은 것은?',
        ['표에 있는 값을 모두 더해 본다',
         '결합을 이룬 두 원소의 값만 고른다',
         '가장 큰 값과 가장 작은 값을 고른다',
         '같은 족에 있는 값들을 골라 본다'], 1,
        '치우침은 그 결합을 이룬 두 원자가 전자쌍을 서로 얼마나 세게 끄는지를 견주는 것이므로, '
        '표에서 필요한 값은 그 두 원소의 것뿐이고 나머지 값은 이 물음과 상관이 없다',
        [('표에 있는 값을 모두 더해 본다', '쓸 값과 안 쓸 값을 가르지 않음', 'proc'),
         ('가장 큰 값과 가장 작은 값을 고른다', '견줄 짝을 표의 양 끝으로 잡음', 'scale'),
         ('같은 족에 있는 값들을 골라 본다', '경향을 보는 일과 견주는 일을 섞음', 'surface')],
        '이 물음에 꼭 있어야 하는 값이 몇 개인지 먼저 세기',
        '치우침 = 두 원자가 서로 끄는 세기의 견줌 → 값은 딱 둘 필요 / 나머지는 상관없음 / '
        '① 안 쓸 값까지 더함 / ③ 표의 양 끝은 이 결합과 무관 / ④ 족의 경향은 다른 물음',
        '여러 원소가 실린 전기음성도 표', '쓸 값만 골라 오는지 확인',
        '이 물음에 꼭 있어야 하는 값이 몇 개인지 먼저 세어 봐.',
        '치우침은 두 원자가 서로 끄는 세기를 견주는 거라 값은 딱 둘만 있으면 돼 — ②가 옳아. '
        '①은 표에 있는 값을 모두 더해 본다 라고 했어. 안 쓸 값까지 더한 거지. '
        '③은 가장 큰 값과 가장 작은 값을 고른다 라고 했어. 표의 양 끝은 이 결합과 무관해. '
        '④는 같은 족에 있는 값들을 골라 본다 라고 했어. 족의 경향은 다른 물음이야. '
        '표를 쓸 때는 물음에 꼭 있어야 하는 값이 몇 개인지부터 센다.',
        {'표에 있는 값을 모두 더해 본다': '안 쓸 값까지야.',
         '가장 큰 값과 가장 작은 값을 고른다': '이 결합과 무관해.',
         '같은 족에 있는 값들을 골라 본다': '다른 물음이야.'},
        '필요한 값의 개수를 세었는가?'))

    # ── M02584 ── C16-011[8] 값이 가장 큰 자리를 짚기 (일반·쉬움) 정답 ④ ──
    it.append(q('M02584', '주기율표에서 전기음성도가 가장 큰 자리를 짚기',
        '일반', 2, '쉬움', 0.58,
        '주기율표에서 전기음성도가 가장 큰 자리가 어디인지 짚으려 한다. 옳은 것은?',
        ['주기율표 왼쪽 아래의 금속 쪽이다',
         '가운데 줄의 원소들 쪽이다',
         '비활성 기체가 있는 맨 끝이다',
         '오른쪽 위에 있는 플루오린 쪽이다'], 3,
        '전기음성도는 같은 주기에서 오른쪽으로 갈수록 커지고 같은 족에서 위로 갈수록 커지므로 '
        '두 경향이 만나는 오른쪽 위가 가장 큰 자리이고, 그 자리에 있는 플루오린이 가장 크다',
        [('주기율표 왼쪽 아래의 금속 쪽이다', '두 경향을 모두 뒤집음', 'sign'),
         ('가운데 줄의 원소들 쪽이다', '가운데를 큰 쪽으로 봄', 'surface'),
         ('비활성 기체가 있는 맨 끝이다', '값을 매기지 않는 자리를 고름', 'conserv')],
        '두 축의 경향을 화살표로 그려 만나는 자리를 찾기',
        '주기: 오른쪽으로 갈수록 커짐 / 족: 위로 갈수록 커짐 → 두 화살표가 만나는 곳은 오른쪽 위 '
        '→ 플루오린 / ① 두 경향이 모두 뒤집힘 / ② 가운데는 큰 쪽이 아님 / ③ 비활성 기체에는 '
        '값을 매기지 않음',
        '전기음성도의 두 경향', '두 축이 만나는 자리를 짚는지 확인',
        '두 축의 경향을 화살표로 그려 만나는 자리를 찾아봐.',
        '오른쪽으로 갈수록 커지고 위로 갈수록 커지니 두 화살표는 오른쪽 위에서 만나 — ④가 옳아. '
        '①은 주기율표 왼쪽 아래의 금속 쪽이다 라고 했어. 두 경향이 모두 뒤집혔지. '
        '②는 가운데 줄의 원소들 쪽이다 라고 했어. 가운데는 큰 쪽이 아니야. '
        '③은 비활성 기체가 있는 맨 끝이다 라고 했어. 거기는 값을 매기지 않아. '
        '경향을 물으면 두 축을 화살표로 그려 만나는 자리를 찾는다.',
        {'주기율표 왼쪽 아래의 금속 쪽이다': '모두 뒤집혔어.',
         '가운데 줄의 원소들 쪽이다': '큰 쪽이 아냐.',
         '비활성 기체가 있는 맨 끝이다': '값을 안 매겨.'},
        '두 축이 만나는 자리를 찾았는가?'))

    # ── M02585 ── C16-012[7] 큰 값에서 작은 값을 뺌 (일반·쉬움) 정답 ③ ──
    it.append(q('M02585', '전기음성도 차를 구하는 뺄셈의 차례를 정하기',
        '일반', 2, '쉬움', 0.60,
        '두 원소의 전기음성도가 각각 3.0 과 2.1 일 때 결합의 성격을 보려고 차를 구한다. 차를 '
        '구하는 방법으로 옳은 것은?',
        ['작은 값에서 큰 값을 빼어 음수로 둔다',
         '두 값을 더한 것을 차로 삼으면 된다',
         '큰 값에서 작은 값을 빼어 양수로 둔다',
         '작은 값으로 큰 값을 나누어 차로 삼는다'], 2,
        '여기서 쓰는 차는 두 값이 얼마나 벌어져 있는지를 나타내는 것이므로 큰 값에서 작은 값을 '
        '빼어 0 보다 큰 수로 두어야 하고, 그래야 차의 크기로 성격을 견줄 수 있다',
        [('작은 값에서 큰 값을 빼어 음수로 둔다', '뺄셈의 차례를 뒤집음', 'sign'),
         ('두 값을 더한 것을 차로 삼으면 된다', '차를 합으로 바꿈', 'conserv'),
         ('작은 값으로 큰 값을 나누어 차로 삼는다', '차를 비로 바꿈', 'proc')],
        '벌어진 정도를 나타내려면 수가 0 보다 커야 함을 적기',
        '벌어진 정도 → 0 보다 큰 수여야 함 → 큰 값에서 작은 값을 뺌 → 3.0 − 2.1 = 0.9 / '
        '① 차례가 뒤집혀 음수가 됨 / ② 합은 벌어진 정도가 아님 / ④ 비는 벌어진 정도가 아님',
        '값이 3.0 과 2.1 인 두 원소', '뺄셈의 차례를 바르게 정하는지 확인',
        '벌어진 정도를 나타내려면 수가 0 보다 커야 함을 적어 봐.',
        '벌어진 정도를 나타내려면 0 보다 큰 수여야 하니 큰 값에서 작은 값을 빼야 해. 3.0 에서 '
        '2.1 을 빼면 0.9 지 — ③이 옳아. '
        '①은 작은 값에서 큰 값을 빼어 음수로 둔다 라고 했어. 차례가 뒤집혔어. '
        '②는 두 값을 더한 것을 차로 삼으면 된다 라고 했어. 합은 벌어진 정도가 아니지. '
        '④는 작은 값으로 큰 값을 나누어 차로 삼는다 라고 했어. 비도 벌어진 정도가 아니야. '
        '차를 구할 때는 그 수가 무엇을 나타내야 하는지부터 적는다.',
        {'작은 값에서 큰 값을 빼어 음수로 둔다': '차례가 뒤집혔어.',
         '두 값을 더한 것을 차로 삼으면 된다': '합은 아냐.',
         '작은 값으로 큰 값을 나누어 차로 삼는다': '비도 아냐.'},
        '그 수가 무엇을 나타내는지 적었는가?'))

    # ── M02586 ── C16-012[8] 표마다 값이 조금 다를 수 있음 (심화·중간) 정답 ① ──
    it.append(q('M02586', '표에 따라 전기음성도 값이 조금 다른 까닭을 짚기',
        '심화', 3, '중간', 0.32,
        '두 책에 실린 전기음성도 표에서 같은 원소의 값이 조금 달랐다. 이 차이에 대해 옳은 것은?',
        ['견주어 매긴 값이라 표마다 다를 수 있다',
         '둘 가운데 하나는 잘못된 값임이 분명하다',
         '값이 다르면 원소도 다른 것이라고 본다',
         '전기음성도는 원래 정해지지 않은 값이다'], 0,
        '전기음성도는 자로 곧바로 재는 양이 아니라 결합에서 끄는 세기를 견주어 매긴 상대값이라 '
        '어떤 방법으로 매겼는지에 따라 조금씩 달라지지만, 원소들의 차례는 대체로 같다',
        [('둘 가운데 하나는 잘못된 값임이 분명하다', '다름을 곧바로 틀림으로 봄', 'overgen'),
         ('값이 다르면 원소도 다른 것이라고 본다', '값의 차이를 원소의 차이로 옮김', 'causal'),
         ('전기음성도는 원래 정해지지 않은 값이다', '조금 다름을 아예 없음으로 넓힘', 'conserv')],
        '이 값이 자로 잰 것인지 견주어 매긴 것인지 먼저 적기',
        '자로 잰 양: 하나로 정해짐 / 견주어 매긴 값: 매기는 방법에 따라 조금 달라짐 → 전기음성도는 '
        '뒤쪽 → 표마다 조금 다름 / ② 다르다고 곧 틀린 것은 아님 / ③ 원소는 그대로 / ④ 대체로 '
        '같은 차례로 정해져 있음',
        '두 책의 전기음성도 표', '상대값이라는 성격에서 차이를 설명하는지 확인',
        '이 값이 자로 잰 것인지 견주어 매긴 것인지 먼저 적어 봐.',
        '전기음성도는 자로 재는 양이 아니라 견주어 매긴 상대값이야. 매기는 방법에 따라 조금씩 '
        '달라지지 — ①이 옳아. '
        '②는 둘 가운데 하나는 잘못된 값임이 분명하다 라고 했어. 다르다고 곧 틀린 건 아니야. '
        '③은 값이 다르면 원소도 다른 것이라고 본다 라고 했어. 원소는 그대로지. '
        '④는 전기음성도는 원래 정해지지 않은 값이다 라고 했어. 차례는 대체로 정해져 있어. '
        '값이 어긋나면 그 값이 어떻게 매겨진 것인지부터 적는다.',
        {'둘 가운데 하나는 잘못된 값임이 분명하다': '틀린 건 아냐.',
         '값이 다르면 원소도 다른 것이라고 본다': '원소는 그대로야.',
         '전기음성도는 원래 정해지지 않은 값이다': '차례는 정해져 있어.'},
        '어떻게 매긴 값인지 적었는가?'))

    # ── M02587 ── C16-014[7] 부호를 에너지 층계로 확인 (일반·중간) 정답 ④ ──
    it.append(q('M02587', '어림한 반응열의 부호를 에너지 층계 그림으로 확인하기',
        '일반', 3, '중간', 0.44,
        '결합 에너지로 어림한 반응열이 음수로 나왔다. 이 값을 그림으로 확인하는 방법으로 '
        '옳은 것은?',
        ['반응물이 생성물보다 낮게 그려진다',
         '두 쪽의 높이가 같게 그려진다',
         '높이는 부호와 상관없이 정해진다',
         '생성물이 반응물보다 낮게 그려진다'], 3,
        '반응열이 음수라는 것은 에너지를 내놓았다는 뜻이므로 남은 에너지가 처음보다 적어져 '
        '생성물 쪽이 반응물 쪽보다 낮은 자리에 그려진다',
        [('반응물이 생성물보다 낮게 그려진다', '높낮이를 뒤집음', 'sign'),
         ('두 쪽의 높이가 같게 그려진다', '내놓은 몫을 지움', 'conserv'),
         ('높이는 부호와 상관없이 정해진다', '부호와 그림을 끊어 놓음', 'proc')],
        '음수라는 말이 에너지가 늘었다는 뜻인지 줄었다는 뜻인지 적기',
        '반응열 음수 → 에너지를 내놓음 → 남은 에너지가 줄어듦 → 생성물이 아래 / ① 위아래가 '
        '뒤집힘 / ② 같으면 내놓은 몫이 0 / ③ 부호가 곧 높낮이를 정함',
        '음수로 나온 어림값', '부호를 그림의 높낮이로 옮기는지 확인',
        '음수라는 말이 에너지가 늘었다는 뜻인지 줄었다는 뜻인지 적어 봐.',
        '반응열이 음수면 에너지를 내놓은 것이니 남은 에너지가 줄어. 생성물이 아래에 그려지지 '
        '— ④가 옳아. '
        '①은 반응물이 생성물보다 낮게 그려진다 라고 했어. 위아래가 뒤집혔어. '
        '②는 두 쪽의 높이가 같게 그려진다 라고 했어. 그러면 내놓은 몫이 0 이지. '
        '③은 높이는 부호와 상관없이 정해진다 라고 했어. 부호가 곧 높낮이를 정해. '
        '부호를 확인할 때는 그것이 늘었다는 뜻인지 줄었다는 뜻인지 먼저 적는다.',
        {'반응물이 생성물보다 낮게 그려진다': '뒤집혔어.',
         '두 쪽의 높이가 같게 그려진다': '몫이 0 이 돼.',
         '높이는 부호와 상관없이 정해진다': '부호가 정해.'},
        '늘었는지 줄었는지 적었는가?'))

    # ── M02588 ── C16-014[8] 필요한 값만 골라 오기 (일반·중간) 정답 ② ──
    it.append(q('M02588', '결합 에너지 표에서 셈에 쓸 값만 골라 오기',
        '일반', 3, '중간', 0.44,
        '결합 에너지 표에는 수십 가지 결합의 값이 실려 있다. 한 반응의 열을 어림할 때 표에서 '
        '값을 골라 오는 방법으로 옳은 것은?',
        ['표에 실린 값을 모두 더해서 가져온다',
         '끊는 결합과 만드는 결합의 값만 온다',
         '가장 큰 값 하나만 골라서 온다',
         '반응물 쪽 값만 골라 오면 된다'], 1,
        '반응열의 어림은 끊은 결합의 에너지 합에서 만든 결합의 에너지 합을 빼는 셈이므로 이 '
        '반응에서 실제로 끊기는 결합과 새로 만들어지는 결합의 값만 있으면 되고 나머지는 쓰지 않는다',
        [('표에 실린 값을 모두 더해서 가져온다', '쓸 값과 안 쓸 값을 가르지 않음', 'proc'),
         ('가장 큰 값 하나만 골라서 온다', '합을 한 값으로 줄임', 'scale'),
         ('반응물 쪽 값만 골라 오면 된다', '빼는 쪽 항을 통째로 지움', 'conserv')],
        '셈에 들어가는 두 항이 무엇인지 적고 표에서 그것만 찾기',
        '두 항: 끊은 결합의 합 · 만든 결합의 합 → 둘에 들어가는 결합만 필요 / ① 안 쓸 값까지 '
        '더함 / ③ 합이 아니라 한 값이 됨 / ④ 만드는 쪽이 빠지면 뺄 것이 없음',
        '수십 가지가 실린 결합 에너지 표', '셈에 들어가는 값만 골라 오는지 확인',
        '셈에 들어가는 두 항이 무엇인지 적고 표에서 그것만 찾아봐.',
        '셈에 들어가는 항은 끊은 결합의 합과 만든 결합의 합 둘이야. 그 둘에 들어가는 값만 있으면 '
        '돼 — ②가 옳아. '
        '①은 표에 실린 값을 모두 더해서 가져온다 라고 했어. 안 쓸 값까지 더한 거지. '
        '③은 가장 큰 값 하나만 골라서 온다 라고 했어. 합이 아니라 한 값이 되잖아. '
        '④는 반응물 쪽 값만 골라 오면 된다 라고 했어. 만드는 쪽이 빠지면 뺄 것이 없어. '
        '표에서 값을 가져올 때는 셈의 항을 먼저 적는다.',
        {'표에 실린 값을 모두 더해서 가져온다': '안 쓸 값까지야.',
         '가장 큰 값 하나만 골라서 온다': '합이 아니게 돼.',
         '반응물 쪽 값만 골라 오면 된다': '뺄 것이 없어.'},
        '셈의 항을 먼저 적었는가?'))

    # ── M02589 ── C16-014[9] 반응열이 몰당 값임 (일반·중간) 정답 ① ──
    it.append(q('M02589', '어림한 반응열에 몰당 단위를 붙여 적기',
        '일반', 3, '중간', 0.42,
        '결합 에너지로 어림한 반응열을 적을 때 단위를 어떻게 붙이는지 묻는다. 옳은 것은?',
        ['반응식대로 일어난 몰당 값으로 적는다',
         '단위 없이 숫자만 적어 두면 그만이다',
         '분자 하나마다의 값으로 적는다',
         '온도와 같은 단위로 적으면 된다'],  0,
        '결합 에너지가 몰당 값으로 주어져 있으므로 그것을 더하고 뺀 반응열도 몰당 값이 되고, '
        '적힌 반응식이 계수대로 한 번 일어났을 때의 값으로 읽어야 한다',
        [('단위 없이 숫자만 적어 두면 그만이다', '값과 단위를 떼어 놓음', 'conserv'),
         ('분자 하나마다의 값으로 적는다', '몰당 값을 낱개당 값으로 봄', 'scale'),
         ('온도와 같은 단위로 적으면 된다', '에너지를 온도로 바꿈', 'surface')],
        '더하고 뺀 값들이 어떤 단위였는지 거슬러 올라가 적기',
        '결합 에너지의 단위: kJ/mol → 더하고 빼도 단위는 그대로 → 반응열도 몰당 값 / 반응식 '
        '계수대로 한 번 일어난 몫 / ② 단위가 없으면 값이 뜻을 잃음 / ③ 낱개당이 아님 / '
        '④ 온도와는 다른 양',
        '어림해 적을 반응열 한 줄', '몰당 값임을 단위로 적는지 확인',
        '더하고 뺀 값들이 어떤 단위였는지 거슬러 올라가 적어 봐.',
        '결합 에너지가 몰당 값이니 그것을 더하고 뺀 반응열도 몰당 값이야. 반응식이 계수대로 한 번 '
        '일어난 몫으로 읽지 — ①이 옳아. '
        '②는 단위 없이 숫자만 적어 두면 그만이다 라고 했어. 단위가 없으면 값이 뜻을 잃어. '
        '③은 분자 하나마다의 값으로 적는다 라고 했어. 낱개당이 아니지. '
        '④는 온도와 같은 단위로 적으면 된다 라고 했어. 온도와는 다른 양이야. '
        '단위를 붙일 때는 재료가 된 값들의 단위를 거슬러 올라간다.',
        {'단위 없이 숫자만 적어 두면 그만이다': '뜻을 잃어.',
         '분자 하나마다의 값으로 적는다': '낱개당이 아냐.',
         '온도와 같은 단위로 적으면 된다': '다른 양이야.'},
        '재료의 단위를 거슬러 올라갔는가?'))

    # ── M02590 ── C16-018[7] 표에서 수소 결합을 하는 물질 고르기 (일반·중간) 정답 ③ ──
    it.append(q('M02590', '표에서 수소 결합을 하는 물질을 골라내기',
        '일반', 3, '중간', 0.42,
        '어떤 표에 여러 분자의 화학식이 적혀 있다. 그 가운데 수소 결합을 하는 것을 골라내는 '
        '방법으로 옳은 것은?',
        ['수소가 들어 있는 것을 모조리 고른다',
         '분자량이 가장 작은 것을 골라내면 그만이다',
         '전기음성도가 큰 원자에 붙은 수소를 본다',
         '수소가 둘 이상 들어 있는 것만 고른다'], 2,
        '수소 결합은 수소가 전기음성도가 아주 큰 원자에 곧바로 붙어 있을 때에만 서므로, 표에서는 '
        '수소가 있는지가 아니라 그 수소가 어떤 원자에 붙어 있는지를 보아야 한다',
        [('수소가 들어 있는 것을 모조리 고른다', '조건의 절반만 보고 넓힘', 'overgen'),
         ('분자량이 가장 작은 것을 골라내면 그만이다', '무게를 조건으로 씀', 'surface'),
         ('수소가 둘 이상 들어 있는 것만 고른다', '조건을 개수로 옮김', 'scale')],
        '화학식에서 수소 옆에 어떤 원자가 적혀 있는지 하나씩 보기',
        '조건: 수소가 전기음성도가 아주 큰 원자에 곧바로 붙어 있을 것 → 붙은 상대를 봄 / '
        '① 수소만 보면 아닌 것까지 들어옴 / ② 무게는 조건이 아님 / ④ 개수는 조건이 아님',
        '분자의 화학식이 실린 표', '붙은 상대를 보고 골라내는지 확인',
        '화학식에서 수소 옆에 어떤 원자가 적혀 있는지 하나씩 봐.',
        '수소 결합은 수소가 전기음성도가 아주 큰 원자에 곧바로 붙어 있어야 서. 붙은 상대를 봐야지 '
        '— ③이 옳아. '
        '①은 수소가 들어 있는 것을 모조리 고른다 라고 했어. 아닌 것까지 들어와. '
        '②는 분자량이 가장 작은 것을 골라내면 그만이다 라고 했어. 무게는 조건이 아니지. '
        '④는 수소가 둘 이상 들어 있는 것만 고른다 라고 했어. 개수도 조건이 아니야. '
        '골라낼 때는 조건을 다 적고 하나씩 맞대어 본다.',
        {'수소가 들어 있는 것을 모조리 고른다': '아닌 것도 들어와.',
         '분자량이 가장 작은 것을 골라내면 그만이다': '무게는 조건이 아냐.',
         '수소가 둘 이상 들어 있는 것만 고른다': '개수도 아냐.'},
        '조건을 다 적고 맞대었는가?'))

    # ── M02591 ── C16-019[7] 표의 빈칸을 결합의 종류에서 채우기 (심화·중간) 정답 ② ──
    it.append(q('M02591', '결합의 종류에서 표의 빈 성질 칸을 채워 넣기',
        '심화', 3, '중간', 0.34,
        '표에 어떤 물질의 결합이 이온 결합이라고만 적혀 있고 고체에서의 전기 전도 칸이 비어 '
        '있다. 그 칸을 채우는 방법으로 옳은 것은?',
        ['알 수 없으므로 비워 두어야 한다',
         '이온이 묶여 있으니 안 통한다고 적는다',
         '이온이 있으니 잘 통한다고 적는다',
         '녹는점을 재어 본 뒤에야 적을 수 있다'], 1,
        '이온 결합 물질은 고체에서 이온이 제자리에 묶여 움직이지 못하므로 전기가 통하지 않고, '
        '결합의 종류를 알면 이 칸은 재어 보지 않아도 채울 수 있다',
        [('알 수 없으므로 비워 두어야 한다', '되돌릴 수 있는 자리를 못 본다고 봄', 'proc'),
         ('이온이 있으니 잘 통한다고 적는다', '이온이 있음과 움직임을 같은 것으로 봄', 'sign'),
         ('녹는점을 재어 본 뒤에야 적을 수 있다', '상관없는 값을 조건으로 걺', 'conserv')],
        '고체에서 전기가 통하려면 무엇이 움직여야 하는지 적기',
        '통하려면 움직이는 전하가 있어야 함 / 이온 결합 고체: 이온이 제자리에 묶임 → 안 통함 → '
        '칸을 채울 수 있음 / ① 종류를 알면 채울 수 있음 / ③ 있는 것과 움직이는 것은 다름 / '
        '④ 녹는점은 이 칸과 상관없음',
        '전도 칸이 빈 이온 결합 물질', '종류에서 성질을 내다보는지 확인',
        '고체에서 전기가 통하려면 무엇이 움직여야 하는지 적어 봐.',
        '통하려면 움직이는 전하가 있어야 하는데 이온 결합 고체에서는 이온이 제자리에 묶여 있어 '
        '— ②가 옳아. '
        '①은 알 수 없으므로 비워 두어야 한다 라고 했어. 종류를 알면 채울 수 있지. '
        '③은 이온이 있으니 잘 통한다고 적는다 라고 했어. 있는 것과 움직이는 것은 달라. '
        '④는 녹는점을 재어 본 뒤에야 적을 수 있다 라고 했어. 녹는점은 이 칸과 상관없어. '
        '빈칸을 채울 때는 그 성질이 무엇에서 나오는지 적는다.',
        {'알 수 없으므로 비워 두어야 한다': '채울 수 있어.',
         '이온이 있으니 잘 통한다고 적는다': '움직여야 통해.',
         '녹는점을 재어 본 뒤에야 적을 수 있다': '상관없어.'},
        '무엇에서 나오는 성질인지 적었는가?'))

    # ── M02592 ── C16-019[8] 두 성질이 어긋날 때 (심화·어려움) 정답 ④ ──
    it.append(q('M02592', '두 성질이 서로 어긋날 때 무엇을 더 재야 하는지 정하기',
        '심화', 4, '어려움', 0.26,
        '어떤 고체가 고체 상태에서 전기를 통했다. 그런데 녹는점은 아주 낮았다. 다음에 무엇을 더 '
        '재야 하는지 옳은 것은?',
        ['녹는점만 믿고 분자 물질로 적는다',
         '전도만 믿고 곧바로 금속으로 적는다',
         '둘 중 하나는 잘못 잰 것이니 버린다',
         '두드려 펴지는지를 더 재어 보면 된다'], 3,
        '두 성질이 서로 다른 답을 가리킬 때는 어느 한쪽을 버리는 것이 아니라 두 후보를 갈라 줄 '
        '세 번째 성질을 재야 하고, 두드려 펴지는지는 금속만 보이는 성질이라 이 자리를 가른다',
        [('녹는점만 믿고 분자 물질로 적는다', '어긋난 한쪽만 골라 씀', 'proc'),
         ('전도만 믿고 곧바로 금속으로 적는다', '다른 쪽을 지우고 단정함', 'sign'),
         ('둘 중 하나는 잘못 잰 것이니 버린다', '어긋남을 잰 잘못으로 돌림', 'conserv')],
        '두 성질이 각각 어떤 후보를 가리키는지 나란히 적기',
        '고체에서 통함 → 금속 쪽 / 녹는점이 낮음 → 분자 물질 쪽 → 후보 둘이 어긋남 → 금속만 '
        '보이는 성질을 하나 더 잼 → 두드려 펴지는지 / ① 한쪽만 골라 씀 / ② 다른 쪽을 지움 / '
        '③ 어긋남이 곧 잰 잘못은 아님',
        '전도와 녹는점이 어긋난 고체', '세 번째 성질을 고르는지 확인',
        '두 성질이 각각 어떤 후보를 가리키는지 나란히 적어 봐.',
        '고체에서 통하니 금속 쪽이고 녹는점이 낮으니 분자 물질 쪽이야. 어긋났으니 금속만 보이는 '
        '성질을 하나 더 재야지 — ④가 옳아. '
        '①은 녹는점만 믿고 분자 물질로 적는다 라고 했어. 한쪽만 골라 쓴 거야. '
        '②는 전도만 믿고 곧바로 금속으로 적는다 라고 했어. 다른 쪽을 지웠지. '
        '③은 둘 중 하나는 잘못 잰 것이니 버린다 라고 했어. 어긋남이 곧 잰 잘못은 아니야. '
        '두 성질이 어긋나면 둘을 갈라 줄 세 번째를 고른다.',
        {'녹는점만 믿고 분자 물질로 적는다': '한쪽만 썼어.',
         '전도만 믿고 곧바로 금속으로 적는다': '다른 쪽을 지웠어.',
         '둘 중 하나는 잘못 잰 것이니 버린다': '잰 잘못은 아냐.'},
        '세 번째 성질을 골랐는가?'))

    return it


_ANON = re.compile(r'원소 (?:가|나|다|A|B|C)(?=의|와|과|는|은|를|이|가|,|·|\s|$)')


def local_checks(items):
    """T15 지역 저작 점검 — P3 의 것을 그대로 잇는다(⑧ 겹침 · ⑨ 길이 포함)."""
    bad = []
    ALLOWED = {'proc', 'sign', 'surface', 'conserv', 'unit', 'scale', 'overgen', 'causal'}
    for x in items:
        ts = [w['type'] for w in x['distractors']]
        if len(set(ts)) < 3:
            bad.append(f"{x['id']} 오답 유형이 겹친다 — {ts}")
        for t in ts:
            if t not in ALLOWED:
                bad.append(f"{x['id']} 허용되지 않은 유형 {t}")
        # ★★이 검사는 P3 3차까지 한 번도 울 수 없었다★★ — sol() 이 해설 끝에 붙이는
        #   단평 블록이 오답 문면을 ★글자 그대로★ 되풀이하므로 `in x['solution']` 은 언제나
        #   참이었다. 해설 산문에서 인용이 흐트러져도 검사는 통과한다(defender 와
        #   factchecker 가 각각 두 자리를 손으로 잡아 냈다). ▸ ★산문 문단만 본다.★
        prose = x['solution'].split('\n\n')[1] if '\n\n' in x['solution'] else x['solution']
        #   ▸ 산문은 간접 인용이라 종결어미가 바뀐다('…때문이다' → '…때문이라고 했어').
        #     ★어미를 뗀 몸통★ 이 그대로 있는지만 본다 — 몸통이 어긋나면 인용이 흐트러진 것이다.
        for w in x['distractors']:
            core = re.sub(r'(이)?다$', '', x['choices'][w['opt']]).strip()
            if core not in prose:
                bad.append(f"{x['id']} {w['opt'] + 1}번 오답 인용이 해설 산문과 어긋난다")
        if not re.search(r'[①②③④](가|이)? 옳아', x['solution']):
            bad.append(f"{x['id']} 해설에 정답 확정 어구가 없다")
        J = ('를|을|은|의|으로|로|에서|에|과|보다|처럼|까지|부터|이고|이다|이야|이라|이지')
        for f in ('stem', 'solution', 'answer_proof'):
            for m in re.finditer(rf'(?<=[가-힣]) ({J})(?=[\s,.·)?!]|$)', x[f]):
                bad.append(f"{x['id']} {f} 조사 앞 빈칸 — {x[f][max(0, m.start() - 14):m.end()]!r}")
        tails = [c.strip()[-3:] for c in x['choices']]
        for t in set(tails):
            if tails.count(t) == 3 and tails[x['answer']] != t:
                bad.append(f"{x['id']} 어미 「{t}」 셋 대 정답 하나 — 꼴로 갈린다")

        def tk(s):
            return {w for w in re.findall(r'[가-힣]{2,}', s)}
        for i in range(4):
            for j in range(i + 1, 4):
                if x['answer'] in (i, j):
                    continue
                a, b = tk(x['choices'][i]), tk(x['choices'][j])
                if a and b and len(a & b) / len(a | b) >= 0.62:
                    bad.append(f"{x['id']} 오답 {i + 1}·{j + 1} 자카드 "
                               f"{len(a & b) / len(a | b):.2f}")
    for x in items:
        s = x['stem']
        if '톱니' in s and '세로축' not in s:
            bad.append(f"{x['id']} 톱니를 쓰면서 세로축을 적지 않았다")
        anon = _ANON.search(s)
        named = re.search(r'(리튬|베릴륨|붕소|탄소|질소|산소|플루오린|네온|나트륨|마그네슘|염소'
                          r'|칼륨|알루미늄|헬륨|수소|아르곤|브로민|Li|Be|B\b|C\b|N\b|O\b|F\b|Ne|Na)', s)
        if anon and named:
            bad.append(f"{x['id']} 익명 원소와 원소 이름을 함께 인쇄했다 — "
                       f"{anon.group(0)!r} · {named.group(0)!r}")
        if '크게 뛰' in s or '급증' in s:
            if not re.search(r'비\(뒤 ÷ 앞\)|비로 재|앞까지의 개수', s):
                bad.append(f"{x['id']} 급증의 셈법을 발문이 정하지 않았다")
    g = sum(1 for x in items if x['track'] == '일반')
    lo = 7 if len(items) >= 10 else max(2, len(items) // 2)
    if g < lo:
        bad.append(f'일반 {g}제 — {len(items)}문항 배치의 하한은 {lo}')
    e = sum(1 for x in items if x['difficulty'] == '쉬움')
    if e > 3:
        bad.append(f'쉬움 {e}제 — 상한 3')

    # ⑧ ★정답이 발문과의 어휘 겹침 유일 최다이면 실패★ (P1 이 값을 치른 자리)
    for x in items:
        st = set(re.findall(r'[0-9A-Za-z가-힣]{2,}', x['stem']))
        ov = [len(set(re.findall(r'[0-9A-Za-z가-힣]{2,}', c)) & st) for c in x['choices']]
        if ov[x['answer']] == max(ov) and ov.count(max(ov)) == 1:
            bad.append(f"{x['id']} 정답이 발문과의 어휘 겹침 최다(유일) — {ov}")

    # ⑨ ★선지 길이 — 정답이 유일 최장/최단이거나 산포가 0.25 를 넘으면 실패★ (P3 이 값을 치렀다)
    for x in items:
        L = [len(c) for c in x['choices']]
        a = L[x['answer']]
        if a == max(L) and L.count(a) == 1:
            bad.append(f"{x['id']} 정답이 유일 최장 — {L}")
        if a == min(L) and L.count(a) == 1:
            bad.append(f"{x['id']} 정답이 유일 최단 — {L}")
        sp = (max(L) - min(L)) / (sum(L) / len(L))
        if sp > 0.25:
            bad.append(f"{x['id']} 선지 길이 산포 {sp:.2f} > 0.25 — {L}")

    print(f"  ▸ 배치 요약 — 유형 {sorted({w['type'] for x in items for w in x['distractors']})}")
    if bad:
        print('\n  ❌ 저작 점검 실패')
        for b in bad:
            print('     ·', b)
        raise SystemExit(1)
    print('  ✅ 저작 점검 통과')



if __name__ == '__main__':
    items = build()
    local_checks(items)
    T.verify(items)
    T.merge_and_house(items)
