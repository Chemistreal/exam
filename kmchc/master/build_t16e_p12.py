# -*- coding: utf-8 -*-
"""T16 P12 (M02901~M02910) — 하나로 못 박기 전에 갈림과 나머지를 함께 본다

  각도: C16-003[8] · 013[4] · 005[4] · 006[7] · 008[7] · 009[8] · 019[6] · 012[8] ·
        015[4] · 015[7]

  정답 자리 1-4-3-4-2-3-2-4-1-3 — anspick.py.
  3위: 901 · 905 · 906 · 907 · 909 / 2위: 902 · 903 · 904 · 908 · 910 (동률 0)
"""
import re

import batch_template as T

T.START_ID = 'M02901'
T.COUNT = 10
T.EXPECT_LEN = 2900
T.THEME = '전자친화도·전기음성도'
T.TT = 16
T.UNIT = 'I'
T.BATCH_NOTE = 'T16 P12 — 하나로 못 박기 전에 갈림과 나머지를 함께 본다.'


def q(i, sk, tr, dok, df, esr, stem, cs, ans, proof, wr, dev, calc, scen, obj,
      lead, prose, wmap, diag):
    x = T.mk(i, sk, tr, dok, df, esr, stem, cs, ans, proof, wr, dev, calc, scen, obj=obj)
    T.sol(x, lead, prose, wmap, diag)
    return x


def build():
    it = []

    # ── M02901 ── C16-003[8] 요인을 하나만 대고 못 박지 않기 (심화·어려움) 정답 ① [3위] ──
    it.append(q('M02901', '요인을 하나만 대고 못 박지 않기',
        '심화', 4, '어려움', 0.28,
        '어떤 학생이 값이 큰 까닭을 물었더니 반지름이 작아서라고만 답했다. 이 답을 다듬는 '
        '방법으로 옳은 것은?',
        ['세 요인을 함께 보고 견주어 적는다',
         '반지름 하나로 못 박아 적어 두면 된다',
         '요인이 여럿이면 아무것도 적지 못한다',
         '핵전하 하나만 들어서 못 박는다'], 0,
        '값을 움직이는 것은 유효 핵전하와 반지름과 들어갈 자리 셋이고 자리마다 어느 쪽이 더 크게 '
        '드는지가 갈리므로, 셋을 함께 놓고 견주어 적어야 한다',
        [('반지름 하나로 못 박아 적어 두면 된다', '요인 하나로 모두를 덮음', 'overgen'),
         ('요인이 여럿이면 아무것도 적지 못한다', '견줄 수 있는 것을 지움', 'conserv'),
         ('핵전하 하나만 들어서 못 박는다', '드는 요인을 하나로 줄임', 'scale')],
        '값을 움직이는 요인을 모두 적고 어느 쪽이 큰지 견주기',
        '요인: 유효 핵전하 · 반지름 · 들어갈 자리 / 자리마다 큰 쪽이 갈림 → 셋을 함께 견줌 / '
        '① 하나로 덮음 / ③ 견줄 수 있음 / ④ 하나로 줄임',
        '까닭을 하나로만 답한 자리', '요인을 하나로 못 박지 않는지 확인',
        '값을 움직이는 요인을 모두 적고 어느 쪽이 큰지 견주어 봐.',
        '값을 움직이는 것은 셋이야. 자리마다 큰 쪽이 갈리니 함께 견주어야지 — ①이 옳아. '
        '②는 반지름 하나로 못 박아 적어 두면 된다 라고 했어. 하나로 다 덮을 수는 없어. '
        '③은 요인이 여럿이면 아무것도 적지 못한다 라고 했어. 견주면 적을 수 있잖아. '
        '④는 핵전하 하나만 들어서 못 박는다 라고 했어. 드는 요인을 하나로 줄인 셈이야. '
        '까닭을 물으면 요인을 모두 놓고 어느 쪽이 큰지 적는다.',
        {'반지름 하나로 못 박아 적어 두면 된다': '하나론 못 덮어.',
         '요인이 여럿이면 아무것도 적지 못한다': '견주면 적어.',
         '핵전하 하나만 들어서 못 박는다': '하나로 줄였어.'},
        '요인을 모두 놓고 견주었는가?'))

    # ── M02902 ── C16-013[4] 한 무리 안에서도 갈림 (일반·중간) 정답 ④ [2위] ──
    it.append(q('M02902', '한 무리 안에서도 값이 갈림을 적기',
        '일반', 3, '중간', 0.44,
        '17족을 한 무리로 묶어 크다고 적어 두었다. 이 적음에 덧붙일 말로 옳은 것은?',
        ['한 무리에 들면 값도 하나로 정해진다',
         '무리 안에서는 어느 값이나 같다',
         '무리가 다르면 갈림도 사라진다',
         '한 무리 안에서도 값이 크게 갈린다'], 3,
        '같은 17족이라도 염소가 가장 크고 불소는 그보다 작으므로, 무리로 굵게 잡은 뒤에는 무리 '
        '안에서도 값이 갈린다는 말을 덧붙여야 한다',
        [('한 무리에 들면 값도 하나로 정해진다', '굵은 어림을 하나의 값으로 못 박음', 'overgen'),
         ('무리 안에서는 어느 값이나 같다', '무리 안의 갈림을 지움', 'conserv'),
         ('무리가 다르면 갈림도 사라진다', '갈림이 어디서 나는지 뒤집음', 'sign')],
        '같은 무리 안의 두 원소를 실제로 견주어 적기',
        '17족: 염소 가장 큼 · 불소는 그보다 작음 → 무리 안에서도 갈림 / 무리는 굵은 어림일 뿐 / '
        '① 하나로 못 박음 / ② 갈림이 있음 / ③ 갈림은 무리 안에서도 남음',
        '17족을 한 무리로 묶어 둔 줄', '무리 안의 갈림을 아는지 확인',
        '같은 무리 안의 두 원소를 실제로 견주어 적어 봐.',
        '같은 17족이라도 염소와 불소가 갈려. 무리는 굵은 어림일 뿐이야 — ④가 옳아. '
        '①은 한 무리에 들면 값도 하나로 정해진다 라고 했어. 하나로 못 박을 수 없지. '
        '②는 무리 안에서는 어느 값이나 같다 라고 했어. 갈림이 있잖아. '
        '③은 무리가 다르면 갈림도 사라진다 라고 했어. 갈림은 무리 안에서도 남아. '
        '무리로 어림했으면 안쪽의 갈림을 함께 적는다.',
        {'한 무리에 들면 값도 하나로 정해진다': '못 박아.',
         '무리 안에서는 어느 값이나 같다': '갈림이 있어.',
         '무리가 다르면 갈림도 사라진다': '안에서도 남아.'},
        '무리 안의 갈림을 적었는가?'))

    # ── M02903 ── C16-005[4] 족 안에서 견주는 차례 (일반·중간) 정답 ③ [2위] ──
    it.append(q('M02903', '족 안에서 두 값을 견주는 차례를 적기',
        '일반', 2, '중간', 0.50,
        '두 원소가 같은 세로줄에 있는지부터 살피려 한다. 견주는 차례로 옳은 것은?',
        ['두 원소의 이름 차례로 값을 늘어놓는다',
         '족을 가리지 않고 위아래만 적는다',
         '같은 족인지 본 뒤 위아래를 짚는다',
         '표를 먼저 보고 경향은 접어 둔다'], 2,
        '같은 족인지를 먼저 확인해야 아래로 갈수록 대체로 작아진다는 족의 경향을 댈 수 있으므로, '
        '족을 확인한 뒤 어느 쪽이 위인지 짚는 차례로 간다',
        [('두 원소의 이름 차례로 값을 늘어놓는다', '이름을 견줌의 잣대로 삼음', 'surface'),
         ('족을 가리지 않고 위아래만 적는다', '확인할 차례를 건너뜀', 'proc'),
         ('표를 먼저 보고 경향은 접어 둔다', '경향을 대는 일을 버림', 'conserv')],
        '두 원소가 같은 세로줄에 있는지 먼저 적기',
        '같은 족 확인 → 위아래 짚기 → 아래로 갈수록 대체로 작아짐 → 둘째 주기 예외 확인 / '
        '① 이름은 잣대가 아님 / ② 건너뜀 / ④ 경향도 대야 함',
        '세로줄을 살피려는 두 원소', '차례를 지키는지 확인',
        '두 원소가 같은 세로줄에 있는지 먼저 적어 봐.',
        '같은 족인지 본 다음에야 위아래로 경향을 댈 수 있어 — ③이 옳아. '
        '①은 두 원소의 이름 차례로 값을 늘어놓는다 라고 했어. 이름은 잣대가 아니지. '
        '②는 족을 가리지 않고 위아래만 적는다 라고 했어. 확인할 차례를 건너뛴 거야. '
        '④는 표를 먼저 보고 경향은 접어 둔다 라고 했어. 경향도 대야 해. '
        '견줄 때는 어느 축 위에 있는지부터 확인한다.',
        {'두 원소의 이름 차례로 값을 늘어놓는다': '잣대가 아냐.',
         '족을 가리지 않고 위아래만 적는다': '차례를 건너뛰었어.',
         '표를 먼저 보고 경향은 접어 둔다': '경향도 대야지.'},
        '같은 족인지 먼저 보았는가?'))

    # ── M02904 ── C16-006[7] 둘을 헷갈린 답이 어긋나는 꼴 (일반·중간) 정답 ④ [2위] ──
    it.append(q('M02904', '두 값을 헷갈린 답이 어떻게 어긋나는지 적기',
        '일반', 3, '중간', 0.42,
        '어떤 학생이 전자 친화도를 물었는데 이온화 에너지를 답했다. 이 답이 어긋나는 꼴로 '
        '옳은 것은?',
        ['둘을 바꾸어도 답은 그대로 남는다',
         '값의 크기만 조금 달라질 뿐 방향은 같다',
         '헷갈려도 시험에서는 드러나지 않는다',
         '드는 쪽과 나오는 쪽이 서로 뒤바뀐다'], 3,
        '이온화 에너지는 전자를 뗄 때 드는 에너지이고 전자 친화도는 받을 때 나오는 에너지이므로, '
        '둘을 바꾸면 에너지가 드는 쪽과 나오는 쪽이 통째로 뒤바뀐다',
        [('둘을 바꾸어도 답은 그대로 남는다', '뒤바뀌는 것을 지움', 'conserv'),
         ('값의 크기만 조금 달라질 뿐 방향은 같다', '방향이 바뀌는 것을 크기 차이로 봄', 'scale'),
         ('헷갈려도 시험에서는 드러나지 않는다', '어긋남이 드러나지 않는다고 봄', 'surface')],
        '두 값이 저마다 에너지가 드는 쪽인지 나오는 쪽인지 적기',
        '이온화 에너지: 떼는 데 듦 / 전자 친화도: 받을 때 나옴 → 바꾸면 방향이 통째로 뒤바뀜 / '
        '① 그대로가 아님 / ② 크기가 아니라 방향 / ③ 문장에서 드러남',
        '두 값을 바꾸어 답한 자리', '어긋남의 꼴을 아는지 확인',
        '두 값이 저마다 에너지가 드는 쪽인지 나오는 쪽인지 적어 봐.',
        '하나는 드는 쪽, 하나는 나오는 쪽이야. 바꾸면 방향이 통째로 뒤바뀌지 '
        '— ④가 옳아. '
        '①은 둘을 바꾸어도 답은 그대로 남는다 라고 했어. 그대로일 리 없잖아. '
        '②는 값의 크기만 조금 달라질 뿐 방향은 같다 라고 했어. 크기가 아니라 방향이 바뀌어. '
        '③은 헷갈려도 시험에서는 드러나지 않는다 라고 했어. 문장에서 바로 드러나지. '
        '두 값을 바꾸어 답하면 방향부터 어긋난다.',
        {'둘을 바꾸어도 답은 그대로 남는다': '그대로가 아냐.',
         '값의 크기만 조금 달라질 뿐 방향은 같다': '방향이 바뀌어.',
         '헷갈려도 시험에서는 드러나지 않는다': '바로 드러나.'},
        '드는 쪽과 나오는 쪽을 적었는가?'))

    # ── M02905 ── C16-008[7] 어느 쪽이 쉬운지 견주기 (일반·중간) 정답 ② [3위] ──
    it.append(q('M02905', '두 원소 가운데 어느 쪽이 음이온이 되기 쉬운지 견주기',
        '일반', 2, '중간', 0.52,
        '표에서 두 원소의 전자 친화도를 찾아 두었다. 어느 쪽이 음이온이 되기 쉬운지 가리는 '
        '방법으로 옳은 것은?',
        ['이름이 앞선 쪽이 음이온이 되기 쉽다',
         '전자 친화도가 큰 쪽이 되기 쉽다',
         '반지름이 큰 쪽이 언제나 되기 쉽다',
         '두 원소는 견줄 수 없는 짝이다'], 1,
        '전자 친화도가 크다는 것은 전자를 받아들일 때 그만큼 많이 내놓는다는 뜻이므로, 다른 조건이 '
        '같다면 값이 큰 쪽이 음이온이 되기 쉽다',
        [('이름이 앞선 쪽이 음이온이 되기 쉽다', '이름을 잣대로 삼음', 'surface'),
         ('반지름이 큰 쪽이 언제나 되기 쉽다', '값 대신 다른 잣대를 못 박음', 'overgen'),
         ('두 원소는 견줄 수 없는 짝이다', '견줄 수 있는 것을 지움', 'conserv')],
        '표에 적힌 두 값 가운데 어느 쪽이 큰지 적기',
        '전자 친화도 큼 → 받을 때 많이 내놓음 → 다른 조건이 같다면 음이온이 되기 쉬움 / '
        '① 이름은 잣대가 아님 / ③ 반지름만으로는 못 정함 / ④ 값이 있으면 견줄 수 있음',
        '두 값을 찾아 둔 표', '값으로 견주는지 확인',
        '표에 적힌 두 값 가운데 어느 쪽이 큰지 적어 봐.',
        '값이 크다는 것은 받을 때 많이 내놓는다는 뜻이야. 그쪽이 되기 쉽지 — ②가 옳아. '
        '①은 이름이 앞선 쪽이 음이온이 되기 쉽다 라고 했어. 이름은 잣대가 아니야. '
        '③은 반지름이 큰 쪽이 언제나 되기 쉽다 라고 했어. 반지름만으로는 못 정해. '
        '④는 두 원소는 견줄 수 없는 짝이다 라고 했어. 값이 있으면 견줄 수 있잖아. '
        '어느 쪽이 쉬운지는 표에 적힌 값으로 가른다.',
        {'이름이 앞선 쪽이 음이온이 되기 쉽다': '잣대가 아냐.',
         '반지름이 큰 쪽이 언제나 되기 쉽다': '못 정해.',
         '두 원소는 견줄 수 없는 짝이다': '견줄 수 있어.'},
        '값으로 견주었는가?'))

    # ── M02906 ── C16-009[8] 둘을 더해 전체를 셈하기 (심화·어려움) 정답 ③ [3위] ──
    it.append(q('M02906', '첫 번째와 두 번째를 더해 전체를 셈하는 차례',
        '심화', 4, '어려움', 0.26,
        '산소가 전자 둘을 받아 산소 2- 이온이 될 때 드나드는 에너지를 셈하려 한다. 첫 번째에서 '
        '141 이 나오고 두 번째에서 780 이 든다면 셈하는 방법으로 옳은 것은?',
        ['두 값을 그냥 더해서 방출로 적는다',
         '첫 번째 값만 보고 전체를 정해 둔다',
         '141 과 780 을 함께 셈한다',
         '780 은 빼고 141 만 쓴다'], 2,
        '첫 번째에서 나온 141 과 두 번째에서 든 780 은 드나드는 쪽이 서로 반대이므로, 두 값을 '
        '방향까지 함께 놓고 셈해야 전체가 드는 쪽임이 드러난다',
        [('두 값을 그냥 더해서 방출로 적는다', '방향이 반대인 값을 같은 쪽으로 더함', 'sign'),
         ('첫 번째 값만 보고 전체를 정해 둔다', '두 번째 자리를 셈에서 뺌', 'scale'),
         ('780 은 빼고 141 만 쓴다', '적혀 있는 값을 지움', 'conserv')],
        '두 값이 저마다 드는 쪽인지 나오는 쪽인지 적기',
        '첫 번째: 141 나옴 / 두 번째: 780 듦 → 방향이 반대 → 함께 셈하면 전체는 드는 쪽 / '
        '① 같은 쪽이 아님 / ② 두 번째도 듦 / ④ 적힌 값을 지움',
        '전자 둘을 받는 산소', '방향까지 함께 셈하는지 확인',
        '두 값이 저마다 드는 쪽인지 나오는 쪽인지 적어 봐.',
        '141 은 나오는 쪽이고 780 은 드는 쪽이야. 방향까지 놓고 함께 셈해야지 — ③이 옳아. '
        '①은 두 값을 그냥 더해서 방출로 적는다 라고 했어. 두 값은 같은 쪽이 아니야. '
        '②는 첫 번째 값만 보고 전체를 정해 둔다 라고 했어. 두 번째도 드는 값이잖아. '
        '④는 780 은 빼고 141 만 쓴다 라고 했어. 적힌 값을 지운 셈이지. '
        '두 자리를 더할 때는 저마다의 방향을 먼저 적는다.',
        {'두 값을 그냥 더해서 방출로 적는다': '같은 쪽이 아냐.',
         '첫 번째 값만 보고 전체를 정해 둔다': '두 번째도 들어.',
         '780 은 빼고 141 만 쓴다': '적힌 값을 지웠어.'},
        '두 값의 방향을 적었는가?'))

    # ── M02907 ── C16-019[6] 뿌리를 묻는 물음과 값을 묻는 물음 (일반·쉬움) 정답 ② [3위] ──
    it.append(q('M02907', '뿌리를 묻는 물음과 값을 묻는 물음을 가르기',
        '일반', 2, '쉬움', 0.60,
        '한 문항은 왜 그렇게 되는지를 묻고 다른 문항은 값이 얼마인지 묻는다. 두 물음을 다루는 '
        '방법으로 옳은 것은?',
        ['두 물음은 언제나 같은 답으로 이어진다',
         '왜 그런지와 얼마인지를 갈라 적는다',
         '까닭을 물어도 표의 숫자를 적으면 된다',
         '값을 물으면 뿌리를 대어 답한다'], 1,
        '왜 그런지를 묻는 물음은 유효 핵전하나 껍질 같은 뿌리를 답으로 받고 얼마인지를 묻는 물음은 '
        '표의 값을 답으로 받으므로, 두 물음을 갈라 적어야 한다',
        [('두 물음은 언제나 같은 답으로 이어진다', '갈리는 두 물음을 하나로 봄', 'conserv'),
         ('까닭을 물어도 표의 숫자를 적으면 된다', '뿌리를 물었는데 값을 냄', 'proc'),
         ('값을 물으면 뿌리를 대어 답한다', '값을 물었는데 뿌리를 냄', 'surface')],
        '물음이 뿌리를 묻는지 값을 묻는지 적기',
        '왜 그런지 → 유효 핵전하 · 껍질 같은 뿌리 / 얼마인지 → 표의 값 / 두 물음이 받는 답이 '
        '다름 / ① 같지 않음 / ③ 뿌리를 물었음 / ④ 값을 물었음',
        '나란히 놓인 두 문항', '두 물음을 가르는지 확인',
        '물음이 뿌리를 묻는지 값을 묻는지 적어 봐.',
        '왜 그런지는 뿌리를 받고, 얼마인지는 표의 값을 받아 — ②가 옳아. '
        '①은 두 물음은 언제나 같은 답으로 이어진다 라고 했어. 받는 답이 다르잖아. '
        '③은 까닭을 물어도 표의 숫자를 적으면 된다 라고 했어. 뿌리를 물은 자리지. '
        '④는 값을 물으면 뿌리를 대어 답한다 라고 했어. 값을 물었으면 값을 내야 해. '
        '물음을 만나면 뿌리를 묻는지 값을 묻는지부터 가른다.',
        {'두 물음은 언제나 같은 답으로 이어진다': '받는 답이 달라.',
         '까닭을 물어도 표의 숫자를 적으면 된다': '뿌리를 물었어.',
         '값을 물으면 뿌리를 대어 답한다': '값을 내야지.'},
        '무엇을 묻는지 가렸는가?'))

    # ── M02908 ── C16-012[8] 할로젠이 두 표에서 위쪽 (일반·쉬움) 정답 ④ [2위] ──
    it.append(q('M02908', '할로젠이 두 표에서 모두 위쪽임을 적기',
        '일반', 2, '쉬움', 0.58,
        '전자 친화도 표와 전기 음성도 표를 나란히 놓고 할로젠 자리를 살폈다. 두 표에서 '
        '할로젠의 자리에 대해 옳은 것은?',
        ['할로젠은 두 표에서 모두 아래쪽에 온다',
         '한 표에서만 위쪽에 온다고 본다',
         '두 표에서 자리는 제각각이 된다',
         '할로젠은 두 표에서 모두 위쪽에 온다'], 3,
        '할로젠은 전자를 하나만 받으면 다 찬 배치가 되고 공유 전자쌍도 세게 당기므로, 두 표 모두에서 '
        '값이 큰 위쪽에 놓인다',
        [('할로젠은 두 표에서 모두 아래쪽에 온다', '위아래를 맞바꿈', 'sign'),
         ('한 표에서만 위쪽에 온다고 본다', '한 표의 자리만 남김', 'scale'),
         ('두 표에서 자리는 제각각이 된다', '나란한 것을 흩어진 것으로 봄', 'conserv')],
        '할로젠이 두 값에서 저마다 어떤 쪽인지 적기',
        '전자 친화도: 하나 받으면 다 찬 배치 → 큼 / 전기 음성도: 전자쌍을 세게 당김 → 큼 → 두 '
        '표 모두 위쪽 / ① 맞바꿈 / ② 두 표 모두임 / ③ 나란함',
        '나란히 놓인 두 표의 할로젠 자리', '두 표에서의 자리를 아는지 확인',
        '할로젠이 두 값에서 저마다 어떤 쪽인지 적어 봐.',
        '하나만 받으면 다 차고 전자쌍도 세게 당겨. 두 표에서 다 위쪽이야 — ④가 옳아. '
        '①은 할로젠은 두 표에서 모두 아래쪽에 온다 라고 했어. 위아래가 맞바뀌었지. '
        '②는 한 표에서만 위쪽에 온다고 본다 라고 했어. 두 표 모두에서 위쪽이야. '
        '③은 두 표에서 자리는 제각각이 된다 라고 했어. 나란히 위쪽이잖아. '
        '두 표를 나란히 놓으면 저마다의 자리를 함께 적는다.',
        {'할로젠은 두 표에서 모두 아래쪽에 온다': '맞바뀌었어.',
         '한 표에서만 위쪽에 온다고 본다': '두 표 모두야.',
         '두 표에서 자리는 제각각이 된다': '나란해.'},
        '두 표의 자리를 함께 적었는가?'))

    # ── M02909 ── C16-015[4] 두 성질이 같은 쪽이면 (일반·중간) 정답 ① [3위] ──
    it.append(q('M02909', '두 성질이 같은 쪽인 두 원소가 만나면 어떻게 되는지 적기',
        '일반', 3, '중간', 0.44,
        '전자를 잘 받아들이는 두 비금속이 만나 결합했다. 공유 전자쌍의 치우침에 대해 '
        '옳은 것은?',
        ['두 성질이 같은 쪽이면 치우침이 작다',
         '두 성질이 같은 쪽이면 더 크게 치우친다',
         '같은 쪽끼리는 아예 결합하지 못하게 된다',
         '치우침은 두 성질과 상관이 없다'], 0,
        '두 원자가 전자쌍을 당기는 힘이 비슷하면 어느 한쪽으로 크게 끌려가지 않으므로, 성질이 같은 '
        '쪽인 두 원소 사이에서는 치우침이 작다',
        [('두 성질이 같은 쪽이면 더 크게 치우친다', '갈릴수록 커지는 것을 뒤집음', 'sign'),
         ('같은 쪽끼리는 아예 결합하지 못하게 된다', '결합하는 것을 지움', 'conserv'),
         ('치우침은 두 성질과 상관이 없다', '까닭을 끊어 버림', 'causal')],
        '두 원자가 전자쌍을 당기는 힘이 얼마나 갈리는지 적기',
        '둘 다 잘 받는 쪽 → 당기는 힘이 비슷함 → 어느 쪽으로도 크게 끌리지 않음 → 치우침 작음 / '
        '① 비슷하면 작아짐 / ③ 결합함 / ④ 성질이 정함',
        '두 비금속이 만난 결합', '갈림이 작을 때를 아는지 확인',
        '두 원자가 전자쌍을 당기는 힘이 얼마나 갈리는지 적어 봐.',
        '둘 다 잘 받는 쪽이라 당기는 힘이 비슷해. 그래서 덜 치우쳐 — ①이 옳아. '
        '②는 두 성질이 같은 쪽이면 더 크게 치우친다 라고 했어. 비슷하면 작아지지. '
        '③은 같은 쪽끼리는 아예 결합하지 못하게 된다 라고 했어. 결합은 하잖아. '
        '④는 치우침은 두 성질과 상관이 없다 라고 했어. 두 성질이 정하는 거야. '
        '치우침은 두 성질이 얼마나 갈리는지로 적는다.',
        {'두 성질이 같은 쪽이면 더 크게 치우친다': '작아져.',
         '같은 쪽끼리는 아예 결합하지 못하게 된다': '결합은 해.',
         '치우침은 두 성질과 상관이 없다': '두 성질이 정해.'},
        '당기는 힘의 갈림을 적었는가?'))

    # ── M02910 ── C16-015[7] 치우침을 어림하는 차례 (일반·중간) 정답 ③ [2위] ──
    it.append(q('M02910', '두 원소를 골라 치우침을 어림하는 차례를 적기',
        '일반', 3, '중간', 0.46,
        '두 원소의 전기 음성도를 표에서 찾아 두고 결합의 치우침을 어림하려 한다. 어림하는 '
        '방법으로 옳은 것은?',
        ['두 값을 더해 치우침을 어림해 본다',
         '이름이 앞선 쪽으로 치우친다고 적어 둔다',
         '두 값의 차를 내어 치우침을 어림한다',
         '어림하지 못하니 표를 먼저 찾는다'], 2,
        '치우침은 두 원자가 전자쌍을 당기는 힘이 얼마나 갈리는지에 달렸으므로, 두 전기 음성도의 '
        '차를 내어 그 차가 클수록 크게 치우친다고 어림한다',
        [('두 값을 더해 치우침을 어림해 본다', '갈림을 재는 셈을 더하기로 바꿈', 'unit'),
         ('이름이 앞선 쪽으로 치우친다고 적어 둔다', '이름을 잣대로 삼음', 'surface'),
         ('어림하지 못하니 표를 먼저 찾는다', '이미 찾아 둔 값을 다시 찾음', 'proc')],
        '두 값이 얼마나 갈리는지를 어떻게 잴지 적기',
        '치우침: 당기는 힘의 갈림에 달림 → 두 값의 차로 잼 → 차가 크면 크게 치우침 / ① 더하면 '
        '갈림이 안 나옴 / ② 이름은 잣대가 아님 / ④ 값은 이미 찾아 둠',
        '표에서 찾아 둔 두 전기 음성도', '갈림을 차로 재는지 확인',
        '두 값이 얼마나 갈리는지를 어떻게 잴지 적어 봐.',
        '치우침은 당기는 힘의 갈림에 달렸어. 두 값의 차로 재지 — ③이 옳아. '
        '①은 두 값을 더해 치우침을 어림해 본다 라고 했어. 더하면 갈림이 나오지 않아. '
        '②는 이름이 앞선 쪽으로 치우친다고 적어 둔다 라고 했어. 이름은 잣대가 아니지. '
        '④는 어림하지 못하니 표를 먼저 찾는다 라고 했어. 값은 이미 찾아 두었잖아. '
        '갈림을 재려면 더하지 말고 차를 낸다.',
        {'두 값을 더해 치우침을 어림해 본다': '갈림이 안 나와.',
         '이름이 앞선 쪽으로 치우친다고 적어 둔다': '잣대가 아냐.',
         '어림하지 못하니 표를 먼저 찾는다': '이미 찾았어.'},
        '차를 내어 재었는가?'))

    return it


_ANON = re.compile(r'원소 (?:가|나|다|A|B|C)(?=의|와|과|는|은|를|이|가|,|·|\s|$)')


def local_checks(items):
    """T15 지역 저작 점검 — P3 의 것을 그대로 잇는다(⑧ 겹침 · ⑨ 길이 포함)."""
    bad = []
    ALLOWED = {'proc', 'sign', 'surface', 'conserv', 'unit', 'scale', 'overgen', 'causal'}
    for x in items:
        ts = [w['type'] for w in x['distractors']]
        if len(set(ts)) < 3:
            bad.append(f"{x['id']} 오답 유형이 겹친다 — {ts}")
        for t in ts:
            if t not in ALLOWED:
                bad.append(f"{x['id']} 허용되지 않은 유형 {t}")
        # ★★이 검사는 P3 3차까지 한 번도 울 수 없었다★★ — sol() 이 해설 끝에 붙이는
        #   단평 블록이 오답 문면을 ★글자 그대로★ 되풀이하므로 `in x['solution']` 은 언제나
        #   참이었다. 해설 산문에서 인용이 흐트러져도 검사는 통과한다(defender 와
        #   factchecker 가 각각 두 자리를 손으로 잡아 냈다). ▸ ★산문 문단만 본다.★
        prose = x['solution'].split('\n\n')[1] if '\n\n' in x['solution'] else x['solution']
        #   ▸ 산문은 간접 인용이라 종결어미가 바뀐다('…때문이다' → '…때문이라고 했어').
        #     ★어미를 뗀 몸통★ 이 그대로 있는지만 본다 — 몸통이 어긋나면 인용이 흐트러진 것이다.
        for w in x['distractors']:
            core = re.sub(r'(이)?다$', '', x['choices'][w['opt']]).strip()
            if core not in prose:
                bad.append(f"{x['id']} {w['opt'] + 1}번 오답 인용이 해설 산문과 어긋난다")
        if not re.search(r'[①②③④](가|이)? 옳아', x['solution']):
            bad.append(f"{x['id']} 해설에 정답 확정 어구가 없다")
        J = ('를|을|은|의|으로|로|에서|에|과|보다|처럼|까지|부터|이고|이다|이야|이라|이지')
        for f in ('stem', 'solution', 'answer_proof'):
            for m in re.finditer(rf'(?<=[가-힣]) ({J})(?=[\s,.·)?!]|$)', x[f]):
                bad.append(f"{x['id']} {f} 조사 앞 빈칸 — {x[f][max(0, m.start() - 14):m.end()]!r}")
        tails = [c.strip()[-3:] for c in x['choices']]
        for t in set(tails):
            if tails.count(t) == 3 and tails[x['answer']] != t:
                bad.append(f"{x['id']} 어미 「{t}」 셋 대 정답 하나 — 꼴로 갈린다")

        def tk(s):
            return {w for w in re.findall(r'[가-힣]{2,}', s)}
        for i in range(4):
            for j in range(i + 1, 4):
                if x['answer'] in (i, j):
                    continue
                a, b = tk(x['choices'][i]), tk(x['choices'][j])
                if a and b and len(a & b) / len(a | b) >= 0.62:
                    bad.append(f"{x['id']} 오답 {i + 1}·{j + 1} 자카드 "
                               f"{len(a & b) / len(a | b):.2f}")
    for x in items:
        s = x['stem']
        if '톱니' in s and '세로축' not in s:
            bad.append(f"{x['id']} 톱니를 쓰면서 세로축을 적지 않았다")
        anon = _ANON.search(s)
        named = re.search(r'(리튬|베릴륨|붕소|탄소|질소|산소|플루오린|네온|나트륨|마그네슘|염소'
                          r'|칼륨|알루미늄|헬륨|수소|아르곤|브로민|Li|Be|B\b|C\b|N\b|O\b|F\b|Ne|Na)', s)
        if anon and named:
            bad.append(f"{x['id']} 익명 원소와 원소 이름을 함께 인쇄했다 — "
                       f"{anon.group(0)!r} · {named.group(0)!r}")
        if '크게 뛰' in s or '급증' in s:
            if not re.search(r'비\(뒤 ÷ 앞\)|비로 재|앞까지의 개수', s):
                bad.append(f"{x['id']} 급증의 셈법을 발문이 정하지 않았다")
    g = sum(1 for x in items if x['track'] == '일반')
    lo = 7 if len(items) >= 10 else max(2, len(items) // 2)
    if g < lo:
        bad.append(f'일반 {g}제 — {len(items)}문항 배치의 하한은 {lo}')
    e = sum(1 for x in items if x['difficulty'] == '쉬움')
    if e > 3:
        bad.append(f'쉬움 {e}제 — 상한 3')

    # ⑧ ★정답이 발문과의 어휘 겹침 유일 최다이면 실패★ (P1 이 값을 치른 자리)
    for x in items:
        st = set(re.findall(r'[0-9A-Za-z가-힣]{2,}', x['stem']))
        ov = [len(set(re.findall(r'[0-9A-Za-z가-힣]{2,}', c)) & st) for c in x['choices']]
        if ov[x['answer']] == max(ov) and ov.count(max(ov)) == 1:
            bad.append(f"{x['id']} 정답이 발문과의 어휘 겹침 최다(유일) — {ov}")

    # ⑨ ★선지 길이 — 정답이 유일 최장/최단이거나 산포가 0.25 를 넘으면 실패★ (P3 이 값을 치렀다)
    for x in items:
        L = [len(c) for c in x['choices']]
        a = L[x['answer']]
        if a == max(L) and L.count(a) == 1:
            bad.append(f"{x['id']} 정답이 유일 최장 — {L}")
        if a == min(L) and L.count(a) == 1:
            bad.append(f"{x['id']} 정답이 유일 최단 — {L}")
        sp = (max(L) - min(L)) / (sum(L) / len(L))
        if sp > 0.25:
            bad.append(f"{x['id']} 선지 길이 산포 {sp:.2f} > 0.25 — {L}")

    print(f"  ▸ 배치 요약 — 유형 {sorted({w['type'] for x in items for w in x['distractors']})}")
    if bad:
        print('\n  ❌ 저작 점검 실패')
        for b in bad:
            print('     ·', b)
        raise SystemExit(1)
    print('  ✅ 저작 점검 통과')



if __name__ == '__main__':
    items = build()
    local_checks(items)
    T.verify(items)
    T.merge_and_house(items)
