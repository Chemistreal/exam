"""T14 마감 3차 조치 — ★세 게이팅 동시 0건 뒤의 문구 다듬기 넷★ (판정을 바꾸지 않는다)

3차 순회 판정 — ★★세 게이팅 동시 0건★★
  · solver      ★차단 0★ — "네 문항 모두 닫아도 되겠습니다"
  · defender    ★블로커 0★ — "네 문항 모두 닫아도 됩니다" (2차 블로커의 대칭 복구를 확인)
  · factchecker ★✗ 0 · 차단 △ 0★ — "2차 △ 넷은 모두 해소 확인, 네 건 다 닫습니다"
  · student-sim  D 4/4 정답 · ★A 4/4 오답★ — "닫아도 됩니다"

■ ★★그런데도 한 회차를 더 쓰는 까닭★★
  세 게이팅이 0 이 된 문면은 ★2차 조치본★ 이고, 셋이 함께 낸 문구 다듬기가 여섯 남았다. 그 가운데
  ★하나는 학생이 읽는 해설의 과잉 주장★ 이라 부채로 넘기지 않는다 —
    ★M02298 해설 '적힌 값 72 와 133 이 그 결과야'★ 가 ★61 pm 이라는 크기까지★ 밀침 증가에
    귀속시킨다. 그런데 72 pm 은 F₂ 결합에서 온 공유 반지름이고 133 pm 은 결정에서 온 이온
    반지름이라 ★두 값의 출처가 이질적★ 이다(factchecker 가 1·2·3차에 걸쳐 세 번 짚었고 defender 가
    3차에 '과잉 주장' 으로 적었다). ★크기를 귀속하지 않고 방향만 귀속한다.★
  ▸ ★★새로 얻는다 — 게이팅이 0 이 된 뒤에도 '학생이 읽는 문면의 과잉 주장' 은 부채로 넘기지
    않는다. 게이팅은 문항이 서는지를 재고, 부채는 서 있는 문항의 흠이다.★★ 세 게이팅이 이것을
    블로커로 세우지 않은 것은 옳다 — 남은 오답 어느 것도 이 틈을 손잡이로 쓸 수 없다(defender).
    그러나 그것은 ★문항이 무너지지 않는다★ 는 뜻이지 ★문면이 참이다★ 는 뜻이 아니다.
  ▸ 함께 받는 셋도 모두 ★같은 성격(문면이 조건을 덜 적었다)★ 이라 한 조치로 묶는다.

■ 받는 문구 다듬기 넷
  ㉠ ★M02298 해설·계산 줄★ — '적힌 값 72 와 133 이 그 결과' → ★커진 방향★ 이 그 결과로 낮춘다.
     (위 ■ 참조. defender 3차 결함 3 · factchecker 1~3차의 척도 혼용 지적.)
  ㉡ ★M02296 ③ 반박에 그림의 조건을 적는다★(factchecker 3차 비차단 △) — '전자가 하나 늘어도
     같은 껍질에 들어가면 값은 오히려 줄어들지' 는 ★양성자도 함께 늘기 때문에★ 줄어드는 것이다.
     전자만 하나 늘면(F 72 → F⁻ 133) 값은 커진다 — ★그것이 바로 같은 배치의 M02298 이다.★
     조건을 적지 않으면 두 문항이 서로 어긋나 보인다. ★한 배치 안에서 같은 낱말이 반대 방향으로
     쓰이면 조건을 문면에 적는다 — 문항끼리도 서로의 검사다.★
  ㉢ ★M02297 발문의 '빠진 축'★(solver 3차) — 답을 미리 가리키는 말이라 약간 순환적으로 들린다.
     '더 있어야 할 축' 으로 바꾼다(물음이 '무엇이 더 있어야 하는가' 이므로 문면이 맞물린다).
  ㉣ ★M02297 ② 반박 첫 문장★(defender 3차 결함 1) — '이미 실려 있는 축을 없는 축으로 보았어' 가
     선지의 '커지는 정도'(증가율)와 A 의 세로축(값)을 같은 것으로 취급한다. ★결정 문장은 그대로
     두고★ 첫 문장을 '그 정도는 A 의 곡선에서 이미 읽힌다' 로 고친다.

■ 받지 않는 것과 그 까닭
  · factchecker: M02296 껍질 정의에 '주양자수가 같은 층(부껍질 제외)' 을 명시 — ★받지 않는다.★
    두 게이팅이 ★현 문면으로 부껍질 읽기가 닫혔다★ 고 적었다(defender: "정의가 2s/2p 를 서로 다른
    '층' 으로 읽는 길을 사실상 막는다" · factchecker: "'층' 은 관례상 전자 껍질을 가리켜 실질 혼동이
    작다"). ★닫힌 자리에 낱말을 더하면 정의문만 길어진다★ — sim 이 이미 발문 길이가 상한에 닿았다고
    적었고, '주양자수' 는 T13 의 낱말이라 이 테마의 평문 수준을 넘긴다.
  · solver: 정의문을 괄호로 축약 — ★받지 않는다.★ 괄호는 이 은행의 발문 문체가 아니다(평문 규약).
  · solver·sim: M02297 오답 하나에 '원자가전자' 를 넣어 정답의 어휘 반향을 희석 — ★받지 않는다★
    (2차에 셈으로 확인했다 — 그 문면은 14~19자로 짧아 정답이 유일 최장이 되고, 맞추어 늘리면
    공백 뺀 산포가 0.29 로 걸린다). ★부채로 적는다 — 이 문항은 화학보다 독해로 기운다★(sim).
  · defender: M02297 ④ 반박의 둘째 갈래가 이 발문이 정의하지 않은 등식(원자 번호 = 양성자 수)을
    쓴다 — ★그대로 둔다.★ 교과 상식이고 ★앞 갈래('반지름은 그다음 고리')가 독립으로 선다★ 고
    defender 자신이 적었다. 빼면 ④ 를 두 겹으로 죽이는 갈래가 없어진다.
  · defender: M02298 의 시야 한정이 겨눌 오답이 없어져 무직이 됐다 — ★존치가 옳다★ 고 defender 가
    같은 항에서 적었다("제거하면 과잉 귀속이 오히려 더 노출된다"). ㉠ 으로 그 과잉을 없애지만,
    시야 한정은 ★다른 이웃 논변을 미리 막는 자리★ 로 남긴다.

■ 부채(마감 문서로 옮긴다) — 모두 정답 판정을 바꾸지 않는다
  · 죽은 선지 넷 — M02295 ④(268 pm: 부호 오류 경로를 밟는 프로필이 없다) · M02296 ①(거울쌍을
    지키는 대가) · M02297 ③(주석에 이온이 한 글자도 없어 갈 동기가 없다) · M02298 ②(④ 를 살린
    맞교환으로 C 가 옮겨 갔다 — ★순 죽은 선지 수는 그대로★).
  · ★M02297 이 화학보다 독해로 기운다★ — 정답이 주석 서술어의 축자 환언이다(sim 3차).
  · ★M02296 발문이 ③ 배제의 절반을 대신해 준다★ — solver 는 문면으로 닫으라 했고 sim 은 힌트가
    된다고 했다. ★두 요구가 반대 방향이라 어느 한쪽을 고르면 다른 쪽이 든다★(자족성 대 변별력).
  · M02296 발문 길이가 상한에 닿았다(sim) · M02298 의 짧은 소거 경로(solver: "수용 가능한 교환") ·
    M02295 발문의 '191 pm 인 화합물도 있다'(실측 C–Br 은 193~194) · ★'핵전하가' 가 오답에만 세 번★
    (배치 요약이 짚었다 — 세 문항의 서로 다른 자리에서 우연히 겹친 토큰이다).
"""
import ast
import importlib
import os
import re
import sys

BASE = os.path.dirname(os.path.abspath(__file__))
SRC = os.path.join(BASE, 'build_t14_fin.py')
body = open(SRC, encoding='utf-8').read()
BEFORE = body


def rep(old, new, n=1):
    global body
    c = body.count(old)
    assert c == n, f'{c}곳(기대 {n}) — {old[:46]!r}'
    body = body.replace(old, new)


print('T14 마감 3차 조치')

# ══ ㉠ M02298 — 크기를 귀속하지 않고 방향만 귀속한다 ════════════════════════════════
rep("""             '하나가 받는 핵의 몫은 아홉일 때보다 줄어듦 → 유효 핵전하는 커지지 않음 / 적힌 값 '
             '72 와 133 이 그 결과',""",
    """             '하나가 받는 핵의 몫은 아홉일 때보다 줄어듦 → 유효 핵전하는 커지지 않음 / 값이 '
             '커진 방향이 그 결과(두 값은 서로 다른 기준으로 잰 값이라 늘어난 크기까지 이 몫에 '
             '돌리지 않는다)',""")
rep("""          '넓어져 — ③이 옳아. 적힌 값 72 와 133 이 그 결과야. '""",
    """          '넓어져 — ③이 옳아. 값이 커진 방향이 그 결과야. '""")

# ══ ㉡ M02296 ③ 반박 — 그림의 조건을 적는다(같은 배치 M02298 과 어긋나 보이지 않게) ══
rep("""          '③은 전자 수가 늘면 커진다고 보았어. 그런데 전자가 하나 늘어도 ★같은 껍질에 들어가면★ '""",
    """          '③은 전자 수가 늘면 커진다고 보았어. 그런데 양성자도 함께 늘어나는 이 그림에서는 '
          '전자가 하나 늘어도 ★같은 껍질에 들어가면★ '""")

# ══ ㉢ M02297 발문 — 답을 미리 가리키는 말을 물음과 맞물리게 ═════════════════════════
rep("""             '양이 한 그림에 함께 실려 있을 때에만 그 주석을 그림으로 확인했다고 하자. 빠진 축은 '
             '그래프 A 에 함께 그린다고 할 때, 이 주석을 그림으로 확인하려면 무엇이 더 있어야 '
             '하는가?',""",
    """             '양이 한 그림에 함께 실려 있을 때에만 그 주석을 그림으로 확인했다고 하자. 더 있어야 '
             '할 축은 그래프 A 에 함께 그린다고 할 때, 이 주석을 그림으로 확인하려면 무엇이 더 '
             '있어야 하는가?',""")

# ══ ㉣ M02297 ② 반박 첫 문장 — 증가율과 값을 같게 취급하지 않는다 ═══════════════════
rep("""          '②는 이미 실려 있는 축을 없는 축으로 보았어. 유효 핵전하는 그래프 A 의 세로축에 이미 '
          '실려 있으니, 그 축을 더해도 빠진 양은 그대로야. '""",
    """          '②는 이미 드러나 있는 것을 없는 축으로 보았어. 유효 핵전하가 커지는 정도는 그래프 A '
          '의 곡선이 오르는 모양에서 이미 읽히고, 그 축을 더해도 빠진 양은 그대로야. '""")

assert body != BEFORE
try:
    ast.parse(body)
except SyntaxError as e:
    raise SystemExit(f'치환 결과가 문법에 어긋난다 — {e.lineno}행: {e.msg}')
open(SRC, 'w', encoding='utf-8').write(body)

bad = []
live = '\n'.join(l for l in body.split('def build()')[1].split('\n')
                 if not l.lstrip().startswith('#'))
flat = re.sub(r"'\s*\n\s*'", '', live)

GONE = [
    ('★M02298 해설의 과잉 귀속 — 크기까지 밀침에 돌렸다★', '적힌 값 72 와 133 이 그 결과야'),
    ('★M02298 계산 줄의 과잉 귀속★', '적힌 값 72 와 133 이 그 결과'),
    ('M02296 ③ 반박에 그림의 조건이 없던 문면', '보았어. 그런데 전자가 하나 늘어도'),
    ('M02297 발문의 순환적 낱말(solver)', '빠진 축은'),
    ('M02297 ② 반박이 증가율과 값을 같게 본 문면(defender)', '이미 실려 있는 축을 없는 축으로 보았어'),
]
WANT = [
    ('★M02298 이 방향만 귀속함★', '값이 커진 방향이 그 결과야'),
    ('★M02298 계산 줄이 기준 차이를 적음★', '서로 다른 기준으로 잰 값이라 늘어난 크기까지 이 몫에 돌리지 않는다'),
    ('★M02296 ③ 반박이 그림의 조건을 적음(factchecker)★', '양성자도 함께 늘어나는 이 그림에서는'),
    ('M02297 발문이 물음과 맞물림', '더 있어야 할 축은 그래프 A 에 함께 그린다고 할 때'),
    ('M02297 ② 반박이 곡선의 모양으로 받음', '커지는 정도는 그래프 A 의 곡선이 오르는 모양에서 이미 읽히고'),
]
KEEP = [
    ('M02295 정답', '114 pm'),
    ('★M02295 의 가장 값진 오답★', "'77 pm'"),
    ('M02296 정답', '급상승은 껍질 때문이고 하강은 핵전하 때문이다'),
    ('★M02296 ①/④ 거울쌍★', '급상승은 핵전하 때문이고 하강은 껍질 때문이다'),
    ('★M02296 ② 두 항 짝 꼴(뒷항 2:2)★', '급상승도 껍질 때문이고 하강도 껍질 때문이다'),
    ('M02296 껍질·핵전하 정의', '껍질은 전자가 들어 있는 층을 뜻하고'),
    ('★M02296 ③ 반박의 반례★', '값은 오히려 줄어들지 — 완만하게 내려가는 구간이 바로 그 자리야'),
    ('M02296 급상승의 조건', '전자가 들어갈 껍질이 하나 더 열릴 때뿐이야'),
    ('M02297 정답', '원자가전자가 받는 끌림의 세기를 나타낸 축'),
    ('M02297 확인의 규정', '두 양이 한 그림에 함께 실려 있을 때에만 그 주석을 그림으로 확인했다고 하자'),
    ('★M02297 ④ 의 결정적 갈래(빼지 않는다)★', '핵전하와 원자 반지름을 함께 담은 그림은 그래프 B 로 이미 있어'),
    ('M02297 ② 반박의 결정 문장', '그 축을 더해도 빠진 양은 그대로야'),
    ('M02298 정답', '전자가 하나 늘어 밀침이 커졌기 때문이다'),
    ('M02298 ④ 의 방향 오류', '전자가 하나 늘어 유효 핵전하가 커졌기 때문이다'),
    ('★M02298 시야 한정(존치 — defender)★', '옆에 놓인 다른 알갱이와의 작용은 생각하지 않을 때'),
    ('★M02298 등전자가 아님을 세우는 발문★', '전자가 들어 있는 껍질 수도 둘로 같다'),
]
for why, txt in GONE:
    if txt in flat:
        bad.append(f'{why} — 문면이 남아 있다: {txt}')
for why, txt in WANT:
    if txt not in flat:
        bad.append(f'{why} — 문면이 들어가지 않았다: {txt}')
for why, txt in KEEP:
    if txt not in flat:
        bad.append(f'{why} — 지켜야 할 문면이 사라졌다: {txt}')
for m in re.finditer(r'★\s+(를|을|은|의|으로|로|에서|에|과|보다|처럼|까지|부터'
                     r'|이고|이다|이야|이라|이지)(?=[\s,.·)?!\'"]|$)', flat):
    bad.append(f'별표 뒤에 조사를 띄웠다 — {flat[max(0, m.start() - 16):m.end()]!r}')

sys.path.insert(0, BASE)
items = importlib.import_module('build_t14_fin').build()
if ''.join(str(x['answer'] + 1) for x in items) != '2413':
    bad.append(f"정답 자리가 설계와 다르다 — {''.join(str(x['answer'] + 1) for x in items)}")
for x in items:
    if len({w['type'] for w in x['distractors']}) < 3:
        bad.append(f"{x['id']} 오답 유형이 겹친다 — {[w['type'] for w in x['distractors']]}")
    for w in x['distractors']:
        if x['choices'][w['opt']] not in x['solution']:
            bad.append(f"{x['id']} {w['opt'] + 1}번 오답 문면이 해설에 없다")
from choiceprobe import probe
for x in items:
    for b in probe(x['choices'], ans=x['answer'], mid=x['id'], verbose=False):
        bad.append(f"{x['id']} 선지 자에 걸림 — {b}")
    ns = [len(c.replace(' ', '')) for c in x['choices']]
    s = (max(ns) - min(ns)) / (sum(ns) / 4)
    if s > 0.25:
        bad.append(f"{x['id']} 공백 뺀 산포 {s:.3f} > 0.25 — {ns}")


def _tail_factor(c):
    for pat, f in (('하강은 껍질', '껍질'), ('하강도 껍질', '껍질'),
                   ('하강은 핵전하', '핵전하'), ('핵전하가 커져 하강', '핵전하')):
        if pat in c:
            return f
    return '?'


m96 = next(x for x in items if x['id'] == 'M02296')
kw = [_tail_factor(c) for c in m96['choices']]
if kw.count('껍질') != 2 or kw.count('핵전하') != 2:
    bad.append(f'M02296 뒷항이 2:2 가 아니다 — {kw}')

# ★선지는 하나도 건드리지 않았다★ — 이 조치는 발문 하나와 해설 넷뿐이다
m98 = next(x for x in items if x['id'] == 'M02298')
if '72' in ' '.join(m98['solution'].split('★')) and '그 결과야' in m98['solution']:
    if '72 와 133 이 그 결과' in m98['solution']:
        bad.append('M02298 해설에 크기 귀속이 남아 있다')

if bad:
    open(SRC, 'w', encoding='utf-8').write(BEFORE)
    print('  ❌ 불변 검사 실패 — 빌드 파일을 되돌렸다')
    for b in bad:
        print('     ·', b)
    raise SystemExit(1)
print('  ✅ 불변 검사 통과 — 발문 하나 · 해설 넷 · 선지는 그대로 · 뒷항 2:2 · 두 계수법 자 안')
