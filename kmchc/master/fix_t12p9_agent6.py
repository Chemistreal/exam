"""T12 P9 6차 순회 조치 — solver 0 · factchecker ✗0 △0 · defender 3(2채택 1반려)

★factchecker 가 처음으로 완전 클린★ (✗0 · △0 · 전 문항 문제없음).
★solver 는 2·3·4·5·6차 모두 10/10 일치 · 조치 0.★
남은 것은 defender 가 집어낸 ★한국어 구문의 중의성★ 두 자리다 — 지침 (u) 계열.

────────────────────────────────────────────────────────────────
■ 채택1 M01895 ① "간격이 점점 좁아지면서 한없이 낮아진다"
  두 낱말이 각각 두 갈래로 읽힌다.
  · "낮아진다"의 주어 — 문두가 −k, −k/4 로 부호를 명시하니 통상은 ★부호 있는 준위 값★ 이지만,
    화학에서 준위를 ★깊이(속박 에너지 |Eₙ|)★ 로 말하는 관행이 있고 그 독법에서는 k, k/4, k/9 … 가
    실제로 계속 낮아진다.
  · "한없이" — (a) 하한 없이 −∞ 로 → 거짓 / (b) ★끝없이 계속★ → 참.
  (깊이 독법 + (b) 독법)이면 ①은 "|Eₙ| 이 멈추지 않고 작아진다"가 되어 ★정답 ③과 같은 말★ 이 된다.
  ▸ "더 큰 음수가 된다"로 못 박는다. −1 → −0.25 는 음수의 크기가 ★작아지는★ 것이므로
    어떤 독법에서도 거짓이고, 앞 절 "간격이 좁아지면서"의 참은 그대로 남아 매력도 유지된다.
  ▸ ★sim(3차)은 '반만 거짓으로 해야 산다', defender(4·5·6차)는 '반이 참이라 위험하다'로
    정반대를 주문해 온 자리다. 이번 문면은 둘 다 만족한다 — 앞 절은 참인 채로 두고
    뒤 절의 중의성만 없앴다.★

■ 채택2 M01891 ② "○ — 진동수의 합이 ○ 이고 h 도 함께 더해지므로"
  "h 도 함께 더해진다"가 축자적으로는 'h 자체가 합산된다'(거짓)이지만,
  한국어로는 ★'h 가 곱해진 항들이 함께 더해진다'★ (= hν₁ + hν₂) 로도 읽힌다.
  그 독법이면 근거가 참이 되어 ②가 정답 ③과 동치가 된다.
  ▸ 주어를 h 로 못 박는다: "h ★자체도★ 더해지므로". 이러면 hν₁+hν₂ = h(ν₁+ν₂) 라는
    사실 하나로 확정적으로 거짓이다.
  ▸ 이것도 (u) 다 — 4차에 내가 이 문면을 넣을 때 주어를 흐린 채로 두었다.

────────────────────────────────────────────────────────────────
★반려★
▸ (defender, ★6회차★) M01889 ③ "전자 한 개를 떼는 데에 드는 에너지"
  이번 논거는 새롭다 — "수소는 전자가 하나뿐이니 '전자 한 개를 떼는'은 (원자마다) 로 읽혀
  몰 위반이 사라지고, 남는 판별 근거가 부호 하나뿐"이라는 것이다.
  ▸ ★그래도 거짓이다.★ 식이 주는 값은 −1312/n² 이고 떼는 데 드는 에너지는 +1312/n² 이다.
    부호가 반대이므로 같은 값일 수 없다. ★하나의 결정적 근거로 확정적으로 거짓이면 좋은 오답이다★ —
    지침 (v) 는 오히려 근거를 필요 이상 쌓지 말라고 한다.
  ▸ 부호를 쟁점화하라는 제안(발문에 "부호까지 포함하여")은 반려한다. '값'이 부호를 포함하는 것은
    자명하고, 덧붙이면 ①④의 층위 축까지 함께 드러나 문항이 헐거워진다.
  ▸ ★여섯 회 연속 같은 자리가 올라왔으나 매번 격파 근거는 같았다 — 얇지만 유효한 여백이다.
    watch 에 새겨 다음 감사에서 다시 다투지 않게 한다.★
▸ (defender 참고) M01896 ① "표기 취약성" — 단위 g 가 누락되거나 kg 로 바뀌면 참이 된다는 경고.
  문항 결함이 아니라 ★뒷날의 편집 위험★ 이므로 watch 에 기록한다.
"""
import json, os

BANK = os.path.join(os.path.dirname(os.path.abspath(__file__)), 'master_bank.json')
CIR = '①②③④'

REPL = {}

# ══ M01895 ① — '낮아진다'의 중의성을 없앤다 ═════════════════
REPL['M01895'] = dict(
    choices=["간격이 좁아지면서 더 큰 음수가 된다", "언제나 같은 간격씩 계속 높아진다",
             "0 에 다가가되 결코 닿지는 않는다", "n 이 어느 값을 넘어서면 0 이 된다"],
    errors={0: "간격이 좁아지는 것은 맞지만 값은 더 큰 음수가 되는 것이 아니라 0 쪽으로 올라간다",
            1: "높아지는 방향은 맞지만 간격이 0.75k 에서 0.049k 로 빠르게 좁아진다",
            3: "0 이 되는 유한한 n 은 없다. 다가가는 것과 닿는 것은 다르다"},
    solution=(
        "수열이 어느 쪽으로 어떻게 가는지를 방향과 간격으로 함께 보는 문항이야.\n\n"
        "[정답] ③ 0 에 다가가되 결코 닿지는 않는다 — 분모가 n 제곱이라 n 이 커질수록 −k/n² 은 0 에 가까워져. "
        "k 를 1 로 두고 적어 보면 −1, −0.25, −0.111, −0.0625, −0.04, −0.0278 이야. 값이 커지되 언제나 음수에 "
        "머물고, 0 이 되는 유한한 n 은 없지 — ③이 옳아. 여기서 갈라야 할 것이 '다가감'과 '닿음'이야. 0 에 "
        "얼마든지 가까워질 수 있다는 것과 어느 n 에서 0 이 된다는 것은 다른 말이야. 간격도 함께 보자. "
        "0.75k, 0.139k, 0.049k 로 빠르게 좁아져. 그러니 방향과 간격을 따로 따로 봐야 해. 간격이 좁아지는 것은 "
        "맞지만 값이 더 깊은 음수로 내려가는 것은 아니고, 값이 0 쪽으로 올라가는 것은 맞지만 같은 간격씩은 아니야.\n\n"
        "① 간격이 좁아지면서 더 큰 음수가 된다: 간격이 좁아지는 것은 맞았는데 값은 더 큰 음수가 되는 것이 아니라 "
        "0 쪽으로 올라가.\n"
        "② 언제나 같은 간격씩 계속 높아진다: 높아지는 방향은 맞았는데 간격이 0.75k 에서 0.049k 로 빠르게 좁아져.\n"
        "④ n 이 어느 값을 넘어서면 0 이 된다: 0 이 되는 유한한 n 은 없어. 다가가는 것과 닿는 것은 달라.\n\n"
        "자가진단: 0 에 다가가되 닿지 않는다. 간격은 빠르게 좁아진다."),
)

# ══ M01891 ② — 'h 도 함께'의 주어를 못 박는다 ══════════════
REPL['M01891'] = dict(
    choices=["× — h 가 세 번 곱해져 세 배가 되므로", "○ — 진동수의 합이 ○ 이고 h 자체도 더해지므로",
             "○ — 진동수의 합이 ○ 이고 h 가 상수이므로", "× — 표에 파장의 합이 × 로 적혀 있으므로"],
    solution=(
        "빈칸 하나를 옆 줄의 근거로 메우는 문항이야. 답만이 아니라 까닭까지 골라야 해.\n\n"
        "[정답] ③ ○ — 진동수의 합이 ○ 이고 h 가 상수이므로 — 진동수의 합이 성립한다고 표에 적혀 있어. "
        "그 세 값에 같은 수 h 를 곱하면 양변이 똑같은 배수로 늘어나니 관계는 그대로 남지. 그러니 hν 의 합도 ○ 야 "
        "— ③이 옳아. 한 번 더 확인해 볼 수도 있어. hν 는 곧 에너지이고, 표 첫 줄에 에너지의 합이 ○ 라 적혀 있잖아. "
        "두 경로가 같은 답을 주니 안심해도 돼. 여기서 붙잡을 것은 h 가 '항마다 곱해지는 같은 수'라는 점이야. "
        "더해지는 항도 아니고, 항마다 달라지는 값도 아니지. 그리고 파장 쪽은 조심해야 해. 파장 자체의 합은 × 인데 "
        "그 역수의 합은 ○ 지. 파장은 진동수와 반비례라서 성질이 그대로 옮겨 가지 않아.\n\n"
        "① × — h 가 세 번 곱해져 세 배가 되므로: 양변에 같은 수를 곱하는 것이라 관계가 흐트러지지 않아.\n"
        "② ○ — 진동수의 합이 ○ 이고 h 자체도 더해지므로: h 는 각 항에 곱해지는 같은 수이지 더해지는 것이 "
        "아니야. hν₁ + hν₂ = h(ν₁+ν₂) 지. 결론은 맞았는데 까닭이 틀렸어.\n"
        "④ × — 표에 파장의 합이 × 로 적혀 있으므로: 파장의 합이 × 인 것은 맞지만, 파장과 hν 는 반비례라 "
        "한쪽의 성질이 그대로 옮겨 가지 않아.\n\n"
        "자가진단: 양변에 같은 수를 곱해도 관계는 남는다. 표에서 × 인 것은 파장의 합뿐."),
)


def spread(ch):
    L = sorted(len(str(c)) for c in ch)
    return (L[3] - L[0]) / ((L[1] + L[2]) / 2)


def rank(it):
    L = [len(str(c)) for c in it['choices']]
    return sorted(range(4), key=lambda i: -L[i]).index(it['answer']) + 1


def main():
    bank = json.load(open(BANK, encoding='utf-8'))
    d = {x['id']: x for x in bank}
    n = 0
    for fid, r in REPL.items():
        x = d[fid]
        for key in ('stem', 'answer_proof', 'solution', 'choices'):
            if key in r:
                assert x[key] != r[key], f'{fid}: {key} 가 이미 같음'
                x[key] = r[key]; n += 1
        for w in x['distractors']:
            for k, fld in (('errors', 'error'), ('types', 'type')):
                if k in r and w['opt'] in r[k]:
                    w[fld] = r[k][w['opt']]; n += 1

    for fid in REPL:
        x = d[fid]
        ch, a, sol = x['choices'], x['answer'], x['solution']
        L = [len(str(c)) for c in ch]
        assert not (L[a] == max(L) and L.count(max(L)) == 1), f'{fid}: 정답=유일 최장 {L}'
        assert not (L[a] == min(L) and L.count(min(L)) == 1), f'{fid}: 정답=유일 최단 {L}'
        if sorted(L)[1] >= 8:
            assert spread(ch) <= 0.25, f'{fid}: 산포 {spread(ch):.3f} {L}'
        assert len(set(ch)) == 4, f'{fid}: 선지 중복'
        assert len(sol) >= 300, f'{fid}: 해설 {len(sol)}자'
        for w in x['distractors']:
            assert ch[w['opt']] in sol, f'{fid} {CIR[w["opt"]]}: 해설에 오답 문면 없음 -> {ch[w["opt"]]}'
        assert ch[a] in sol, f'{fid}: 해설에 정답 문면 없음'
        for f in ('stem', 'solution'):
            for bad in ('**', '★'):
                assert bad not in x[f], f'{fid}: {f} 에 {bad}'
    # M01891 은 ○ 둘 · 어미 통일을 계속 지켜야 한다
    assert sum(c.startswith('○') for c in d['M01891']['choices']) == 2, 'M01891: ○ 가 둘이 아님'
    assert all(c.endswith('므로') for c in d['M01891']['choices']), 'M01891: 선지 어미 불일치'

    STALE = {
        # 발문의 '한없이 키워 갈 때'는 정당하다 — 걷어낼 것은 선지 쪽 '한없이 낮아진다'뿐
        'M01895': ['한없이 낮아진다'],
        'M01891': ['h 도 함께 더해지므로'],
    }
    for fid, pats in STALE.items():
        blob = json.dumps(d[fid], ensure_ascii=False)
        for p in pats:
            assert p not in blob, f'{fid}: 걷어낸 문면 잔존 -> {p}'

    json.dump(bank, open(BANK, 'w', encoding='utf-8'), ensure_ascii=False, indent=1)
    print(f'P9 6차 조치 {n}곳 · 채택 2(둘 다 한국어 구문 중의성) · 반려 2')
    for fid in sorted(REPL):
        x = d[fid]
        L = [len(str(c)) for c in x['choices']]
        print(f'  {fid} 정답{CIR[x["answer"]]} 길이{L} 산포{spread(x["choices"]):.2f} '
              f'길이순위{rank(x)} 해설{len(x["solution"])}자')


if __name__ == '__main__':
    main()
