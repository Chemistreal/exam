# -*- coding: utf-8 -*-
"""T16 P6 (M02841~M02850) — 표를 펴기 전에 무엇을 묻는지 한 줄로 적는다

  각도: C16-001[4] · 002[7] · 003[2] · 006[4] · 007[3] · 011[7] · 012[4] · 015[3] ·
        016[4] · 018[5]

  ★정답 자리 3·2·1·3·1·4·2·1·3·4★ (앞 꼬리 2-1-3-1-4-2-4-2 를 이어 G3f 를 간격 5 까지 셈했다)
  병합 뒤 ①713②712③713④712 — 편차 1

  ★길이 규칙 — P5 에서 찾은 폭★
  동률을 피하려 벌리면 산포가 넘고, 산포를 지키려 모으면 동률이 는다. 두 조건이
  함께 서는 폭은 ★네 선지를 2~3 자 안에서 층지게 두는 것★ 이다.
  3위: 841 · 843 · 845 · 847 · 849 / 2위: 842 · 844 · 846 · 848 · 850
"""
import re

import batch_template as T

T.START_ID = 'M02841'
T.COUNT = 10
T.EXPECT_LEN = 2840
T.THEME = '전자친화도·전기음성도'
T.TT = 16
T.UNIT = 'I'
T.BATCH_NOTE = 'T16 P6 — 표를 펴기 전에 무엇을 묻는지 한 줄로 적는다.'


def q(i, sk, tr, dok, df, esr, stem, cs, ans, proof, wr, dev, calc, scen, obj,
      lead, prose, wmap, diag):
    x = T.mk(i, sk, tr, dok, df, esr, stem, cs, ans, proof, wr, dev, calc, scen, obj=obj)
    T.sol(x, lead, prose, wmap, diag)
    return x


def build():
    it = []

    # ── M02841 ── C16-001[4] 음이온이 생기는 쪽 (일반·쉬움) 정답 ③ [3위] ──
    it.append(q('M02841', '전자 친화도가 음이온이 생기는 쪽의 값임을 적기',
        '일반', 1, '쉬움', 0.60,
        '전자 친화도를 잴 때 원자가 어떤 알갱이로 바뀌는지 물었다. 옳은 것은?',
        ['전하를 띠지 않은 채로 남는다',
         '전자를 잃고서 양이온이 된다',
         '전자를 얻어 음이온이 된다',
         '두 조각으로 갈라져 간다'], 2,
        '전자 친화도는 전자 하나를 받아들일 때의 값이므로 원자는 음전하를 하나 얻어 음이온이 된다',
        [('전하를 띠지 않은 채로 남는다', '전하가 생기는 것을 지움', 'conserv'),
         ('전자를 잃고서 양이온이 된다', '얻는 쪽과 잃는 쪽을 맞바꿈', 'sign'),
         ('두 조각으로 갈라져 간다', '없는 일을 만들어 붙임', 'proc')],
        '전자를 하나 받으면 전하가 어떻게 되는지 적기',
        '전자 하나 받음 → 음전하 하나 늚 → 음이온 / X(g) + e⁻ → X⁻(g) / ① 전하가 생김 / '
        '② 잃는 쪽은 이온화 에너지 / ④ 쪼개지지 않음',
        '전자를 하나 받은 원자', '값이 가리키는 알갱이를 아는지 확인',
        '전자를 하나 받으면 전하가 어떻게 되는지 적어 봐.',
        '전자를 하나 받으면 음전하가 하나 늘어 음이온이 돼 — ③이 옳아. '
        '①은 전하를 띠지 않은 채로 남는다 라고 했어. 전하가 생기잖아. '
        '②는 전자를 잃고서 양이온이 된다 라고 했어. 잃는 쪽은 다른 값 이야기야. '
        '④는 두 조각으로 갈라져 간다 라고 했어. 쪼개지지는 않아. '
        '값을 물으면 그 일이 끝난 뒤 무엇이 되는지 적는다.',
        {'전하를 띠지 않은 채로 남는다': '전하가 생겨.',
         '전자를 잃고서 양이온이 된다': '다른 값 이야기야.',
         '두 조각으로 갈라져 간다': '쪼개지지 않아.'},
        '무엇이 되는지 적었는가?'))

    # ── M02842 ── C16-002[7] 부호 규약을 어디서 찾을지 (일반·중간) 정답 ② [2위] ──
    it.append(q('M02842', '문제에서 부호 규약을 어디서 찾을지 정하기',
        '일반', 2, '중간', 0.48,
        '문제에 실린 표가 부호를 어떻게 쓰는지 알아야 한다. 어디를 보아야 하는지 옳은 것은?',
        ['값이 큰 칸을 골라 눈으로 짐작한다',
         '표의 머리말이나 아래 주석을 본다',
         '앞 단원의 표를 그대로 쓴다',
         '부호는 늘 같으니 볼 것이 없다'], 1,
        '부호를 어떻게 쓰는지는 표를 만든 쪽이 머리말이나 아래 주석에 적어 두므로, 짐작하지 말고 '
        '그 자리를 찾아 읽어야 한다',
        [('값이 큰 칸을 골라 눈으로 짐작한다', '적혀 있는 것을 짐작으로 바꿈', 'proc'),
         ('앞 단원의 표를 그대로 쓴다', '표마다 다를 수 있음을 지움', 'conserv'),
         ('부호는 늘 같으니 볼 것이 없다', '갈릴 수 있는 것을 하나로 넓힘', 'overgen')],
        '표를 만든 쪽이 규약을 어디에 적어 두는지 적기',
        '부호 규약: 머리말이나 아래 주석에 적힘 / 짐작하거나 다른 표에서 가져오지 않음 / ① 짐작 / '
        '③ 표마다 다를 수 있음 / ④ 늘 같지 않음',
        '부호가 적힌 문제 속 표', '규약이 적힌 자리를 아는지 확인',
        '표를 만든 쪽이 규약을 어디에 적어 두는지 적어 봐.',
        '부호 규약은 머리말이나 아래 주석에 적혀 있어. 거기를 찾아 읽어야지 — ②가 옳아. '
        '①은 값이 큰 칸을 골라 눈으로 짐작한다 라고 했어. 적혀 있는 것을 짐작할 까닭이 없지. '
        '③은 앞 단원의 표를 그대로 쓴다 라고 했어. 표마다 다를 수 있어. '
        '④는 부호는 늘 같으니 볼 것이 없다 라고 했어. 늘 같지는 않아. '
        '표를 읽기 전에 규약이 적힌 자리를 먼저 찾는다.',
        {'값이 큰 칸을 골라 눈으로 짐작한다': '적혀 있어.',
         '앞 단원의 표를 그대로 쓴다': '표마다 달라.',
         '부호는 늘 같으니 볼 것이 없다': '늘 같지 않아.'},
        '규약이 적힌 자리를 찾았는가?'))

    # ── M02843 ── C16-003[2] 두 요인이 같은 쪽 (일반·중간) 정답 ① [3위] ──
    it.append(q('M02843', '유효 핵전하와 반지름이 같은 쪽으로 움직이는 자리를 짚기',
        '일반', 3, '중간', 0.42,
        '같은 주기를 오른쪽으로 가면 유효 핵전하는 커지고 반지름은 작아진다. 이 두 가지가 '
        '전자를 붙드는 힘에 어떻게 드는지 옳은 것은?',
        ['둘 다 세게 붙드는 쪽으로 민다',
         '둘이 서로 맞서서 그 힘이 상쇄된다',
         '둘 가운데 하나만 힘에 든다',
         '둘 다 약하게 붙드는 쪽으로 민다'], 0,
        '유효 핵전하가 커지면 더 세게 당기고 반지름이 작아지면 더 가까이 놓여 역시 세게 당기므로, '
        '두 요인이 같은 쪽으로 힘을 민다',
        [('둘이 서로 맞서서 그 힘이 상쇄된다', '같은 쪽인 것을 맞선다고 봄', 'sign'),
         ('둘 가운데 하나만 힘에 든다', '드는 요인 하나를 지움', 'conserv'),
         ('둘 다 약하게 붙드는 쪽으로 민다', '미는 방향을 뒤집음', 'scale')],
        '두 요인이 각각 힘을 어느 쪽으로 미는지 따로 적기',
        '유효 핵전하 ↑ → 세게 당김 / 반지름 ↓ → 가까워짐 → 세게 당김 → 두 요인이 같은 쪽 / '
        '② 맞서지 않음 / ③ 둘 다 듦 / ④ 세게 붙드는 쪽',
        '오른쪽으로 간 같은 주기', '두 요인의 방향을 따로 보는지 확인',
        '두 요인이 각각 힘을 어느 쪽으로 미는지 따로 적어 봐.',
        '핵전하가 커져도 세게 당기고 반지름이 작아져도 세게 당겨. 같은 쪽이지 — ①이 옳아. '
        '②는 둘이 서로 맞서서 그 힘이 상쇄된다 라고 했어. 맞서지 않아. '
        '③은 둘 가운데 하나만 힘에 든다 라고 했어. 둘 다 들잖아. '
        '④는 둘 다 약하게 붙드는 쪽으로 민다 라고 했어. 세게 붙드는 쪽이야. '
        '요인이 여럿이면 각각 어느 쪽으로 미는지 따로 적는다.',
        {'둘이 서로 맞서서 그 힘이 상쇄된다': '맞서지 않아.',
         '둘 가운데 하나만 힘에 든다': '둘 다 들어.',
         '둘 다 약하게 붙드는 쪽으로 민다': '세게 붙들어.'},
        '요인마다 방향을 적었는가?'))

    # ── M02844 ── C16-006[4] 두 값이 모두 큰 무리 (심화·중간) 정답 ③ [2위] ──
    it.append(q('M02844', '이온화 에너지와 전자 친화도가 함께 큰 무리를 짚기',
        '심화', 3, '중간', 0.34,
        '이온화 에너지와 전자 친화도가 둘 다 큰 무리를 찾으려 한다. 옳은 것은?',
        ['1족이 두 값 모두 큰 무리가 된다',
         '2족이 두 값 모두 큰 자리에 든다',
         '17족이 두 값 모두 크게 나온다',
         '두 값이 함께 큰 무리는 없다'], 2,
        '17족은 전자를 떼기도 어렵고 받아들이기도 잘하는 자리라 이온화 에너지와 전자 친화도가 함께 '
        '크게 나온다',
        [('1족이 두 값 모두 큰 무리가 된다', '두 값이 다 작은 쪽을 고름', 'sign'),
         ('2족이 두 값 모두 큰 자리에 든다', '한쪽이 떨어지는 자리를 고름', 'surface'),
         ('두 값이 함께 큰 무리는 없다', '있는 자리를 없다고 함', 'conserv')],
        '무리마다 두 값이 어떻게 나오는지 나란히 적기',
        '1족: 떼기 쉬움 · 잘 안 받음 → 둘 다 작음 / 2족: 전자 친화도가 떨어짐 / 17족: 떼기 어렵고 '
        '잘 받음 → 둘 다 큼 / ④ 있는 자리임',
        '두 값이 함께 큰 자리 찾기', '두 값을 나란히 놓고 보는지 확인',
        '무리마다 두 값이 어떻게 나오는지 나란히 적어 봐.',
        '17족은 떼기도 어렵고 받아들이기도 잘해. 두 값이 함께 크지 — ③이 옳아. '
        '①은 1족이 두 값 모두 큰 무리가 된다 라고 했어. 1족은 둘 다 작아. '
        '②는 2족이 두 값 모두 큰 자리에 든다 라고 했어. 전자 친화도가 떨어지는 자리지. '
        '④는 두 값이 함께 큰 무리는 없다 라고 했어. 있는 자리야. '
        '두 값을 함께 물으면 무리마다 나란히 적어 본다.',
        {'1족이 두 값 모두 큰 무리가 된다': '둘 다 작아.',
         '2족이 두 값 모두 큰 자리에 든다': '한쪽이 떨어져.',
         '두 값이 함께 큰 무리는 없다': '있는 자리야.'},
        '무리마다 나란히 적었는가?'))

    # ── M02845 ── C16-007[3] 표가 다르면 값도 다를 수 있음 (일반·쉬움) 정답 ① [3위] ──
    it.append(q('M02845', '표가 다르면 값이 조금 다를 수 있음을 적기',
        '일반', 2, '쉬움', 0.56,
        '두 책의 표에서 같은 원소의 값이 조금 달랐다. 이 차이에 대해 옳은 것은?',
        ['재는 방법이 달라 조금 갈릴 수 있다',
         '둘 가운데 하나는 꼭 틀린 값인 것이다',
         '값이 다르면 원소도 다른 것이 된다',
         '표는 늘 똑같은 값을 실어야 한다'], 0,
        '값은 재는 방법과 다듬는 방식에 따라 조금씩 갈릴 수 있으므로 작은 차이는 어느 한쪽이 틀린 '
        '것이 아니라 표마다 있는 폭이다',
        [('둘 가운데 하나는 꼭 틀린 값인 것이다', '있을 수 있는 폭을 잘못으로 봄', 'proc'),
         ('값이 다르면 원소도 다른 것이 된다', '값의 차이를 원소의 차이로 봄', 'surface'),
         ('표는 늘 똑같은 값을 실어야 한다', '폭이 있음을 지움', 'conserv')],
        '값이 어떻게 얻어진 숫자인지 한 줄로 적기',
        '값: 재고 다듬어 얻은 숫자 → 방법에 따라 조금 갈림 / 작은 차이는 폭 / ② 틀린 것이 아님 / '
        '③ 원소는 같음 / ④ 폭이 있음',
        '두 책에서 조금 다른 값', '작은 차이를 폭으로 읽는지 확인',
        '값이 어떻게 얻어진 숫자인지 한 줄로 적어 봐.',
        '값은 재고 다듬어 얻은 숫자야. 방법이 다르면 조금 갈릴 수 있지 — ①이 옳아. '
        '②는 둘 가운데 하나는 꼭 틀린 값인 것이다 라고 했어. 틀린 것이 아니야. '
        '③은 값이 다르면 원소도 다른 것이 된다 라고 했어. 원소는 같잖아. '
        '④는 표는 늘 똑같은 값을 실어야 한다 라고 했어. 폭이 있는 값이지. '
        '숫자를 볼 때는 그것이 어떻게 얻어졌는지 적는다.',
        {'둘 가운데 하나는 꼭 틀린 값인 것이다': '틀린 게 아냐.',
         '값이 다르면 원소도 다른 것이 된다': '원소는 같아.',
         '표는 늘 똑같은 값을 실어야 한다': '폭이 있어.'},
        '어떻게 얻어졌는지 적었는가?'))

    # ── M02846 ── C16-011[7] 까닭을 적는 차례 (심화·어려움) 정답 ④ [2위] ──
    it.append(q('M02846', '경향의 까닭을 물으면 무엇부터 적을지 차례를 두기',
        '심화', 4, '어려움', 0.26,
        '전기 음성도가 왜 그렇게 놓이는지 묻는 물음을 받았다. 답을 적는 차례로 옳은 것은?',
        ['표에서 값을 옮겨 적고 끝낸다',
         '경향의 방향만 한 줄로 적는다',
         '외운 문장을 그대로 옮겨 적어 낸다',
         '무엇이 달라지는지부터 먼저 적는다'], 3,
        '까닭을 묻는 물음은 값이나 방향이 아니라 그 축에서 무엇이 달라지는지를 먼저 적어야 답이 '
        '되므로, 유효 핵전하와 반지름의 변화를 앞에 둔다',
        [('표에서 값을 옮겨 적고 끝낸다', '까닭 대신 값을 적음', 'proc'),
         ('경향의 방향만 한 줄로 적는다', '방향을 까닭으로 갈음함', 'surface'),
         ('외운 문장을 그대로 옮겨 적어 낸다', '까닭을 외운 말로 갈음함', 'conserv')],
        '까닭을 묻는 물음이 무엇을 받고 싶어 하는지 적기',
        '까닭을 묻는 물음: 그 축에서 무엇이 달라지는가 / 값·방향·외운 문장은 까닭이 아님 → 유효 '
        '핵전하와 반지름의 변화를 먼저 / ① 값 / ② 방향 / ③ 외운 말',
        '까닭을 묻는 물음 하나', '까닭과 값을 가르는지 확인',
        '까닭을 묻는 물음이 무엇을 받고 싶어 하는지 적어 봐.',
        '까닭을 물으면 그 축에서 무엇이 달라지는지부터 적어야 해 — ④가 옳아. '
        '①은 표에서 값을 옮겨 적고 끝낸다 라고 했어. 값은 까닭이 아니야. '
        '②는 경향의 방향만 한 줄로 적는다 라고 했어. 방향도 까닭이 아니지. '
        '③은 외운 문장을 그대로 옮겨 적어 낸다 라고 했어. 외운 말로는 답이 되지 않아. '
        '까닭을 물으면 무엇이 달라지는지를 앞에 둔다.',
        {'표에서 값을 옮겨 적고 끝낸다': '값은 까닭이 아냐.',
         '경향의 방향만 한 줄로 적는다': '방향도 아냐.',
         '외운 문장을 그대로 옮겨 적어 낸다': '답이 안 돼.'},
        '무엇이 달라지는지 앞에 두었는가?'))

    # ── M02847 ── C16-012[4] 두 표의 첫 줄을 견주기 (일반·중간) 정답 ② [3위] ──
    it.append(q('M02847', '두 표를 나란히 놓고 첫 줄을 견주어 보기',
        '일반', 3, '중간', 0.40,
        '전자 친화도 표와 전기 음성도 표를 나란히 놓았다. 두 표를 견주는 첫 걸음으로 '
        '옳은 것은?',
        ['두 표의 값을 칸마다 더해서 본다',
         '두 표에서 첫째인 원소를 견준다',
         '두 표의 자릿수를 맞춰 둔다',
         '한 표만 보고서 나머지를 어림한다'], 1,
        '두 표가 어디에서 갈리는지 가장 빨리 드러나는 자리는 첫째인 원소이므로, 두 표의 첫 줄을 '
        '먼저 견주어 본다',
        [('두 표의 값을 칸마다 더해서 본다', '갈래가 다른 값을 셈으로 섞음', 'proc'),
         ('두 표의 자릿수를 맞춰 둔다', '겉모습을 먼저 봄', 'surface'),
         ('한 표만 보고서 나머지를 어림한다', '견줄 것을 하나로 줄임', 'scale')],
        '두 표가 갈리는 자리가 어디에서 먼저 드러나는지 적기',
        '두 표의 첫째: 전기 음성도는 불소 · 전자 친화도는 염소 → 첫 줄에서 이미 갈림 / ① 더할 '
        '값이 아님 / ③ 겉모습 / ④ 어림하면 놓침',
        '나란히 놓은 두 표', '견줌의 첫 걸음을 아는지 확인',
        '두 표가 갈리는 자리가 어디에서 먼저 드러나는지 적어 봐.',
        '두 표는 첫째인 원소부터 갈려. 거기를 먼저 견주면 되지 — ②가 옳아. '
        '①은 두 표의 값을 칸마다 더해서 본다 라고 했어. 더할 값이 아니야. '
        '③은 두 표의 자릿수를 맞춰 둔다 라고 했어. 그건 겉모습이지. '
        '④는 한 표만 보고서 나머지를 어림한다 라고 했어. 어림하면 갈리는 자리를 놓쳐. '
        '두 표를 견줄 때는 갈림이 먼저 드러나는 자리를 잡는다.',
        {'두 표의 값을 칸마다 더해서 본다': '더할 값이 아냐.',
         '두 표의 자릿수를 맞춰 둔다': '겉모습이야.',
         '한 표만 보고서 나머지를 어림한다': '갈림을 놓쳐.'},
        '먼저 드러나는 자리를 잡았는가?'))

    # ── M02848 ── C16-015[3] 금속과 비금속이 만나는 자리 (일반·중간) 정답 ① [2위] ──
    it.append(q('M02848', '금속과 비금속이 만나면 치우침이 커짐을 짚기',
        '일반', 3, '중간', 0.40,
        '금속 원소와 비금속 원소가 만나 결합했다. 전자쌍의 치우침에 대해 옳은 것은?',
        ['비금속 쪽으로 크게 치우쳐 간다',
         '금속 쪽으로 아주 크게 치우쳐 간다',
         '한가운데에 놓여 치우치지 않는다',
         '치우침 방향은 정할 수가 없다'], 0,
        '금속은 전자를 내주기 쉽고 비금속은 받아들이기 쉬우므로 전자쌍이 비금속 쪽으로 크게 '
        '치우친다',
        [('금속 쪽으로 아주 크게 치우쳐 간다', '치우치는 쪽을 맞바꿈', 'sign'),
         ('한가운데에 놓여 치우치지 않는다', '두 성질이 같을 때의 그림을 가져옴', 'surface'),
         ('치우침 방향은 정할 수가 없다', '정할 수 있는 것을 지움', 'conserv')],
        '두 쪽이 전자를 내주는 쪽인지 받는 쪽인지 적기',
        '금속: 내주기 쉬움 / 비금속: 받아들이기 쉬움 → 전자쌍이 비금속 쪽으로 / ② 거꾸로 / '
        '③ 성질이 같을 때 / ④ 정할 수 있음',
        '금속과 비금속이 만난 결합', '치우침의 방향을 정하는지 확인',
        '두 쪽이 전자를 내주는 쪽인지 받는 쪽인지 적어 봐.',
        '금속은 내주는 쪽, 비금속은 받는 쪽이야. 전자쌍은 비금속으로 치우치지 — ①이 옳아. '
        '②는 금속 쪽으로 아주 크게 치우쳐 간다 라고 했어. 거꾸로야. '
        '③은 한가운데에 놓여 치우치지 않는다 라고 했어. 두 성질이 같을 때의 그림이지. '
        '④는 치우침 방향은 정할 수가 없다 라고 했어. 정할 수 있어. '
        '방향을 물으면 두 쪽이 어느 쪽인지부터 적는다.',
        {'금속 쪽으로 아주 크게 치우쳐 간다': '거꾸로야.',
         '한가운데에 놓여 치우치지 않는다': '성질이 같을 때야.',
         '치우침 방향은 정할 수가 없다': '정할 수 있어.'},
        '두 쪽이 어느 쪽인지 적었는가?'))

    # ── M02849 ── C16-016[4] 값이 같아도 성질이 다를 수 있음 (심화·어려움) 정답 ③ [3위] ──
    it.append(q('M02849', '값이 같아도 성질이 다를 수 있음을 적기',
        '심화', 4, '어려움', 0.26,
        '두 원소의 전기 음성도 값이 거의 같게 적혀 있었다. 두 원소로 만든 물질에 대해 '
        '옳은 것은?',
        ['값이 같으니 성질까지 같아진다',
         '값이 같으면 같은 원소라는 말이 된다',
         '값이 같아도 성질은 갈릴 수 있다',
         '값이 같으면 견줄 것이 없어진다'], 2,
        '전기 음성도는 여러 성질 가운데 하나일 뿐이고 반지름이나 껍질 수 같은 다른 것들이 다르면 '
        '만들어지는 물질의 성질도 갈린다',
        [('값이 같으니 성질까지 같아진다', '한 값으로 성질을 못 박음', 'overgen'),
         ('값이 같으면 같은 원소라는 말이 된다', '값의 같음을 원소의 같음으로 봄', 'surface'),
         ('값이 같으면 견줄 것이 없어진다', '다른 잣대가 있음을 지움', 'conserv')],
        '두 원소가 이 값 말고 무엇이 다른지 적기',
        '전기 음성도: 여러 성질 가운데 하나 / 반지름·껍질 수 등이 다름 → 물질의 성질도 갈림 / '
        '① 한 값으로 못 박음 / ② 원소는 다름 / ④ 다른 잣대가 있음',
        '값이 거의 같은 두 원소', '한 값으로 성질을 못 박지 않는지 확인',
        '두 원소가 이 값 말고 무엇이 다른지 적어 봐.',
        '전기 음성도는 여러 성질 가운데 하나야. 다른 것이 다르면 성질도 갈리지 — ③이 옳아. '
        '①은 값이 같으니 성질까지 같아진다 라고 했어. 한 값으로 못 박을 수 없어. '
        '②는 값이 같으면 같은 원소라는 말이 된다 라고 했어. 원소는 다르잖아. '
        '④는 값이 같으면 견줄 것이 없어진다 라고 했어. 다른 잣대가 있지. '
        '한 값이 같다고 나머지까지 같다고 적지 않는다.',
        {'값이 같으니 성질까지 같아진다': '못 박을 수 없어.',
         '값이 같으면 같은 원소라는 말이 된다': '원소는 달라.',
         '값이 같으면 견줄 것이 없어진다': '다른 잣대 있어.'},
        '이 값 말고 다른 것을 적었는가?'))

    # ── M02850 ── C16-018[5] 묻는 값을 먼저 적기 (일반·중간) 정답 ④ [2위] ──
    it.append(q('M02850', '묻는 값을 먼저 적어 놓고 셈에 들어가기',
        '일반', 3, '중간', 0.38,
        '두 값이 함께 실린 자료를 받아 물음을 풀려 한다. 셈에 들어가기 전에 할 일로 '
        '옳은 것은?',
        ['자료에 실린 값을 모두 옮겨서 적는다',
         '눈에 먼저 띄는 값부터 셈해 본다',
         '자료를 덮고 기억으로 풀어 본다',
         '묻는 값이 무엇인지 먼저 적어 둔다'], 3,
        '두 값이 함께 실려 있으면 셈하는 도중에 엉뚱한 값을 집기 쉬우므로, 묻는 값이 무엇인지 먼저 '
        '적어 두고 시작해야 한다',
        [('자료에 실린 값을 모두 옮겨서 적는다', '쓰지 않을 값까지 끌어옴', 'scale'),
         ('눈에 먼저 띄는 값부터 셈해 본다', '고르는 잣대를 눈에 맡김', 'surface'),
         ('자료를 덮고 기억으로 풀어 본다', '주어진 자료를 버림', 'conserv')],
        '자료에 값이 몇 갈래로 실려 있는지 세어 보기',
        '두 갈래가 함께 실림 → 도중에 엉뚱한 값을 집기 쉬움 → 묻는 값을 먼저 적어 둠 / ① 다 옮길 '
        '까닭 없음 / ② 눈은 잣대가 아님 / ③ 자료를 버림',
        '두 값이 함께 실린 자료', '묻는 값을 먼저 붙드는지 확인',
        '자료에 값이 몇 갈래로 실려 있는지 세어 봐.',
        '두 갈래가 함께 실려 있으면 엉뚱한 값을 집기 쉬워. 묻는 값을 먼저 적어 둬 — ④가 옳아. '
        '①은 자료에 실린 값을 모두 옮겨서 적는다 라고 했어. 다 옮길 까닭이 없지. '
        '②는 눈에 먼저 띄는 값부터 셈해 본다 라고 했어. 눈은 잣대가 아니야. '
        '③은 자료를 덮고 기억으로 풀어 본다 라고 했어. 주어진 자료를 버리는 거야. '
        '셈에 들어가기 전에 무엇을 묻는지 적어 둔다.',
        {'자료에 실린 값을 모두 옮겨서 적는다': '까닭이 없어.',
         '눈에 먼저 띄는 값부터 셈해 본다': '눈은 잣대가 아냐.',
         '자료를 덮고 기억으로 풀어 본다': '자료를 버려.'},
        '묻는 값을 먼저 적었는가?'))

    return it


_ANON = re.compile(r'원소 (?:가|나|다|A|B|C)(?=의|와|과|는|은|를|이|가|,|·|\s|$)')


def local_checks(items):
    """T15 지역 저작 점검 — P3 의 것을 그대로 잇는다(⑧ 겹침 · ⑨ 길이 포함)."""
    bad = []
    ALLOWED = {'proc', 'sign', 'surface', 'conserv', 'unit', 'scale', 'overgen', 'causal'}
    for x in items:
        ts = [w['type'] for w in x['distractors']]
        if len(set(ts)) < 3:
            bad.append(f"{x['id']} 오답 유형이 겹친다 — {ts}")
        for t in ts:
            if t not in ALLOWED:
                bad.append(f"{x['id']} 허용되지 않은 유형 {t}")
        # ★★이 검사는 P3 3차까지 한 번도 울 수 없었다★★ — sol() 이 해설 끝에 붙이는
        #   단평 블록이 오답 문면을 ★글자 그대로★ 되풀이하므로 `in x['solution']` 은 언제나
        #   참이었다. 해설 산문에서 인용이 흐트러져도 검사는 통과한다(defender 와
        #   factchecker 가 각각 두 자리를 손으로 잡아 냈다). ▸ ★산문 문단만 본다.★
        prose = x['solution'].split('\n\n')[1] if '\n\n' in x['solution'] else x['solution']
        #   ▸ 산문은 간접 인용이라 종결어미가 바뀐다('…때문이다' → '…때문이라고 했어').
        #     ★어미를 뗀 몸통★ 이 그대로 있는지만 본다 — 몸통이 어긋나면 인용이 흐트러진 것이다.
        for w in x['distractors']:
            core = re.sub(r'(이)?다$', '', x['choices'][w['opt']]).strip()
            if core not in prose:
                bad.append(f"{x['id']} {w['opt'] + 1}번 오답 인용이 해설 산문과 어긋난다")
        if not re.search(r'[①②③④](가|이)? 옳아', x['solution']):
            bad.append(f"{x['id']} 해설에 정답 확정 어구가 없다")
        J = ('를|을|은|의|으로|로|에서|에|과|보다|처럼|까지|부터|이고|이다|이야|이라|이지')
        for f in ('stem', 'solution', 'answer_proof'):
            for m in re.finditer(rf'(?<=[가-힣]) ({J})(?=[\s,.·)?!]|$)', x[f]):
                bad.append(f"{x['id']} {f} 조사 앞 빈칸 — {x[f][max(0, m.start() - 14):m.end()]!r}")
        tails = [c.strip()[-3:] for c in x['choices']]
        for t in set(tails):
            if tails.count(t) == 3 and tails[x['answer']] != t:
                bad.append(f"{x['id']} 어미 「{t}」 셋 대 정답 하나 — 꼴로 갈린다")

        def tk(s):
            return {w for w in re.findall(r'[가-힣]{2,}', s)}
        for i in range(4):
            for j in range(i + 1, 4):
                if x['answer'] in (i, j):
                    continue
                a, b = tk(x['choices'][i]), tk(x['choices'][j])
                if a and b and len(a & b) / len(a | b) >= 0.62:
                    bad.append(f"{x['id']} 오답 {i + 1}·{j + 1} 자카드 "
                               f"{len(a & b) / len(a | b):.2f}")
    for x in items:
        s = x['stem']
        if '톱니' in s and '세로축' not in s:
            bad.append(f"{x['id']} 톱니를 쓰면서 세로축을 적지 않았다")
        anon = _ANON.search(s)
        named = re.search(r'(리튬|베릴륨|붕소|탄소|질소|산소|플루오린|네온|나트륨|마그네슘|염소'
                          r'|칼륨|알루미늄|헬륨|수소|아르곤|브로민|Li|Be|B\b|C\b|N\b|O\b|F\b|Ne|Na)', s)
        if anon and named:
            bad.append(f"{x['id']} 익명 원소와 원소 이름을 함께 인쇄했다 — "
                       f"{anon.group(0)!r} · {named.group(0)!r}")
        if '크게 뛰' in s or '급증' in s:
            if not re.search(r'비\(뒤 ÷ 앞\)|비로 재|앞까지의 개수', s):
                bad.append(f"{x['id']} 급증의 셈법을 발문이 정하지 않았다")
    g = sum(1 for x in items if x['track'] == '일반')
    lo = 7 if len(items) >= 10 else max(2, len(items) // 2)
    if g < lo:
        bad.append(f'일반 {g}제 — {len(items)}문항 배치의 하한은 {lo}')
    e = sum(1 for x in items if x['difficulty'] == '쉬움')
    if e > 3:
        bad.append(f'쉬움 {e}제 — 상한 3')

    # ⑧ ★정답이 발문과의 어휘 겹침 유일 최다이면 실패★ (P1 이 값을 치른 자리)
    for x in items:
        st = set(re.findall(r'[0-9A-Za-z가-힣]{2,}', x['stem']))
        ov = [len(set(re.findall(r'[0-9A-Za-z가-힣]{2,}', c)) & st) for c in x['choices']]
        if ov[x['answer']] == max(ov) and ov.count(max(ov)) == 1:
            bad.append(f"{x['id']} 정답이 발문과의 어휘 겹침 최다(유일) — {ov}")

    # ⑨ ★선지 길이 — 정답이 유일 최장/최단이거나 산포가 0.25 를 넘으면 실패★ (P3 이 값을 치렀다)
    for x in items:
        L = [len(c) for c in x['choices']]
        a = L[x['answer']]
        if a == max(L) and L.count(a) == 1:
            bad.append(f"{x['id']} 정답이 유일 최장 — {L}")
        if a == min(L) and L.count(a) == 1:
            bad.append(f"{x['id']} 정답이 유일 최단 — {L}")
        sp = (max(L) - min(L)) / (sum(L) / len(L))
        if sp > 0.25:
            bad.append(f"{x['id']} 선지 길이 산포 {sp:.2f} > 0.25 — {L}")

    print(f"  ▸ 배치 요약 — 유형 {sorted({w['type'] for x in items for w in x['distractors']})}")
    if bad:
        print('\n  ❌ 저작 점검 실패')
        for b in bad:
            print('     ·', b)
        raise SystemExit(1)
    print('  ✅ 저작 점검 통과')



if __name__ == '__main__':
    items = build()
    local_checks(items)
    T.verify(items)
    T.merge_and_house(items)
