# -*- coding: utf-8 -*-
"""T13 P6 (M02021~M02030) 층5 3차 조치 — 2중 정답 하나, 무지식 경로 셋, 절단 흔적 하나.

2차 순회: solver 마감 불가 · defender 마감 불가 · factchecker 마감 불가 ·
student-sim 마감 불가(A 0 / B 0 / C 7 / D 10 · A·B 10/10 분리 · D 전항 정답)

받은 것 여섯
──────────────────────────────────────────────────────────────────────────
1. ★M02026 ④ — 2중 정답 (solver 상 · defender 상 · sim 경고)★
   '전자가 자전하는 방향을 알려 주는' 은 교과서가 스핀을 "자전 방향" 이라 부르기
   때문에 ★교과서 관용으로 참으로 읽힌다.★ 오답이 참으로 읽히는 네 경로 가운데
   '교과서 관용' 에 정확히 걸렸다.
   → '전자가 자전하는 속도를 나타내는 유일한 양자수다' 로 간다.
     ▸ sim 안(속도)을 solver 안('값이 정수로만 주어지는')보다 앞세운 까닭:
       발문이 스핀양자수를 물었는데 ④만 '자전' 을 말한다. 표면 일치의 끌림이
       그대로 남아 B 의 착지점이 살아 있다. solver 안은 mₛ = ±1/2 을 아는
       순간 지워져 착지점이 사라진다.
     ▸ '팽이처럼 도는 방향' 도 검토했으나 반려했다 — 팽이 비유는 교과서가
       스핀을 처음 소개할 때 쓰는 말이라 같은 관용 경로가 다시 열린다.
       ★속도★ 는 어느 교과서도 쓰지 않아 해석 여지가 없다.
     ▸ 길이 27·27·24·26 — 정답 27 이 최장이지만 ①과 동률이라 단독이 아니다.

2. ★M02022 — 무지식 2단계 경로가 살아 있다 (solver 중상 · sim: ③ 죽은 선지)★
   3·6 / 5·10 / 5·32 / 7·14 에서 ③만 '전자 = 2 × 오비탈' 을 어겨 이상치로
   지워지고, 남은 6·10·14 의 등차열에서 가운데를 고르면 정답에 닿는다.
   ▸ ★한 자리만 갈아서는 닫히지 않는다 — 구조적 제약을 먼저 적어 둔다.★
     오비탈 수는 2l+1 이라 ★언제나 홀수★ 이고 정원은 그 두 배다. 그러니
     네 선지가 모두 정합하려면 오비탈 수가 {1, 3, 5, 7} 넷일 수밖에 없고,
     G6(앞자리 오름차순)이 자리를 1·3·5·7 로 못 박는다. 곧 ★정답값이 자리를
     정한다.★ 4d(정답 5)로는 정답이 ③ 자리에 갈 수밖에 없다.
     → 배치의 정답 차례(②)를 지키려면 묻는 부껍질이 ★p★ 여야 한다.
   → 발문을 4d 에서 ★4p★ 로 옮기고 선지를 1·2 / 3·6 / 5·10 / 7·14 완전
     사다리로 놓는다. 정답은 ② 3 개 · 6 개. 네 칸이 s·p·d·f 의 값이라
     ★세 오답 모두 기호를 l 로 옮기는 첫걸음에서 미끄러진 실재 경로★ 를 얻고,
     사다리가 완전해 이상치도 등차 누락도 없다.
   ▸ 대안(①을 '3 개 · 10 개' 로 갈아 정합 축을 2 : 2 로)도 검산했으나 반려했다.
     오비탈 축의 최빈값 5 와 전자 축의 최빈값 10 이 만나는 칸이 ★정답★ 이라
     축별 최빈값 교차가 그대로 정답을 가리킨다(M02023 과 같은 결함).
   ▸ 남는 하중 — 정답값 3 이 M02021(3 종류)과 겹친다. 골격이 전혀 달라
     R3 는 아니지만 ★조판 때 떨어뜨릴 것★ 을 watch 에 적는다. 살아 있는
     우회로가 잠재 하중보다 앞선다.

3. ★M02023 — 축별 최빈값 교차가 정답을 가리킨다 (solver 중상)★
   심지 축 Ar·Ne·Kr·Ar 에서 Ar 이 둘, 뒤 축 4s²·4s²·4s²·3s² 에서 4s² 가 셋.
   두 최빈값이 만나는 칸이 [Ar] 4s² = 정답이다. ★지식 없이 도달한다.★
   → ③ [Kr] 4s² → ★[Ne] 3s²★ 로 갈아 완전한 2 × 2 를 만든다.
     심지 Ar 2 : Ne 2, 뒤 4s² 2 : 3s² 2 — 어느 축에도 다수가 없다.
   ▸ 잃는 것: "심지는 그 원소보다 앞에 와야 한다"(Kr) 는 각도. sim 이 표준
     저빈도 필러로 분류한 자리라 손실이 작고, ㉢(2 × 2 는 다수결 누설을 끊는
     가장 값싼 장치)이 우선한다.
   ▸ factchecker △ 도 함께 받는다 — ④ [Ar] 3s² 는 18 + 2 = 20 이라 ★자가진단의
     전자 수 검산을 통과해 버리는 유일한 선지★ 다. 검산을 한 겹 더 얹는다.

4. ★M02024 — 주어가 열려 있다 (defender 중)★
   '어떤 원소' 라 이온을 배제하지 못한다. Ar²⁺ 도 3p⁴ 로 끝나므로 ④ 18 이
   동시에 성립한다. → '어떤 ★중성 원자★ 의' 로 못 박고, 까닭을 해설에 남긴다.

5. ★M02029 ② — 더 가까운 오답으로 (sim 중)★
   (2,0,0,+1/2)/(3,0,0,+1/2)은 n 이 달라 누구나 허용으로 본다.
   → (3, 2, 0, +1/2) 와 (3, 1, 0, +1/2) — n·mₗ·mₛ 가 같고 l 만 다르다.
     네 선지가 ★l 만 다름 / mₗ 만 다름 / mₛ 만 다름 / 하나도 다르지 않음★
     으로 가지런해져, 어느 자리가 갈려야 공존하는지를 정면으로 묻게 된다.
   ▸ ★defender 의 이 자리 지적은 근거가 틀렸다 — 기록해 둔다.★
     defender 2차는 "②는 바닥상태 독법에서 성립하지 않는다" 며 중성·바닥상태
     가드를 요구했다. 검산했다 — 나트륨 바닥상태 1s² 2s² 2p⁶ 3s¹ 에서
     (2,0,0,+1/2)은 2s² 의 한쪽이고 (3,0,0,+1/2)은 3s¹ 이라 ★실제로 함께
     있다.★ 게다가 발문은 두 전자의 양자수를 적었을 뿐 그 원자의 전자가
     둘뿐이라고 말하지 않는다. 가드는 넣지 않는다.

6. ★M02030 — 절단 흔적과 죽은 선지 (factchecker 사실 오류 · solver 중 · sim 상)★
   (a) "여기서 두 가지를 붙들자. 첫째, …" 로 끝나고 ★둘째가 없다.★ 지난
       순회에서 교과 밖 문장을 지우며 남은 자국이다. → 둘째를 채운다.
   (b) ③ 리튬 이온(Li⁺) 은 무해하지만 무매력이다(sim 상). 게다가 원소 축
       He 2 · 전하 축 (+1, 0, 0, −1) 에서 0 이 2 인데, 두 최빈값의 교차가
       ★He 중성 = ②★ 라 아직 정답을 가리키지는 않는다.
       → ③ → ★리튬 원자(Li)★. "최외각 전자 하나 = 일전자계" 를 정면으로
         겨냥한다(전자 셋이라 복수 정답 위험 없음). solver·sim 이 함께 낸 안이며
         solver 는 ④ 자리를 제안했으나 죽은 자리는 ③ 이므로 ③ 에 놓는다.
       ▸ 덤 — calc_check 가 이미 "He⁺ 1 / He 2 / ★Li 3★ / H⁻ 2" 로 적혀 있었다.
         ③ 을 Li⁺ 로 바꾼 1차 조치 때 함께 고치지 않은 자국이라 지금 정합해진다.

반려한 것 셋
──────────────────────────────────────────────────────────────────────────
 · ★M02021 ② '2 종류' → '9 종류'(sim 중)★ — 반려. 9 는 M 껍질의 오비탈 수라
   각도는 좋으나 G6 가 앞자리 숫자의 오름차순을 요구해 9 는 ④ 자리로 갈 수밖에
   없다. 그러면 ④ '4 종류'(부양자수를 0~n 으로 세는 표준 실패 지점 · 이번 모의에서
   B 의 착지점)를 버리게 된다. ★살아 있는 착지점을 버려 죽은 자리를 살리는 것은
   맞바꿈이 아니다.★ 개념 대장에 남긴다.
 · ★M02027 ② → '전자를 하나 얻어 더 안정한 배치가 되었다'(sim 중)★ — 반려.
   새 ②는 ④('전자 수가 그대로이므로')와 ★같은 까닭(전자 수 세기)으로 함께
   지워진다.★ 지금의 ②는 '2p 가 이미 꽉 찼다' 는 다른 까닭으로 지워지므로
   네 선지의 판별 근거가 갈려 있다. 자멸하는 선지가 근거가 겹치는 선지보다 낫다.
 · ★M02025 범위(factchecker △)★ — 문항은 그대로 둔다. 대신 ★범위 기준을 못
   박는다★(설계 문서 §7 에 옮긴다).
     ㉠ 부껍질 기호는 ★f 까지★ 정답의 근거로 쓴다. g 이상은 쓰지 않는다
       (M02021 을 O 껍질에서 M 껍질로 옮긴 판정과 같은 잣대).
     ㉡ 전이금속은 ★Z ≤ 30 까지★ 범위 안으로 보되 ★심화★ 로 표시한다.
       개념 대장이 교재에서 뽑은 각도이고 채움 순서·제거 순서가 교재 본문에 있다.
"""
import json
from collections import Counter

BANK = 'master/master_bank.json'
MK = ['①', '②', '③', '④']
IDS = ['M%05d' % n for n in range(2021, 2031)]
TOUCHED = {'M02022', 'M02023', 'M02024', 'M02026', 'M02029', 'M02030'}


def sub(it, old, new, n=1):
    assert it['solution'].count(old) == n, f"{it['id']}: '{old[:40]}' {it['solution'].count(old)}회"
    it['solution'] = it['solution'].replace(old, new)


def setc(it, choices, wrongs):
    assert len(choices) == 4 and len(set(choices)) == 4, it['id']
    it['choices'] = choices
    ds = [{'opt': choices.index(w[0]), 'error': w[1], 'type': w[2]} for w in wrongs]
    assert len(ds) == 3 and all(d['opt'] != it['answer'] for d in ds), it['id']
    it['distractors'] = sorted(ds, key=lambda d: d['opt'])


def resol(it, lead, cor, wmap, diag):
    a = it['answer']
    body = [lead, '', f"[정답] {MK[a]} {it['choices'][a]} — {cor}", '']
    for k in range(4):
        if k != a:
            body.append(f"{MK[k]} {it['choices'][k]}: {wmap[it['choices'][k]]}")
    body += ['', f"자가진단: {diag}"]
    it['solution'] = '\n'.join(body)


bank = json.load(open(BANK, encoding='utf-8'))
D = {i['id']: i for i in bank}
before = {k: json.loads(json.dumps(D[k])) for k in IDS}

# ── 1. M02026 ④ — 교과서 관용으로 참이 되는 문장을 지운다 ──────────────────
it = D['M02026']
old4 = '전자가 자전하는 방향을 알려 주는 유일한 양자수다'
new4 = '전자가 자전하는 속도를 나타내는 유일한 양자수다'
assert it['choices'][3] == old4, it['choices']
it['choices'][3] = new4
for d in it['distractors']:
    if d['opt'] == 3:
        d['error'] = 'mₛ 를 회전의 빠르기로 봄 — 스핀은 실제 자전이 아니고 속도를 담지도 않는다'
sub(it, '이름이 스핀이라고 해서 전자가 팽이처럼 실제로 돈다는 뜻은 아니라는 점도 '
        '함께 붙들어야 해. mₛ 는 실험으로 드러난 두 갈래 상태에 붙인 이름이지 '
        '회전 방향을 재는 값이 아니야.',
        '이름이 스핀이라고 해서 전자가 팽이처럼 실제로 돈다는 뜻은 아니라는 점도 '
        '함께 붙들어야 해. mₛ 는 실험으로 드러난 두 갈래 상태에 붙인 이름이야. '
        '교과서가 그 두 갈래를 "자전 방향" 이라 부르기는 해도, 그 값이 재는 것은 '
        '도는 빠르기가 아니라 두 갈래 가운데 어느 쪽인지일 뿐이지.')
sub(it, f'④ {old4}: 스핀은 이름만 회전을 닮았을 뿐 실제 자전이 아니야. '
        f'mₛ 는 두 갈래로 갈리는 상태에 붙인 값이지 도는 방향을 재는 값이 아니지.',
        f'④ {new4}: mₛ 가 가질 수 있는 값은 +1/2 과 −1/2 둘뿐이라 빠르기 같은 것을 '
        f'담을 자리가 없어. 스핀은 이름만 회전을 닮았을 뿐 실제 자전이 아니고, '
        f'네 양자수 가운데 속도를 나타내는 것은 하나도 없어.')

# ── 2. M02022 — 완전 사다리로 옮긴다 (4d → 4p) ────────────────────────────
it = D['M02022']
assert it['answer'] == 1
it['stem'] = '4p 부껍질의 오비탈 개수와 그 부껍질에 들어갈 수 있는 최대 전자 수를 옳게 짝지은 것은?'
setc(it, ['1 개 · 2 개', '3 개 · 6 개', '5 개 · 10 개', '7 개 · 14 개'], [
    ('1 개 · 2 개', '부껍질 기호를 s 로 보아 l = 0 으로 셈 — 1 개와 2 개는 s 의 값이다', 'proc'),
    ('5 개 · 10 개', '부껍질 기호를 d 로 보아 l = 2 로 셈 — 5 개와 10 개는 d 의 값이다', 'proc'),
    ('7 개 · 14 개', '부껍질 기호를 f 로 보아 l = 3 으로 셈 — 7 개와 14 개는 f 의 값이다', 'proc'),
])
it['answer_proof'] = 'p 는 l = 1 이므로 오비탈은 2l+1 = 3, 정원은 그 두 배인 6'
it['calc_check'] = 'p → l = 1 → 오비탈 2(1)+1 = 3 · 정원 2×3 = 6'
it['expected_solve_rate'] = 0.6
resol(it,
      '두 값을 함께 물었어. 한쪽만 맞혀서는 고를 수 없게 짜 두었지.',
      '앞자리 4 는 껍질을 말할 뿐이니 일단 옆으로 밀어 두자. 갈림길은 p 야. '
      '부껍질 기호는 s, p, d, f 차례로 부양자수 0, 1, 2, 3 에 해당하니 p 는 l 이 1 이지. '
      '오비탈의 개수는 자기양자수가 몇 가지인지와 같고, 그 값은 −l 부터 +l 까지라 2l+1 개야. '
      'l 이 1 이니 −1, 0, +1 세 개지. 정원은 오비탈 하나에 둘씩 들어가니 그 두 배인 여섯이야 '
      '— ②가 옳아. 네 선지가 1·2, 3·6, 5·10, 7·14 로 나란히 놓인 것을 눈여겨보자. '
      's, p, d, f 의 값을 차례로 적어 둔 것이라 기호를 l 로 옮기는 첫걸음에서 미끄러지면 '
      '그대로 옆 칸에 내려앉게 돼. 오비탈 수가 언제나 홀수라는 점, 정원은 그 두 배라 '
      '언제나 짝수라는 점, 그리고 그 두 배가 오비탈 하나에 전자가 둘까지 들어가는 데서 '
      '나온다는 점을 함께 붙들면 어느 부껍질을 물어도 바로 답할 수 있어. 하나 더, '
      '부껍질의 정원과 껍질 전체의 정원은 다른 값이야. N 껍질 전체는 2n² 이라 서른둘이지만, '
      '그것은 4s·4p·4d·4f 를 모두 합한 값이지 4p 하나의 정원이 아니야.',
      {'1 개 · 2 개': '하나와 둘은 s 부껍질의 값이야. s 는 mₗ 이 0 하나뿐이지. p 는 l 이 하나 더 커.',
       '5 개 · 10 개': '다섯과 열은 d 부껍질의 값이야. 한 칸을 건너뛰어 l 을 2 로 읽었지.',
       '7 개 · 14 개': '일곱과 열넷은 f 부껍질의 값이야. p 에서 두 칸이나 건너뛴 셈이지.'},
      '부껍질을 만나면 기호를 l 로 먼저 옮기고 2l+1 과 그 두 배를 나란히 적는다.')

# ── 3. M02023 ③ — 2 × 2 를 완성한다 · ④ 에 검산 한 겹 ──────────────────────
it = D['M02023']
assert it['choices'][2] == '[Kr] 4s²', it['choices']
it['choices'][2] = '[Ne] 3s²'
for d in it['distractors']:
    if d['opt'] == 2:
        d['error'] = '심지도 남는 부분도 한 주기씩 위로 밀어 적음 — 열둘뿐이라 마그네슘의 배치다'
sub(it, '③ [Kr] 4s²: 크립톤은 36 이라 칼슘보다 뒤에 있어. '
        '심지는 그 원소보다 앞에 오는 것이라야 해.',
        '③ [Ne] 3s²: 네온의 열에 둘을 더해도 열둘이야. 칼슘이 아니라 마그네슘의 배치지. '
        '심지도 남는 부분도 한 주기씩 위로 밀어 적은 꼴이야.')
sub(it, '④ [Ar] 3s²: 심지는 잘 골랐어. 그런데 3s 는 이미 아르곤 안에 들어 있어 '
        '뒤에 또 적으면 겹쳐 세는 셈이야. 남는 것은 4s² 지.',
        '④ [Ar] 3s²: 심지는 잘 골랐어. 그런데 3s 는 이미 아르곤 안에 들어 있어 '
        '뒤에 또 적으면 겹쳐 세는 셈이야. 남는 것은 4s² 지. 전자 수만 더하면 '
        '18 더하기 2 로 스물이 되어 검산을 통과해 버리니, 뒤에 적은 부껍질이 심지 안에 '
        '이미 들어 있지는 않은지 한 번 더 봐야 해.')
sub(it, '자가진단: 심지를 고르면 그 전자 수와 뒤에 적은 수를 더해 원자 번호와 맞춰 본다.',
        '자가진단: 심지의 전자 수와 뒤에 적은 수를 더해 원자 번호와 맞추고, '
        '뒤에 적은 부껍질이 심지 안에 이미 든 것은 아닌지 본다.')

# ── 4. M02024 — 주어를 중성 원자로 못 박는다 ──────────────────────────────
it = D['M02024']
assert it['stem'] == '어떤 원소의 바닥상태 전자배치가 3p⁴ 로 끝난다. 이 원소의 원자 번호는?'
it['stem'] = '어떤 중성 원자의 바닥상태 전자배치가 3p⁴ 로 끝난다. 이 원자의 원자 번호는?'
sub(it, '이 원소는 황이지. 네 선지가 모두 실재하는 원자 번호라는 점을 눈여겨보자.',
        '이 원소는 황이지. 발문이 "중성 원자" 라고 못 박은 것도 눈여겨보자. 이온까지 '
        '열어 두면 아르곤에서 전자 둘을 뺀 것도 3p⁴ 로 끝나 버려 답이 둘이 되거든. '
        '네 선지가 모두 실재하는 원자 번호라는 점도 눈여겨보자.')

# ── 5. M02029 ② — n 이 다른 짝을 l 만 다른 짝으로 ─────────────────────────
it = D['M02029']
old2 = '(2, 0, 0, +1/2) 와 (3, 0, 0, +1/2)'
new2 = '(3, 2, 0, +1/2) 와 (3, 1, 0, +1/2)'
assert it['choices'][1] == old2, it['choices']
assert len(new2) == len(old2)
it['choices'][1] = new2
for d in it['distractors']:
    if d['opt'] == 1:
        d['error'] = 'n·mₗ·mₛ 가 같다는 것만 보고 불가로 판정 — l 이 달라 3d 와 3p 로 부껍질부터 다르다'
it['calc_check'] = ('① mₗ 다름 → 다른 오비탈 / ② l 다름 → 3d 와 3p / '
                    '③ mₛ 다름 → 같은 오비탈의 짝 / ④ 네 값이 모두 같음 → 불가')
sub(it, '②는 n 이 2 와 3 으로 달라 껍질부터 다르고, ',
        '②는 네 자리 가운데 셋이 같지만 l 이 2 와 1 로 갈려 3d 와 3p 라는 다른 '
        '부껍질이고, ')
sub(it, f'② {old2}: 뒤 세 값이 같아 보여도 n 이 2 와 3 으로 달라. 2s 와 3s 라 껍질부터 다르지.',
        f'② {new2}: 네 자리 가운데 셋이 같아 가장 헷갈리는 짝이야. 그런데 l 이 2 와 1 로 '
        f'갈려 하나는 3d, 다른 하나는 3p 지. 부껍질이 다르면 오비탈도 다르니 함께 있을 수 있어.')

# ── 6. M02030 — 절단된 "둘째" 를 채우고 ③ 을 살린다 ───────────────────────
it = D['M02030']
assert it['choices'][2] == '리튬 이온(Li⁺)', it['choices']
it['choices'][2] = '리튬 원자(Li)'
for d in it['distractors']:
    if d['opt'] == 2:
        d['error'] = '최외각 전자가 하나인 것을 일전자계로 봄 — 리튬 원자는 전자가 모두 셋이다'
sub(it, '헬륨 원자는 둘, 리튬 이온은 셋에서 하나를 잃어 둘, 수소 음이온은 수소가 '
        '하나 더 받은 것이니 둘이지.',
        '헬륨 원자는 둘, 리튬 원자는 셋, 수소 음이온은 수소가 하나 더 받은 것이니 둘이지.')
sub(it, '여기서 두 가지를 붙들자. 첫째, 갈라짐을 정하는 것은 원소의 이름이 아니라 '
        '전자의 개수야.',
        '여기서 두 가지를 붙들자. 첫째, 갈라짐을 정하는 것은 원소의 이름이 아니라 '
        '전자의 개수야. 둘째, 최외각 전자가 하나인 것과 전자가 모두 해서 하나인 것은 '
        '전혀 다른 말이야. 리튬 원자는 최외각에 전자가 하나뿐이지만 안쪽 1s 에 둘이 더 '
        '있어 일전자계가 아니지.')
sub(it, '③ 리튬 이온(Li⁺): 리튬 이온은 전자를 하나 잃어 둘이 남아. '
        '이온이라고 해서 전자가 하나가 되는 것은 아니야.',
        '③ 리튬 원자(Li): 최외각에 든 전자가 2s¹ 하나인 것은 맞아. 그렇지만 안쪽 1s² 의 '
        '둘까지 세면 모두 셋이야. 갈라짐은 전자를 하나도 빼지 않고 세어 따져야 해.')

# ── 검사 ──────────────────────────────────────────────────────────────────
items = [D[k] for k in IDS]
assert Counter(i['answer'] for i in items) == {0: 3, 1: 3, 2: 2, 3: 2}
for k in IDS:
    assert D[k]['answer'] == before[k]['answer'], k
for k in set(IDS) - TOUCHED:
    assert D[k] == before[k], k

for it in items:
    cs = it['choices']
    assert len(cs) == 4 and len(set(cs)) == 4, it['id']
    assert sorted(d['opt'] for d in it['distractors']) == sorted(set(range(4)) - {it['answer']}), it['id']
    assert len(it['solution']) >= 300, (it['id'], len(it['solution']))
    assert '★' not in it['stem'] and '★' not in it['solution'], it['id']
    assert it['objective'], it['id']
    L = sorted(len(c) for c in cs)
    a = len(cs[it['answer']])
    assert not (a == L[3] and L[2] < L[3]), (it['id'], 'G3 최장 단독', cs)
    assert not (a == L[0] and L[0] < L[1]), (it['id'], 'G3 최단 단독', cs)
    mid = (L[1] + L[2]) / 2
    if mid >= 8:
        assert (L[3] - L[0]) / mid <= 0.25, (it['id'], 'G3b', cs, L)
    assert '가 옳아' in it['solution'] or '이 옳아' in it['solution'], it['id']
    for k in range(4):
        assert MK[k] + ' ' + cs[k] in it['solution'], (it['id'], MK[k], cs[k])

# ── 문항 전용 검산 ────────────────────────────────────────────────────────
# ㉠ M02022 — 완전 사다리인지 (오비탈 홀수 · 정원 = 두 배 · 앞자리 오름차순)
pair = [tuple(int(x.split()[0]) for x in c.split(' · ')) for c in D['M02022']['choices']]
assert pair == [(1, 2), (3, 6), (5, 10), (7, 14)], pair
assert all(o % 2 == 1 for o, e in pair), '오비탈 수가 홀수가 아닌 선지가 있다'
assert all(e == 2 * o for o, e in pair), '정원 = 2 × 오비탈 을 어긴 선지가 있다 (이상치)'
assert [o for o, e in pair] == sorted(o for o, e in pair), 'G6 앞자리 오름차순'
assert pair[D['M02022']['answer']] == (3, 6) and '4p' in D['M02022']['stem']
# 사다리의 네 칸이 s·p·d·f 의 실제 값과 하나씩 맞는지
assert pair == [(2 * l + 1, 2 * (2 * l + 1)) for l in range(4)], '실재하지 않는 값이 있다'

# ㉡ M02023 — 심지 축 · 뒤 축이 각각 2 : 2 인지 (최빈값 교차 차단)
core = [c.split(']')[0] + ']' for c in D['M02023']['choices']]
tail = [c.split('] ')[1] for c in D['M02023']['choices']]
assert Counter(core) == {'[Ar]': 2, '[Ne]': 2}, core
assert Counter(tail) == {'4s²': 2, '3s²': 2}, tail
# 전자 수 검산을 통과하는 선지는 정답과 ④ 둘뿐 — 해설이 그 사실을 말하는지
Z = {'[He]': 2, '[Ne]': 10, '[Ar]': 18, '[Kr]': 36}
tot = [Z[core[i]] + 2 for i in range(4)]
assert [i for i, t in enumerate(tot) if t == 20] == [0, 3], tot
assert '검산을 통과해 버리니' in D['M02023']['solution']

# ㉢ M02029 — 네 선지가 '무엇 하나만 다른가' 로 가지런한지
def qs(c):
    return [tuple(p.strip(' ()').split(', ')) for p in c.split(' 와 ')]
diff = []
for c in D['M02029']['choices']:
    x, y = qs(c)
    diff.append([i for i in range(4) if x[i] != y[i]])
assert diff == [[2], [1], [3], []], diff        # mₗ만 / l만 / mₛ만 / 하나도 안 다름
assert D['M02029']['answer'] == 3 and diff[3] == []

# ㉣ M02030 — 원소 축 · 전하 축의 최빈값 교차가 정답을 가리키지 않는지
ne = {'헬륨 이온(He⁺)': ('He', 1), '헬륨 원자(He)': ('He', 2),
      '리튬 원자(Li)': ('Li', 3), '수소 음이온(H⁻)': ('H', 2)}
el = [ne[c][0] for c in D['M02030']['choices']]
ch = ['+' if '⁺' in c else ('−' if '⁻' in c else '0') for c in D['M02030']['choices']]
top_el = Counter(el).most_common(1)[0][0]
top_ch = Counter(ch).most_common(1)[0][0]
cross = [i for i in range(4) if el[i] == top_el and ch[i] == top_ch]
assert cross != [D['M02030']['answer']], '축별 최빈값 교차가 정답을 가리킨다'
# 전자 하나뿐인 화학종은 정답 하나뿐
one = [i for i, c in enumerate(D['M02030']['choices']) if ne[c][1] == 1]
assert one == [D['M02030']['answer']], one
assert '둘째,' in D['M02030']['solution']

# ㉤ M02024 — 주어가 닫혔는지
assert '중성 원자' in D['M02024']['stem'] and '이온까지' in D['M02024']['solution']

# ㉥ M02026 — 교과서 관용으로 참이 되는 낱말이 선지에서 사라졌는지
assert not any('자전하는 방향' in c for c in D['M02026']['choices'])
assert '자전하는 속도' in D['M02026']['choices'][3]

json.dump(bank, open(BANK, 'w', encoding='utf-8'), ensure_ascii=False, indent=1)
print('T13 P6 3차 조치 완료 — 6 문항')
print('  M02022 사다리', pair, '| 정답', D['M02022']['choices'][1])
print('  M02023 심지', Counter(core), '· 뒤', Counter(tail), '| 검산 통과 선지', [i + 1 for i, t in enumerate(tot) if t == 20])
print('  M02029 갈리는 자리', diff)
print('  M02030 원소', el, '· 전하', ch, '| 교차', cross, '· 정답', D['M02030']['answer'])
