"""T12 P14 3차 조치 — 오답 교체 4 · 해설 손질 6

■ ★반박이 정답 문단을 무력화하던 자리 — M01942 ④ (factchecker 우선순위 1)★
  ④ 는 '410 nm 이고, ★발머 계열이므로★ 가시광선 안' 인데 반박이 '판정은 맞았어' 로 열었다.
  그런데 본문은 바로 그 추론을 ★"'발머 = 가시광선'이라는 외움은 표에 실린 네 선에서만
  통하는 말"★ 이라며 무효로 선언한다. 결론값이 우연히 참인 것을 통째로 승인하면
  본문 경고가 무뎌진다. ★값과 근거를 갈라★ 적는다.

■ ★자가진단이 본문이 금지한 걸음을 저지르던 자리 — M01940 (factchecker 우선순위 2)★
  2차에 본문에 '통과 하나만으로는 퍼진 전하 모형으로도 설명이 되니 아직 결론이 아니지' 를
  넣었는데(M01939 와의 정합을 위해), ★자가진단은 '통과 = 빈 공간' 그대로였다.★
  세 자리 가운데 한 자리만 다른 말을 하고 있었다 — 2차 조치가 만든 어긋남이다.

■ ★변별력 없는 반박 — M01943 ③ (factchecker)★
  ③ 은 'n → m 칸과 m → n 칸(전치 쌍)' 을 주장하는데 반박이 '더하는 두 칸은 서로 다른 행에
  있지' 로 받았다. ★전치 쌍도 서로 다른 행에 있다.★ 실제 반박점은 더하는 두 칸이
  (1, m)과 (∞, m), 곧 ★같은 열의 두 행★ 이라는 것이다.

■ 오답 교체 넷
  ▸ M01941 ① → 'He⁺ 에는 n² 꼴의 준위 구조 자체가 없다' (defender)
    지금 문면('이 가정 때문에 … 쓸 수 없다')은 ★가정이 문자 그대로 '수소 원자'라 못 박았으니
    He⁺ 는 대상 밖★ 이라고 우길 여지를 남긴다(defender 3차 '중' — 이 배치에서 유일하게 실제
    이의제기가 들어올 수 있는 자리). 물리 주장으로 바꾸면 He⁺ 가 Z² 배로 n² 구조를 그대로
    갖는다는 사실이 곧바로 반증이 된다. ★'수소 전용' 반쪽 이해를 겨누는 구실은 그대로다★
    (sim 표에서 C 가 이 자리를 골랐다 — 이 배치의 두 변별 문항 가운데 하나다).
  ▸ M01941 ④ → '준위 값 안에는 전자와 핵 사이의 끌힘이 들어 있지 않다' (solver·sim 함께)
    지금 문면('보어가 몰라서 좁혔다')은 ★동기 추정형★ 이라 내용을 몰라도 지워진다.
    새 문면은 준위 값의 음(−)부호가 곧 쿨롱 인력이라는 점에서 확실히 거짓이고,
    ★(적용 범위 ①③) × (상호작용 ②④) 2 × 2 격자★ 를 새로 세운다 — ② 는 반발이 없다(참),
    ④ 는 끌힘이 없다(거짓). 반발과 끌힘을 뒤섞는 C 층을 겨눈다.
  ▸ M01937 ④ → '두 원소가 서로 만들 수 있는 화합물은 한 가지뿐이다' (sim 2·3차 연속)
    '언제나 1 : 1 의 개수비로만' 은 문면만 보고도 터무니없어 두 회차 내리 죽었다.
    새 문면은 ★CO 와 CO₂ 로 곧바로 반증★ 되고 그 반증이 배수 비례 단원 안에서 끝난다.
    돌턴의 최대 단순성 가정에 실제로 딸려 있던 생각이라 발문에도 들어맞는다.
  ▸ M01940 ① → '(+)전하가 알파 입자보다 가벼워 막지 못한 것이다' (sim 2·3차 연속)
    2차의 '무거운 것이 없다' 는 되튐과 대놓고 부딪혀 여전히 죽었다. 새 문면은
    ★통과 한쪽만 쓰는 오류 유형은 그대로 살리되 자기모순을 없앤다.★

■ 해설 손질
  M01937 ① 반박의 폐기 근거를 본문('전자와 원자핵')과 맞춤 · ④ 반박 주술 정리
  M01941 '가르는 것은 원소 이름이 아니라 전자의 개수야' 가 본문과 반박에 토씨까지 같게
       두 번 나오던 것 정리
  M01942 ③ 반박의 '-고' 뒤 줄표로 문장이 끊기던 것 · ★91.1 nm 가 hc 를 담은 기준값★ 이라는
       한 마디 추가 — M01944 가 'h·c 는 파장으로 바꿀 때 필요' 라 하는데 M01942 는 h·c 없이
       파장을 구해, 두 문항이 학습자에게 상충으로 읽힌다(factchecker 교차 점검)

■ 반려
  ▸ sim — M01939 ③ 교체: 2차에는 '죽은 선지 없음'이었다. 두 회차 연속 미충족.
  ▸ sim — M01943 ④ 교체('한가운데에 있기 때문'): ★2차에 방금 바꾼 자리★ 이고, 새 안도
    발문의 3/4 · 1/4 로 곧바로 반증되어 지금 것과 사정이 같다. defender 3차는 지금 ④ 를
    '중하'(우길 여지가 있는 쪽)로 보았다 — 죽지 않았다.
  ▸ sim — M01944 ② 를 '빛의 속도'로: ★2차에 solver 의 'h 와 c 는 늘 짝이라 동반 소거된다'는
    지적을 받아 뺀 것★ 을 되돌리는 제안이다. sim 자신도 그 요령 위험을 함께 적었다.
  ▸ solver — M01945 ②③ 거울쌍 소거: ★개념 대장 각도 C12-N01[1] 자체가 '위치만'과 '운동량만'
    둘을 나란히 놓고 각각 왜 오답인지를 묻는다.★ 한쪽을 빼면 각도가 사라진다. 거울쌍으로
    4 → 2 가 되어도 남는 둘(①④)을 가르려면 '한쪽씩은 잴 수 있다'는 실제 지식이 필요하다.
    ▸ solver 가 낸 대안 소재(불확정성을 장치 한계로 오해하는 오개념)는 좋다 —
      C12-N01[5](확률 경계면)와 함께 P15·P16 에서 쓴다.
  ▸ solver — M01937 의 의미상 절대성: 표면 극단어는 2차에 ② 를 고쳐 2 : 2 로 맞췄다.
    돌턴의 명제는 본디 모두 전칭 진술이라 의미상 절대성은 이 각도에서 없앨 수 없다.
  ▸ sim — M01942 ③ · M01945 ②③ · M01946 ①④ · M01938 ②: sim 스스로 격자 보존·복수정답
    위험을 들어 유지 권고했다. 같은 판단이다.

■ ★C = D 8/10 (sim 2·3차 같음) — 배치 수준 부채로 기록★
  변별이 M01941 과 M01942 둘에 몰려 있다. 이 배치는 쉬움 2 · 중간 7 · 어려움 1 로 짰으니
  확인 문항이 많은 것은 설계값이지만, ★T12 마감 4제와 P16 은 상위권 변별 쪽으로 기울일 것.★
"""
import json, os

BANK = os.path.join(os.path.dirname(os.path.abspath(__file__)), 'master_bank.json')
MK = '①②③④'


def parts(it):
    lines = it['solution'].split('\n')
    lead = lines[0]
    diag = [L for L in lines if L.startswith('자가진단:')][0]
    a = it['answer']
    head = f'[정답] {MK[a]} {it["choices"][a]} — '
    hit = [L for L in lines if L.startswith('[정답] ')]
    assert len(hit) == 1 and hit[0].startswith(head), it['id']
    cor = hit[0][len(head):]
    reb = {}
    for k in range(4):
        if k == a:
            continue
        pfx = f'{MK[k]} {it["choices"][k]}: '
        h = [L for L in lines if L.startswith(pfx)]
        assert len(h) == 1, (it['id'], k)
        reb[k] = h[0][len(pfx):]
    return lead, cor, reb, diag


def emit(it, lead, cor, reb, diag):
    a = it['answer']
    body = [lead, '', f'[정답] {MK[a]} {it["choices"][a]} — {cor}', '']
    body += [f'{MK[k]} {it["choices"][k]}: {reb[k]}' for k in range(4) if k != a]
    body += ['', diag]
    it['solution'] = '\n'.join(body)


def swap_choice(it, idx, new_txt, new_reb, err=None, typ=None):
    assert idx != it['answer'], it['id']
    lead, cor, reb, diag = parts(it)
    it['choices'][idx] = new_txt
    reb[idx] = new_reb
    emit(it, lead, cor, reb, diag)
    for dd in it['distractors']:
        if dd['opt'] == idx:
            if err:
                dd['error'] = err
            if typ:
                dd['type'] = typ
            break
    else:
        raise AssertionError(f"{it['id']}: distractor {idx} 없음")


def sub(it, old, new, n=1):
    assert it['solution'].count(old) == n, f"{it['id']}: '{old[:30]}' {it['solution'].count(old)}회"
    it['solution'] = it['solution'].replace(old, new)


def main():
    bank = json.load(open(BANK, encoding='utf-8'))
    d = {x['id']: x for x in bank}

    # ══ M01937 ══════════════════════════════════════════════════════════
    it = d['M01937']
    swap_choice(it, 3, '두 원소가 서로 만들 수 있는 화합물은 한 가지뿐이다',
                '돌턴은 가장 단순한 짝을 먼저 가정했지만 그 생각은 폐기됐어. 탄소와 산소만 해도 '
                'CO 와 CO₂ 두 가지를 만들고, 질소와 산소는 더 여러 가지를 만들지.',
                '한 쌍의 원소가 한 화합물만 만든다는 최대 단순성 가정을 유효한 것으로 봄', 'overgen')
    sub(it, '전자·양성자·중성자가 나오면서 무너진 문장이야. 원자는 더 작은 것들로 되어 있지.',
        '전자와 원자핵이 나오면서 무너진 문장이야. 원자는 더 작은 것들로 되어 있지.')

    # ══ M01940 ══════════════════════════════════════════════════════════
    it = d['M01940']
    swap_choice(it, 0, '원자 속 (+)전하가 알파 입자보다 가벼워 막지 못한 것이다',
                '가벼운 것에 부딪히면 밀려나는 쪽은 그쪽이지 알파 입자가 아니야. 8000 개에 '
                '1 개꼴로 되튀었다는 것이 바로 알파 입자보다 무거운 것이 있다는 증거지.',
                '통과 쪽만 읽고 표적이 가볍다고 봄 — 되튐이 그 반대를 말한다', 'proc')
    sub(it, '자가진단: 통과 = 빈 공간 · 드문 되튐 = 작고 무거운 핵. 확률 ≠ 부피비.',
        '자가진단: 통과만으로는 아직 미결 · 되튐까지 겹쳐야 작고 무거운 핵. 확률 ≠ 부피비.')

    # ══ M01941 ══════════════════════════════════════════════════════════
    it = d['M01941']
    swap_choice(it, 0, '헬륨 이온 He⁺ 에는 n² 꼴의 준위 구조 자체가 없다',
                'He⁺ 도 전자가 하나뿐인 수소꼴 이온이야. 준위는 n² 꼴 그대로이고 핵전하가 두 '
                '배라 네 배 깊어질 뿐이지.',
                '한정을 원소 이름으로 읽어 전자 하나짜리 이온까지 배제함', 'overgen')
    swap_choice(it, 3, '이 모형의 준위 값 안에는 전자와 핵 사이의 끌힘이 들어 있지 않다',
                '준위 값이 음수인 것 자체가 전자가 핵에 붙들려 있다는 뜻이야. 끌힘이 없으면 '
                '속박도 없고 −1312/n² 이라는 값도 나오지 않지. 빠진 것은 끌힘이 아니라 '
                '전자끼리의 밀힘이야.',
                '빠진 상호작용이 전자끼리의 반발인데 전자와 핵의 인력으로 바꿔 읽음', 'sign')
    sub(it, '가르는 것은 원소 이름이 아니라 전자의 '
            '개수야. 보어 모형의 한계가 다전자 원자에 있다는 말이 바로 이 자리에서 나와.',
        '가르는 것은 원소 이름이 아니라 전자의 개수지. 보어 모형의 한계가 다전자 원자에 '
        '있다는 말이 바로 이 자리에서 나와.')

    # ══ M01942 ══════════════════════════════════════════════════════════
    it = d['M01942']
    lead, cor, reb, diag = parts(it)
    reb[2] = ('410 nm 는 발문의 표에 이미 실려 있는 6 → 2 짜리야. 출발을 7 이 아니라 6 으로 '
              '센 값이지. 게다가 410 은 400 보다 기니 가시광선 안이라, 값도 판정도 어긋났어.')
    reb[3] = ('결론값만 놓고 보면 410 nm 는 가시광선 안이 맞아. 그런데 근거가 틀렸어 — '
              '가시광선 안인 까닭은 400 보다 길어서지 발머 계열이라서가 아니야. 게다가 그 값은 '
              '표에 실린 6 → 2 선이고, 7 → 2 는 그보다 짧은 397 nm 지.')
    emit(it, lead, cor, reb, diag)
    sub(it, '에너지와 파장은 반비례하고 k 에 해당하는 파장이 91.1 nm 이니까',
        '에너지와 파장은 반비례하고 k 에 해당하는 파장이 91.1 nm 이니까 — 이 91.1 nm 이 '
        '플랑크 상수와 빛의 속력을 이미 품은 기준값이라 따로 상수를 끌어올 일이 없어 —')

    # ══ M01943 ══════════════════════════════════════════════════════════
    sub(d['M01943'], '게다가 여기서 더하는 두 칸은 서로 다른 행에 있지.',
        '게다가 여기서 더하는 두 칸은 (1, m)과 (∞, m), 곧 같은 열의 두 행이지 서로 뒤집은 '
        '짝이 아니야.')

    for x in bank:
        assert '**' not in x['stem'] and '**' not in x['solution'], x['id']
        assert '★' not in x['stem'] and '★' not in x['solution'], x['id']

    json.dump(bank, open(BANK, 'w', encoding='utf-8'), ensure_ascii=False, indent=1)
    print('P14 3차 조치 완료 — 오답 교체 4 · 해설 손질 6')
    seq, ranks = [], []
    for i in range(1937, 1947):
        x = d[f'M0{i}']
        L = [len(c) for c in x['choices']]
        s = sorted(L)
        sp = (s[3] - s[0]) / ((s[1] + s[2]) / 2)
        rk = sorted(range(4), key=lambda j: -L[j]).index(x['answer']) + 1
        seq.append(x['answer'] + 1)
        ranks.append(rk)
        flag = '' if (sp <= 0.25 or s[1] < 8) else '  ← G3b'
        if L[x['answer']] == s[3] and L.count(s[3]) == 1:
            flag += '  ← G3 최장'
        if L[x['answer']] == s[0] and L.count(s[0]) == 1:
            flag += '  ← G3 최단'
        print(f'  {x["id"]} 정답{MK[x["answer"]]} {L} 산포{sp:.2f} 순위{rk}{flag}')
    from collections import Counter
    print('  정답 차례', '-'.join(map(str, seq)), '| 길이순위',
          dict(sorted(Counter(ranks).items())))


if __name__ == '__main__':
    main()
