# -*- coding: utf-8 -*-
"""T12 마감 14제(M01957~M01970) 층5 순회 — 15차 조치.

14차: **defender 0 건**(정답 붕괴 0 · 오답 승격 0). solver 는 문항 내부
결함 0 건이지만 형태 단서 셋을 새로 짚었고, 그 셋은 모두 '내용을 몰라도
좁혀진다' 는 (ㄴ) 위반이라 실체가 있다.

────────────────────────────────────────────────────────────────
㉠ M01961 — 포함 관계가 자리를 옮겨 되살아났다
────────────────────────────────────────────────────────────────
13차에 ④('눈에 보이는 네 선이 전부')를 ③ 과의 포함 관계 때문에 갈아
끼웠는데, solver 14차가 **남은 포함 관계**를 찾았다.

    ③ = "선의 개수가 유한해서 파장이 가장 짧은 선이 따로 있다"
    ② = "짧은 파장 쪽으로 갈수록 간격이 넓어진다"   ⟹ ③
    ④ = "간격이 어디서나 거의 같다"                 ⟹ ③

발머 선이 놓이는 구간은 364.4 ~ 656 nm 로 **유계**다. 유계 구간에서 간격이
넓어지거나 고르면 선은 유한할 수밖에 없다. 곧 ② 나 ④ 가 참이면 ③ 도 참이
되므로, 단일정답 규칙만으로 ②·④ 가 함께 지워진다.

★그리고 이것은 자리를 옮겨서 될 일이 아니었다★ — 유계 구간에서 선이
무한하려면 간격이 줄어드는 수밖에 없으므로, **'③ = 유한하다' 를 두는 한
어떤 틀린 간격 서술도 ③ 을 함의한다.** 13차는 ④ 를 바꿨지만 고쳐야 할
것은 ③ 이었다.

→ ③ 을 개수 진술에서 떼어 낸다. '모여드는 자리에 놓인 선이 파장이 가장
짧은 선이다' — 계열 한계를 실제 방출선으로 보는 바로 그 혼동만 남기고,
유한/무한 주장을 빼서 ②·④ 와의 함의를 끊는다. ② 나 ④ 가 참이라면 애초에
'모여드는 자리' 가 없으므로 함의가 성립하지 않는다.

★교훈★ 포함 관계를 끊을 때는 **함의를 받는 쪽(결론)을 고쳐야 한다.**
함의를 보내는 쪽을 하나씩 갈아 봐야 다른 것이 그 자리를 메운다.

────────────────────────────────────────────────────────────────
㉡ M01967 — 계산하지 않고 '가시광선 밖 유일 선지' 로 풀린다
────────────────────────────────────────────────────────────────
선지가 91.1 · 273.3 · 364.4 · 819.9 nm 였고 **700 nm 를 넘는 것은 정답
하나뿐**이었다. 게다가 그 400~700 nm 경계는 같은 배치의 M01962·M01970 이
발문으로 세워 준다. "파셴은 적외선" 만 외운 학생이 나머지 셋을 자외선·
가시광선으로 지우고 정답에 닿는다(solver 14차). 오답 셋이 모두
91.1 × {1, 3, 4} 이고 정답만 ×9 라는 패턴 노출도 겹쳤다.

→ ①(91.1, '상수를 그대로') 을 **파셴 계열에서 파장이 가장 긴 선**
1874.1 nm 로 바꾼다.

    4 → 3 : 1/λ = (1/91.1)(1/9 − 1/16) = (1/91.1)(7/144)
            λ = 91.1 × 144/7 = 1874.1 nm

이제 적외선이 둘이라 대역 판별만으로는 갈리지 않는다. 그리고 새 오답은
**계열 한계(극한)와 계열의 실제 첫 선을 혼동하는** 자리라, M01961 ③ 이
묻는 것과 같은 구분을 다른 각도로 요구한다. 오름차순(G6)에 따라 정답 자리가
④ → ③ 으로 옮겨 가고, 은행 정답 분포는 ①493 ②492 ③493 ④492 로 편차 1 을
지킨다. 배치의 정답 차례에도 등차 줄이 생기지 않는다.

────────────────────────────────────────────────────────────────
㉢ M01957 — 정답만 낱말이 다르다(이질항)
────────────────────────────────────────────────────────────────
①②③ 이 모두 '질량' 을 말하고 ④ 만 '(+)전하' 를 말했다. 무엇을 묻는지
몰라도 **혼자 다른 낱말을 쓴 선지**를 고르면 맞는다(solver 14차).

→ ① 의 결론을 '원자 질량이 한곳에 몰려 있다' 에서 '(+)전하가 고르게 퍼져
있다' 로 바꾼다. 전하 둘 · 질량 둘로 균형이 맞고, 새 문면은 **톰슨 모형의
추론 그대로**라 오답으로서 더 매력적이다. 덤으로 defender 가 세 회차 동안
'가장 약한 오답' 으로 표시해 온 자리(통과 → 질량 집중이 2 단으로 이으면
참에 가까워진다)도 함께 닫힌다 — 통과가 전하 분포를 말해 주지 않는 것은
어느 독법으로도 확실하다.

────────────────────────────────────────────────────────────────
받아들이지 않은 것
────────────────────────────────────────────────────────────────
· **M01968 → M01963 우회(solver 14차 1순위)** — M01968 의 '1000 nm ↔
  120 kJ/mol' 을 가져오면 120 × 1000/410 = 292.7 로 M01963 이 한 줄에
  풀린다. 사실이다. 그러나 (가) 어떤 (λ, E) 짝을 써도 같은 hc 가 나오므로
  기준값을 바꿔서는 막을 수 없고, (나) 파장 목록을 지우면 8차에 되살린
  ②(뒤섞음 함정)가 죽는다 — sim 이 '배치 최고의 문항' 이라 한 자리다.
  (다) 그 경로는 E ∝ 1/λ 라는 **옳은 물리**이지 정답의 누출이 아니다.
  ★마감 부채로 기록한다.★
· **M01958 ④ 교체(solver 네 번째 제안)** — 네 회차 연속 반려. ④ 가
  '러더퍼드에게는 참이지만 보어의 가정은 아닌' 것이라서 이 문항이 2 단
  판단이 된다.
· **M01965 선지 구조 조정 · M01962/M01969 틀 중복** — 이미 마감 부채로
  기록되어 있다. 배치의 절반을 다시 짜는 일이라 T13 설계 규약으로 넘긴다.
"""
import json

BANK = 'master/master_bank.json'
MK = ['①', '②', '③', '④']

bank = json.load(open(BANK, encoding='utf-8'))
by = {x['id']: x for x in bank}


def parts(it):
    lines = it['solution'].split('\n')
    lead = lines[0]
    diag = [L for L in lines if L.startswith('자가진단:')][0]
    a = it['answer']
    head = f'[정답] {MK[a]} {it["choices"][a]} — '
    hit = [L for L in lines if L.startswith('[정답] ')]
    assert len(hit) == 1 and hit[0].startswith(head), it['id']
    cor = hit[0][len(head):]
    reb = {}
    for k in range(4):
        if k == a:
            continue
        pfx = f'{MK[k]} {it["choices"][k]}: '
        h = [L for L in lines if L.startswith(pfx)]
        assert len(h) == 1, (it['id'], k)
        reb[k] = h[0][len(pfx):]
    return lead, cor, reb, diag


def emit(it, lead, cor, reb, diag):
    a = it['answer']
    body = [lead, '', f'[정답] {MK[a]} {it["choices"][a]} — {cor}', '']
    body += [f'{MK[k]} {it["choices"][k]}: {reb[k]}' for k in range(4) if k != a]
    body += ['', diag]
    it['solution'] = '\n'.join(body)


def sub(it, old, new, n=1):
    assert it['solution'].count(old) == n, f"{it['id']}: '{old[:40]}' {it['solution'].count(old)}회"
    it['solution'] = it['solution'].replace(old, new)


def field(it, key, old, new):
    assert it[key].count(old) == 1, (it['id'], key)
    it[key] = it[key].replace(old, new)


def dmeta(it, idx, error=None, typ=None):
    for dd in it['distractors']:
        if dd['opt'] == idx:
            if error:
                dd['error'] = error
            if typ:
                dd['type'] = typ
            return
    raise AssertionError(f"{it['id']}: distractor {idx} 없음")


def retext(it, idx, new_txt, new_reb=None):
    assert idx != it['answer'], it['id']
    lead, cor, reb, diag = parts(it)
    it['choices'][idx] = new_txt
    if new_reb is not None:
        reb[idx] = new_reb
    emit(it, lead, cor, reb, diag)


def audit(it):
    lead, cor, reb, diag = parts(it)
    a = it['answer']
    for k in range(4):
        if k == a:
            continue
        for tail in ('가 옳아', '이 옳아', '가 맞아', '이 맞아'):
            assert MK[k] + tail not in cor, f"{it['id']}: 정답 본문에 {MK[k]}{tail}"
    assert (MK[a] + '가 옳아' in cor) or (MK[a] + '이 옳아' in cor), f"{it['id']}: 정답 번호 미표시"


def g3(it):
    L, a = [len(c) for c in it['choices']], it['answer']
    assert not (L[a] == max(L) and L.count(max(L)) == 1), f"{it['id']}: G3 유일최장"
    assert not (L[a] == min(L) and L.count(min(L)) == 1), f"{it['id']}: G3 유일최단"
    s = sorted(L)
    if (s[1] + s[2]) / 2 >= 8:
        sp = (s[3] - s[0]) / ((s[1] + s[2]) / 2)
        assert sp <= 0.25, f"{it['id']}: G3b 산포 {sp:.2f}"
    return L, sorted(range(4), key=lambda i: -L[i]).index(a) + 1


# ── ㉠ M01961 ③ : 개수 진술을 떼어 함의를 끊는다 ──────────────────────
it = by['M01961']
retext(it, 2, '모여드는 자리에 놓인 선이 파장이 가장 짧은 선이다',
       '모여드는 자리를 선 하나로 본 거야. 거기는 선들이 한없이 다가가는 값일 뿐 '
       '그 자리에 선이 놓여 있지는 않아. 출발 준위를 계속 올릴 수 있으니 선은 끝없이 '
       '이어지고, 그래서 가장 짧은 선이라는 것도 없지.')
dmeta(it, 2, error='선들이 모여드는 극한을 실제로 나오는 한 개의 선으로 봄', typ='conserv')
sub(it, '둘째, 계열 한계는 선들이 다가가는 자리이지 거기에 선이 하나 놓여 있는 것이 '
        '아니야. 그러니 "가장 짧은 선" 이라는 것은 없어.',
    '둘째, 계열 한계는 선들이 한없이 다가가는 값이지 거기에 선이 하나 놓여 있는 것이 '
    '아니야. 다가가되 닿지는 않으니 "가장 짧은 선" 이라는 것도 없지.')
field(it, 'answer_proof', '계열 한계는 선들이 다가가는 극한이지 실제로 나오는 한 개의 선이 아니다',
      '계열 한계는 선들이 한없이 다가가되 닿지 않는 극한이지 실제로 나오는 한 개의 선이 아니다')

# ── ㉡ M01967 : ① 을 파셴 최장선으로 — 대역 판별 우회로를 끊는다 ─────
it = by['M01967']
assert it['answer'] == 3, it['id']
lead, cor, reb, diag = parts(it)
C67 = ['약 273.3 nm', '약 364.4 nm', '약 819.9 nm', '약 1874.1 nm']
it['choices'] = list(C67)
it['answer'] = 2
it['distractors'] = [
    {'opt': 0, 'error': '괄호를 1/3 로 보아 제곱을 빠뜨리고 91.1 × 3 을 계산함', 'type': 'proc'},
    {'opt': 1, 'error': '도착 준위를 2 로 잡아 발머 계열의 한계 91.1 × 4 를 구함', 'type': 'proc'},
    {'opt': 3, 'error': '모여드는 끝자리 대신 파셴 계열에서 파장이 가장 긴 선(4 → 3)을 구함', 'type': 'conserv'},
]
cor = ('먼저 계열이 모여드는 끝자리가 어떤 전이인지부터 잡자. 도착이 3 으로 고정된 파셴 '
       '계열에서 출발을 한없이 올리면 1/n² 이 0 으로 가니 괄호는 1/9 에서 멈춰. 그 자리가 '
       '선들이 모여드는 끝, 곧 계열 한계야. 이제 식에 넣자. 1/λ 이 1/91.1 곱하기 괄호 값이니, '
       'λ 는 91.1 을 괄호 값으로 나눈 것이야. 나누는 것이 1/9 이니 91.1 에 9 를 곱하는 셈이지. '
       '91.1 × 9 = 819.9 — ③이 옳아. 규칙으로 적어 두면 계열 한계 파장은 91.1 × m² 야. '
       '라이먼은 m = 1 이라 91.1 nm 이고, 발머는 m = 2 라 91.1 × 4 = 364.4 nm 지. '
       '여기서 걸려 넘어지기 쉬운 곳이 셋이야. 하나는 m 을 제곱하지 않고 그냥 곱하는 것, '
       '둘은 도착 준위를 발머의 2 로 잘못 잡는 것, 셋은 모여드는 끝자리 대신 그 계열에서 '
       '파장이 가장 긴 선을 구하는 것이지. 끝자리와 첫 선은 계열의 양 끝이라 값이 아주 멀어.')
reb = {
    0: ('91.1 에 3 을 곱한 값이야. 도착 준위가 3 인 것은 맞게 봤는데 괄호가 1/3 이 아니라 '
        '1/3² 곧 1/9 이라는 것을 놓쳤어. 식의 괄호 안은 준위 번호의 제곱이거든.'),
    1: ('91.1 에 4 를 곱한 값이니 이것은 발머 계열(도착 2)이 모여드는 끝자리야. '
        '묻는 것은 파셴 계열이라 도착이 3 이지.'),
    3: ('파셴 계열에서 파장이 가장 긴 선, 곧 4 → 3 의 값이야. 괄호에 1/9 − 1/16 = 7/144 를 '
        '넣으면 91.1 × 144/7 = 1874.1 이 나오지. 그건 계열의 첫 선이고, 묻는 것은 선들이 '
        '모여드는 반대쪽 끝이야.'),
}
diag = '자가진단: 계열 한계 = 91.1 × m². 파셴은 m = 3 이라 91.1 × 9 = 819.9 nm.'
emit(it, lead, cor, reb, diag)
field(it, 'answer_proof',
      '파셴은 m = 3 이므로 91.1 × 9 = 819.9 nm 다. 라이먼은 91.1 nm, 발머는 91.1 × 4 = 364.4 nm 이며, '
      '제곱을 빠뜨려 91.1 × 3 을 하면 273.3 nm 가 나온다',
      '파셴은 m = 3 이므로 91.1 × 9 = 819.9 nm 다. 발머 한계는 91.1 × 4 = 364.4 nm 이고, '
      '제곱을 빠뜨려 91.1 × 3 을 하면 273.3 nm 가 나온다. 파셴 계열에서 파장이 가장 긴 선은 '
      '4 → 3 이라 괄호가 1/9 − 1/16 = 7/144 이고 91.1 × 144/7 = 1874.1 nm 로, 계열 한계와는 반대쪽 끝이다')
field(it, 'device', '상수와 도착 준위의 제곱', '계열의 양 끝 — 첫 선과 한계')

# ── ㉡-2 M01961 ④ : 죽은 선지를 '한계 위치를 반대로' 로 교체 ────────
#   sim 14차 — '간격이 눈금자처럼 고르다'(13차 신설)는 그 심상을 가진 응시자가
#   없어 완전히 죽었다. 오개념형은 ②(넓어짐)로, 암기형은 ③으로 가기 때문이다.
#   대신 계열이 긴 파장 쪽으로도 이어진다고 보는 자리를 만든다 — 656 nm(3 → 2)가
#   발머의 최장선이고 그 바깥에는 선이 없다.
it = by['M01961']
retext(it, 3, '656 nm 보다 긴 쪽으로도 발머 계열 선이 이어진다',
       '계열이 양쪽으로 뻗는다고 본 거야. 도착이 2 로 고정이니 출발이 가장 낮은 3 → 2 의 '
       '656 nm 가 발머에서 파장이 가장 긴 선이고, 그보다 긴 쪽에는 발머 선이 없어. '
       '선이 이어지는 쪽은 짧은 파장 방향뿐이지.')
dmeta(it, 3, error='계열이 양쪽으로 뻗는다고 보아 최장선 656 nm 바깥에도 선이 있다고 함', typ='overgen')

# ── ㉢ M01957 ① : 이질항 해소 ────────────────────────────────────────
it = by['M01957']
retext(it, 0, '대부분 그대로 지나갔다 — (+)전하가 고르게 퍼져 있다',
       '톰슨 모형이 그리던 그림이야. 그런데 대부분이 지나갔다는 것이 곧바로 말해 주는 것은 '
       '지나는 길이 비어 있다는 것뿐이고, 전하가 어떻게 놓여 있는지는 알려 주지 않아. '
       '전하가 좁은 곳에 몰려 있다는 것은 크게 휘어진 쪽이 말해 주지.')
dmeta(it, 0, error='통과가 전하 분포까지 말해 준다고 보아 톰슨 모형의 그림을 그대로 둠', typ='causal')

# ── ㉣ factchecker 14차 여섯 ─────────────────────────────────────────
#   [A] M01960 — 13차에 중복을 걷어낸 자리에 꼬리가 남아 문장이 끊겼고, 남은
#       조언("방향을 먼저 정하면 칸을 다 세지 않아도 돼")은 ∞ 함정을 막기는커녕
#       번호가 붙어 보이는 ∞↔6 을 최소로 집게 미는 역효과였다.
#   [D] "촘촘함은 유한한 준위끼리에만 견주자" 는 이 표에서만 참이다 — 표의 최상단
#       유한 준위가 3 이면 ∞↔3 = k/9 = 0.111 이 2↔3 = 5k/36 = 0.139 보다 작아
#       뒤집힌다. 규칙으로 승격시키지 말고 개별 판정으로 되돌린다.
#   [E] M01961·M01963 은 ∞ 를 '닿지 않는 극한' 으로 다루는데 M01960 만 실재하는
#       전이처럼 불렀다. 어법을 맞춘다.
it = by['M01960']
sub(it, '∞ 와 6 사이는 번호로는 붙어 보여도 값이 k/36 = 0.0278 이라 11k/900 = 0.0122 보다 '
        '크니, 촘촘함은 유한한 준위끼리에만 견주자.',
    '∞ 와 6 사이는 번호로는 붙어 보여도 값이 k/36 = 0.0278 이라 11k/900 = 0.0122 보다 '
    '크니 이 표에서는 최소 후보에서 빠져.')
sub(it, '여기서 흔히 걸리는 자리가 둘 있어. 하나는 앞에서 본 ∞ 행이고, 표에서 두 끝을 찾을 '
        '때는 식이 커지고 작아지는 방향을 먼저 정하면 칸을 다 세지 않아도 돼. 다른 하나는 '
        '제곱을 빠뜨리는 거야. 1/5 에서 1/6 을 빼면 1/30 이 되어 30 배가 나오는데, 준위 '
        '에너지는 n 이 아니라 n² 에 반비례하니 1/25 와 1/36 으로 빼야 하지.',
    '여기서 흔히 걸리는 자리가 둘이야. 하나는 ∞ 행을 최소 후보로 집는 것이고, 다른 하나는 '
    '제곱을 빠뜨리는 것이지. 어느 쪽이든 칸을 눈으로 훑지 말고 값을 실제로 견줘야 갈려.')
sub(it, '앞의 항이 가장 크고 뒤의 항이 가장 작아야 하니까 m = 1, n = ∞ 일 때 k 가 되지.',
    '앞의 항이 가장 크고 뒤의 항이 가장 작아야 하니까 m = 1, n = ∞ 일 때 k 가 되지. '
    '여기서 ∞ 는 실제 준위가 아니라 한없이 올렸을 때 다가가는 극한이고, 표는 그 극한값을 '
    '한 행으로 실어 둔 거야.')
sub(it, '자가진단: 최대 k(1 ↔ ∞) · 최소 11k/900(5 ↔ 6) → 82 배.',
    '자가진단: 최대 k(1 ↔ ∞) · 최소는 표 안에서 11k/900(5 ↔ 6) → 82 배.')

#   [C] M01970 자가진단이 축 방향 조건을 떼고 '왼쪽이 짧은 쪽' 을 보편 규칙처럼 적었다.
sub(by['M01970'], '자가진단: 왼쪽이 짧은 쪽. 한 계열 안에서는 파장이 짧을수록 출발 준위가 높다.',
    '자가진단: 축이 400 에서 700 으로 커지니 왼쪽이 짧은 쪽. 한 계열 안에서는 파장이 '
    '짧을수록 출발 준위가 높다.')

#   [G] M01963 ① 만 값의 정체만 밝히고 오류 경로가 없었다.
sub(by['M01963'], '① 약 182 kJ/mol: 이건 656 nm 인 3 → 2 의 값이야. 발머에서 파장이 가장 긴 쪽이지.',
    '① 약 182 kJ/mol: 뒤섞인 목록을 줄 세우지 않고 눈에 익은 656 nm 를 집었을 때 나오는 '
    '값이야. 656 은 3 → 2 로 발머에서 파장이 가장 긴 쪽이고, 묻는 것은 가장 짧은 쪽이지.')
dmeta(by['M01963'], 0, error='목록을 줄 세우지 않고 눈에 익은 656 nm 를 집어 3 → 2 를 계산함')

# ── 검사 ─────────────────────────────────────────────────────────────
for n in range(1957, 1971):
    it = by[f'M0{n}']
    audit(it)
    L, r = g3(it)
    assert len(it['solution']) >= 300, it['id']
    if n in (1957, 1961, 1967):
        print(f'  {it["id"]} 정답 {MK[it["answer"]]} · 길이 {L} · 순위 {r} · 해설 {len(it["solution"])}자')

seq = [by[f'M0{n}']['answer'] for n in range(1957, 1971)]
print('정답 차례:', ''.join(MK[v] for v in seq), '| 개수', {MK[v]: seq.count(v) for v in range(4)})
bad = []
for d in (1, 2, 3, 4, 5):
    for i in range(len(seq)):
        if i - d >= 0 and seq[i - d] == seq[i]:
            continue
        L = 1
        while i + L * d < len(seq) and seq[i + L * d] == seq[i]:
            L += 1
        if L >= 3:
            bad.append((MK[seq[i]], d, [i + k * d + 1 for k in range(L)]))
print('등차 줄(3 이상):', bad if bad else '없음')
assert not bad

json.dump(bank, open(BANK, 'w', encoding='utf-8'), ensure_ascii=False, indent=1)
print('15차 조치 완료')
