# -*- coding: utf-8 -*-
"""T13 P7 (M02031~M02040) 층5 3차 조치 — 내가 2차에 만든 사실 오류 둘과 죽은 선지 다섯.

2차 순회: solver 마감 가능(F1 0 · 1차 여덟 자리 모두 막힘 확인) ·
defender 마감 가능(★변호 성공 0건★) · factchecker ★마감 불가(확정 오류 2)★ ·
student-sim 마감 보류(A 0 / B 0 / C 8 / D 10 · 죽은 선지 5)

★가장 아픈 것 — 2차 조치가 사실 오류 둘을 새로 낳았다★
  ㉠ M02033 ③ 을 `3d⁵` → `3p⁵` 로 갈면서 ★해설의 예시 원자를 함께 갱신하지 않았다.★
     3p⁵ 는 인이 아니라 ★염소★ 다(인은 3p³).
  ㉡ M02035 ① 을 '붕소와 탄소' → '붕소와 산소' 로 갈면서 ★옛 문장을 새 원소에 그대로
     얹었다.★ 그런데 그 문장의 셈이 아예 성립하지 않는다 — "2p⁴ 를 짝 셋으로 몰아넣으면
     홀전자가 하나" 는 짝 셋이 전자 여섯이라 넷과 맞지 않고, 더 근본적으로 ★오비탈 셋에
     전자 넷을 어떻게 넣어도 홀전자는 0 아니면 2 뿐이라 1 이 되는 배열이 없다.★
  ▸ ㉡ 은 문장 오류가 아니라 ★판정을 뒤집는다★ — sim 이 낸 그 안은 "산소의 홀전자를
    하나로 세는 오독" 을 전제했는데 그런 오독이 산술적으로 불가능하므로, 그 선지는
    살아난 것이 아니라 더 확실히 죽은 자리다.
  ▸ ★선지를 갈 때는 그 선지를 가리키던 해설·주석을 같은 호흡에 갈 것★ — P6 에서
    "받은 문장을 넣을 때 같은 말이 이미 있는지 볼 것" 을 배웠는데, 이번에는 ★남긴
    문장이 새 선지와 맞는지★ 를 놓쳤다. 같은 무늬의 뒷면이다.

받은 것 일곱
──────────────────────────────────────────────────────────────────────────
1. ★M02033 — 사실 오류 + 발문의 얇은 여지 + 죽은 선지 (셋이 한 자리에)★
   ㉠ '인의 배치' → '염소의 배치'(factchecker 확정 오류).
   ㉡ 발문 '그 부껍질의 정원' 이 ★'오비탈 개수'로 미끄러질 여지★ 가 얇게 남았다
      (defender 위생2). → '그 부껍질에 들어갈 수 있는 최대 전자 수' 로 못 박는다.
   ㉢ ② `3f⁷` 은 교체했는데도 되살아나지 않았다 — ★3f 가 있다고 믿어도 7 < 14 라
      정원 검산에서 반드시 탈락한다. 오개념 둘이 서로를 상쇄한다★(sim).
      → sim 안 `2d¹¹`. 2d 가 있다고 믿는 학생에게 11 > 10 이라 ★두 조건을 다 만족하는
        것처럼 보인다.★ 처음으로 유인력이 생긴다.
   ▸ 축 구조가 바뀐다 — 존재 X : O = 2 : 2 는 그대로이나 초과 : 이내가 3 : 1 이 된다.
     ★교차는 여전히 막힌다★ — 존재 축에 최빈값이 없어 두 축의 최빈값을 만나게 할 수
     없고, 초과 축의 이상치는 ③(오답)을 가리킨다.
2. ★M02035 ① — 세 검증자가 같은 자리로 모였다★
   solver(원소 출현이 B2·C1·N2·O3 로 무너졌고 ★탄소가 여덟 칸 중 한 번, 그것도 정답
   선지에서만★ 나온다) · defender(같은 지적) · factchecker(그 선지의 근거가 산술적으로
   불가능) → ★'붕소와 탄소' 로 되돌린다.★ 근거도 성립하는 것으로 다시 단다 —
   붕소와 탄소는 ★둘 다 2p 에 짝을 이룬 전자가 없다★ 는 점이 같아 홀전자 수도 같다고
   보는 경로다. 원소 출현이 2 : 2 : 2 : 2 로 복원된다.
   ▸ 정답이 '탄소와 산소' 인 이상 ★네 원소가 각각 두 번씩 나오는 조합은 이 하나뿐★ 이다.
3. ★M02037 — ④ 가 ① 의 일반화라 오답이 실질 둘 (defender 위생1) + ③ 이 새로 죽음(sim)
   + 정답만 유보형 (solver F-C)★ — 세 지적이 한 문항에 겹쳤다.
   ㉠ ④ '오비탈은 전자가 들어올 때 비로소 만들어진다' 는 ① '전자가 없으므로 3d 는
      없다' 의 일반화라 ★④ 가 참이면 ① 도 참★ 이다. 존재론 축에서 한 번에 닫히고
      "서로 동의하는 두 선지의 반대편이 정답" 이라는 단서를 만든다.
      → defender 안대로 축을 옮긴다 — '아르곤에서는 3s 와 3p 의 에너지가 서로 같다'
        (수소의 축퇴를 다전자에 적용하는 표준 오개념).
   ㉡ ③ 은 발문이 준 18 전자 배치와 정면 충돌해 어떤 오개념도 지지하지 않는다(sim).
      → '아르곤에는 전자 수와 같은 열여덟 개의 오비탈이 있다'(★전자 하나당 오비탈
        하나★ 라는 별개 오개념).
   ㉢ ② 만 "…있고 다만 …" 꼴이라 ★단정/유보 축이 3 : 1★ 이었다(solver).
      → '아르곤에도 4s 오비탈은 있으며 전자만 없다' 로 단정형으로 다시 적는다.
   ▸ 결과 — 극성 부정 2 : 긍정 2, 어조 전부 단정, 탈락 까닭 셋(존재=점유 / 전자 하나당
     오비탈 하나 / 같은 껍질이면 에너지 같음)이 서로 다른 축에 놓인다.
4. ★M02038 — '가장 포괄적·가장 큰 값' 경로 (solver F-A 중) + ② 죽은 선지 (sim)★
   ③ 만 부껍질을 셋 나열하고(나머지 2·2·1) 값도 최대라 ★"가장 많이 담은 것 / 가장 큰
   수" 라는 시험요령만으로 도달한다.★ 그리고 ②(3d·4p, 16)는 3d 와 4p 는 알면서 4s 만
   빠뜨리는 학생상이 성립하지 않는다.
   → 두 안을 합친다. ② → '4s 와 3d 를 채우는 동안이라 열두 개다'(sim, 4p 를 잊는 실제
     실수) · ④ → '4s·3d·4f 를 채우는 동안이라 스물여섯 개다'(solver, 4f 도 4주기로 봄).
     나열 수가 2 · 2 · 3 · 3 이 되어 정답이 최다가 아니고, 값이 8 · 12 · 18 · 26 이라
     ★정답이 최대도 최소도 아니다.★
   ▸ 관계식 재검산 — 2의 거듭제곱 {8}, 4의 배수 {8, 12}, 홀수 {} 로 ★어느 것도 3 : 1 로
     갈리지 않는다.★
5. ★M02032 ③ — 약한 선지 (sim)★ '크기는 그대로' 가 "커진다" 는 직관과 정면으로 부딪쳐
   아무도 집지 않는다. → '오비탈이 커지지만 그만큼 개수도 함께 늘어난다' — 앞절을 참으로
   두어 직관 충돌을 없애고 ★껍질 오비탈 수(n²) 혼동★ 을 잡는다.
   ▸ 덤으로 sim 이 짚은 ★문항 간 어조 단서★ 도 함께 준다 — M02032 ① · M02037 ② ·
     M02039 ① 이 모두 유일한 완화 진술이라 어조만으로 세 문항을 쓸어 담을 수 있었다.
     M02037 ② 는 단정형으로, M02032 ③ 은 완화형으로 바꿔 두 문항에서 어조를 2 : 2 로
     맞춘다(M02039 는 ② 가 이미 완화형이라 그대로 둔다).
6. ★M02034 ④ — 죽은 선지 (sim)★ "3s 를 통째로 건너뛴다" 는 오개념이 실재하지 않는다.
   → sim 안 `1s² 2s² 2p⁶ 3s² 3d¹`. 전자 13 개를 지키면서 ★"3s 다음은 3d" 라는 실재하는
     채움 순서 오개념★ 을 잡는다. 넷 중 셋이 13 개라는 성질도 그대로다.
   ▸ 해설의 '비워 둔' 도 고친다(factchecker △1) — 3s¹ 은 비어 있는 것이 아니라 자리가
     하나 남은 상태이고, 옛 ④(3s 를 통째로 건너뜀)와 표현이 겹쳐 둘을 가르기 어려웠다.
7. ★M02040 ③ — C·D 분기 복원 (sim) + 해설 어절 (factchecker △3)★
   현행 ③('짝을 이룬 전자의 개수가 홀수다')은 ★어느 원자에서도 거짓★ 이라 검산 한 번에
   지워진다. 2차에 C·D 분기가 3문항 → 2문항으로 줄었는데 그 줄어든 자리가 여기다.
   → '짝지은 전자가 홀전자보다 많다' — ★수소에서만 거짓인 경계 사례★ 라 질소·나트륨으로
     검산한 C 를 끌어온다. / '빠져나가도' → '빼도'(전자가 실제로 이탈하는 것으로 읽힌다).

반려한 것 넷 (근거를 남긴다)
──────────────────────────────────────────────────────────────────────────
 · ★M02035 를 탄소·질소·산소·플루오린으로 통째로 옮기기 (sim)★ — 반려.
   sim 안의 ①('질소와 플루오린')은 훈트를 무시하면 둘 다 홀전자 1 이 되어 ★유인력이
   확실히 세다.★ 그러나 ㉠ 이 문항은 ★멈춤 규칙★ 에 해당한다(죽은 선지 1 · D 정답 ·
   복수 정답 없음), ㉡ 같은 문항을 세 번째로 다시 쓰는 일이며, ㉢ '플루오린' 은 넉 자라
   두 자 이름 셋과 섞이면 길이 균형이 깨진다. ★다음 개정 후보로 적어 둔다.★
 · ★M02031 ① 을 (2, 1, +1) 로 (solver F-F)★ — 반려. mₗ 비영값을 셋으로 늘리자는
   안이지만 새 ① 의 탈락 까닭이 ④ 와 ★같은 '끝값 배제'★ 가 된다. 50 % 축소는 4지선다
   에서 허용 범위이고, 남은 둘을 가르려면 |mₗ| ≤ l 지식이 반드시 필요하다(solver 도
   "F5 는 아니다" 로 적었다).
 · ★M02039 의 규칙 축 2 : 1 : 1 (solver F-E)★ — 반려. solver 스스로 "defender 지적대로
   등급 대비가 핵심 변별이므로 그대로 두는 편이 낫다" 고 적었다.
 · ★M02037 ③ 을 '전자가 든 아홉 개가 전부다' 로 (sim 의 첫 안)★ — 반려. sim 스스로
   ★② 와 참·거짓 배타쌍이 되는 부작용★ 을 적었다. 둘째 안(열여덟 개)을 받는다.

★기록 — 검증자가 확인해 준 것★
 · defender: M02040 ③ 을 '개수' 로 못 박은 1차 조치가 ★실제 구멍을 막았다★ —
   '짝의 수' 였다면 리튬(짝 1개)에서 참이 되어 복수 정답이었다.
 · defender: M02039 의 ③④ 는 술어를 공유하지만 ★①과 ④가 상호 배타★ 라 복수 정답이
   아니라 정답 소멸 방향이다. 등급 대비는 그대로 살아 있다.
 · sim: M02034 정답 문자열이 M02035 발문에 없음을 재확인 — ★누출 해소 ✓★
 · sim: 발문에서 '홀수' 를 걷어내자 ★A 의 어휘 일치 정답이 사라졌다★(A 1 → 0).
"""
import json
import re
from collections import Counter

BANK = 'master/master_bank.json'
MK = ['①', '②', '③', '④']
IDS = ['M%05d' % n for n in range(2031, 2041)]
TOUCHED = {'M02032', 'M02033', 'M02034', 'M02035', 'M02037', 'M02038', 'M02040'}


def sub(it, old, new, n=1):
    assert it['solution'].count(old) == n, f"{it['id']}: '{old[:40]}' {it['solution'].count(old)}회"
    it['solution'] = it['solution'].replace(old, new)


def setc(it, choices, ans, wrongs):
    assert len(choices) == 4 and len(set(choices)) == 4, it['id']
    it['choices'] = choices
    it['answer'] = ans
    ds = [{'opt': choices.index(w[0]), 'error': w[1], 'type': w[2]} for w in wrongs]
    assert len(ds) == 3 and all(d['opt'] != ans for d in ds), it['id']
    it['distractors'] = sorted(ds, key=lambda d: d['opt'])


def resol(it, lead, cor, wmap, diag):
    a = it['answer']
    body = [lead, '', f"[정답] {MK[a]} {it['choices'][a]} — {cor}", '']
    for k in range(4):
        if k != a:
            body.append(f"{MK[k]} {it['choices'][k]}: {wmap[it['choices'][k]]}")
    body += ['', f"자가진단: {diag}"]
    it['solution'] = '\n'.join(body)


bank = json.load(open(BANK, encoding='utf-8'))
D = {i['id']: i for i in bank}
before = {k: json.loads(json.dumps(D[k])) for k in IDS}

# ── 1. M02032 ③ — 앞절을 참으로 두어 직관 충돌을 없앤다 · 어조 2:2 ─────────
it = D['M02032']
assert it['choices'][2] == '오비탈의 크기는 그대로이고 개수만 늘어난다', it['choices']
it['choices'][2] = '오비탈이 커지지만 그만큼 개수도 함께 늘어난다'
for d in it['distractors']:
    if d['opt'] == 2:
        d['error'] = '껍질 전체의 오비탈 수 n² 가 느는 것을 s 오비탈 개수로 옮겨 봄 — s 는 껍질마다 하나다'
sub(it, '오비탈의 크기는 그대로이고 개수만 늘어난다:\n'
        "              's 부껍질의 오비탈은 어느 껍질에서나 하나뿐이야. 늘어나는 것은 크기지 개수가 아니야.'"
        if False else
        '③ 오비탈의 크기는 그대로이고 개수만 늘어난다: s 부껍질의 오비탈은 어느 껍질에서나 '
        '하나뿐이야. 늘어나는 것은 크기지 개수가 아니야.',
        '③ 오비탈이 커지지만 그만큼 개수도 함께 늘어난다: 커진다는 앞말은 맞아. 그런데 '
        's 부껍질의 오비탈은 어느 껍질에서나 하나뿐이야. 늘어나는 것은 껍질 전체의 오비탈 '
        '수(n²)지 s 오비탈의 개수가 아니지.')

# ── 2. M02033 — 사실 오류 · 발문 · 죽은 선지 ② ────────────────────────────
it = D['M02033']
it['stem'] = ('다음 표기 가운데, 그 오비탈은 실제로 있지만 위첨자가 '
              '그 부껍질에 들어갈 수 있는 최대 전자 수를 넘어선 것은?')
setc(it, ['1p⁷', '2d¹¹', '3p⁵', '3p⁷'], 3, [
    ('1p⁷', '오비탈의 존재를 확인하지 않고 위첨자만 봄 — 1p 는 아예 없다', 'proc'),
    ('2d¹¹', '2d 가 있다고 보아 11 > 10 만으로 판정 — l 은 n 보다 작아야 한다', 'proc'),
    ('3p⁵', 'p 의 정원을 오비탈 수 3 으로 보아 5 를 초과로 봄 — 정원은 그 두 배인 6 이다',
     'proc'),
])
it['calc_check'] = '1p 없음·7>6 / 2d 없음·11>10 / 3p 있음·5≤6 / 3p 있음·7>6'
resol(it, '잣대가 둘이야. 하나만 보면 반드시 걸려 넘어지게 짜 두었지.',
      '두 가지를 따로 물어야 해. 첫째, 그 오비탈이 실제로 있는가. 부양자수는 주양자수보다 '
      '작아야 하니 1p 와 2d 는 아예 없는 오비탈이야. 둘째, 위첨자가 그 부껍질에 들어갈 수 '
      '있는 최대 전자 수를 넘지 않는가. p 는 여섯, d 는 열이지. 이제 넷을 표로 놓아 보자. '
      '1p⁷ 은 오비탈이 없고 위첨자도 넘었어. 2d¹¹ 도 오비탈이 없고 위첨자도 넘었지. '
      '3p⁵ 는 오비탈이 있고 다섯도 여섯 안이라 아무 문제가 없어 — 염소의 배치에 실제로 '
      '나오지. 3p⁷ 만이 오비탈은 있는데 위첨자가 여섯을 넘었어 — ④가 옳아. '
      '발문이 "그 오비탈은 실제로 있지만" 이라고 앞을 묶어 두었다는 점을 놓치지 말자. '
      '그 한 마디가 1p⁷ 과 2d¹¹ 을 답에서 밀어내는 유일한 못이야. 위첨자가 넘었다는 것만 '
      '보고 고르면 셋 가운데 아무거나 집게 돼.',
      {'1p⁷': '위첨자가 일곱이라 여섯을 넘은 것은 맞아. 그런데 1p 라는 오비탈 자체가 없어. '
              'n 이 1 이면 l 은 0 뿐이라 p 가 설 자리가 없지.',
       '2d¹¹': '열하나가 열을 넘은 것도 맞아. 그런데 2d 도 없는 오비탈이야. n 이 2 이면 '
               'l 은 0 과 1 뿐이라 d 가 설 자리가 없지. 두 조건을 다 만족하는 것처럼 보이지만 '
               '앞 조건에서 이미 걸려.',
       '3p⁵': '3p 는 실재하고 다섯도 정원 여섯 안이야. 셋을 정원으로 착각하기 쉬운데 셋은 '
              '오비탈의 개수고 정원은 그 두 배지.'},
      '표기를 만나면 "있는 오비탈인가" 와 "위첨자가 정원 안인가" 를 따로 적는다.')

# ── 3. M02034 ④ — 죽은 선지를 실재하는 채움 순서 오개념으로 ───────────────
it = D['M02034']
assert it['choices'][3] == '1s² 2s² 2p⁶ 3p³', it['choices']
it['choices'][3] = '1s² 2s² 2p⁶ 3s² 3d¹'
for d in it['distractors']:
    if d['opt'] == 3:
        d['error'] = '3s 다음이 3d 라고 보아 남은 하나를 3d 에 넣음 — 3p 가 3d 보다 먼저다'
sub(it, '3s 가 하나뿐인데 3p 에 둘이 들어가 있으니, 더 낮은 3s 를 비워 둔 셈이야.',
        '3s 가 하나뿐인데 3p 에 둘이 들어가 있으니, 더 낮은 3s 에 빈자리를 하나 남긴 채 '
        '위를 채운 셈이야.')
sub(it, '③ 1s² 2s² 2p⁶ 3s¹ 3p²: 적힌 그대로야. 전자 수는 맞지만 더 낮은 3s 를 비워 둔 채 '
        '3p 를 채웠으니 바닥상태가 아니라 들뜬 상태지.',
        '③ 1s² 2s² 2p⁶ 3s¹ 3p²: 적힌 그대로야. 전자 수는 맞지만 더 낮은 3s 에 빈자리를 '
        '남긴 채 3p 를 채웠으니 바닥상태가 아니라 들뜬 상태지.')
sub(it, '④ 1s² 2s² 2p⁶ 3p³: 전자 수는 열셋으로 맞아. 그런데 3s 를 통째로 건너뛰었어. '
        '3s 가 3p 보다 낮으니 먼저 채워야 해.',
        '④ 1s² 2s² 2p⁶ 3s² 3d¹: 전자 수는 열셋으로 맞아. 그런데 3s 다음 자리는 3d 가 '
        '아니라 3p 야. 채우는 차례는 3s, 3p 로 이어지고 3d 는 4s 보다도 뒤지.')

# ── 4. M02035 ① — 되돌리고 근거를 성립하는 것으로 다시 단다 ───────────────
it = D['M02035']
assert it['choices'][0] == '붕소와 산소', it['choices']
it['choices'][0] = '붕소와 탄소'
for d in it['distractors']:
    if d['opt'] == 0:
        d['error'] = '2p 에 짝을 이룬 전자가 없다는 점이 같아 홀전자 수도 같다고 봄'
sub(it, '① 붕소와 산소: 붕소는 하나, 산소는 둘이야. 2p⁴ 를 짝 셋으로 몰아 넣으면 홀전자가 '
        '하나로 보이지만, 오비탈이 셋뿐이라 그렇게는 담기지 않아.',
        '① 붕소와 탄소: 둘 다 2p 에 짝을 이룬 전자가 하나도 없다는 점은 같아. 그렇지만 '
        '홀전자는 붕소가 하나, 탄소가 둘이야. 짝이 없다는 것과 홀전자 수가 같다는 것은 '
        '다른 말이지.')

# ── 5. M02037 — 축을 셋으로 나누고 어조를 단정으로 통일 ───────────────────
it = D['M02037']
setc(it, ['전자가 없으므로 아르곤에 3d 오비탈은 없다',
          '아르곤에도 4s 오비탈은 있으며 전자만 없다',
          '아르곤에는 전자 수와 같은 열여덟 개의 오비탈이 있다',
          '아르곤에서는 3s 와 3p 의 에너지가 서로 같다'], 1, [
    ('전자가 없으므로 아르곤에 3d 오비탈은 없다',
     '"전자가 없다 = 오비탈이 없다" 로 봄 — 존재와 점유를 뒤섞었다', 'proc'),
    ('아르곤에는 전자 수와 같은 열여덟 개의 오비탈이 있다',
     '전자 하나에 오비탈 하나씩이라고 봄 — 오비탈 하나에 전자가 둘까지 들어간다', 'proc'),
    ('아르곤에서는 3s 와 3p 의 에너지가 서로 같다',
     '수소의 축퇴를 다전자 원자에 적용 — 전자가 둘 이상이면 같은 껍질에서도 갈라진다',
     'proc'),
])
resol(it, '"없다" 는 말이 두 가지로 쓰여. 오비탈이 없는 것과 전자가 없는 것은 달라.',
      '오비탈은 전자가 들어오기를 기다리는 자리야. 전자가 몇 개든 그 자리는 원자마다 '
      '이미 정해져 있지. 아르곤은 전자가 열여덟이라 3p 까지 채우고 멈춘 것뿐이고, '
      '그 위의 4s 도 3d 도 자리는 그대로 있어. 다만 전자가 없을 뿐이야 — ②가 옳아. '
      '이 구분이 왜 중요한지 보자. 칼륨은 아르곤보다 전자가 하나 더 많은데, 그 전자가 '
      '들어가는 4s 가 아르곤에는 없다가 칼륨에서 새로 생겨나는 것이 아니야. 원래 있던 '
      '빈자리에 들어가는 것이지. 빈 오비탈도 에너지가 정해져 있고, 그래서 어느 자리부터 '
      '채울지를 미리 견줄 수 있는 거야. 표기에 적히는 것은 전자가 든 자리뿐이라는 점도 '
      '함께 붙들자. 적히지 않았다고 없는 것이 아니야.',
      {'전자가 없으므로 아르곤에 3d 오비탈은 없다':
          '전자가 없는 것과 오비탈이 없는 것은 달라. 3d 는 n 이 3, l 이 2 라 아무 문제 없이 '
          '있는 오비탈이고 아르곤에서는 비어 있을 뿐이야.',
       '아르곤에는 전자 수와 같은 열여덟 개의 오비탈이 있다':
          '오비탈 하나에는 전자가 둘까지 들어가. 아르곤에서 전자가 든 오비탈은 1s, 2s, 2p 셋, '
          '3s, 3p 셋으로 아홉이고, 비어 있는 오비탈까지 세면 열여덟에서 멈추지도 않아.',
       '아르곤에서는 3s 와 3p 의 에너지가 서로 같다':
          '같은 껍질이라도 전자가 둘 이상이면 서로 밀어내 준위가 갈라져. 3s 가 3p 보다 낮지. '
          '같은 껍질에서 에너지가 같은 것은 전자가 하나뿐인 계에서야.'},
      '"없다" 가 나오면 오비탈이 없는 것인지 전자가 없는 것인지 먼저 가른다.')

# ── 6. M02038 ②④ — 나열 수와 값의 이상치를 함께 없앤다 ───────────────────
it = D['M02038']
setc(it, ['4s 와 4p 를 채우는 동안이라 여덟 개다',
          '4s 와 3d 를 채우는 동안이라 열두 개다',
          '4s·3d·4p 를 채우는 동안이라 열여덟 개다',
          '4s·3d·4f 를 채우는 동안이라 스물여섯 개다'], 2, [
    ('4s 와 4p 를 채우는 동안이라 여덟 개다',
     '4s 와 4p 사이에 끼어드는 3d 열 개를 빠뜨림', 'proc'),
    ('4s 와 3d 를 채우는 동안이라 열두 개다',
     '3d 를 넣고 나서 뒤따르는 4p 여섯 개를 빠뜨림', 'proc'),
    ('4s·3d·4f 를 채우는 동안이라 스물여섯 개다',
     '껍질 번호가 4 인 4f 도 4주기라고 봄 — 4f 는 6주기에서 채워진다', 'proc'),
])
resol(it, '주기의 길이는 그 주기에서 채우는 부껍질을 모두 더한 값이야. 하나만 빠지거나 '
          '하나만 더 들어와도 값이 크게 어긋나지.',
      '채움 순서를 4주기 구간만 떼어 보자. 3주기는 3p 가 차면서 끝나고, 그다음으로 '
      '에너지가 낮은 것은 3d 가 아니라 4s 야. 그러니 4주기는 4s 부터 시작해. 4s 에 둘이 '
      '차면 그다음이 3d 로 열이 들어가고, 이어서 4p 에 여섯이 들어가며 4주기가 끝나지. '
      '더하면 2 더하기 10 더하기 6 으로 열여덟이야 — ③이 옳아. 여기서 두 가지를 붙들자. '
      '첫째, 주기 번호와 껍질 번호가 늘 같지는 않아. 4주기 한가운데에 채워지는 3d 는 '
      '껍질 번호가 3 이지만 어엿한 4주기의 몫이고, 거꾸로 껍질 번호가 4 인 4d 와 4f 는 '
      '4주기에 들지 않아. 둘째, 주기의 길이와 껍질의 정원은 다른 값이야. N 껍질의 정원은 '
      '2n² 로 서른둘이지만 그 가운데 4주기에서 채워지는 것은 4s 와 4p 뿐이지. '
      '어디서 시작해 어디서 끝나는지를 먼저 못 박고 그 사이의 부껍질만 더하는 것이 요령이야.',
      {'4s 와 4p 를 채우는 동안이라 여덟 개다':
          '4s 와 4p 사이에 3d 가 끼어들어. 그 열 개가 곧 4주기의 전이 원소야.',
       '4s 와 3d 를 채우는 동안이라 열두 개다':
          '3d 까지는 옳게 세었는데 그 뒤가 남았어. 3d 가 다 차면 4p 로 여섯이 더 들어가고 '
          '거기서 주기가 끝나지.',
       '4s·3d·4f 를 채우는 동안이라 스물여섯 개다':
          '4f 는 껍질 번호만 4 일 뿐 6주기에서 채워져. 껍질 번호로 주기를 가르면 안 돼 — '
          '3d 가 4주기인 것이 바로 그 반례지.'},
      '주기의 길이를 물으면 어디서 시작해 어디서 끝나는지부터 못 박고 그 사이만 더한다.')

# ── 7. M02040 ③ — 수소에서만 거짓인 경계 사례로 C·D 분기 복원 ─────────────
it = D['M02040']
assert it['choices'][2] == '짝을 이룬 전자의 개수가 홀수다', it['choices']
it['choices'][2] = '짝지은 전자가 홀전자보다 많다'
for d in it['distractors']:
    if d['opt'] == 2:
        d['error'] = '질소·나트륨만 검산하고 수소를 빠뜨림 — 수소는 짝지은 전자가 0, 홀전자가 1 이다'
sub(it, '거꾸로야. 짝을 이룬 전자가 짝수 개 빠져나가도 남는 수의 홀짝은 처음 그대로거든.',
        '거꾸로야. 전체에서 짝수 개를 빼도 남는 수의 홀짝은 처음 그대로거든.')
sub(it, '③ 짝을 이룬 전자의 개수가 홀수다: 짝을 이룬 전자는 오비탈마다 둘씩 묶이니 그 '
        '개수는 언제나 짝수야. 홀수가 될 수 없지.',
        '③ 짝지은 전자가 홀전자보다 많다: 질소는 넷 대 셋, 나트륨은 열 대 하나라 맞는 것처럼 '
        '보여. 그런데 수소는 짝지은 전자가 하나도 없고 홀전자가 하나야. 반례가 하나라도 '
        '있으면 "반드시" 는 무너지지.')

# ── 검사 ──────────────────────────────────────────────────────────────────
items = [D[k] for k in IDS]
assert Counter(i['answer'] for i in items) == {2: 3, 0: 2, 3: 3, 1: 2}
for k in IDS:
    assert D[k]['answer'] == before[k]['answer'], (k, '정답 자리는 바뀌지 않는다')
for k in set(IDS) - TOUCHED:
    assert D[k] == before[k], k

for it in items:
    cs = it['choices']
    assert len(cs) == 4 and len(set(cs)) == 4, it['id']
    assert sorted(d['opt'] for d in it['distractors']) == sorted(set(range(4)) - {it['answer']}), it['id']
    assert len(it['solution']) >= 300, (it['id'], len(it['solution']))
    assert '★' not in it['stem'] and '★' not in it['solution'], it['id']
    assert it['objective'], it['id']
    L = sorted(len(c) for c in cs)
    a = len(cs[it['answer']])
    assert not (a == L[3] and L[2] < L[3]), (it['id'], 'G3 최장 단독', cs)
    assert not (a == L[0] and L[0] < L[1]), (it['id'], 'G3 최단 단독', cs)
    mid = (L[1] + L[2]) / 2
    if mid >= 8:
        assert (L[3] - L[0]) / mid <= 0.25, (it['id'], 'G3b', cs, L)
    assert '가 옳아' in it['solution'] or '이 옳아' in it['solution'], it['id']
    for k in range(4):
        assert MK[k] + ' ' + cs[k] in it['solution'], (it['id'], MK[k], cs[k])

# ── 2차에 넣은 검사 셋을 그대로 다시 돌린다 ────────────────────────────────
for x in items:
    ans = x['choices'][x['answer']]
    if len(ans) >= 6:
        for y in items:
            if y['id'] == x['id']:
                continue
            assert ans not in y['stem'], (x['id'], '정답이 발문에 노출', y['id'])
            assert ans not in ' '.join(y['choices']), (x['id'], '정답이 선지에 노출', y['id'])

# ▸ 한글 수사는 ★쓰인 꼴 그대로★ 넣을 것 — '열둘' 만 넣어 두었더니 '열두 개' 가 잡히지
#   않아 M02038 이 또 검사에서 빠져나갈 뻔했다(2차에 앞자리 숫자 오독으로 한 번, 이번에
#   수사 표기로 한 번 — ★검사가 조용히 헛도는 것이 가장 위험하다★).
NUMW = {'여덟': 8, '열': 10, '열두': 12, '열둘': 12, '열넷': 14, '열여섯': 16,
        '열여덟': 18, '스물여섯': 26, '서른둘': 32}


def vals(it):
    out = []
    for c in it['choices']:
        m = re.fullmatch(r'\s*(\d+)\s*개?\s*', c)
        if m:
            out.append(int(m.group(1)))
            continue
        hit = [v for w, v in NUMW.items() if w + ' 개' in c]
        out.append(max(hit) if hit else None)
    return out


REL = [('2의 거듭제곱', lambda v: v > 0 and (v & (v - 1)) == 0),
       ('4의 배수', lambda v: v % 4 == 0),
       ('홀수', lambda v: v % 2 == 1)]
for it in items:
    v = vals(it)
    if any(x is None for x in v) or len(set(v)) < 4:
        continue
    for name, f in REL:
        ok = [i for i in range(4) if f(v[i])]
        if len(ok) == 3 and it['answer'] not in ok:
            raise AssertionError(f"{it['id']}: 「{name}」 을 어기는 선지가 정답 하나뿐 {v}")
        if len(ok) == 1 and it['answer'] in ok:
            raise AssertionError(f"{it['id']}: 「{name}」 을 만족하는 선지가 정답 하나뿐 {v}")
    # ▸ ★순수 수치 선지에서는 이 검사를 걸 수 없다.★ G6 가 오름차순을 요구하므로
    #   정답의 자리가 곧 정답값의 크기 순위이고, 배치가 정답 자리를 ④ 로 정해 두면
    #   정답이 최댓값이 되는 것을 피할 수 없다(M02036 이 그 경우다 — 정답 9 는
    #   2·7·8 이 각각 그 부분합이라 구조적으로 가장 크다).
    #   문장 안에 값이 박힌 선지(M02038)에서만 자리와 크기가 따로 놀아 검사가 뜻을 갖는다.
    pure = all(re.fullmatch(r'\s*\d+\s*개?\s*', c) for c in it['choices'])
    if not pure:
        assert v[it['answer']] not in (max(v), min(v)), (it['id'], '정답이 최대 또는 최소', v)

NEG = ('않', '없', '아니', '못')
for it in items:
    neg = [i for i in range(4) if any(t in it['choices'][i] for t in NEG)]
    if len(neg) == 1:
        assert neg[0] != it['answer'], (it['id'], '정답만 부정형', it['choices'])
    if len(neg) == 3:
        pos = (set(range(4)) - set(neg)).pop()
        assert pos != it['answer'], (it['id'], '정답만 긍정형', it['choices'])

for it in items:
    for w in ('홀수', '짝수'):
        if w in it['stem']:
            hit = [i for i in range(4) if w in it['choices'][i]]
            assert hit != [it['answer']], (it['id'], f'어휘 메아리 {w}', it['choices'])

# ── ★새 검사 — 어조(완화 표현)가 정답에만 있지 않은가★ ────────────────────
#   sim 이 문항 간 패턴으로 잡았다 — 유일한 완화 진술이 세 문항에서 모두 정답이었다.
HEDGE = ('지만', '어도', '으나', '다만', '뿐')
for it in items:
    hg = [i for i in range(4) if any(t in it['choices'][i] for t in HEDGE)]
    if len(hg) == 1:
        assert hg[0] != it['answer'], (it['id'], '완화 표현이 정답에만', it['choices'])

# ── ★새 검사 — 나열 개수가 정답에만 최다이지 않은가★ ─────────────────────
for it in items:
    cnt = [len(re.findall(r'[1-9][spdf]', c)) for c in it['choices']]
    if max(cnt) >= 2 and cnt.count(max(cnt)) == 1:
        assert cnt.index(max(cnt)) != it['answer'], (it['id'], '정답만 최다 나열', cnt)

# ── 문항 전용 ─────────────────────────────────────────────────────────────
assert '염소의 배치' in D['M02033']['solution'] and '인의 배치' not in D['M02033']['solution']
assert '들어갈 수 있는 최대 전자 수' in D['M02033']['stem']
# M02037 — 탈락 까닭이 세 축으로 갈렸는가 (존재=점유 / 전자 하나당 오비탈 하나 /
#   같은 껍질이면 에너지 같음). 선지 문면으로 축을 식별한다.
c37 = D['M02037']['choices']
assert '3d 오비탈은 없다' in c37[0] and '열여덟 개의 오비탈' in c37[2] and '에너지가 서로 같다' in c37[3]
assert c37[1] == '아르곤에도 4s 오비탈은 있으며 전자만 없다'
assert vals(D['M02038']) == [8, 12, 18, 26], vals(D['M02038'])
el = Counter(w for c in D['M02035']['choices'] for w in c.split('와 '))
assert set(el.values()) == {2}, ('M02035 원소 출현 불균형', el)

json.dump(bank, open(BANK, 'w', encoding='utf-8'), ensure_ascii=False, indent=1)
print('T13 P7 3차 조치 완료 — 7 문항')
print('  M02038 값', vals(D['M02038']), '· 나열 수', [len(re.findall(r'[1-9][spdf]', c)) for c in D['M02038']['choices']])
print('  M02035 원소 출현', dict(el))
print('  M02033 발문 최대 전자 수로 못 박음 · 염소로 정정')
